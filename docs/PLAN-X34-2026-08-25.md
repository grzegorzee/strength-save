# PLAN X34 — poprawki kreatora po przeglądzie X33 na urządzeniu (krok 5A odchudzony, nowy ekran 6/6 "Start planu", "Wybierz inny plan" w podglądzie)

> Data: 2026-08-25 wieczór. Status: ZATWIERDZONY (decyzje właściciela ze screena IMG_1243 i głosówki). Baza: X33 na main (`ce6a23d7`).
> Kierunek: "raz a dobrze": implementacja + e2e + screeny na viewport iPhone'a + przewinięcie każdego ekranu.

## 0. Decyzje właściciela (wiążące)

1. **Ekran 5A** zostaje ekranem WYBORU: nagłówek + dwie karty + "Ułóż własny plan" + "Biblioteka planów na N dni". Z 5A ZNIKA: linia podsumowania odpowiedzi ("3 dni w tygodniu · redukcja · średniozaawansowany" + "Pn · Śr · Pt"), przycisk "Zmień ustawienia" (to onboarding, user właśnie to przeszedł; wstecz = strzałka), linia ustawień (nazwa/długość/start), oba dotychczasowe CTA. Na kartach ZNIKA wiersz "Pierwszy trening: …". Nagłówek zostaje w duchu "Plany na 3 dni w tygodniu" (może być lekko przeredagowany, np. kicker "Dopasowane do Ciebie" + tytuł "Plany na {days} dni w tygodniu").
2. **CTA na 5A**: jeden, krótki: **"Wybierz start planu"** (`ob.match.next`), prowadzi do NOWEGO ekranu 6/6.
3. **Nowy ekran 6/6 "Start planu"** (`ob.start.*`): nazwa planu (pełna szerokość, default z szablonu; własny plan = nazwa z buildera), czas trwania (kafle 8 / 12 / 16 + wartość szablonu z "polecane", "Inna" → picker), start planu (chipy poniedziałków, domyślnie najbliższy). Na dole DWA przyciski: główny **spersonalizowany celem**: `build_muscle` → "Zacznij budować masę", `peak_strength` → "Zacznij budować siłę", `fat_loss` → "Zacznij redukcję", `athletic` → "Zacznij trening atletyczny" (klucze `ob.start.cta.{objective}`, pl+en; fallback "Rozpocznij plan"); drugorzędny **"Podgląd planu"** (przewinięty na górę). Główny = zapis od razu (dzisiejsza ścieżka `skipPreview: true`).
4. **Podgląd planu**: na samym dole DWA przyciski: "Zatwierdź i zacznij" (jak dziś) + **"Wybierz inny plan"** (`ob.preview.chooseOther`), który wraca do ekranu 5A z zachowanym stanem kreatora (wybrana karta, ustawienia z 6/6 zapamiętane, bez ponownego przerywnika). Dotyczy Onboarding i NewPlan.
5. **Przerywnik "Dobieram plany"**: 3,5 s (zamiast ~0,9 s), z animacją paska i trzema wierszami (Poziom / Cel / Częstotliwość) pojawiającymi się kolejno (np. co 0,7 s), żeby wyglądało na realne dobieranie. Pomijany przy powrocie z 6/6 lub z podglądu, przy "Wybierz inny plan", przy wznowieniu szkicu. Test z fake timers; w e2e klik czeka na zniknięcie nakładki (Playwright auto-wait), ale dopisz jawny `expect(overlay).toBeHidden({ timeout: 6000 })` w helperze.
6. Licznik kroków: onboarding 1..6 (`StepHeader total=6`), replan 2..6. Krok 6 ma strzałkę wstecz do 5A.

## 1. Kontrakt danych i zapisu (BEZ ZMIAN)

`PlanWizardChoice`, `buildPlanCycleChoice`, `choice` na cyklu, `onboardingAnswers`, `startCycleWithPlan`, `completeOnboardingPlan`: nietknięte. Zmienia się tylko, KTÓRY ekran zbiera nazwę/długość/start (6/6 zamiast linii na 5A) i gdzie są przyciski. `onConfirm(choice, { skipPreview })` zostaje: 6/6 główny CTA = `skipPreview: true`, "Podgląd planu" = `skipPreview: false`. Test równości payloadu obu ścieżek zostaje (zaktualizuj o ekran 6).

## 2. Komponenty

- `PlanWizard.tsx`: krok 5 (mode recommend) odchudzony wg pkt 0.1-0.2; nowy `step === 6` (`PlanStartStep` jako nowy komponent w src/components/, przyjmujący: chosen template/własny plan, name/weeks/startDate + settery, objective, onBack, onStart, onPreview, isSaving, error). `PlanSettingsRow` przestaje być używany na 5A: jeśli nic innego go nie używa, USUŃ komponent, jego test i klucze i18n (nie zostawiaj martwego kodu). `PlanChoiceCard`: usuń wiersz "Pierwszy trening" (prop i klucz i18n `ob.match.firstWorkout`).
- `PlanPreview.tsx`: nowy prop `onChooseOther` + drugi przycisk na dole (wariant secondary, pełna szerokość, pod "Zatwierdź i zacznij"). `Onboarding.tsx` / `NewPlan.tsx`: `onChooseOther` = powrót do kreatora na krok 5 z zachowanym stanem (`resume` + `resumeStep: 5`, jak dzisiejsze wznowienie szkicu; upewnij się, że `picked`, nazwa, tygodnie i start wracają 1:1; przerywnik NIE odpala się przy `resumeStep`).
- i18n: nowe klucze do OBU plików pl.ts i en.ts; usuwane klucze z OBU (`ob.match.start`, `ob.match.firstWorkout`, `ob.precision.answers`, `ob.precision.change` jeśli nigdzie indziej nieużywane, klucze `PlanSettingsRow`). Zadnych emoji. Baseline natywny (zasada 7): kafle/chipy `touch-action: manipulation`, bez zaznaczania tekstu.

## 3. Testy (obowiązkowe)

- Unit (vitest): 5A bez podsumowania/"Zmień ustawienia"/"Pierwszy trening"; CTA "Wybierz start planu" → krok 6 z domyślnymi (nazwa szablonu, tygodnie szablonu "polecane", najbliższy poniedziałek); CTA spersonalizowane dla 4 celów (+ własny plan); "Podgląd planu" z 6/6; przerywnik 3,5 s z fake timers (3 wiersze po kolei, pomijany przy powrocie); "Wybierz inny plan" w podglądzie wraca na 5A z zachowaną kartą i ustawieniami (Onboarding i NewPlan); równość payloadu "Zacznij…" vs "Podgląd → Zatwierdź"; własny plan (PlanBuilder) przechodzi przez 6/6; niezmienniki: onboarding od kroku 1 do końca, replan od kroku 2, wznowienie szkicu, route-smoke.
- E2E (Playwright, chromium + webkit, `--workers=2`): zaktualizuj `e2e/full-app.spec.ts`, `e2e/replan.spec.ts`, `e2e/onboarding-accent.spec.ts`, `e2e/auth-registration.spec.ts` i helper `advanceWizardToStep5` (+ nowy `advanceWizardToStep6`). Nowe scenariusze: (a) onboarding 1→6 → "Zacznij redukcję" → Dashboard; (b) 6/6 → "Podgląd planu" → "Wybierz inny plan" → 5A z zaznaczoną kartą → karta 2 → 6/6 → podgląd → "Zatwierdź i zacznij"; (c) replan 2→6 → podgląd → zatwierdź; (d) biblioteka → wybór → 6/6.
- **Weryfikacja wizualna (osobny agent QA):** Playwright na viewport iPhone 15 (393×852, deviceScaleFactor 3, `hasTouch`), tryb E2E: zrzuty PEŁNEJ STRONY i viewportu dla: krok 4, przerywnik (w połowie animacji), 5A (góra + po przewinięciu do dołu; karty, biblioteka rozwinięta z chipami), 6/6 (góra + dół; kafle, chipy poniedziałków, oba przyciski), podgląd planu (góra + sam dół z oboma przyciskami), 5A po "Wybierz inny plan". Sprawdzenie: CTA widoczne bez ukrycia pod bottom nav / safe area, brak poziomego scrolla (`document.documentElement.scrollWidth <= innerWidth`), nagłówki nie ucinają się, nazwa planu 60 znaków mieści się w polu (test z długą nazwą), przerywnik faktycznie widoczny ≥3 s (pomiar `performance.now()`). Zrzuty do `tmp/qa-x34/` + raport tekstowy z listą odchyleń. Zrzuty ogląda orkiestrator.

## 4. Bramki i wydanie

vitest pełny, typecheck, lint 0 err, no-emoji, build web+mobile, dist-smoke, bundle-budget, e2e (4 specy × chromium+webkit) zielone, raport QA bez odchyleń (albo odchylenia naprawione). Wydanie: web + iOS **121** + AAB **v36** (rules i functions bez zmian). Wpis DECYZJE + pamięć.
