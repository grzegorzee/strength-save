# PLAN PRO-D: Gamifikacja progresu (duma na wierzch)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Odznaki w jednym kształcie z materiałowymi tierami i ghost-slotami, poziom z paskiem postępu w Profilu, heatmapa konsekwencji na ekranie Postępów, powiadomienie o zdobytym kamieniu milowym.

**Architecture:** Nowy prezentacyjny komponent `AchievementBadge` (heksagon w CSS clip-path, 4 materiały, stan ghost) zastępuje szare kafle z `Lock` w `Achievements.tsx`. Logika osiągnięć zostaje nietknięta (`achievements-utils.ts` dostaje tylko czysty `diffMilestones`). Istniejący, nieużywany `TrainingHeatmap.tsx` zostaje osadzony w Postępach. Tier w Profilu renderuje pola `progress`/`next`, które `computeTier` (`src/lib/tier.ts`) już zwraca.

**Tech Stack:** React 18 + TS, CSS clip-path (bez SVG assetów), lucide-react, vitest + RTL.

## Global Constraints

- Nagradzamy wyłącznie realny trening: żadnych punktów za czynności obsługowe (podpięcia, buty, polecenia). Score tieru zostaje: treningi + 2×PR (`tier.ts:16`).
- Jeden kształt odznaki w całym systemie (heksagon). Tier przez materiał (brąz → srebro → złoto → platyna), nie przez tęczę. Zero maskotek; symbol w środku = lucide.
- Nazwy odznak pozostają z rzeczywistości treningowej (istniejące klucze i18n milestones — bez zmian treści).
- Ghost (niezdobyte) = ten sam kształt, tło ~8% krycia, przerywany kontur, bez kłódki.
- No-Line Rule (docs/DESIGN.md): sekcjonowanie tłem, nie borderami 1px.
- i18n do OBU plików; po każdym tasku testy + typecheck + commit.

---

### Task 1: Komponent `AchievementBadge`

**Files:**
- Create: `src/components/kinetic/AchievementBadge.tsx`
- Test: `src/components/__tests__/achievement-badge.test.tsx`

**Interfaces:**
- Produces:

```ts
type BadgeTier = 'bronze' | 'silver' | 'gold' | 'platinum';
interface AchievementBadgeProps {
  label: string;            // np. "50 treningów"
  sublabel?: string;        // np. data zdobycia albo "brakuje 21"
  earned: boolean;
  tier: BadgeTier;
  icon: LucideIcon;         // symbol w środku (Trophy/Dumbbell/Target/…)
  size?: 'sm' | 'md';       // sm = rząd w Profilu, md = siatka Postępów
  progress?: number;        // 0-1, pasek pod ghostem (opcjonalny)
}
```

- [ ] **Step 1: Failing test**

```tsx
// src/components/__tests__/achievement-badge.test.tsx
import { describe, expect, it } from 'vitest';
import { render } from '@testing-library/react';
import { Trophy } from 'lucide-react';
import { AchievementBadge } from '@/components/kinetic/AchievementBadge';

describe('AchievementBadge', () => {
  it('zdobyta: materiał tieru, bez ghost', () => {
    const { getByTestId } = render(
      <AchievementBadge label="50 treningów" earned tier="gold" icon={Trophy} />,
    );
    expect(getByTestId('badge-hex').dataset.tier).toBe('gold');
    expect(getByTestId('badge-hex').dataset.earned).toBe('true');
  });
  it('ghost: earned=false, ten sam kształt', () => {
    const { getByTestId } = render(
      <AchievementBadge label="100 treningów" earned={false} tier="gold" icon={Trophy} />,
    );
    expect(getByTestId('badge-hex').dataset.earned).toBe('false');
  });
});
```

- [ ] **Step 2: Uruchom, ma paść.**
- [ ] **Step 3: Implementacja**

```tsx
// src/components/kinetic/AchievementBadge.tsx
// Odznaka systemu (PRO-D): jeden kształt (heksagon), tier przez materiał,
// niezdobyta = ghost (ten sam kształt, 8% krycia, przerywany kontur).
import type { LucideIcon } from 'lucide-react';
import { cn } from '@/lib/utils';

export type BadgeTier = 'bronze' | 'silver' | 'gold' | 'platinum';

const TIER_GRADIENTS: Record<BadgeTier, string> = {
  bronze: 'linear-gradient(160deg,#c98d5f 0%,#8a5a35 55%,#6e4527 100%)',
  silver: 'linear-gradient(160deg,#e8eaea 0%,#a9adad 55%,#7e8282 100%)',
  gold: 'linear-gradient(160deg,#f4de7d 0%,#d4af37 55%,#a8842a 100%)',
  platinum: 'linear-gradient(160deg,#dfe8ef 0%,#9fb2c4 55%,#6d7f92 100%)',
};

const TIER_INK: Record<BadgeTier, string> = {
  bronze: '#2b1a0e',
  silver: '#24262a',
  gold: '#2e2405',
  platinum: '#1c2430',
};

const HEX_CLIP = 'polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%)';

interface AchievementBadgeProps {
  label: string;
  sublabel?: string;
  earned: boolean;
  tier: BadgeTier;
  icon: LucideIcon;
  size?: 'sm' | 'md';
  progress?: number;
}

export const AchievementBadge = ({
  label, sublabel, earned, tier, icon: Icon, size = 'md', progress,
}: AchievementBadgeProps) => {
  const hexSize = size === 'md' ? 'h-[76px] w-[68px]' : 'h-[52px] w-[46px]';
  const iconSize = size === 'md' ? 'h-6 w-6' : 'h-4 w-4';
  return (
    <div className={cn('flex flex-col items-center text-center', size === 'md' ? 'w-24' : 'w-16')}>
      <div
        data-testid="badge-hex"
        data-tier={tier}
        data-earned={String(earned)}
        className={cn('flex items-center justify-center', hexSize)}
        style={earned
          ? { clipPath: HEX_CLIP, background: TIER_GRADIENTS[tier] }
          : { clipPath: HEX_CLIP, background: 'rgba(255,255,255,0.05)' }}
      >
        <Icon
          className={iconSize}
          style={{ color: earned ? TIER_INK[tier] : 'hsl(var(--muted-foreground) / 0.5)' }}
          aria-hidden
        />
      </div>
      <p className={cn(
        'mt-2 font-bold leading-tight',
        size === 'md' ? 'text-xs' : 'text-[10px]',
        earned ? 'text-foreground' : 'text-muted-foreground',
      )}>
        {label}
      </p>
      {sublabel && (
        <p className="mt-0.5 text-[10px] tabular-nums text-muted-foreground/70">{sublabel}</p>
      )}
      {!earned && typeof progress === 'number' && (
        <div className="mt-1.5 h-1 w-12 overflow-hidden rounded-full bg-surface-highest">
          <div className="h-full rounded-full bg-primary/60" style={{ width: `${Math.round(progress * 100)}%` }} />
        </div>
      )}
    </div>
  );
};
```

Uwaga: clip-path ucina cień, dlatego głębię daje sam gradient (highlight u góry, cień u dołu) — celowo bez box-shadow.

- [ ] **Step 4: Test zielony.**
- [ ] **Step 5: Commit** — `git commit -m "feat(kinetic): AchievementBadge — heksagon, materiały, ghost (PRO-D T1)"`

---

### Task 2: Postępy na nowych odznakach

**Files:**
- Modify: `src/pages/Achievements.tsx:347-392` (milestones), `:395-436` (odznaki specjalne)

**Interfaces:**
- Consumes: `AchievementBadge` (T1); `computeMilestones` zwraca `Milestone { category: 'workouts'|'tonnage'|'records', threshold, achieved, progress }` (`src/lib/achievements-utils.ts:209-229` — przed implementacją potwierdź dokładne pola: `sed -n '209,250p' src/lib/achievements-utils.ts`).

- [ ] **Step 1: Mapowanie tieru z pozycji progu** — dodaj w `src/lib/achievements-utils.ts` (na końcu pliku; z utils, bo w T6 użyje go też Profil):

```ts
import type { BadgeTier } from '@/components/kinetic/AchievementBadge';

// Tier = pozycja progu w kategorii: pierwsze dwa progi brąz, kolejne dwa srebro,
// dalej złoto, ostatni platyna (5 progów/kategorię: [b, b, s, g, p]).
export const tierForIndex = (idx: number, total: number): BadgeTier => {
  if (idx >= total - 1) return 'platinum';
  if (idx >= total - 2) return 'gold';
  if (idx >= 2) return 'silver';
  return 'bronze';
};
```

W `Achievements.tsx` importuj: `import { AchievementBadge } from '@/components/kinetic/AchievementBadge';` oraz `import { tierForIndex } from '@/lib/achievements-utils';`.

- [ ] **Step 2: Render milestones** — zamień siatkę kafli (ikona+Lock+pasek) na:

```tsx
<div className="grid grid-cols-3 gap-x-2 gap-y-6 sm:grid-cols-4">
  {milestones.map((m, idx, arr) => {
    const catItems = arr.filter((x) => x.category === m.category);
    const catIdx = catItems.indexOf(m);
    const Icon = m.category === 'workouts' ? Trophy : m.category === 'tonnage' ? Dumbbell : Target;
    return (
      <AchievementBadge
        key={`${m.category}-${m.threshold}`}
        label={milestoneLabel(m)}   // istniejąca funkcja/i18n z obecnego renderu — użyj tej samej
        sublabel={m.achieved ? undefined : `${Math.round(m.progress * 100)}%`}
        earned={m.achieved}
        tier={tierForIndex(catIdx, catItems.length)}
        icon={Icon}
        progress={m.achieved ? undefined : m.progress}
      />
    );
  })}
</div>
```

(`milestoneLabel` = dotychczasowy sposób budowania etykiety kafla — przenieś 1:1 z usuwanego kodu; import `Lock` usuń, jeśli nigdzie indziej nieużywany.)

- [ ] **Step 3: Odznaki specjalne** — analogicznie: `computeSpecialBadges` → `AchievementBadge` z `tier="silver"` (jednolity materiał dla specjalnych) i ikonami jak dziś (Sunrise/RotateCcw/Swords/CalendarCheck).
- [ ] **Step 4: Testy + wizualna weryfikacja** — `npm run test`; dev: `/achievements` — ghost sloty widoczne, zdobyte w materiale, zero `Lock`.
- [ ] **Step 5: Commit** — `git commit -m "feat(achievements): odznaki heksagonalne z ghost-slotami (PRO-D T2)"`

---

### Task 3: Poziom z paskiem postępu w Profilu

**Files:**
- Modify: `src/pages/Profile.tsx` (nagłówek, okolice `:322-343` — pod `ProfileHeaderChips`)
- Test: `src/components/__tests__/profile-tier-progress.test.tsx` (albo rozszerzenie istniejącego testu Profilu: `grep -rln "ProfileHeaderChips" src --include=*.test.*`)

**Interfaces:**
- Consumes: `computeTier(totalWorkouts, totalPRs, lang): TierInfo { label, level, progress, next }` (`src/lib/tier.ts:25-46`) — wywołanie już istnieje w `Profile.tsx:66`, pola `progress`/`next` są dziś ignorowane.

- [ ] **Step 1: Implementacja** — pod rzędem chipów w nagłówku Profilu:

```tsx
// tier już policzony wyżej (Profile.tsx:66): const tier = computeTier(completedCount, 0, lang);
{tier.next && (
  <div className="mx-auto mt-3 w-full max-w-[240px]" data-testid="tier-progress">
    <div className="h-1.5 w-full overflow-hidden rounded-full bg-surface-highest">
      <div
        className="h-full rounded-full bg-primary transition-[width]"
        style={{ width: `${Math.round(tier.progress * 100)}%` }}
      />
    </div>
    <p className="mt-1.5 text-center text-[11px] text-muted-foreground">
      {t('profile.tier.next', { next: tier.next })}
    </p>
  </div>
)}
```

Klucze:

```ts
// pl.ts
'profile.tier.next': 'Następny poziom: {next}',
// en.ts
'profile.tier.next': 'Next level: {next}',
```

- [ ] **Step 2: Test** — render Profilu (z mockami jak w istniejących testach Profilu) → `getByTestId('tier-progress')` obecny, szerokość paska odpowiada `progress`. Na poziomie elite (`next === null`) pasek się nie renderuje.
- [ ] **Step 3: Commit** — `git commit -m "feat(profile): pasek postępu poziomu (PRO-D T3)"`

---

### Task 4: Heatmapa konsekwencji w Postępach

**Files:**
- Modify: `src/pages/Achievements.tsx` (nowa sekcja nad milestones)
- Read first: `src/components/TrainingHeatmap.tsx` (komponent istnieje, jest NIEUŻYWANY — sprawdź jego props: `sed -n '1,60p' src/components/TrainingHeatmap.tsx`)

- [ ] **Step 1: Osadzenie** — jeśli `TrainingHeatmap` przyjmuje `workouts: WorkoutSession[]` (Achievements ma je już w zasięgu dla statystyk), dodaj sekcję:

```tsx
<Card>
  <CardContent className="py-5">
    <div className="mb-3 flex items-center gap-2">
      <CalendarCheck className="h-4 w-4 text-primary" />
      <h2 className="text-sm font-bold uppercase tracking-[0.08em]">{t('achievements.consistency')}</h2>
    </div>
    <TrainingHeatmap workouts={workouts} />
  </CardContent>
</Card>
```

Klucze:

```ts
// pl.ts
'achievements.consistency': 'Konsekwencja',
// en.ts
'achievements.consistency': 'Consistency',
```

Jeśli props komponentu różnią się od założenia (inne nazwy/kształt), dostosuj wywołanie do faktycznego interfejsu — bez modyfikacji samego `TrainingHeatmap` poza naprawą ewentualnych błędów typów (komponent nigdy nie był używany, może wymagać drobnej reanimacji; wszelkie zmiany w nim = osobny commit z opisem).

- [ ] **Step 2: Weryfikacja wizualna** — `/achievements`: heatmapa renderuje ukończone treningi, miesiące czytelne, dark mode kontrast OK (tła z `/10`-`/60` krycia primary, nie pełne).
- [ ] **Step 3: Test** — snapshot/RTL: heatmapa obecna przy `workouts.length > 0`, nieobecna (lub pusty stan) przy 0.
- [ ] **Step 4: Commit** — `git commit -m "feat(achievements): heatmapa konsekwencji (PRO-D T4)"`

---

### Task 5: Powiadomienie o zdobytym kamieniu milowym

**Files:**
- Modify: `src/lib/achievements-utils.ts` (nowa czysta funkcja `diffMilestones`)
- Modify: `src/pages/WorkoutDay.tsx` (wywołanie po zakończeniu treningu, obok emisji PR z PRO-B T4)
- Test: `src/lib/__tests__/achievements-diff.test.ts`

**Interfaces:**
- Consumes: `computeMilestones(stats)` (`achievements-utils.ts:229`) i `addInboxItem` (PRO-B). Wymaga PRO-B wdrożonego wcześniej.
- Produces: `diffMilestones(before: Milestone[], after: Milestone[]): Milestone[]` — kamienie świeżo osiągnięte.

- [ ] **Step 1: Failing test**

```ts
// src/lib/__tests__/achievements-diff.test.ts
import { describe, expect, it } from 'vitest';
import { diffMilestones, type Milestone } from '@/lib/achievements-utils';

const m = (threshold: number, achieved: boolean): Milestone =>
  ({ category: 'workouts', threshold, achieved, progress: achieved ? 1 : 0.5 } as Milestone);

describe('diffMilestones', () => {
  it('zwraca tylko świeżo osiągnięte progi', () => {
    const before = [m(10, true), m(25, false)];
    const after = [m(10, true), m(25, true)];
    expect(diffMilestones(before, after).map((x) => x.threshold)).toEqual([25]);
  });
  it('bez zmian: pusta lista', () => {
    const same = [m(10, true), m(25, false)];
    expect(diffMilestones(same, same)).toEqual([]);
  });
});
```

- [ ] **Step 2: Implementacja**

```ts
// achievements-utils.ts — na końcu pliku:
/** Kamienie milowe osiągnięte w after, a nie w before (klucz: category+threshold). */
export const diffMilestones = (before: Milestone[], after: Milestone[]): Milestone[] => {
  const wasAchieved = new Set(
    before.filter((m) => m.achieved).map((m) => `${m.category}:${m.threshold}`),
  );
  return after.filter((m) => m.achieved && !wasAchieved.has(`${m.category}:${m.threshold}`));
};
```

- [ ] **Step 3: Wywołanie w WorkoutDay** — w miejscu finalizacji treningu (obok emisji PR): policz `computeMilestones` na statsach sprzed i po dopisaniu treningu, dla każdego świeżego kamienia:

```tsx
diffMilestones(milestonesBefore, milestonesAfter).forEach((m) => {
  addInboxItem(uid, {
    type: 'badge',
    title: t('inbox.badge.title'),
    body: milestoneLabel(m), // ta sama etykieta co na ekranie Postępów
  });
});
```

Klucze:

```ts
// pl.ts
'inbox.badge.title': 'Nowa odznaka',
// en.ts
'inbox.badge.title': 'New badge',
```

Jeśli statystyki „przed" nie są dostępne w miejscu finalizacji bez dodatkowego odczytu, policz je z już załadowanej listy `workouts` bez bieżącej sesji (filtr po id) — zero nowych odczytów Firestore.

- [ ] **Step 4: Testy zielone + sekwencja** — trening nr 10 (fixture) → inbox dostaje wpis `badge`; trening zwykły → brak wpisu.
- [ ] **Step 5: Commit** — `git commit -m "feat(achievements): powiadomienie o świeżym kamieniu milowym (PRO-D T5)"`

---

### Task 6: Sekcja dumy w Profilu (3 ostatnie odznaki)

**Files:**
- Modify: `src/pages/Profile.tsx` (nowa sekcja między nagłówkiem a „TRENING")

- [ ] **Step 1: Implementacja** — `SectionCard` z rzędem `AchievementBadge size="sm"` dla 3 najwyższych zdobytych kamieni. `Milestone` NIE ma pól tier/icon — zbuduj je tym samym mapowaniem co ekran Postępów (wynieś `tierForIndex` z `Achievements.tsx` do `src/lib/achievements-utils.ts` i importuj w obu miejscach):

```tsx
import { tierForIndex } from '@/lib/achievements-utils';

const CATEGORY_ICONS = { workouts: Trophy, tonnage: Dumbbell, records: Target } as const;

const recentBadges = milestones
  .filter((m) => m.achieved)
  .sort((a, b) => b.threshold - a.threshold)
  .slice(0, 3)
  .map((m) => {
    const catItems = milestones.filter((x) => x.category === m.category);
    return {
      ...m,
      tier: tierForIndex(catItems.indexOf(m), catItems.length),
      icon: CATEGORY_ICONS[m.category],
    };
  });
```

```tsx
<SectionCard label={t('profile.pride.label')}>
  <div className="flex items-center justify-between">
    <div className="flex gap-3">
      {recentBadges.map((b) => (
        <AchievementBadge key={`${b.category}-${b.threshold}`} size="sm"
          label={milestoneLabel(b)} earned tier={b.tier} icon={b.icon} />
      ))}
    </div>
    <button type="button" onClick={() => navigate('/achievements')}
      className="flex items-center gap-1 text-xs font-bold uppercase tracking-wide text-primary">
      {t('profile.pride.all')} <ChevronRight className="h-3.5 w-3.5" />
    </button>
  </div>
</SectionCard>
```

Klucze: `profile.pride.label` = 'Osiągnięcia' / 'Achievements', `profile.pride.all` = 'Wszystkie' / 'All'. Przy zerze zdobytych odznak sekcja się NIE renderuje (żadnych pustych placeholderów w Profilu). Dane: ten sam `workouts`/aggregate co `completedCount` w `Profile.tsx:65` — bez nowych listenerów; jeśli policzenie milestones wymaga pełnej listy treningów, użyj agregatu (workoutCount/tonnage) i renderuj tylko kamienie kategorii `workouts`+`tonnage`.

- [ ] **Step 2: Weryfikacja** — Profil: sekcja z odznakami nad TRENING, klik → Postępy.
- [ ] **Step 3: Commit** — `git commit -m "feat(profile): sekcja dumy z ostatnimi odznakami (PRO-D T6)"`

---

## Wydanie

- [ ] `npm run test && npm run typecheck && npm run lint && npm run build && npm run check:no-emoji`
- [ ] Wizualnie: /achievements (odznaki + heatmapa), /profile (pasek poziomu, sekcja dumy), inbox (wpis badge po treningu-fixture).
- [ ] Checklist wdrożeniowy z CLAUDE.md (web deploy, iOS bump, TestFlight, DECYZJE.md).
