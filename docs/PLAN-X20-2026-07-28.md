# PLAN X20: Naprawa zgłoszeń z TestFlight builda 78 (2026-07-28)

> **Dla agenta wykonawcy:** WYMAGANY SUB-SKILL: `superpowers:executing-plans` lub `superpowers:subagent-driven-development`.
> Numeracja zadań: Z154-Z161 (X19 skończył na Z153).
> Źródło: zgłoszenia usera z realnego użycia builda 78 (screeny 2026-07-28) + weryfikacja root cause w kodzie (4 niezależne audyty, 2026-07-28).

**Goal:** Osiem zgłoszeń domkniętych: (1) analityka nie robi czarnego ekranu po powrocie z tła, (2) push "idź na trening" nie przychodzi w trakcie treningu, (3) apka w 100% po angielsku (nazwy ćwiczeń), (4) timer przerwy włączany/wyłączany przełącznikiem w ustawieniach (default ON), (5) kafle statystyk bez ucinania "...", (6) klawiatura nie zasłania modala edycji profilu, (7) mail tygodniowy z pełnym podsumowaniem zamiast dwóch liczb, (8) usunięcie treningu (z potwierdzeniem) dostępne także z widoku otwartego treningu.

---

## 0. TWARDE ZASADY

1. **Dane usera są święte.** Zero zapisów na produkcyjnych danych. Jedyny zapis "na żywo" to testowy mail digest na adres usera (Z160, krok jawnie oznaczony).
2. **Wątek animacji ćwiczeń NIE należy do X20.** W repo wiszą niezacommitowane zmiany innego wątku: `CONTEXT.md`, `src/components/ExerciseCard.tsx`, `src/lib/exercise-media.ts`, `src/test/exercise-media.test.ts`, folder `animacje-cwiczen/`. Commity X20 stage'ują WYŁĄCZNIE pliki zmienione w ramach X20 (imienne `git add`, nigdy `git add -A`). `ExerciseCard.tsx` jest wspólny (Z157 go dotyka): przed pierwszym commitem dotykającym tego pliku los WIP animacji musi być rozstrzygnięty z userem (osobny commit WIP albo stash). Bez decyzji usera — nie ruszać Z157, reszta zadań może iść.
3. **Reguła 5 CLAUDE.md.** Każda zmiana ma test niezmiennika starego przepływu; sekwencje, nie pojedyncze ekrany. Scenariusz przerwania (plan → wyjście → szybki trening → powrót) obowiązuje przed buildem.
4. **i18n do OBU plików** (`src/i18n/locales/pl.ts` + `en.ts`), kg kanoniczne w Firestore, konwersje tylko w UI.
5. **Timer: decyzja domyślna ON.** User: "działał super". Przełącznik w ustawieniach steruje TYLKO timerem przerwy (+ Watch + wiersze ustawień). EMOM/AMRAP i timer rozgrzewki ZOSTAJĄ wyłączone za osobną flagą buildową — mają tylko `setInterval`, przy zgaszonym ekranie milkną (dług Z10, `docs/AUDYT-FIX-PLAN-2026-06-29.md:87,131`). Włączenie ich bez local notifications naruszałoby zasadę "co się dzieje, gdy ekran gaśnie".
6. **Bundle budget bez podnoszenia limitu** (~20 KB zapasu po X19). `@capacitor/keyboard` (Z159) to głównie kod natywny; `check:bundle-budget` musi przejść.
7. **Deploy:** functions (Z155, Z160) + web + iOS build 79 (bump `CURRENT_PROJECT_VERSION` ×6, `release-ios.sh`, potem OBOWIĄZKOWO `testflight_external.py`). Jeden build iOS na końcu trainu.
8. **Surgical changes.** 7 zgłoszeń = 7 izolowanych zmian, osobne commity `(Z15x)` w opisie, rollback punktowy.

---

## ROOT CAUSE (zweryfikowane w kodzie 2026-07-28)

| # | Zgłoszenie | Root cause | Dowód |
|---|---|---|---|
| Z154 | Czarny ekran analityki po powrocie z tła | Handler `vite:preloadError` w `main.tsx:8-11` robi `preventDefault()` → `__vitePreload` NIE rzuca → catch w `lazyWithRetry` to martwy kod, `lazy` dostaje `undefined` (TypeError) → `requestGuardedReload("chunk")` reloaduje BEZ licznika antypętlowego (guard sessionStorage siedzi w martwym kodzie). 3 zakładki analityki (`Analytics.tsx:39-41`) + `Achievements.tsx:32` + `Measurements.tsx:19` używają gołego `React.lazy`. Zero telemetrii chunków (brak `window.onerror`/`unhandledrejection`). | `src/main.tsx:8-11`, `src/lib/lazy-with-retry.ts:24-36`, `src/lib/pwa-update-guard.ts:58-74`, bundle `ios/App/App/public/assets/index-CqvzJ1AE.js` |
| Z155 | Push "idź na trening" w trakcie treningu | Guard z X18C (`daily-reminder.ts:101-105`) sprawdza `startedAt \|\| completed`, ale klient wysyła `startedAt` TYLKO przy finalnym syncu (`workout-sync-engine.ts:258`, warunek `requiresFinal &&`), a `createWorkoutSession` tworzy dokument bez `startedAt` (`useFirebaseWorkouts.ts:115-126`). Aktywny trening = doc z `completed:false`, bez `startedAt` → push idzie. Test X18C mockował dokument, jakiego klient nigdy nie produkuje (`daily-reminder.test.ts:70-80`). | `functions/src/daily-reminder.ts:101-105,196-209` |
| Z156 | Nazwy ćwiczeń po polsku przy EN | `localizeExerciseName` wołane ad hoc w warstwie prezentacji; 5 ścieżek buduje nazwy własnymi mapami z pominięciem lokalizacji: `weekly-summary.ts` (domyślna zakładka analityki!), `rza-metrics.ts`, `all-time-stats.ts` (ulubione ćwiczenie na dashboardzie), `cycle-insights.ts`/`CycleDetail.tsx:114`, `ExerciseProgressionDialog`. Dodatkowo 12 nazw z `planTemplates.ts` nie ma wpisów w `EXERCISE_NAME_EN` (m.in. gołe "Przysiad ze sztangą") oraz `isBodyweightExercise` dostaje nazwy przetłumaczone → w EN zawsze `false`. | `src/components/analytics/AnalyticsWeeklyTab.tsx:87`, `src/components/RzaMetricsCard.tsx:75`, `src/components/AllTimeStatsSheet.tsx:44`, `src/components/CycleDetail.tsx:114`, `src/data/exercise-i18n.ts:9`, `src/lib/exercise-utils.ts:118-121` |
| Z157 | Timer wyłączony, brak przełącznika | Jedna flaga buildowa `VITE_FEATURE_WORKOUT_TIMERS` gasi naraz: rest timer, EMOM/AMRAP, rozgrzewkę, Watch, wiersze ustawień (12 call-sites, 7 plików). W repo wszędzie OFF (`.env` nie zawiera flagi). Flaga jest już GETTEREM (`feature-flags.ts:20-24`) → warstwa "ustawienie usera" w jednym miejscu obejmie wszystkie call-sites. Rules Firestore zmian nie wymagają. | `src/lib/feature-flags.ts:20-24`, mapa call-sites w raporcie audytu |
| Z158 | Kafle statystyk ucinane "..." | Jedna klasa `truncate` na wierszu wartości kafla, wspólna dla liczb i długich tekstów; sztywny `grid-cols-2` bez kafla pełnej szerokości. | `src/components/AllTimeStatsSheet.tsx:96-103` |
| Z159 | Klawiatura zasłania "Edytuj profil" | `DialogContent` centrowany `top-50%/translate-y-[-50%]` względem layout viewportu; klawiatura iOS nie zmienia `100dvh` ani `safe-area-inset-bottom`. W projekcie ZERO mechanizmów keyboard-aware: brak `@capacitor/keyboard`, `visualViewport`, `env(keyboard-inset-height)`. Ten sam problem mają wszystkie modale z inputami (ExercisePicker, AddCardioDialog, delete-account). Rekomendacja wisiała w audycie 2026-06-10 (`audit/AUDYT_KOMPLETNY_2026-06-10.md:680`), niezrealizowana. | `src/components/ui/dialog.tsx:39`, `src/pages/Profile.tsx:311-327`, `package.json:41-52` |
| Z160 | Mail tygodniowy "porażka" | Szablon = nierozbudowane MVP: 2 kafle, `display:flex` (Gmail/Outlook wycinają → layout w pionie), hardkod PL (mimo `users.language` w bazie), tonaż liczony inną metodą niż w apce (`reps*weight` zamiast `setTonnage`), zawsze kg, brak stopki/preheadera, `w.exercises.reduce` bez guardu (crash z 2026-07-20 → user cicho wypada z wysyłki). Wszystkie dane tygodnia już pobrane (pełne dokumenty `workouts`), gotowe moduły w kliencie (`weekly-summary.ts`, `pr-utils.ts`, `summary-utils.ts`) nie sportowane do functions. | `functions/src/weekly-digest.ts:113-119,139-199,253` |

| Z161 | Brak opcji usunięcia treningu tam, gdzie user jej szuka | Usuwanie z potwierdzeniem ISTNIEJE w Historii (`WorkoutHistory.tsx:359-368` przycisk per trening, `:459-483` AlertDialog, `deleteWorkoutEverywhere` czyści dokument w chmurze + lokalny szkic + kolejkę syncu, `workout-delete.ts:17-46`). Luka: widok otwartego treningu (`WorkoutDay.tsx`, wejście przez "Otwórz trening" albo z Dashboardu) nie ma ŻADNEJ opcji usunięcia (`grep Trash\|deleteWorkout` = 0 trafień). Rules dopuszczają delete własnych `workouts`. | `src/pages/WorkoutHistory.tsx:359,459`, `src/lib/workout-delete.ts:17`, `src/pages/WorkoutDay.tsx` |

Zgłoszenie "usunięcie serii" — user potwierdza, że działa dobrze. BEZ ZMIAN.

---

## FAZA 1: Z154. Analityka bez czarnego ekranu (P1, stabilność)

**Pliki:** Modify: `src/main.tsx`, `src/lib/lazy-with-retry.ts`, `src/lib/pwa-update-guard.ts`, `src/pages/Analytics.tsx:39-41,372-388`, `src/pages/Achievements.tsx:32,324`, `src/pages/Measurements.tsx:19,111`, `src/App.tsx:107-111,279`. Create: `src/lib/global-error-telemetry.ts`. Test: `src/test/lazy-with-retry.test.ts` (nowy).

- [ ] Krok 1: **Testy failing** dla `lazyWithRetry` (`src/test/lazy-with-retry.test.ts`, vitest, mock `requestGuardedReload`):
  - importer odrzuca raz, drugi import się udaje → komponent się ładuje, reload NIE wołany;
  - importer zwraca `undefined`/moduł bez `default` → traktowane jak błąd (dziś: przechodzi dalej i wybucha w React);
  - importer odrzuca zawsze → `requestGuardedReload` wołany raz, przy drugim wywołaniu guard sessionStorage blokuje (rethrow zamiast reloadu).
- [ ] Krok 2: Implementacja `lazy-with-retry.ts`: walidacja kształtu modułu (`if (!mod?.default) throw new Error('chunk-empty:' + key)`), jeden retry w miejscu z ~500 ms backoff PRZED ścieżką reloadu, istniejący guard sessionStorage zostaje. Testy z kroku 1 zielone.
- [ ] Krok 3: `src/main.tsx:8-11` — USUNĄĆ handler `vite:preloadError` (z `preventDefault()` włącznie). Od teraz błąd preloadu RZUCA i dolatuje do `lazyWithRetry`. To odblokowuje cały mechanizm z kroku 2.
- [ ] Krok 4: `pwa-update-guard.ts` `requestGuardedReload`: licznik antypętlowy w sessionStorage (max 2 reloady / 60 s; powyżej → `return false` + telemetria `code:'reload-loop-guard'`). Test jednostkowy w istniejącym pliku testów guarda (albo nowy describe w teście z kroku 1).
- [ ] Krok 5: Goły `lazy(` → `lazyWithRetry(...)`: `Analytics.tsx:39-41` (klucze `lazy-retry:analytics-weekly/-charts/-strava`), `Achievements.tsx:32`, `Measurements.tsx:19`.
- [ ] Krok 6: `Analytics.tsx:372-388` — każdy `TabsContent` owinięty `<ErrorBoundary uid={uid}>` z widocznym fallbackiem (tekst + przycisk "Spróbuj ponownie" resetujący boundary przez zmianę `key`). `App.tsx:279` — top-level boundary dostaje `uid` (crash przed zalogowaniem: raport pomijamy, rules wymagają auth).
- [ ] Krok 7: Nowy `src/lib/global-error-telemetry.ts`: `window.onerror` + `unhandledrejection` → `reportClientError(uid, { code: 'window-error' | 'unhandled-rejection', ... })`; init w `App.tsx` z uid z auth (raport tylko gdy uid znany; limit sesyjny z `error-telemetry.ts` obowiązuje).
- [ ] Krok 8: Kosmetyka fallbacków: `AppLoader` (`App.tsx:107-111`) `min-h-screen` → `min-h-[50vh]` (spinner w viewporcie przy przescrollowanej stronie); `Measurements.tsx:111` `fallback={null}` → `<div className="h-[220px]" />`.
- [ ] Krok 9: `npm run test` + `typecheck` + `lint`; commit `fix(analytics): dziala retry lazy chunkow, guard antypetlowy reloadu, ErrorBoundary per zakladka, telemetria window-error (Z154)`.

**Weryfikacja na urządzeniu (checklist przed buildem):** analityka → tło (zgaszony ekran 2+ min) → powrót → zakładki Tygodnie/Wykresy renderują się; tryb samolotowy → wejście w nieodwiedzoną zakładkę → widoczny fallback z "Spróbuj ponownie" zamiast czerni.

## FAZA 2: Z155. Push nie przychodzi w trakcie treningu (P1)

**Pliki:** Modify: `src/hooks/useFirebaseWorkouts.ts:115-126`, `src/lib/workout-sync-engine.ts:258`, `functions/src/daily-reminder.ts:101-105`. Test: `functions/src/daily-reminder.test.ts`, istniejące testy sync engine.

- [ ] Krok 1: **Test failing (functions):** w `daily-reminder.test.ts` przypadek `getTodayWorkout: async () => ({ completed: false })` (bez `startedAt` — tak wygląda REALNY dokument aktywnego treningu) → oczekiwane: `skippedActive`, push NIE wysłany. Dziś ten test padnie — to jest luka X18C.
- [ ] Krok 2: Fix guarda `daily-reminder.ts:102`: `if (todayWorkout) { skippedActive += 1; return; }` — dokument `workouts` na dziś powstaje wyłącznie po starcie treningu, samo istnienie = user już dziś zaczął/skończył. Test z kroku 1 zielony; istniejące przypadki (null → wysyłka, completed → skip) zostają zielone.
- [ ] Krok 3: **Klient — `startedAt` od pierwszego zapisu.** (a) `useFirebaseWorkouts.ts` `createWorkoutSession`: dodać `startedAt: Date.now()` do payloadu tworzenia. (b) `workout-sync-engine.ts:258`: `...(requiresFinal && draft.startedAt ? ...)` → `...(draft.startedAt ? { startedAt: draft.startedAt } : {})` — checkpointy też niosą `startedAt`. Testy: istniejący test sync engine + asercja, że payload checkpointu zawiera `startedAt` gdy draft go ma.
- [ ] Krok 4: **Weryfikacja rules:** sprawdzić w `firestore.rules`, czy walidacja CREATE dokumentu `workouts` (jeśli używa `hasOnly`) dopuszcza `startedAt` (update dopuszcza — final sync już go pisze). Jeśli nie — dopisać do listy + `npm run test:rules` + deploy rules razem z bramkami.
- [ ] Krok 5: `cd functions && npm test`; commit `fix(reminder): pomijaj push gdy sesja dnia istnieje; startedAt od utworzenia sesji i checkpointow (Z155)`.

**Znany margines:** okno między startem treningu a pierwszym checkpointem online (do 5 min / offline) nadal bez dokumentu → push może przejść. Akceptowalne; domknięcie wymagałoby wcześniejszej promocji sesji provisional (poza zakresem X20, wpis do TECH DEBT w PLAN.md).

## FAZA 3: Z156. Apka w 100% po angielsku — nazwy ćwiczeń (P2)

**Pliki:** Modify: `src/data/exercise-i18n.ts`, `src/lib/weekly-summary.ts`, `src/components/analytics/AnalyticsWeeklyTab.tsx`, `src/components/RzaMetricsCard.tsx`, `src/components/AllTimeStatsSheet.tsx:44`, `src/components/CycleDetail.tsx:114`, `src/components/ExerciseProgressionDialog.tsx`, `src/pages/Achievements.tsx:598-601`, `src/pages/ExerciseDetail.tsx:98`, `src/components/analytics/AnalyticsChartsTab.tsx:217-232`, `src/lib/exercise-utils.ts:118-121`. Test: `src/test/exercise-i18n-coverage.test.ts` (nowy), `src/test/weekly-summary.test.ts`.

- [ ] Krok 1: **Test inwentarzowy (failing):** nowy `exercise-i18n-coverage.test.ts` — każda nazwa ćwiczenia użyta w `defaultPlan` (`data/trainingPlan.ts`) i we WSZYSTKICH szablonach `planTemplates.ts` ma wpis w `EXERCISE_NAME_EN` (albo jest identyczna w EN). Oczekiwane: FAIL, 12 braków: `Przysiad ze sztangą, Przysiady bułgarskie, Wyciskanie sztangi na ławce poziomej, Rozpiętki na ławce poziomej, Wyciskanie hantli siedząc, Martwy ciąg ze sztangą, Wiosłowanie hantlą w opadzie, Pompki na poręczach, Wyciskanie hantla po skosie w górę, OHP, Uginania łokci ze sztangą stojąc, Uginania młotkowe hantlą`.
- [ ] Krok 2: Dopisać 12 tłumaczeń do `EXERCISE_NAME_EN` (np. `'Przysiad ze sztangą': 'Barbell Squat'`, `'OHP': 'Overhead Press'`, `'Pompki na poręczach': 'Dips'` itd.). Test z kroku 1 zielony. Ten test zostaje na stałe — blokuje przyszłe luki w mapie.
- [ ] Krok 3: `weekly-summary.ts` — `buildLocalWeeklySummaries(...)` dostaje parametr `lang`; PR-y pushowane jako `localizeExerciseName(name, lang)`. Wywołanie w `AnalyticsWeeklyTab.tsx:26-29` przekazuje `lang` (jest w scope, linia 19). Test jednostkowy: summary w `lang:'en'` zwraca EN nazwę PR.
- [ ] Krok 4: Punktowe lokalizacje w renderze: `AllTimeStatsSheet.tsx:44` → `value: localizeExerciseName(stats.favoriteExercise.name, lang)` (`lang` już jest, linia 29); `CycleDetail.tsx:114` → `localizeExerciseName(pr.exerciseName, lang)` (import już jest — niespójność wewnątrz jednego pliku, linia 142 robi to poprawnie); `RzaMetricsCard.tsx:75` → lokalizacja przy renderze.
- [ ] Krok 5: **Kontrakt `ExerciseProgressionDialog`: prop = nazwa KANONICZNA PL** (dialog i tak szuka po PL w `exerciseLibrary`, linie 55-57), lokalizacja do wyświetlenia WEWNĄTRZ dialogu (`:107,127,184`). Poprawić wywołania: `RzaMetricsCard.tsx:72,88` (już przekazuje kanoniczną — OK po zmianie), `Achievements.tsx:598` i `ExerciseDetail.tsx:98` (dziś przekazują przetłumaczoną → przekazać kanoniczną). Komentarz kontraktu przy propsie.
- [ ] Krok 6: **`isBodyweightExercise` przyjmuje kanoniczną PL** (komentarz kontraktu w `exercise-utils.ts:118`). Poprawić wywołania, które dziś podają nazwę przetłumaczoną: `AnalyticsChartsTab.tsx:232` (trzymać `canonicalName` obok zlokalizowanej z linii 217), `Achievements.tsx:601`, `RzaMetricsCard.tsx:91`. Efekt uboczny naprawiony: wykresy ćwiczeń bodyweight w EN znów pokazują reps, nie kg.
- [ ] Krok 7: **Test sekwencji "przełącz język":** vitest render `AnalyticsWeeklyTab` + `AllTimeStatsSheet` z workoutem zawierającym "Przysiad ze sztangą" w `lang:'en'` → widoczne "Barbell Squat", zero polskich znaków diakrytycznych w nazwach. `npm run test` + `typecheck` (i18n w obu plikach locales!) + `lint`; commit `fix(i18n): nazwy cwiczen lokalizowane w analityce, dashboardzie, cyklach i dialogu progresji; 12 brakow w mapie EN; kontrakt canonical-PL (Z156)`.

## FAZA 4: Z157. Timer przerwy: przełącznik w ustawieniach, default ON (P2)

⚠️ Dotyka `ExerciseCard.tsx` — najpierw rozstrzygnięcie WIP animacji (twarda zasada 2).

**Pliki:** Create: `src/lib/workout-timers-setting.ts`. Modify: `src/lib/feature-flags.ts`, `src/vite-env.d.ts`, `src/components/ExerciseCard.tsx:312,897,1258`, `src/components/WarmupRoutineDialog.tsx:49,86,146`, `src/pages/Profile.tsx` (sekcja Preferencje treningowe), `src/i18n/locales/pl.ts` + `en.ts`. Test: `src/test/feature-flags.test.ts` (nowy), `e2e/exercise-card-v3.spec.ts:248-267`.

- [ ] Krok 1: Nowy `src/lib/workout-timers-setting.ts` wzorem `keep-awake.ts:17-31`: klucz `fittracker_workout_timers_v1`, `readWorkoutTimersSetting(): boolean | null` (null = nie ustawiono), `setWorkoutTimersEnabled(v: boolean)`, try/catch na localStorage. Test jednostkowy get/set/null.
- [ ] Krok 2: **Test failing precedencji flagi:** `FEATURE_FLAGS.workoutTimers` = `e2eOverride ?? userSetting ?? default ON`. Przypadki: brak wszystkiego → `true`; `setWorkoutTimersEnabled(false)` → `false`; e2e override wygrywa z ustawieniem.
- [ ] Krok 3: Implementacja w `feature-flags.ts`: getter `workoutTimers` → `e2eOverride('workoutTimers') ?? readWorkoutTimersSetting() ?? (import.meta.env.VITE_FEATURE_WORKOUT_TIMERS !== 'false')`. **Nowy getter `intervalTimers`** (EMOM/AMRAP + rozgrzewka): `e2eOverride('intervalTimers') ?? import.meta.env.VITE_FEATURE_INTERVAL_TIMERS === 'true'` (default OFF, twarda zasada 5). Typ w `vite-env.d.ts`.
- [ ] Krok 4: Przełączyć call-sites EMOM/AMRAP i rozgrzewki na `intervalTimers`: `ExerciseCard.tsx:312` (intervalSpec), `:897` (chip), `:1258` (render IntervalTimer), `WarmupRoutineDialog.tsx:49,86,146`. Pozostałe call-sites (`ExerciseCard.tsx:436,1039` rest timer, hooki Watch `useWatchWorkoutSync.ts:72-73`, `useWatchPlanPreview.ts:50-51,92-93`, wiersze w `Profile.tsx:193-206,235-237`) zostają na `workoutTimers` — automatycznie honorują toggle przez getter, bez zmian w tych plikach. Przy okazji usunąć martwe importy `FEATURE_FLAGS` z `Dashboard.tsx:33` i `WorkoutDay.tsx:66` (to NASZE osierocone importy w obszarze zmiany).
- [ ] Krok 5: UI: w `Profile.tsx`, sekcja "Preferencje treningowe", NAD wierszem "Domyślny czas odpoczynku": `SettingRow` z `Switch` (wzorzec `NotificationSettings.tsx:53-71` bez części Firestore): label i18n `profile.restTimerToggle` = "Timer przerwy" / "Rest timer", opis `profile.restTimerToggleDesc` = "Odliczanie przerwy po odhaczeniu serii" / "Countdown after checking off a set". `onCheckedChange` → `setWorkoutTimersEnabled(v)` + lokalny state (wiersze czasu odpoczynku i dźwięku chowają się natychmiast, bo warunkowane tym samym getterem + re-render ze state). Persystencja per urządzenie (localStorage), świadomie BEZ mirrora do Firestore (jak keep-awake).
- [ ] Krok 6: Testy do przepisania: `e2e/exercise-card-v3.spec.ts:248-267` ("rest timer is globally unavailable...") → wyłączenie przez `localStorage['fittracker_workout_timers_v1']='false'` w `addInitScript` zamiast braku flagi env; describe "Pasek przerwy" (`:355-365`) — override e2e zostaje, działa bez zmian. Sprawdzić mock w `src/test/rest-timer-controller.test.tsx:33` (kształt się nie zmienia — getter dalej zwraca boolean).
- [ ] Krok 7: `npm run test` + e2e:mock + `typecheck` + `lint`; commit `feat(timers): timer przerwy z przelacznikiem w ustawieniach (default ON); EMOM/rozgrzewka za osobna flaga buildowa (Z157)`.

**Weryfikacja na urządzeniu:** odhacz serię → RestBar startuje; zgaś ekran → local notification + gong na deadline; wyłącz w Profilu → odhaczenie serii NIE startuje timera; włącz ponownie → działa. Konsekwencja web: po deploy timer domyślnie ON także na webie (domyka wiszącą decyzję "flaga web" z X18).

## FAZA 5: Z158. Kafle statystyk bez "..." (P3)

**Pliki:** Modify: `src/components/AllTimeStatsSheet.tsx:37-54,96-103`. Test: `src/test/all-time-stats-sheet.test.tsx` (nowy lub istniejący render test).

- [ ] Krok 1: **Test failing (render):** sheet z `favoriteExercise: { name: 'Wyciskanie hantla po skosie w górę', sessions: 12 }` i `firstWorkoutDate` → pełny tekst w DOM, element wartości BEZ klasy `truncate`, kafel z `col-span-2`.
- [ ] Krok 2: Implementacja: tablica `tiles` dostaje pola `wide?: boolean; text?: boolean` (ulubione ćwiczenie + "Trenujesz od" → `wide: true, text: true`); render:
  ```tsx
  <div key={label} className={cn('rounded-xl bg-muted/40 px-3 py-2.5', wide && 'col-span-2')}>
    <p className="text-[10px] font-bold uppercase tracking-[0.14em] text-muted-foreground/70">{label}</p>
    <p className={cn('text-lg font-bold', text ? 'break-words leading-tight' : 'truncate tabular-nums')}>{value}</p>
  </div>
  ```
  Kafle liczbowe bez zmian (truncate + tabular-nums zostaje). Sheet ma `overflow-y-auto` — wzrost pionowy bezpieczny.
- [ ] Krok 3: `npm run test`; commit `fix(stats): kafle tekstowe pelna szerokosc + zawijanie zamiast wielokropka (Z158)`.

Uwaga kolejności: Z156 krok 4 zmienia linię 44 tego samego pliku (lokalizacja nazwy) — wykonywać FAZY 3 i 5 sekwencyjnie, nie równolegle.

## FAZA 6: Z159. Klawiatura nie zasłania modali (P2, zmiana natywna)

**Pliki:** Modify: `package.json` (+`@capacitor/keyboard`), `capacitor.config.ts`, `src/components/ui/dialog.tsx:39`, `src/components/ui/alert-dialog.tsx:37`, `src/main.tsx` lub `src/App.tsx` (init). Create: `src/lib/keyboard-inset.ts`. Test: `src/test/keyboard-inset.test.ts` (nowy).

- [ ] Krok 1: `npm i @capacitor/keyboard`; w `capacitor.config.ts` plugins: `Keyboard: { resize: 'none' }` — ŻADNEJ zmiany globalnego layoutu (resize webview wywróciłby fixed bottom bary WorkoutDay; reguła 5 CLAUDE.md). ⚠️ Po `npm i` nowej zależności natywnej: `pkill -f vite` + wyczyść `node_modules/.vite` przed e2e (reguła 9 CLAUDE.md).
- [ ] Krok 2: Nowy `src/lib/keyboard-inset.ts`: na natywnym iOS `Keyboard.addListener('keyboardWillShow', e => root.style.setProperty('--keyboard-inset', `${e.keyboardHeight}px`))` + `keyboardWillHide` → `0px`; na webie fallback z `visualViewport` (`resize` → inset = `max(0, innerHeight - vv.height)`); `initKeyboardInset()` idempotentne, wołane raz przy starcie appki. Test jednostkowy webowego fallbacku (mock visualViewport).
- [ ] Krok 3: `dialog.tsx:39` i `alert-dialog.tsx:37` — centrowanie względem WIDOCZNEGO viewportu:
  - `top-[50%]` → `top-[calc((100dvh-var(--keyboard-inset,0px))/2)]`
  - `max-h-[calc(100dvh_-_...)]` → dodatkowo odjąć `var(--keyboard-inset,0px)`
  - dodać `transition-[top] duration-200` (płynne podnoszenie).
  Jedna zmiana naprawia WSZYSTKIE dialogi z inputami (profil, ExercisePicker, AddCardioDialog, delete-account, notatki w ExerciseCard).
- [ ] Krok 4: Bramki: `npm run build` + `check:bundle-budget` (plugin ~1-2 KB JS) + `build:mobile` + `check:dist-smoke`; e2e:mock pełne (po restarcie vite z kroku 1).
- [ ] Krok 5: Commit `fix(ui): dialogi centrowane wzgledem widocznego viewportu przy klawiaturze iOS (@capacitor/keyboard, resize none) (Z159)`.

**Weryfikacja na urządzeniu (obowiązkowa — jsdom tego nie pokryje):** Profil → Edytuj profil → tap w NAZWA → tytuł, pole i ZAPISZ w całości nad klawiaturą; ExercisePicker (search z autoFocus); delete-account; notatka w ExerciseCard podczas treningu; obrót zamknij/otwórz klawiaturę ×3 (brak "pływania" modala).

## FAZA 7: Z160. Mail tygodniowy z prawdziwym podsumowaniem (P2, backend)

**Pliki:** Create: `functions/src/weekly-digest-html.ts`, `functions/src/weekly-digest-stats.ts`. Modify: `functions/src/weekly-digest.ts`. Test: `functions/src/weekly-digest-html.test.ts`, `functions/src/weekly-digest-stats.test.ts` (nowe).

- [ ] Krok 1: **Nowy moduł statystyk `weekly-digest-stats.ts`** (czyste funkcje, testowalne bez Firestore) — port sprawdzonej logiki klienta:
  - `setTonnage` z `src/lib/summary-utils.ts:28-36` (serie czasowe/dystansowe, guard na `exercises` nie-tablicę — crash 2026-07-20 nie może cicho wykluczać usera z wysyłki);
  - `computeWeekStats(workouts)`: sesje, serie robocze, powtórzenia, tonaż kg, czas (`durationSec ?? completedAt-startedAt`), top 3 ćwiczenia wg tonażu (nazwy ze snapshotów `ex.name`);
  - `detectNewPRs` port z `src/lib/pr-utils.ts:156` (PR-y tygodnia względem historii);
  - `compareWeeks(current, previous)`: delty sesji/tonażu.
  Testy jednostkowe każdej funkcji, w tym zgodność tonażu z metodą apki i guard na uszkodzone dokumenty.
- [ ] Krok 2: **Nowy builder `weekly-digest-html.ts`:** `buildWeeklyDigest({ stats, prs, comparison, strava, lang, unit, displayName, range })` → `{ subject, html }`.
  - i18n PL/EN w module (wzorzec `functions/src/email-templates.ts`: typ `Lang`, `esc()`, `APP_DEEP_LINK`);
  - layout WYŁĄCZNIE `<table>` (zero `display:flex` — Gmail/Outlook), preheader, sekcje: nagłówek z zakresem dat → siatka kafli (treningi, tonaż w jednostce usera, serie, powtórzenia, czas na siłowni) → "vs poprzedni tydzień" (▲/▼) → PR-y tygodnia (lista, nazwy ćwiczeń przez port `localizeExerciseName` przy `lang==='en'`) → top 3 ćwiczenia → sekcja biegowa (istniejące dane Strava) → CTA (deep link + fallback web) → stopka: "Wyłączysz w aplikacji: Ustawienia → Powiadomienia";
  - jednostki wg `preferences.unit` (port `formatTonnage` z `src/lib/units.ts`).
  Testy: brak `display:flex` w wyniku; wariant PL i EN (tytuły sekcji); escaping nazwy ćwiczenia z `<`; kg vs lbs; pusty tydzień Strava → sekcja pominięta.
- [ ] Krok 3: `weekly-digest.ts` — spięcie: `select(...)` rozszerzyć o `"language", "displayName", "preferences"` (+ typ `DigestUser`, linie 41-46); druga kwerenda o poprzedni tydzień (wzorzec `Promise.all` z linii 82-85) + kwerenda historii do PR-ów (pełne dokumenty sprzed `weekStart`; przy obecnej skali userów akceptowalne — odnotować koszt w komentarzu); wywołanie buildera zamiast inline template. Temat: `💪 {N} treningów, {tonaż} — Twój tydzień {zakres}` / EN odpowiednik.
- [ ] Krok 4: `cd functions && npm test` + `npm run build` (tsc). Commit `feat(digest): pelne podsumowanie tygodnia — statystyki, PRy, porownanie WoW, i18n PL/EN, jednostki usera, szablon table-based (Z160)`.
- [ ] Krok 5: Deploy `firebase deploy --only functions` (razem z Z155). **[WYMAGA POTWIERDZENIA USERA]** wysyłka testowa digestu na `g.jasionowicz@gmail.com` (functions:shell / ręczny trigger) → user ocenia szablon w realnej skrzynce (Gmail iOS + desktop) PRZED poniedziałkową wysyłką automatyczną.

## FAZA 8: Z161. Usuwanie treningu z widoku treningu, z potwierdzeniem (P2)

**Stan zastany (nie ruszać):** Historia MA pełny przepływ usuwania: przycisk per trening → `AlertDialog` z opisem dnia i daty → `deleteWorkoutEverywhere` (dokument + szkic IDB + kolejka syncu) → toast. Z161 wystawia TĘ SAMĄ ścieżkę w `WorkoutDay` dla zapisanej sesji.

**Pliki:** Modify: `src/pages/WorkoutDay.tsx`, `src/i18n/locales/pl.ts` + `en.ts` (tylko jeżeli potrzebny nowy klucz; preferuj reuse `history.delete*`). Test: `e2e/workout-delete-from-day.spec.ts` (nowy).

- [ ] Krok 1: **Test e2e (mock) failing:** seed zakończonego treningu (`fittracker_e2e_workouts`) → wejście w `/workout/:dayId?date=...&session=...` → akcja "Usuń trening" → dialog potwierdzenia widoczny (tytuł + dzień + data) → "Anuluj" nic nie zmienia → ponownie → potwierdź → redirect do `/history`, trening zniknął z listy i z `fittracker_e2e_workouts`. Wzorce: `data-testid="history-delete-dialog"` z `WorkoutHistory.tsx:460`.
- [ ] Krok 2: Implementacja w `WorkoutDay.tsx`: dla ZAPISANEJ sesji (jest `sessionId` remote/istniejący dokument; widok zakończonego treningu) dodać akcję "Usuń trening" (destructive, ikona `Trash2`) w istniejącym menu akcji nagłówka; jeśli widok zakończonej sesji menu nie ma, przycisk `variant="ghost"` destructive na dole podsumowania. `AlertDialog` 1:1 wzorem `WorkoutHistory.tsx:459-483` (reuse kluczy `history.deleteTitle/deleteDesc/delete`), po sukcesie `deleteWorkoutEverywhere(uid, sessionId)` → toast `history.deleted` → `navigate('/history')`.
- [ ] Krok 3: **Niezmienniki (reguła 5 + 6 CLAUDE.md):** (a) akcja NIE pokazuje się dla treningu W TOKU ani niezapisanego szkicu (aktywna sesja ma własną ścieżkę odrzucenia; przypadkowe usunięcie sesji w trakcie = utrata serii); (b) usunięcie z `WorkoutDay` przechodzi przez `deleteWorkoutEverywhere`, NIGDY przez goły `deleteDoc` (inaczej szkic wskrzesi trening — komentarz w `workout-delete.ts:8-10`); (c) test niezmiennika: wejście w AKTYWNY trening nie renderuje akcji usuwania.
- [ ] Krok 4: `npm run test` + nowy spec e2e + `typecheck` + `lint`; commit `feat(workout): usuwanie zapisanego treningu z widoku treningu, z potwierdzeniem (Z161)`.

**Weryfikacja na urządzeniu:** otwórz wczorajszy trening → Usuń → potwierdzenie → znika z historii i ze statystyk (kafle dashboardu przeliczone); trening w toku nie ma opcji usunięcia.

---

## BRAMKI (checklist przed wdrożeniem — po WSZYSTKICH fazach)

- [ ] `npm run test` (wszystkie zielone), `npm run typecheck`, `npm run lint`, `npm run build`
- [ ] `cd functions && npm test` (Z155 + Z160)
- [ ] `check:bundle-budget` (limit bez zmian; Z159 dodaje plugin)
- [ ] `build:mobile` + `check:dist-smoke`; `npm run build` (web) + `check:dist-offline`; `e2e:mock` — PO restarcie vite (reguła 9: po `npm i @capacitor/keyboard` obowiązkowo `pkill -f vite` + czyszczenie `node_modules/.vite`)
- [ ] Scenariusz **background/resume** na realnym iPhone (Z154, Z157): zgaś ekran na analityce i na treningu z odpalonym timerem, odczekaj, wróć
- [ ] Scenariusz **przerwania** (reguła 5): plan → wyjście → szybki trening → powrót do planu (wszystkie ćwiczenia na miejscu) → zakończenie → sync
- [ ] Scenariusz **klawiatury** (Z159) na realnym iPhone: wszystkie modale z inputami
- [ ] Scenariusz **języka** (Z156): przełącz EN → dashboard (ulubione ćwiczenie), analityka Tygodnie + Wykresy + Maszyna, szczegóły cyklu — zero polskich nazw
- [ ] `firebase deploy --only functions` (+ rules jeżeli Z155 krok 4 tego wymaga)
- [ ] Commit + push na `main`; **web:** `npm run deploy` (gh-pages) + weryfikacja hasha na live
- [ ] **iOS:** bump `CURRENT_PROJECT_VERSION` → **79** (6 wystąpień), `scripts/release-ios.sh "Analityka bez czarnego ekranu, timer przerwy z przełącznikiem, EN w analityce, modale nad klawiaturą, kafle bez ucinania"`, potem `uv run --with "pyjwt[crypto]" --with requests scripts/testflight_external.py 79 --whats-new "..."`
- [ ] Wpis do `DECYZJE.md` (per zadanie: co, root cause, weryfikacja) + TECH DEBT w `PLAN.md`: (a) promocja sesji provisional przed pierwszym checkpointem (margines Z155), (b) EMOM/AMRAP + rozgrzewka bez local notifications (warunek zdjęcia flagi `intervalTimers`)

## KOLEJNOŚĆ I ZALEŻNOŚCI

- Z154 i Z155 (P1) najpierw — niezależne od siebie, mogą iść równolegle.
- Z156 przed Z158 (oba dotykają `AllTimeStatsSheet.tsx` — sekwencyjnie).
- Z157 czeka na decyzję usera ws. WIP animacji w `ExerciseCard.tsx` (twarda zasada 2). [ROZSTRZYGNIĘTE 2026-07-28: WIP zacommitowany, Z157 odblokowane]
- Z161 po Z157 (oba dotykają `WorkoutDay.tsx` — sekwencyjnie, choć zakresy się nie nakładają).
- Z159 jako ostatnia zmiana klienta (nowa zależność natywna → restart vite → pełne e2e raz).
- Z160 niezależny (functions), deploy razem z Z155.
