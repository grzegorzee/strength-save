# AUDYT i18n: skan całej aplikacji (2026-07-28)

> Cel: znaleźć wszystkie miejsca, gdzie tekst widoczny dla usera nie idzie przez system tłumaczeń albo miesza języki. Kontekst: apka będzie tłumaczona na kolejne języki (np. hiszpański).
> Zakres skanu: 420 plików `.ts/.tsx` w `src/`, 36 w `functions/src/`, 9 `.mc` w `garmin/source/`, 7 `.swift` (Watch), HTML/manifest.
> Plan naprawy: `docs/PLAN-X21-2026-07-28.md` (Z162-Z168).

## Stan bazowy: DOBRY

- 1733 klucze w `src/i18n/locales/pl.ts` i `en.ts`, parytet wymuszony typem (`en: Record<keyof typeof pl, string>`).
- Warstwa `lib/` używa `translate(lang, key)`, komponenty `useTranslation()`.
- Resolver nazw ćwiczeń (`exercise-name-resolver.ts`) + helpery `exercise-i18n.ts`, `plan-i18n.ts`. PL kanoniczne w Firestore, tłumaczone tylko wyświetlanie.
- Toasty, daty, share, PDF: zlokalizowane. Test `exercise-i18n-coverage.test.ts` pilnuje pokrycia nazw ćwiczeń.

## Znalezione problemy

### A. Rozgrzewka (zgłoszenie usera)

1. **Odhaczenia znikają**: `WarmupRoutineDialog.tsx:63-68` czyści `checked` przy każdym zamknięciu dialogu, a stan (`:32`, `useState<Set<number>>`) nie jest nigdzie persystowany (zero IndexedDB/localStorage/Firestore). Dodatkowo klucz = indeks pozycji (niestabilny przy zmianie focusu). Brak jakichkolwiek testów tego dialogu.
2. **Mieszane nazwy w PL**: `pl.ts:1438-1440, 1451-1452, 1457`: `'Jumping Jacks'`, `'Kręcenie biodrami (Hip Circles)'`, `'Cat-Cow (na czworakach)'`, `"Child's Pose"`, `'Pigeon Pose'` w polskim słowniku.

### B. Mieszanie PL/EN widoczne już dziś w trybie EN

| Miejsce | Problem |
|---|---|
| `src/types/strava.ts:56-60` | Nazwy stref HR po polsku (`Regeneracja`, `Próg`...), renderowane wprost w `HRZoneDistribution.tsx:38` i `StravaActivityDetail.tsx:164` |
| `src/lib/strava-utils.ts:72-76` | `getWeekLabel`: `'Ten tydz.'`, `'Ost. tydz.'`, `'{n} tyg. temu'` hardcoded PL |
| `src/lib/strava-utils.ts:2,284` | `date-fns/locale/pl` na sztywno: nazwy miesięcy zawsze po polsku |
| `src/lib/plan-i18n.ts` | Brak `tpl-rza-3` w `PLAN_DESC`: polski opis planu RZA V-Taper dla usera EN (`planTemplates.ts:554`) |
| `src/hooks/useWatchPlanPreview.ts:42-58, 84-105` | Payload bez pola `lang`: podgląd planu na Apple Watch zawsze po polsku (`useWatchWorkoutSync.ts:73` przekazuje `lang` tylko w aktywnym treningu) |
| `pl.ts:1465-1469` | Tiery osiągnięć (`Newcomer`, `Rookie`, `Pro Tier`...) po angielsku w obu językach (branding? decyzja usera) |

### C. Panel admina: największe skupisko (7 plików, ~70 stringów PL poza t())

Pliki mają po 30-50 wywołań `t()`, ale równolegle dziesiątki literałów PL (niespójność w obrębie jednego ekranu):

- `src/pages/admin/AdminDashboard.tsx` (~30: dialogi potwierdzeń, toasty, kafle "Puls aplikacji", filtry, dialogi maila/cohort/zawieszenia, linie 479-960)
- `src/components/admin/AdminCommsCard.tsx` (10), `src/pages/admin/UsersActivityTable.tsx` (6), `src/components/admin/AdminFeatureFlagsCard.tsx` (5), `src/components/admin/AdminUserLogs.tsx` (3), `src/pages/admin/admin-user-types.ts:6`, `src/lib/admin-audit.ts:50,54` (renderowane w `AdminUserDetail`)

### D. Hardcodowane EN widoczne dla usera

- `src/components/ui/dialog.tsx:49`, `sheet.tsx:62`: sr-only `Close`
- `src/components/AppNavigation.tsx:134`: `aria-label` `Expand/Collapse navigation`
- `src/pages/Settings.tsx:341,353`: label + aria `Max HR`
- `src/lib/cycle-actions.ts:89,122,137`: `'Active cycle was not created'`, `'Could not complete onboarding'` (pokazywane w Dashboard/Cycles/NewPlan/Onboarding)
- Nieużywane w apce (zero importów, zostawione): `ui/pagination.tsx`, `ui/breadcrumb.tsx`, `ui/sidebar.tsx`

### E. Powierzchnie serwerowe (functions)

- `functions/src/daily-reminder.ts:114-115`: push "Czas na trening" w 100% PL, zero i18n mimo `users.language` w bazie; `${focus}` kanoniczny PL
- `functions/src/email-templates.ts:90-104` + `registration.ts:780`: mail zaproszenia w 100% PL, bez parametru `lang` (pozostałe maile: verification/welcome/accessChanged/digest mają PL/EN)
- `functions/src/registration.ts:359-362`, `admin-api.ts:228,247`: komunikaty błędów PL w HttpsError
- `functions/src/garmin-day.ts:150,174`: kanoniczne PL nazwy ćwiczeń i focus na zegarku Garmin (niezależnie od języka)

### F. Zegarki i statyka

- Apple Watch: `WorkoutDay.tsx:1596-1598` wysyła `dayName`/`focus`/nazwy ćwiczeń kanoniczne PL (EN user: angielski chrome + polskie dane); `ios/App/WatchWidgets/StrengthWidgets.swift:46` opis widgetu PL hardcoded
- Garmin: `resources/strings` EN + `resources-pol` PL poprawnie; ES = nowy katalog `resources-spa`
- `index.html` (lang="pl", title, description), manifest PWA w `vite.config.ts:59-61`, `public/strava-callback.html`: jeden język na sztywno
- `landing/legal/`: tylko `terms/privacy` w PL i EN

### G. Architektura jest DWUJĘZYCZNA, nie n-językowa

Komentarz w `i18n/index.ts` ("reszta zadziała automatycznie po dodaniu locale") jest nieprawdziwy. Twarde `pl|en` w ~20 miejscach:

- `src/i18n/index.ts:35` (`dateLocale` binarny), `src/pages/Profile.tsx:260-261` (selector języka hardcoded, nie czyta `LANGUAGES`)
- `src/data/exercise-i18n.ts:534,549,569`, `src/data/exercise-details.ts:1742`, `src/lib/plan-i18n.ts:47-138` (wzorzec `lang === 'en' ? EN : PL`; ES dostałby PL)
- Typy: `src/lib/user-profile.ts:69`, `src/lib/registration-api.ts:13-14,64` (`'pl' | 'en'` union)
- `functions/src/email-templates.ts:5` (`type Lang = "pl" | "en"`), `weekly-digest.ts:69,154`, `weekly-digest-html.ts:31,52`
- `src/components/AllTimeStatsSheet.tsx:53`, `src/pages/ExerciseDetail.tsx:38`, `src/pages/Paywall.tsx:128` (legal `-pl` sufiks)
- Watch: `WorkoutModels.swift:52-78` (własny `enum L10n` z parami PL/EN)

### H. Koszt dodania hiszpańskiego (dane)

Poza `es.ts` (1733 klucze) hiszpański wymaga hiszpańskich nakładek danych: `EXERCISE_NAME_EN` (~330 nazw), `EXERCISE_INSTRUCTION_EN`, `exercise-details-en.ts` (1711 linii), `CATEGORY_LABEL_EN`, `PLAN_NAME`/`PLAN_DESC`/`FOCUS_TOKEN_EN`/`WEEKDAY_EN`, duplikat `functions/src/exercise-name-en.ts`, `garmin/resources-spa`, Watch `L10n`, `landing/legal` ES. Wszystkie te struktury mają dziś tylko wariant EN: X21 przestawia wzorzec na nakładki per język (`Record<lang, ...>`), treść ES to osobny projekt.

## Świadomie PL (NIE ruszać, kanoniczne)

Nazwy ćwiczeń/dni/focus w `src/data/*` (514 ćwiczeń), `plan-cycle-utils.ts` (dni tygodnia), `workout-import/mapper.ts` (mapowanie hevy/strong na PL), `rza-progression.ts` (klucze lookup), `RacePredictor.tsx` (klucze mapy emoji), fixtury E2E w `registration-api.ts`, słowo potwierdzenia `USUŃ` w `Profile.tsx:74`, mini-słownik `ErrorBoundary` (żyje poza LanguageProvider).
