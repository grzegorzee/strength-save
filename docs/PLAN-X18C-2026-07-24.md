# PLAN X18C: Powiadomienia bez spamu, koniec widgetu odtwarzacza, czytelny pasek tygodnia (2026-07-24)

> **Dla agenta wykonawcy:** WYMAGANY SUB-SKILL: `superpowers:executing-plans`. TRYB 100% AUTONOMICZNY.
> Priorytet P2. Wykonać PO X18A i X18B. Dotyka Cloud Functions (deploy functions) i ścieżki dźwięku timera.

**Goal:** Poranne przypomnienie nie przychodzi, gdy trening już trwa albo jest zrobiony. Dźwięk końca przerwy (gong) gra jak dotąd, ale apka przestaje udawać odtwarzacz muzyki na lock screenie. Pasek pod "Plan tygodnia" mówi sam za siebie zamiast wyglądać jak usterka.

**Zgłoszenia usera (2026-07-24):**
1. "Dostaję rano powiadomienie push o 7, a ja już jestem na treningu i mam trening rozpoczęty. Tak ma być?" + banner pokazuje się też W apce podczas treningu (podwójnie: systemowy + toast).
2. Widget odtwarzacza "StrengthSave - Tracker Tr..." z play/pauzą i paskiem 0:02 na lock screenie.
3. "Plan tygodnia wygląda dziwnie, peak na jeden dzień, nie wiem co to oznacza."

**Ustalenie usera (2026-07-24):** dźwięk GONGU zostaje domyślny; dźwięki mają działać w kolejnych buildach. Nic w tym planie nie może zepsuć słyszalności sygnału.

---

## 0. TWARDE ZASADY

1. **Dźwięk jest ważniejszy niż estetyka lock screenu.** Jeśli którykolwiek krok Z147 pogorszy słyszalność gongu na urządzeniu, wracamy do wariantu poprzedniego i eskalujemy do plan B (natywne odtwarzanie), zamiast kombinować.
2. Kategoria `.playback` w `AppDelegate` ZOSTAJE (to ona daje dźwięk mimo bocznego przełącznika ciszy w foregroundzie, decyzja 2026-07-20). Zmieniamy sposób ODTWARZANIA, nie kategorię.
3. Pliki dźwiękowe w DWÓCH miejscach (root bundla iOS + web assets) to świadoma decyzja, zostaje bez zmian (`DECYZJE.md` 2026-07-20 pkt 3).
4. Cloud Functions: zmiany w `daily-reminder.ts` przez `DailyReminderDeps` (testowalne bez emulatora). Deploy `--only functions`.
5. Nowe klucze i18n do OBU plików. Surgical changes: nie przebudowujemy Dashboardu, tylko pasek i jego opis.

---

## ROOT CAUSE (zweryfikowany w kodzie)

1. **Poranny push mimo treningu:** `runDailyReminder` (`functions/src/daily-reminder.ts:74-89`, harmonogram `every day 07:00` Europe/Warsaw, linie 126-131) filtruje tylko: token, `notificationPrefs.dailyReminder`, dostęp, dzień treningowy w planie. NIE czyta kolekcji `workouts`, więc nie wie, że trening na dziś już wystartował (pola `startedAt`/`completed` są zapisywane przez sync: `workout-sync-engine.ts:249-252`). W foregroundzie prezentacja jest PODWÓJNA: systemowy banner (`capacitor.config.ts:19-21`, `presentationOptions: ['alert','badge','sound']`) plus bezwarunkowy toast (`PushRegistrar.tsx:20-25` przez `listenForegroundPush`, `push-notifications.ts:195-202`).
2. **Widget odtwarzacza:** kategoria `.playback` aktywowana na stałe przy starcie apki (`AppDelegate.swift:26-37`, `setActive(true)`) + odtwarzanie wav przez `HTMLAudioElement` w WKWebView (`timer-sound.ts:34,45`). iOS rejestruje media element WebView w Now Playing; pasek "0:02" to długość `rest_bell.wav` (~2,1 s). Żadnego jawnego kodu Now Playing w projekcie nie ma (sprawdzone). Blokada wygaszania to osobny mechanizm (`keep-awake`), niewinny.
3. **Pasek tygodnia:** `HybridWeekStrip` (osadzony pod nagłówkiem "Plan tygodnia", `Dashboard.tsx:936-944`) pokazuje WYKONANE obciążenie hybrydowe (czas x RPE + TRIMP cardio, `hybrid-load.ts:89-122`), skalowane WZGLĘDEM najmocniejszego dnia tygodnia (`HybridWeekStrip.tsx:50,74-75`), bez minimalnej wysokości słupka (poniżej ~1/80 maksimum słupek znika) i bez legendy (opis tylko w atrybucie `title`, na dotyku martwy). Nagłówek obiecuje PLAN, pasek pokazuje HISTORIĘ obciążenia: stąd konfuzja.

---

## FAZA 1: Z146. Poranne przypomnienie z głową (test first)

- [x] Krok 1: Testy `runDailyReminder` (rozszerz `DailyReminderDeps` o odczyt dzisiejszego treningu, wzorzec istniejących testów functions): *(dowód: `cd functions && npx vitest run src/daily-reminder.test.ts` — FAIL 2 przed implementacją, PASS 13/13 po; 4 nowe przypadki zgodnie z listą)*
  - user z dokumentem `workouts` na dziś z `startedAt` (rozpoczęty) → POMINIĘTY;
  - user z dokumentem `completed: true` na dziś → POMINIĘTY;
  - user bez dokumentu na dziś → dostaje push (bez regresji);
  - odczyt treningu tylko dla userów, którzy przeszli dotychczasowe filtry (koszt: 1 query per kandydat, nie per user).
- [x] Krok 2: FAIL → implementacja → PASS. Query: `workouts` where `userId == uid` and `date == today`, limit 1. Ograniczenie świadome i opisane w kodzie: draft offline (IndexedDB) nie jest widoczny dla backendu, więc trening rozpoczęty offline bez syncu nadal dostanie push. Akceptowalne: autosave syncuje `startedAt` przy pierwszym zapisie online. *(dowód: getTodayWorkout w deps + licznik skippedActive w wyniku; functions build OK)*
- [x] Krok 3: Sprawdź indeks kompozytowy dla query (lekcja X12: composite index w scheduled fn). Jeśli potrzebny, dodaj do `firestore.indexes.json` PRZED deployem. *(dowód: firestore.indexes.json ma composite workouts (userId ASC, date DESC) — pokrywa equality+equality z limitem)*
- [x] Krok 4: Deduplikacja foregroundu: *(dowód: data.type='daily-reminder' w sendEachForMulticast; shouldShowForegroundPushToast w czystym module push-foreground (3/3 testy) wpięty w PushRegistrar; capacitor.config presentationOptions=['badge','sound']; test ręczny na urządzeniu czeka na usera)*
  - payload FCM dostaje `data.type: 'daily-reminder'` (funkcja) i klient przestaje pokazywać toast dla tego typu, gdy user ma aktywną sesję treningową (sprawdzenie aktywnego draftu/ścieżki `/workout/` w handlerze `PushRegistrar.tsx:20-25`);
  - usuń `'alert'` z `presentationOptions` w `capacitor.config.ts:19-21` (zostaje `badge`,`sound`): w foregroundzie prezentację przejmuje w całości kontrolowany toast, znika podwójny banner. Test ręczny na urządzeniu: push w tle → normalny systemowy banner; push w foregroundzie → tylko toast (albo nic, gdy trening aktywny).
- [x] Krok 5: Deploy `firebase deploy --only functions` + commit `fix(reminder): pomijaj userow z rozpoczetym lub ukonczonym treningiem, jeden banner w foregroundzie (Z146)`. *(dowód: commit Z146; "Deploy complete!", gcloud describe dailyTrainingReminder: updateTime 2026-07-24T09:26Z, state ACTIVE)*

---

## FAZA 2: Z147. Gong bez widgetu odtwarzacza (test first + obowiązkowy test na urządzeniu)

Kierunek: odtwarzanie plików wav w foregroundzie przez **WebAudio (AudioContext + AudioBuffer)** zamiast `HTMLAudioElement`. Czysty WebAudio nie tworzy wpisu w Now Playing (nie ma media elementu). Decyzja 2026-07-20 pkt 4 ("HTMLAudioElement przed WebAudio") dotyczyła SYNTEZY, nie odtwarzania zdekodowanego pliku; ten plan ją świadomie rewiduje i wymaga wpisu w DECYZJE.md.

- [x] Krok 1: Testy `timer-sound.ts` (mock AudioContext, wzorzec istniejących testów dźwięku): `playTimerSound('finish')` dekoduje wybrany plik (`loadRestSound().file`) do `AudioBuffer` (fetch raz, cache w module), gra przez `bufferSource`; fallback na dotychczasową syntezę WebAudio, gdy fetch/decode padnie. `previewRestSound` (Ustawienia) tą samą ścieżką. *(dowód: `npx vitest run src/test/timer-sound.test.ts` — FAIL 4 na starym kodzie, PASS 5/5 po implementacji)*
- [x] Krok 2: FAIL → implementacja → PASS. Usuń ścieżkę `new Audio(...)` (`timer-sound.ts:34,45`); `unlockTimerSound` (gest przy odhaczeniu) odblokowuje ten sam AudioContext, bez zmian kontraktu. *(dowód: playFile przez fetch+decodeAudioData+bufferSource; unlock dodatkowo prefetchuje wybrany plik w geście)*
- [x] Krok 3: Weryfikacja, że NIC innego nie tworzy media elementów (grep po `new Audio(` i `<audio` w src/). Notyfikacja systemowa (dźwięk z pliku w bundlu iOS) bez zmian: to nie ona tworzy Now Playing. *(dowód: grep "new Audio(" i "<audio" w src/ poza testami = 0 wystąpień)*
- [ ] Krok 4 [FIZYCZNY IPHONE — CZEKA NA USERA]: RĘCZNY test na fizycznym iPhone (bramka twardej zasady 1):
  - koniec przerwy w foregroundzie → gong słychać (także z bocznym przełącznikiem ciszy);
  - lock screen / Control Center PO dźwięku → BRAK widgetu odtwarzacza StrengthSave;
  - odsłuch w Ustawieniach działa;
  - koniec przerwy przy zgaszonym ekranie → notyfikacja z dźwiękiem gongu jak dotąd.
- [ ] Krok 5: PLAN B (tylko jeśli WebAudio nie zagra stabilnie w WKWebView): odtwarzanie natywne przez mały bridge w Swift (AVAudioPlayer na plikach z bundla, aktywacja sesji na czas dźwięku, `setActive(false, .notifyOthersOnDeactivation)` po). Nie wchodzić w to bez dowodu, że krok 4 padł.
- [x] Krok 6: Domyślny dźwięk: GONG (ustalenie usera 2026-07-24). Zweryfikuj, że wybór usera i default (`rest-sound.ts:26`) wskazują wariant gongu i że nazwa w Ustawieniach jest jednoznaczna. Commit `fix(sound): gong przez WebAudio, bez wpisu Now Playing (Z147)`. *(dowód: DEFAULT_REST_SOUND='bell' → rest_bell.wav (gong/dzwon bokserski); etykieta ujednoznaczniona "Gong (dzwon bokserski)"/"Gong (boxing bell)" w OBU plikach i18n; commit Z147)*

---

## FAZA 3: Z148. Pasek tygodnia, który się tłumaczy (test first)

Pasek zostaje (wartościowa metryka obciążenia), ale przestaje udawać plan i dostaje czytelność:

- [x] Krok 1: Testy komponentu `HybridWeekStrip`: *(dowód: `npx vitest run src/test/hybrid-week-strip.test.tsx` — FAIL 4 na starym kodzie, PASS 5/5 po)*
  - słupek z `totalLoad > 0` ma wysokość co najmniej 3 px (koniec znikających dni);
  - legenda widoczna w komponencie (siła = kolor primary, cardio = cyan), nie tylko w `title`;
  - dzień z planu (weekday z aktywnego planu treningowego) ma oznaczenie pod etykietą (kropka), niezależnie od tego, czy było obciążenie;
  - komponent ma własny podtytuł/etykietę metryki ("obciążenie treningowe: czas x intensywność"), i18n w OBU plikach.
- [x] Krok 2: FAIL → implementacja → PASS. Zmiany w `HybridWeekStrip.tsx` (min-height w liniach 74-75, legenda + podpis, kropki dni planowych z propsa `plannedWeekdays`; Dashboard przekazuje weekdays z aktywnego planu, który już ma w stanie). *(dowód: MIN_STACK_PX=3, strip-legend, plan-day-dot, plannedWeekdays={trainingPlan.map(d => d.weekday)}; klucze hybrid.stripTitle/stripHint w pl.ts i en.ts)*
- [x] Krok 3: Nagłówek sekcji: pasek obciążenia dostaje WŁASNY mikronagłówek wewnątrz komponentu, a "Plan tygodnia" zostaje nagłówkiem listy kart dni (to ona jest planem). Zero przebudowy układu, tylko etykiety. *(dowód: mikronagłówek w komponencie, nagłówek sekcji Dashboardu nietknięty)*
- [x] Krok 4: Zrzut przed/po (symulator wystarczy, to czysty render). Commit `fix(dashboard): pasek obciazenia z legenda, min wysokoscia i oznaczeniem dni planu (Z148)`. *(dowód: docs/assets/z148-week-strip-przed.png — jeden słupek, piątkowe obciążenie niewidoczne, zero opisu; z148-week-strip-po.png — nagłówek, min-wysokość, legenda, kropki pon/śr/pt; commit — 7 plików)*

---

## BRAMKI (checklist przed wdrożeniem)

- [x] `npm run test`, `npm run typecheck`, `npm run lint`, `npm run build` + testy functions (`functions/`: build + test) *(dowód: vitest 1031/1031, functions daily-reminder 13/13 + build OK, typecheck/lint/build OK)*
- [x] `check:bundle-budget` (24 KB zapasu; Z147 usuwa kod HTMLAudio, Z148 dodaje mało; pilnować) *(dowód: "initial JS 1515062 bytes (limit 1536000)" — ~20 KB zapasu)*
- [x] `build:mobile` + `check:dist-smoke`, `check:dist-offline`, `e2e:mock` *(dowód: dist-smoke PASS, dist-offline PASS na web dist, e2e:mock 179/179)*
- [ ] [FIZYCZNY IPHONE — CZEKA NA USERA] Scenariusz background/resume na urządzeniu (dźwięk + notyfikacje, krok 4 z Z147 obowiązkowy)
- [x] Deploy: functions (`--only functions`), web (`npm run deploy`), iOS: bump `CURRENT_PROJECT_VERSION` (6 wystąpień) + `release-ios.sh` + `testflight_external.py <build> --whats-new` (w "co testować": brak porannego pusha przy rozpoczętym treningu, brak widgetu odtwarzacza, gong działa, pasek tygodnia z legendą) *(dowód: functions ACTIVE updateTime 09:26Z; web live index-DlTInM09.js; build 77 UPLOAD OK, obie grupy HTTP 204, whatsNew 200, betaReviewState=APPROVED)*
- [x] Wpis do `DECYZJE.md`: rewizja decyzji "HTMLAudioElement przed WebAudio" (dotyczyła syntezy, nie plików) + warunek pomijania porannego pusha + los `presentationOptions` *(dowód: wpis "2026-07-24 — X18C" w DECYZJE.md)*
