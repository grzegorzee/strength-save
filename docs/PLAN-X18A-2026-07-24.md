# PLAN X18A: Ochrona danych sesji. Reset serii po edycji planu dnia + czas treningu 48h (2026-07-24)

> **Dla agenta wykonawcy:** WYMAGANY SUB-SKILL: `superpowers:executing-plans`. TRYB 100% AUTONOMICZNY.
> Priorytet P0 (utrata danych usera). Wykonać PRZED X18B i X18C.

**Goal:** Edycja planu dnia w trakcie treningu przestaje kasować odhaczone serie, a czas trwania treningu przestaje kłamać, gdy user kliknie "Zakończ trening" długo po ostatniej serii.

**Zgłoszenia usera (2026-07-24, realny trening):**
1. "Po dodaniu ćwiczenia do planu dnia zresetowało mi wszystkie wpisane osiągnięcia z danego dnia."
2. "Zapomniałem zapisać treningu i zapisałem go dopiero 48h po zakończeniu. Wyszło, jakbym ćwiczył przez 48h, a trening trwał około godziny" (screenshot: Czas 48:08:47).

---

## 0. TWARDE ZASADY

1. **Dane usera są święte.** Żadnych testów na realnym koncie. Naprawa produkcyjnego rekordu wyłącznie po dry-run i potwierdzeniu usera.
2. **Niezmiennik sesji (reguła 5 z CLAUDE.md):** start treningu NIGDY nie nadpisuje istniejącego draftu, który ma jakąkolwiek treść (odhaczone serie, wpisane wartości, notatki). Start może draft tylko UTWORZYĆ (gdy go nie ma) albo UZUPEŁNIĆ o brakujące ćwiczenia.
3. **Testuj SEKWENCJĘ, nie ekran:** obowiązkowy test przepływu start → odhaczenie serii → edycja planu dnia → powrót → wszystko na miejscu.
4. **Surgical changes:** nie ruszamy przy okazji rozjazdu id `day-N` vs `${startDate}-dN` w `syncActiveCycle` (`training-plan-save.ts:67-70`, `plan-cycle-utils.ts:77-78`). To utajony hazard, ale NIE on skasował serie. Wpis do PLAN.md backlog, osobne zadanie.
5. Kg kanoniczne, i18n do OBU plików, żadnych nowych zależności.

---

## ROOT CAUSE (zweryfikowany w kodzie, nie zgadnięty)

### Bug 1: reset serii po edycji planu dnia

Łańcuch zdarzeń:

1. Dashboard nawiguje do treningu z `?autostart=true` (`Dashboard.tsx:615,663,1012`). Parametr NIGDY nie jest zdejmowany z URL, więc żyje w historii przeglądarki.
2. "Edytuj plan dnia" robi `navigate('/plan/edit')` (`WorkoutDay.tsx:2421-2432`). WorkoutDay się ODMONTOWUJE.
3. Powrót (back) wraca na wpis historii `/workout/day-N?date=...&autostart=true`. Świeży mount = świeży `autostartDone = useRef(false)` (`WorkoutDay.tsx:186`).
4. Efekt autostartu (`WorkoutDay.tsx:1046-1067`) odpala się, gdy `startSourcesReady` staje się true. W domknięciu `sessionId` jest jeszcze `null` (siostrzany efekt hydracji z linii 846 ustawia go setState'em, niewidocznym w tym samym przebiegu) → `handleStartWorkout()` startuje NA ŻYWEJ SESJI.
5. `handleStartWorkout` (`WorkoutDay.tsx:1223-1392`):
   - sesja provisional/offline → `result.existing === false` → gałąź `else` (linia 1328): bezwarunkowy `setExerciseSets(prefilled)` + `persistDraftSnapshot(initialDraft)` z `version: 1` → **deterministyczne wymazanie draftu**;
   - sesja remote → gałąź `existing` (linia 1294): `exerciseSetsRef.current` na świeżym mouncie bywa `{}` (wyścig z hydracją) → prefill wszystkiego + `saveDraftSnapshot({ exerciseSets: filled })` → **wymazanie draftu zależne od wyścigu**.

Ścieżka "Kontynuuj trening" z Dashboardu jest bezpieczna, bo nawiguje BEZ `autostart` (`Dashboard.tsx:613-615`, `workout-resume.ts:24-25`). Dlatego bug objawia się tylko przy powrocie przez historię.

Pokrycie testami: ZERO. Żaden test (unit/e2e) nie robi sekwencji start → serie → /plan/edit → powrót.

### Bug 2: czas 48:08:47

- `durationSec = finalizedAt - startedAt` bez żadnego capa (`workout-sync-engine.ts:239-252`).
- `finalizedAt` to moment kliknięcia "Zakończ trening" (`WorkoutDay.tsx:1660-1662`), nie moment realnego końca ćwiczeń.
- Draft MA znacznik ostatniej aktywności (`updatedAt`, bumpowany przy każdym snapshocie: `workout-draft-db.ts:36,581,618`), ale nikt go nie używa do liczenia czasu. Serie nie mają timestampów odhaczenia (`types/index.ts:13-16`).
- Wyświetlanie: `fmtDuration` + `sessionDurationSec` (`WorkoutDay.tsx:81-87,1927-1934,2050`); wszystkie warianty liczą z tej samej różnicy.

---

## FAZA 1: Z141. Start treningu nie niszczy żywego draftu

### Zadanie Z141.1: autostart jest jednorazowy i szanuje istniejący draft (test first)

- [x] Krok 1: Test (vitest, wzorzec `workout-day-view.test.ts` / testy hooków): wyodrębnij decyzję autostartu do czystej funkcji `shouldAutostartWorkout({ autostart, sessionId, draftForPage })` w `src/lib/workout-hydration.ts` (albo nowy `src/lib/workout-autostart.ts`). Przypadki: *(dowód: `npx vitest run src/test/workout-autostart.test.ts` — najpierw FAIL "Failed to resolve import @/lib/workout-autostart", po implementacji PASS 11/0)*
  - `autostart=true`, brak draftu, brak sesji → `start`
  - `autostart=true`, jest draft z ≥1 serią odhaczoną / ≥1 niepustym setem → `resume` (NIE start)
  - `autostart=true`, `sessionId` już ustawione → `scroll-only`
  - `autostart=false` → `none`
- [x] Krok 2: FAIL → implementacja → PASS. Efekt autostartu w `WorkoutDay.tsx:1046-1067` używa tej funkcji; przy `resume` zachowuje się jak wejście bez autostartu (hydracja z draftu robi swoje, bez `handleStartWorkout`). *(dowód: WorkoutDay.tsx efekt autostartu woła shouldAutostartWorkout z currentPageDraft; typecheck zielony)*
- [x] Krok 3: Po skonsumowaniu autostartu ZDEJMIJ parametr z URL: `setSearchParams` z `{ replace: true }` (usuń `autostart`, zostaw `date`/`session`). Wpis historii przestaje być miną. Test: po konsumpcji parametr zniknął z URL. *(dowód: `stripAutostartParam` + testy w workout-autostart.test.ts: usuwa autostart, zostawia date/session, null gdy brak parametru)*

### Zadanie Z141.2: guard bliźniak w handleStartWorkout (defense in depth, test first)

Nawet gdyby ktoś w przyszłości zawołał `handleStartWorkout` na żywej sesji z innego miejsca, nie może stracić danych.

- [x] Krok 1: Testy dla logiki startu (wyodrębnij czystą funkcję budowy stanu startowego, np. `buildStartExerciseSets({ daySnapshot, existingDraft, existingSets, weeklyTargets, ... })`):
  *(dowód: `npx vitest run src/test/workout-start.test.ts` — najpierw FAIL 8/18 (funkcje nie istniały), po implementacji PASS 18/18; buildStartExerciseSets + buildStartDraft w src/lib/workout-start.ts)*
  - istnieje draft z treścią dla (uid, dayId, date) → wynik ZAWIERA serie z draftu bez zmian, dokłada tylko brakujące ćwiczenia (dokładnie jak komentarz przy incydencie 2026-07-20, `WorkoutDay.tsx:1302-1304`);
  - gałąź `else` (existing=false): jeżeli w IndexedDB jest draft z treścią dla tej strony → NIE budujemy `initialDraft` od zera; adoptujemy draft (serie, notatki, startedAt zostają), aktualizując tylko sessionId/sessionOrigin/cloud-meta;
  - brak draftu → dotychczasowy prefill bez zmian.
- [x] Krok 2: FAIL → implementacja w obu gałęziach (`WorkoutDay.tsx:1294-1327` i `1328-1387`) → PASS. *(dowód: handleStartWorkout używa buildStartExerciseSets w gałęzi existing i buildStartDraft+adoptableDraft w gałęzi else; npm run test 998/998, typecheck+lint OK)*
- [x] Krok 3: Gałąź `existing` dodatkowo: `filled` budowane z `existingDraft.exerciseSets ?? exerciseSetsRef.current` (draft z bazy jest źródłem prawdy, ref bywa pusty na świeżym mouncie). *(dowód: draftSets: currentPageDraft?.exerciseSets ?? null z fallbackiem stateSets w buildStartExerciseSets)*

### Zadanie Z141.3: test SEKWENCJI e2e (niezmiennik reguły 5)

- [x] Krok 1: Nowy scenariusz w e2e (wzorzec `e2e/continue-workout.spec.ts`): start treningu z planu (`?autostart=true`) → odhacz 2 serie z wpisanymi wartościami → wejdź w `/plan/edit` → dodaj ćwiczenie do dzisiejszego dnia → wróć (history back, NIE nowa nawigacja) → asercje: *(dowód: `npx playwright test e2e/plan-edit-during-workout.spec.ts` — na kodzie sprzed fixa (stash WorkoutDay+workout-start) 2 FAILED, na fixie 2 PASSED w 7.3 s; w teście też asercja `not.toHaveURL(/autostart/)`)*
  - obie odhaczone serie NADAL odhaczone, wartości nietknięte,
  - nowe ćwiczenie widoczne na liście dnia (merge plan+draft z `buildDayFromDraft` działa),
  - draft w IndexedDB ma tę samą sesję (sessionId niezmienione) i niezresetowaną wersję.
- [x] Krok 2: Drugi wariant: to samo, ale start OFFLINE (sesja provisional) → powrót → serie na miejscu. To pokrywa deterministyczną gałąź `else`. *(dowód: test "wariant offline (sesja provisional)" w tym samym spec, RED na starym / GREEN na nowym; context.setOffline + rozgrzanie lazy chunków online)*
- [x] Krok 3: Commit `fix(workout): autostart nie resetuje żywego draftu po edycji planu dnia (Z141)`. *(dowód: commit "fix(workout): autostart nie resetuje żywego draftu..." — 8 plików, 757 insercji; poprzedza go commit "fix(dev): jawne optimizeDeps.entries")*

> Znalezisko środowiskowe przy Z141.3: dev server wisiał na skanie zależności (page.goto timeout w e2e), bo domyślny glob `**/*.html` vite trafiał w `build/sim` (3.1 GB derivedData symulatora z cyklicznymi symlinkami xcframeworków). Fix: `optimizeDeps.entries: ['index.html']` w vite.config.ts (osobny commit).

---

## FAZA 2: Z142. Uczciwy czas trwania treningu

### Zadanie Z142.1: znacznik ostatniej realnej aktywności (test first)

- [x] Krok 1: Testy draftu (`workout-draft-db` / snapshot): nowe opcjonalne pole `lastActivityAt` w `ActiveWorkoutDraft`. Bumpowane WYŁĄCZNIE przy realnych akcjach treningowych: toggle ukończenia serii, zmiana kg/powtórzeń, dodanie/usunięcie serii, notatka. NIE bumpowane przy `persistDraftSnapshot({ finalizedAt })` ani przy technicznych snapshotach (scroll, promocja provisional→remote). Pole opcjonalne = stare drafty czytają się bez migracji. *(dowód: `npx vitest run src/test/workout-draft-snapshot.test.ts` — najpierw FAIL 4, po implementacji PASS; detekcja przez contentUnchanged w buildWorkoutDraftSnapshot, normalizeDraft przenosi pole, mergePromotedDraft spreaduje bez bumpu)*
- [x] Krok 2: FAIL → implementacja → PASS. *(dowód: snapshot+draft-db 43/43 PASS, typecheck OK)*

### Zadanie Z142.2: clamp duration przy finalizacji (test first)

- [x] Krok 1: Testy `workout-sync-engine` (finalny payload): *(dowód: describe "uczciwy durationSec przy finalizacji (Z142)" w workout-sync-engine.test.ts — przed implementacją FAIL 1 (clamp 48h), po PASS 13/13; normalny przepływ 3900 s, porzucona sesja 3780 s, stary draft 176 400 s + asercja completedAt=finalizedAt)*
  - normalny przepływ: finalizacja 5 min po ostatniej aktywności → `durationSec = finalizedAt - startedAt` (bez zmian);
  - porzucona sesja: finalizacja 48h po ostatniej aktywności → `durationSec = lastActivityAt - startedAt` (+ stały bufor domykania, patrz niżej);
  - brak `lastActivityAt` w starym drafcie → zachowanie dotychczasowe (bez regresji).
- [x] Krok 2: FAIL → implementacja w `workout-sync-engine.ts:239-252`: `effectiveEnd = (finalizedAt - lastActivityAt > PRÓG) ? lastActivityAt + BUFOR : finalizedAt`. PRÓG = 60 min, BUFOR = 3 min (czas na odłożenie sprzętu; stała nazwana, z komentarzem dlaczego). `completedAt` ZOSTAJE momentem faktycznego zapisu (porządek syncu i sortowanie historii bez zmian, kłamał tylko durationSec). *(dowód: src/lib/workout-duration.ts — ABANDONED_SESSION_THRESHOLD_MS=60 min, SESSION_CLOSEOUT_BUFFER_MS=3 min; silnik używa computeEffectiveDurationSec)*
- [x] Krok 3: Ten sam clamp w podsumowaniu na ekranie (`WorkoutDay.tsx:1927-1934`), żeby liczba na kaflu "Czas" była spójna z tym, co pójdzie do Firestore. Jedna funkcja `computeEffectiveDurationSec(...)` w lib, używana w obu miejscach (jedno źródło prawdy). *(dowód: draftDurationSec w WorkoutDay.tsx liczony przez computeEffectiveDurationSec; typecheck+lint OK, pełny vitest 1006/1006)*
- [x] Krok 4: Commit `fix(workout): duration liczony do ostatniej aktywności, nie do klikniecia Zakoncz (Z142)`. *(dowód: commit — 8 plików, 163 insercje)*

### Zadanie Z142.3: naprawa produkcyjnego rekordu usera [WYMAGA POTWIERDZENIA USERA]

- [x] Krok 1: Skrypt naprawczy (wzorzec istniejących narzędzi naprawy danych z M15): znajdź w `workouts` usera rekordy z `durationSec > 12h`, pokaż DRY-RUN (id, data, obecny czas, proponowany czas). Dla rekordu z 48:08:47 brak `lastActivityAt` historycznie, więc propozycja: `completedSets × 3 min` (ta sama heurystyka co fallback w `hybrid-load.ts:56-58`) albo wartość podana przez usera (mówi: około godziny). *(dowód: `node scripts/repair-duration-outliers.mjs dry-run --email g.jasionowicz@gmail.com` → 73 treningi, 1 outlier: workout-U6GDdfg7GmP1k1xJuISIsK9uSUE2-day-2-2026-07-21, Wtorek, obecnie 48:08:47, 17 serii roboczych, propozycja 00:51:00; wariant `--minutes 60` → 01:00:00; nic nie zapisano)*
- [ ] Krok 2 [WYMAGA POTWIERDZENIA USERA]: Pokaż dry-run userowi, czekaj na potwierdzenie, dopiero wtedy zapis (`apply --workout workout-U6GDdfg7GmP1k1xJuISIsK9uSUE2-day-2-2026-07-21 --confirm U6GDdfg7GmP1k1xJuISIsK9uSUE2`, opcjonalnie `--minutes 60`). Zaproponuj wcześniej eksport danych (zasada projektu).

---

## BRAMKI (checklist przed wdrożeniem, z CLAUDE.md)

- [x] `npm run test` (wszystkie zielone), `npm run typecheck`, `npm run lint`, `npm run build` *(dowód: vitest 1006/1006, TYPECHECK-OK, LINT-OK, build OK; jednorazowy flake exercise-picker.test pod pełnym obciążeniem, solo 7/7 i powtórka pełna zielona)*
- [x] `check:bundle-budget` (UWAGA: po X17D zostały 24 KB zapasu; Z141/Z142 to logika, nie UI, powinno się zmieścić; jak nie, code-splitting przed podnoszeniem limitu) *(dowód: "Bundle budget passed: initial JS 1514702 bytes (limit 1536000)" — 21 KB zapasu)*
- [x] `build:mobile` + `check:dist-smoke`, `check:dist-offline`, `e2e:mock` *(dowód: dist-smoke passed; dist-offline passed NA BUILDZIE WEB — uwaga: build:mobile wyłącza SW, więc offline check musi iść po `npm run build`; e2e:mock 179/179 PASS w 116 s)*
- [ ] [FIZYCZNY IPHONE — CZEKA NA USERA] Scenariusz PRZERWANIA na realnym urządzeniu: start z planu → serie → edycja planu dnia → powrót → wszystko na miejscu → zakończenie → sync
- [ ] [FIZYCZNY IPHONE — CZEKA NA USERA] Scenariusz background/resume (dotykamy draftu i cyklu życia)
- [ ] Commit + push na main; web: `npm run deploy`
- [ ] iOS: bump `CURRENT_PROJECT_VERSION` (6 wystąpień), `scripts/release-ios.sh`, potem OBOWIĄZKOWO `testflight_external.py <build> --whats-new`
- [x] Wpis do `DECYZJE.md` (root cause: autostart w historii URL + brak guarda w handleStartWorkout; duration bez capa) + backlog PLAN.md: rozjazd id dni cyklu (twarda zasada 4) *(dowód: wpis "2026-07-24 — X18A" w DECYZJE.md, wiersz w TECH DEBT PLAN.md)*
