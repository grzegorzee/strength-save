# PLAN X22 — zgłoszenia z builda 80: usuwanie serii, Dashboard, wideo, dźwięk/ekran, separator, share (Z170-Z181)

> **Dla agenta wykonującego:** prowadź wykonanie skillem `superpowers:executing-plans`. Postęp = checkboxy `- [ ]` w TYM pliku (odhaczaj z jednolinijkowym dowodem: komenda + wynik, commituj plan razem z kodem). Przy bugu w trakcie: `superpowers:systematic-debugging`.

**Cel:** naprawić wszystkie zgłoszenia usera z realnego treningu na buildzie 80 (2026-08-03) + dodać szablony udostępniania z logo. Root cause każdego buga zweryfikowany w kodzie 2026-08-03 (6 równoległych analiz, file:line poniżej).

**Data:** 2026-08-03 · **Build docelowy iOS:** 81 · **Poprzedni plan:** X21 (Z162-Z168)

---

## 0. TWARDE ZASADY (nadrzędne wobec tempa)

- **NIGDY `git add -A` / `git add .`** W repo: nietrackowany `animacje-cwiczen/` (699 MB) oraz zastane zmiany `android/app/capacitor.build.gradle` + `android/capacitor.settings.gradle` (NIE commituj ich w ramach X22). Stage'uj pliki imiennie.
- **Dane usera święte.** Zero zapisów na produkcyjnych danych. Odczyt `users/{uid}.preferences.timerSound` (Z177) = wyłącznie read-only.
- **Reguła 5 CLAUDE.md:** każda zmiana ma test niezmiennika starego przepływu. Testuj SEKWENCJE (start → wyjście → powrót → zakończenie → sync), nie ekrany.
- **Reguła 6 CLAUDE.md:** każdy stan błędu ma wyjście dla usera.
- **Nowe klucze i18n do OBU plików** (`pl.ts` + `en.ts`), inaczej typecheck padnie.
- **Bundle budget:** initial JS 1 530 547 / 1 536 000 B — zapas ~5.4 KB! `check:bundle-budget` padnie → NIE podnoś limitu; code-splitting (np. lazy import `html2canvas-pro` w Z179 zdejmuje ~150 KB z chunka WorkoutDay).
- **Firestore rules mają schema-checks** — NIE dodawaj nowych pól do kształtów zapisywanych do Firestore (SetData bez pola `id`! patrz Z171 — rozwiązanie referencyjne).
- **Wersja 1.0.0 na sztywno.** Bump wyłącznie `CURRENT_PROJECT_VERSION` → **81** (6 wystąpień; pilnuje `release-ios-preflight.mjs`).
- **Reguła 9 CLAUDE.md:** e2e masowo czerwone / wolne → najpierw `pkill -f vite` + wyczyść `node_modules/.vite`.
- Kolejność dist-checków: `npm run build:mobile` + `check:dist-smoke`, POTEM `npm run build` + `check:dist-offline`.
- Chirurgiczne commity per zadanie, `(Z17x)` w opisie. Wpis do `DECYZJE.md` po każdej fazie.
- Coś poza zakresem → sekcja ODŁOŻONE na końcu tego pliku (jednozdaniowy opis), nie rozszerzaj planu.

---

## ROOT CAUSE — mapa (zweryfikowane w kodzie)

| Bug | Root cause | Dowód |
|---|---|---|
| USUŃ w dialogu nic nie robi | Z159 (build 79) dodała `transition-[top]` względem `--keyboard-inset`: po tapnięciu X klawiatura się chowa i dialog zjeżdża ~150 px pod palcem przez 200 ms; tap trafia w overlay → dialog zamyka się bez akcji. Wzmacniacze: USUŃ ~36 px wysokości, X 24×32 px, `removeSetAt` przy złym indeksie robi cichy no-op | `ui/dialog.tsx:44`, `ExerciseCard.tsx:475-495, 722, 1189-1197`, commit `383337e6` |
| Dialog pyta o „zapisane dane" przy świeżej serii | `handleAddSet` kopiuje reps/weight z ostatniej serii → nietknięta seria zawsze przechodzi `setHasData` | `ExerciseCard.tsx:459-467, 484-487` |
| „Stary plan" na Dashboardzie | Dashboard renderuje wbudowany `defaultPlan` (focus „Klatka / Przysiad / Środek Pleców" = dosłownie `trainingPlan.ts:96`): stan startowy hooka + błąd snapshotu podmienia dobry plan na default + gate ładowania nie czeka na `planIsLoaded` | `useTrainingPlan.ts:29, 90-102`, `Dashboard.tsx:506` |
| „Pominięte" w środę, która nie nadeszła | `today = useMemo(() => new Date(), [])` — zamrożone przy mount, WKWebView żyje dniami | `Dashboard.tsx:180`, `TrainingDayCard.tsx:21-22` |
| Baner „offline" wisi mimo 5G | provisional po `Promise.race` z timeoutem 2 s; ŻADNA ścieżka nie promuje aktywnej sesji poza ekranem treningu (`AutoSyncOnReconnect` filtruje `finalSyncPending`) | `WorkoutDay.tsx:1313-1348`, `AutoSyncOnReconnect.tsx:67-68` |
| Dwa CTA „Kontynuuj trening" naraz | baner sync i karta dnia czytają ten sam draft bez wykluczenia | `Dashboard.tsx:547, 584-624` |
| „Odhaczone serie: 0" | heurystyka `pickActiveDraft` wybiera świeży provisional zamiast realnej sesji; autostart z kafla potrafi nadpisać draft (`version=1`, brak guardu wersji w IDB) | `workout-draft-db.ts:380-388, 461, 801-803`, `Dashboard.tsx:1011-1014` |
| Wideo: nieruchoma klatka | 7 autoodtwarzanych `<video>` naraz na ekranie treningu (limit dekoderów iOS) + `opacity-80` i `backdrop-blur-sm` NA wideo (znany freeze WebKit); zero obsługi błędów. Animacje weszły commitem `wip` 532a2d74 do builda 79/80 bez review | `ExerciseCard.tsx:861-875`, `exercise-media.ts:29-174` |
| Biblioteka: wideo nigdy nie startuje | podgląd gated hoverem (`onMouseEnter`), na dotyku nie istnieje | `ExerciseLibrary.tsx:74-77` |
| Cisza dźwięków do restartu apki | media sessions wideo wpychają współdzielony `AudioContext` w stan `interrupted`; kod obsługuje tylko `'suspended'` (3 miejsca), singleton nigdy nieodtwarzany; sesja audio `.playback` ustawiana RAZ na starcie, bez reaktywacji | `timer-sound.ts:9, 52, 80, 134`, `AppDelegate.swift:14, 26-37` |
| Ekran gaśnie mimo ustawienia | `keepScreenAwake()` wołane raz per (`sessionId`,`isCompleted`), zero re-apply po powrocie z tła, błędy pluginu połykane, `isKeptAwake()` nieużywane | `WorkoutDay.tsx:271-278`, `keep-awake.ts:34-42` |
| Pułapka: znika wiersz „Dźwięk" | wyłączenie przełącznika „Timer przerwy" (Z157) chowa też ustawienie dźwięku | `Profile.tsx:250` |
| „47,3" vs „49.6" | input `type="number"` + `parseFloat(...) || 0`; przecinek z klawiatury PL = tekst, którego React nie nadpisuje; wariant WebKit sanitize→`""` daje **zapis 0 kg (cicha utrata)**; `Number("47,3")`=NaN blokuje zapis pomiarów ciała; RPE z przecinkiem = NaN znikające po powrocie | `ExerciseCard.tsx:773-789, 780`, `MeasurementsForm.tsx:47`, `react-dom` loose `!=` dla type=number |
| Crash po „Dodaj zdjęcie" | `capture="environment"` wymusza aparat, a `Info.plist` nie ma `NSCameraUsageDescription` → TCC ubija proces; drugi czynnik: 12 MP bez downscale (kilka kopii base64 w pamięci) | `ShareWorkoutDialog.tsx:109-116, 63-69`, `ios/App/App/Info.plist` |
| „Pobierz" nic nie robi | `<a download>` — WKWebView to ignoruje, brak gałęzi natywnej | `ShareWorkoutDialog.tsx:78-84` |
| Obraz 1.3 MB | PNG 1080×1920 z kanałem alpha | `share-utils.ts:197-207` |

---

## FAZA 1 — usuwanie serii (Z170, Z171)

### Z170: dialog potwierdzenia — stabilny i klikalny

**Pliki:** `src/components/ui/dialog.tsx`, `src/components/ExerciseCard.tsx`, test: `src/test/exercise-card-layout.test.tsx`, e2e: `e2e/exercise-card-v3.spec.ts`.

- [x] **Test first (unit):** w `exercise-card-layout.test.tsx` dopisz test „pointer down poza dialogiem potwierdzenia NIE zamyka go" (otwórz dialog przez X na serii z danymi, `fireEvent.pointerDown(document.body)`, asercja: dialog nadal w DOM). Uruchom `npx vitest run src/test/exercise-card-layout.test.tsx` — musi PAŚĆ (dziś Radix zamyka). *(DOWÓD: bieg 1 = 26 pass / 1 fail; potrzebny tick `setTimeout(0)` + click po pointerdown, bo Radix podpina listener asynchronicznie i domyka na click przy pointerType touch)*
- [x] **Fix geometrii:** w `ui/dialog.tsx:44` usuń `transition-[top] duration-200` (pozycjonowanie względem `--keyboard-inset` ZOSTAJE — to fix Z159; znika tylko animacja przesuwu, przez którą dialog jeździ pod palcem). Niezmiennik: `src/test/keyboard-inset.test.ts` dalej zielony. *(DOWÓD: vitest exercise-card-layout + keyboard-inset = 30 pass / 0 fail)*
- [x] **Fix outside-dismiss:** na `DialogContent` dialogu potwierdzenia usunięcia (`ExerciseCard.tsx:~1178`) dodaj `onInteractOutside={(e) => e.preventDefault()}` — destrukcyjne potwierdzenie zamyka się TYLKO przez ANULUJ / X dialogu. *(DOWÓD: test Z170 zielony po fixie)*
- [x] **Fix hit targets:** przyciski dialogu (`ExerciseCard.tsx:1181-1197`): `min-h-[44px] min-w-[88px]`; przycisk X przy serii (`:715-726` i `:825-836`): `h-11 w-11` (44 px). Dodaj `data-testid="remove-set-confirm"` i `data-testid="remove-set-cancel"`. *(DOWÓD: wdrożone + ostatnia kolumna gridu 22px→44px, żeby 44px X nie nachodził na checkmark; layout testy zielone)*
- [x] **Weryfikacja:** `npx vitest run src/test/exercise-card-layout.test.tsx` + `npm run typecheck`. Commit `fix(card): dialog usuwania serii stabilny (bez animacji top, bez outside-dismiss, tap targets 44px) (Z170)`. *(DOWÓD: vitest 30 pass, tsc bez błędów)*

### Z171: usuwanie po referencji + dialog tylko dla realnych danych

**Pliki:** `src/components/ExerciseCard.tsx`, test: `src/test/exercise-card-layout.test.tsx`.

**Decyzja architektoniczna:** NIE dodajemy pola `id` do `SetData` (kształt idzie do Firestore, rules mają schema-checks). Zamiast tego `pendingRemove` trzyma REFERENCJĘ do obiektu serii.

- [x] **Test first:** przepisz test usuwania tak, by rodzic-wrapper oddawał `savedSets` z powrotem do karty (kontrolowany round-trip) i asercja sprawdzała **CO usunięto**: render `[W, s1(60×8), s2(60×8), s3(dodana ad-hoc)]` → usuń s3 → `onSetsChange` dostał dokładnie `[W, s1, s2]` (nie `toHaveBeenCalledTimes(1)`). Drugi test: pomiędzy otwarciem dialogu a klikiem USUŃ wrapper podmienia `savedSets` (symulacja hydracji draftu) → po USUŃ żadna zła seria nie znika, dialog się zamyka. Oba muszą PAŚĆ przed fixem (drugi zwłaszcza). *(DOWÓD: bieg RED = 28 pass / 2 fail — padły dokładnie oba nowe: „świeża seria..." i „podmiana savedSets...")*
- [x] **Implementacja:** `pendingRemoveIndex: number | null` → `pendingRemove: SetData | null`. Wiersze przekazują obiekt serii (`handleRemoveSet(set)`), znika `sets.indexOf(set)` (`:1027-1036`). `removeSetAt` → `removeSet(target: SetData)`: *(DOWÓD: wdrożone; `sets.indexOf` w JSX zostaje dla globalIndex innych handlerów — ścieżka USUWANIA w 100% referencyjna; stale-ref → `reportClientError` code `remove-set-stale-ref` zamiast nowego eventu telemetrii, bo whitelist counters w rules odrzuciłaby cały zapis dzienny)*
  ```ts
  const next = sets.filter((s) => s !== target);
  if (next.length === sets.length) {
    trackTelemetryEvent('remove-set-stale-ref', { exerciseId: exercise.id });
    setPendingRemove(null);
    return;
  }
  ```
- [x] **Reset przy resync:** w efekcie resync (`ExerciseCard.tsx:339-347`) dodaj `setPendingRemove(null)` — otwarty dialog nie przeżywa podmiany `sets`. *(DOWÓD: test „podmiana savedSets..." GREEN po fixie)*
- [x] **Dialog tylko dla realnych danych:** `touchedSets = useRef(new WeakSet<SetData>())`; `handleSetChange` dodaje NOWY obiekt serii do WeakSet. `setHasData(set)` = `set.completed === true || touchedSets.current.has(set)`. Świeża seria z `handleAddSet` (prefill) → kasowanie bez dialogu. Test: dodaj serię → X → brak dialogu, seria znika. *(DOWÓD: 3 nowe testy + rewrite testu Z130 na semantykę „odhaczona pyta"; `handleToggleComplete` też dodaje do WeakSet — odznaczona seria dalej chroniona; vitest 30/30)*
- [x] **E2E (sekwencja):** w `e2e/exercise-card-v3.spec.ts` nowy scenariusz: start treningu → „Dodaj serię" → wpisz wagę w NOWEJ serii (przez `fill`) → odhacz ją → tap X → dialog → tap `remove-set-confirm` → liczba wierszy serii spada o 1 → wyjdź na Dashboard → wróć → seria NADAL usunięta (draft round-trip). *(DOWÓD: spec 23 passed, w tym „Z171: usunięta seria nie wraca..." z markerem trwałości draftu — waga serii 1 wraca po round-tripie)*
- [x] **Bramki fazy:** `npm run test`, `npm run typecheck`, `npm run lint`, `npm run e2e:mock` (przy failach reguła 9). Commit `fix(card): usuwanie serii po referencji + dialog tylko dla realnych danych (Z171)` + wpis DECYZJE.md (FAZA 1). *(DOWÓD: test 125 plików / 1123 pass, typecheck 0, lint 0, e2e:mock 189 passed)*

---

## FAZA 2 — Dashboard i sesja (Z172-Z175)

### Z172: Dashboard nie renderuje defaultPlan, gdy plan usera nieznany

**Pliki:** `src/hooks/useTrainingPlan.ts`, `src/pages/Dashboard.tsx`, test: NOWY `src/test/dashboard-plan-source.test.tsx`.

- [x] **Test first:** RTL test Dashboardu (mock hooków jak w istniejących testach stron): (a) `planIsLoaded=false` → sekcja „Plan tygodnia" i karta dnia NIE renderują się (brak tekstu „Klatka / Przysiad / Środek Pleców"), (b) błąd snapshotu po wcześniejszym dobrym planie → nadal plan usera, nie default. Musi PAŚĆ. *(DOWÓD: NOWY dashboard-plan-source.test.tsx, bieg RED = 2 failed (a: multiple „Klatka/Przysiad" na ekranie; b: plan wraca do default po onError) / 2 passed (niezmienniki))*
- [x] **`useTrainingPlan.ts:98-102`:** handler błędu NIE robi `setPlan(defaultPlan)` — zostawia poprzedni stan, ustawia `setIsLoaded(true)` + nowy stan `planError: true` (eksponowany z hooka). *(DOWÓD: wdrożone + `setPlanError(false)` przy każdym dobrym snapshocie; testy GREEN 4/4)*
- [x] **`Dashboard.tsx:506`:** gate ładowania → `if (!isLoaded || !planIsLoaded)`. (Spinner nie zawiśnie: `isLoaded` ustawiane też w error-handlerach obu hooków — zweryfikuj to i zapisz w komentarzu.) *(DOWÓD: zweryfikowane — workout-read-store.ts:156 `isLoaded: true` przy błędzie, useTrainingPlan error handler `setIsLoaded(true)`; komentarz w kodzie)*
- [x] **Niezmiennik:** WorkoutDay i onboarding bez planu (nowe konto) działają jak dotąd — `defaultPlan` jako fallback dla KONTA BEZ DOKUMENTU planu zostaje (`useTrainingPlan.ts:90-95` bez zmian). Test: mock „brak dokumentu" → Dashboard renderuje plan domyślny (to legalne). *(DOWÓD: test „niezmiennik: konto BEZ dokumentu planu dostaje defaultPlan" zielony przed i po fixie; pełny suite 126 plików / 1127 testów)*
- [x] Commit `fix(dashboard): nie renderuj defaultPlan gdy plan usera nieznany; błąd snapshotu nie podmienia planu (Z172)`.

### Z173: świeże `today` + spójny lookup ukończenia

**Pliki:** NOWY `src/hooks/useToday.ts`, `src/pages/Dashboard.tsx`, test: NOWY `src/test/use-today.test.ts` + `src/test/dashboard-plan-source.test.tsx`.

- [x] **Test first (`use-today.test.ts`):** `vi.setSystemTime(niedziela 23:50)` → mount → `setSystemTime(poniedziałek 00:10)` → `dispatchEvent(new Event('visibilitychange'))` → hook zwraca poniedziałek; w obrębie tego samego dnia referencja STABILNA (nie powoduje re-renderów). *(DOWÓD: RED = brak modułu; GREEN = 3/3 w tym rollover, stabilna referencja i timer północy advanceTimersByTime)*
- [x] **`useToday.ts`:** `useState(() => startOfToday())`; efekt: listenery `visibilitychange`, `focus`, `App.addListener('appStateChange')` (przez istniejący wrapper `app-lifecycle.ts`) + `setTimeout` do najbliższej północy; update TYLKO gdy zmienił się `dateStr`. *(DOWÓD: wdrożone — addAppStateListener pokrywa visibilitychange na webie i appStateChange natywnie, do tego focus + timer re-armowany po każdym refresh)*
- [x] **`Dashboard.tsx:180`:** `const today = useToday()`. Wszystkie pochodne (`thisWeek`, `todayTraining`, `draftResume`, `weeklyKm`, `HybridWeekStrip`) przeliczają się same. *(DOWÓD: useMemo(() => new Date(), []) zastąpione, deps memo nietknięte)*
- [x] **`Dashboard.tsx:353-357` i `:1000-1004`:** przekazuj `today` do `findWorkoutForRoute` (guard `date >= today` zaczyna działać — jak w `WorkoutDay.tsx:305-314`). Test: ukończony wczoraj trening innego dnia planu nie oznacza dzisiejszego kafla jako UKOŃCZONE. *(DOWÓD: test kafla RED przed fixem — ✅ od treningu legacy-day z tej samej daty; GREEN po; pełny suite 127 plików / 1131)*
- [x] Commit `fix(dashboard): swieze today (rollover) + lookup ukonczenia z guardem daty (Z173)`.

### Z174: jedna prawda o aktywnej sesji na Dashboardzie

**Pliki:** `src/pages/Dashboard.tsx`, `src/lib/workout-day-view.ts` (eksport utila), test: NOWY `src/test/dashboard-active-session.test.tsx`.

- [x] **Test first:** draft provisional, `date=today`, `dayId` zgodny → na Dashboardzie DOKŁADNIE JEDEN przycisk „Kontynuuj trening" (`getAllByText` długość 1); `completedSets` liczone bez rozgrzewki (draft z 3 odhaczonymi roboczymi + 1 rozgrzewkową → „3"). Musi PAŚĆ. *(DOWÓD: NOWY dashboard-active-session.test.tsx, RED = „got 2" przyciski i autostart w nawigacji kafla; test niezmiennika draftu innego dnia zielony od początku)*
- [x] **Wspólny licznik:** wyekstrahuj z `workout-day-view.ts:73-83` util `countCompletedWorkingSets(exerciseSets)` i użyj go w `Dashboard.tsx:592` (koniec rozjazdu z ekranem treningu). *(DOWÓD: util deleguje do sessionStats; test „Odhaczone serie: 3" przy 3 roboczych + 1 rozgrzewkowej GREEN)*
- [x] **Wykluczenie CTA:** gdy karta dnia renderuje `continueDraft` (CTA), baner sync (`Dashboard.tsx:547-578`) degraduje się do wiersza informacyjnego BEZ przycisku „Kontynuuj trening" (zostaje tekst statusu + ewentualnie „Otwórz Sync Center"). Gdy karta dnia nie pokazuje CTA (draft z innego dnia/daty) — baner zostaje z przyciskiem jak dziś. Nowe klucze i18n, jeśli potrzebne, do OBU locale. *(DOWÓD: wspólny memo todayContinueDraft; przycisk banera chowany tylko przy todayContinueDraft && draftResume.resume; ZERO nowych kluczy i18n; testy 3/3 GREEN)*
- [x] **Kafel tygodnia przy żywym drafcie:** `Dashboard.tsx:1011-1014` — jeśli dzień ma draft spełniający `isDraftContinuableToday` + zgodny `dayId`, nawiguj na `continuableDraftTarget(draft)` (z `?session=`), NIE `?autostart=true`. Test: mock draftu → klik kafla → navigate z `session=`, bez `autostart`. *(DOWÓD: test „kafel tygodnia... nawiguje z ?session=" RED→GREEN; pełny suite 128 plików / 1134)*
- [x] Commit `fix(dashboard): jeden CTA aktywnej sesji, odhaczone serie bez rozgrzewki, kafel bez autostartu przy zywym drafcie (Z174)`.

### Z175: promocja provisional bez wchodzenia w trening + guard wersji draftu

**Pliki:** `src/components/AutoSyncOnReconnect.tsx`, `src/pages/WorkoutDay.tsx`, `src/lib/workout-draft-db.ts`, testy: `src/test/auto-sync-provisional.test.ts` (NOWY), `src/test/workout-draft-db.test.ts`.

- [x] **Test first (promocja):** draft `sessionOrigin='provisional'`, `dirty=true`, `finalSyncPending=false` + event `online` → `syncWorkoutSession` wywołane (kind `checkpoint`). Musi PAŚĆ (dziś filtr wycina). *(DOWÓD: NOWY auto-sync-provisional.test.ts, RED = spy niewywołany; niezmienniki „dirty remote nieruszany" i „finalSyncPending jako final" zielone od początku)*
- [x] **`AutoSyncOnReconnect.tsx:67-68`:** filtr → `(entry.finalSyncPending || draft.sessionOrigin === 'provisional') && !conflictSessionIds.has(...)`. Niezmiennik: wpisy remote bez finalSyncPending dalej NIE są ruszane (test). *(DOWÓD: wdrożone + kind per wpis (final/checkpoint) i phase w reportClientError; testy 3/3 GREEN)*
- [x] **Flush przy wyjściu z treningu:** w cleanup unmount `WorkoutDay` (`:1136-1142`) dodaj fire-and-forget: flush draftu + `syncWorkoutSession(kind:'checkpoint')` dla sesji provisional/dirty. Pierwszy checkpoint po starcie provisional: 15 s zamiast 5 min (stała obok `CHECKPOINT_INTERVAL_MS`, `WorkoutDay.tsx:80`). *(DOWÓD: wdrożone — refy persistDraftSnapshot/syncDraftToFirebase dla cleanupu z pustymi deps, PROVISIONAL_FIRST_CHECKPOINT_MS=15s w efekcie checkpointów z dep sessionOrigin)*
- [x] **Test first (guard wersji):** w `workout-draft-db.test.ts`: `saveActiveDraft(version=5, 3 odhaczone)` → `saveActiveDraft(version=1, ten sam klucz, 0 odhaczonych)` → w IDB nadal wersja 5 z odhaczeniami. Musi PAŚĆ. *(DOWÓD: RED = „expected 1 to be 5"; GREEN po fixie + test niezmiennika „wyższa wersja nadpisuje")*
- [x] **`workout-draft-db.ts` (`saveActiveDraft`, okolice `:461`):** read-before-write w TEJ SAMEJ transakcji IDB: jeśli istniejący draft pod kluczem ma `version >` zapisywanej → nie nadpisuj, zwróć istniejący (caller adoptuje). Niezmienniki: promocja provisional→remote, tombstony i legalne nadpisania (wyższa wersja) działają jak dotąd — istniejące testy `workout-draft-db.test.ts:428-520` zielone. *(DOWÓD: guard w runWrite za flagą skipIfNewerExists (tylko ścieżka saveActiveDraft; redirect/tombstone/update nietknięte); cały plik 36/36 GREEN)*
- [x] **Bramki fazy:** pełne (`test`, `typecheck`, `lint`, `e2e:mock`). Commit per zadanie + wpis DECYZJE.md (FAZA 2). Sekwencja obowiązkowa: plan → wyjście → szybki trening → powrót do planu → wszystkie ćwiczenia na miejscu → zakończenie → sync. *(DOWÓD: test 129 plików / 1139, typecheck 0, lint 0, e2e:mock 189 passed — w tym sekwencja `full-app.spec.ts:1422` „plan → wyjście → szybki trening → powrót")*

---

## FAZA 3 — wideo ćwiczeń (Z176)

### Z176: miniatury bez autoplay, dialog z twardym startem, biblioteka bez hovera

**Pliki:** `src/components/ExerciseCard.tsx`, `src/pages/ExerciseDetail.tsx`, `src/pages/ExerciseLibrary.tsx`, `src/lib/app-telemetry.ts` (tylko użycie), e2e: NOWY `e2e/exercise-video.spec.ts` + fixture `e2e/fixtures/sample-video.mp4` (wygeneruj: `ffmpeg -f lavfi -i testsrc=duration=1:size=320x240:rate=12 -pix_fmt yuv420p e2e/fixtures/sample-video.mp4`).

- [x] **Miniatura karty (`ExerciseCard.tsx:861-875`):** usuń `autoPlay loop`; dodaj `preload="metadata"` i `muted playsInline` (zostają); usuń `opacity-80` z video i `backdrop-blur-sm` z nakładki (`:869-871`) — przyciemnienie przez zwykły gradient/`bg-black/30` BEZ backdrop-filter. `onError` → `trackTelemetryEvent('exercise-video-error', { slug, code: e.currentTarget.error?.code })` + fallback: ikona zamiast video. *(DOWÓD: wdrożone; telemetria przez reportClientError code `exercise-video-error` — jak w Z171, nowy event w counters wywaliłby hasOnly w rules; fallback = ikona Dumbbell przez stan thumbVideoFailed)*
- [x] **Dialog (`ExerciseCard.tsx:1234-1257`):** `ref` na video; `onLoadedMetadata` → `v.muted = true; v.play().catch(err => { trackTelemetryEvent('exercise-video-play-blocked', {...}); setVideoControls(true); })`; gdy fallback → atrybut `controls` (user ma przycisk play — reguła 6). To samo w hero `ExerciseDetail.tsx:69`. *(DOWÓD: wdrożone bez atrybutu autoplay (odmowę widać jako rejection); hero dodatkowo chowa dekoracyjny badge Play przy controls i zdejmuje pointer-events z gradientu)*
- [x] **Biblioteka (`ExerciseLibrary.tsx:74-77`):** usuń gating `onMouseEnter/onMouseLeave/onFocus/onBlur`; zostaje IntersectionObserver (`isVisible`) + start na tap (toggle `previewActive` w `onClick`), max 1 aktywny podgląd naraz. *(DOWÓD: stan podglądu wyniesiony do rodzica (activePreviewName), tap w miniaturę z stopPropagation — nie otwiera szczegółów; data-testid exercise-preview-thumb)*
- [x] **E2E (webkit):** `exercise-video.spec.ts`: `page.route('**/media.gjasionowicz.pl/**', route => route.fulfill({ path: 'e2e/fixtures/sample-video.mp4', contentType: 'video/mp4' }))` → wejście na trening → miniatury widoczne i ŻADNA nie gra (`paused === true`); otwarcie dialogu wideo → po 1.5 s `currentTime > 0` LUB widoczne `controls` (fallback też jest sukcesem — asercja alternatywy). *(DOWÓD: fixture wygenerowana ffmpeg (7.6 KB), RED = miniatury grały + brak testid; GREEN 3/3, w e2e:mock spec biega na chromium, projekt webkit w configu dla pełnych biegów)*
- [x] **Niezmiennik:** `exercise-card-layout.test.tsx` (miniatura obecna, layout) zielony bez zmian asercji. *(DOWÓD: pełny suite 129 plików / 1139 pass bez dotykania asercji layoutu)*
- [x] **Bramki fazy** + commit `fix(media): miniatury bez autoplay + twardy start w dialogu + biblioteka bez hovera (Z176)` + DECYZJE.md. ODŁOŻONE (dopisz): postery .jpg na Bunny CDN (ffmpeg pierwsza klatka + `animacje-cwiczen/na_bunny.sh`) — osobny wątek z folderem animacji. *(DOWÓD: test 1139, typecheck 0, lint 0, e2e:mock 192 passed; postery już w sekcji ODŁOŻONE)*

---

## FAZA 4 — dźwięk + keep-awake (Z177)

### Z177: AudioContext odporny na iOS, reaktywacja sesji audio, keep-awake self-healing

**Pliki:** `src/lib/timer-sound.ts`, `ios/App/App/AppDelegate.swift`, `src/lib/keep-awake.ts`, `src/pages/WorkoutDay.tsx`, `src/pages/Profile.tsx`, testy: `src/test/timer-sound.test.ts`, NOWY `src/test/keep-awake.test.ts`.

- [x] **Test first (audio):** w `timer-sound.test.ts` przypadki: ctx w stanie `'interrupted'` → `playTimerSound` woła `resume()`; ctx `'closed'` → moduł tworzy NOWY kontekst i gra; `playSynth` z rzucającym ctx NIE propaguje wyjątku. Muszą PAŚĆ. *(DOWÓD: RED = 3/3 nowe padły (resume niewywołany, 1 instancja ctx, wyjątek propagował); GREEN 8/8)*
- [x] **`timer-sound.ts`:** trzy miejsca `if (c.state === 'suspended')` (`:52, :80, :134`) → `if (c.state !== 'running') void c.resume()`; w `getCtx`: gdy `ctx.state === 'closed'` → `ctx = null` i utwórz nowy; `playSynth` w try/catch z telemetrią. *(DOWÓD: helper resumeIfNotRunning w 3 miejscach; getCtx odtwarza kontekst po closed; telemetria przez NOWY reportClientErrorWithCurrentUid z global-error-telemetry — moduł bez kontekstu uid)*
- [x] **`AppDelegate.swift`:** wynieś `configureAudioSession()` do metody; wołaj ją też w `applicationDidBecomeActive`; dodaj obserwatora `AVAudioSession.interruptionNotification` → przy `.ended` ponowne `setActive(true)`. (Weryfikacja: kompilacja przez `npm run mobile:sync` + build w release pipeline; brak testów Swift — scenariusz urządzeniowy w KROKACH USERA.) *(DOWÓD: metoda już istniała (X18C); dodane wywołanie w applicationDidBecomeActive + obserwator interruption .ended → configureAudioSession; xcodebuild Debug na symulator exit 0)*
- [x] **Test first (keep-awake):** NOWY `keep-awake.test.ts`: ustawienie ON + mock pluginu → `keepScreenAwake` wołane ponownie po `appStateChange(isActive=true)`; błąd pluginu → telemetria (nie cisza); ustawienie OFF → brak wywołań. Muszą PAŚĆ. *(DOWÓD: RED = 3 padły / 2 niezmienniki zielone; GREEN 5/5)*
- [x] **`keep-awake.ts` + `WorkoutDay.tsx`:** w istniejącym listenerze `appStateChange` (`WorkoutDay.tsx:1193`) przy `isActive=true` i żywej sesji → `void keepScreenAwake()`; dodatkowo re-apply przy starcie każdej przerwy (w miejscu startu timera, `ExerciseCard` woła przez prop lub w `RestBar` mount). `keep-awake.ts:38-40`: catch → `trackTelemetryEvent('keep-awake-error', ...)`. Po `keepAwake()` weryfikacja `isKeptAwake()` (jeśli plugin zwraca false → telemetria). *(DOWÓD: samonaprawa w LIBIE (held + addAppStateListener — działa niezależnie od ekranu) + re-apply w listenerze WorkoutDay (guard isCompleted, dep dodany) + re-apply w handleRestStart; telemetria keep-awake-error / keep-awake-not-applied przez client_errors)*
- [x] **Pułapka UI (reguła 6):** `Profile.tsx:250` — wiersz „Dźwięk" widoczny NIEZALEŻNIE od przełącznika „Timer przerwy" (przenieś poza warunek `FEATURE_FLAGS.workoutTimers`). *(DOWÓD: warunek zdjęty, komentarz z uzasadnieniem)*
- [x] **Diagnoza danych (read-only):** odczytaj `users/{uid}.preferences.timerSound` dla konta `g.jasionowicz@gmail.com` (skrypt read-only/konsola). Jeśli `false` → wpisz wynik do raportu (NIE zmieniaj danych — decyzja usera). *(DOWÓD: REST runQuery z tokenem gcloud --account g.jasionowicz@gmail.com: uid U6GDdfg7GmP1k1xJuISIsK9uSUE2, preferences.timerSound = TRUE — mirror nie wyciszał; zero zapisów, KROK USERA 5 bezprzedmiotowy)*
- [x] **Bramki fazy** + commit `fix(audio): AudioContext odporny na interrupted/closed + reaktywacja sesji audio + keep-awake self-healing (Z177)` + DECYZJE.md. *(DOWÓD: test 130 plików / 1147 (fix: rest-settings.test z mockiem telemetrii — nowy import ciągnął Firebase do jsdom), typecheck 0, lint 0, e2e:mock 192 passed)*

---

## FAZA 5 — separator dziesiętny (Z178)

### Z178: przecinek akceptowany wszędzie, wyświetlanie jednolicie z kropką, koniec cichego zerowania

**Pliki:** NOWY `src/lib/decimal-input.ts`, `src/components/ExerciseCard.tsx`, `src/components/MeasurementsForm.tsx`, `src/components/PlateCalculatorSheet.tsx`, `src/components/AddCardioDialog.tsx`, `src/components/ui/chart.tsx`, testy: NOWY `src/test/decimal-input.test.ts`, NOWY `src/test/exercise-card-decimal-input.test.tsx`.

- [x] **Test first (util):** `decimal-input.test.ts`: `parseDecimalInput('47,3')===47.3`, `'47.3'===47.3`, `'47'===47`, `''===null`, `'abc'===null`, `'47,'===null`, `'.'===null`, `'1 234,5'===1234.5` (spacja + U+00A0/U+202F). *(DOWÓD: 4/4 GREEN; guard na końcowy separator dodany jawnie — Number('47.') to 47, a spec każe null)*
- [ ] **`decimal-input.ts`:**
  ```ts
  export const parseDecimalInput = (raw: string): number | null => {
    const s = raw.trim().replace(/[\s  ]/g, '').replace(',', '.');
    if (!s) return null;
    const n = Number(s);
    return Number.isFinite(n) ? n : null;
  };
  export const formatDecimalInput = (n: number, decimals = 1): string =>
    String(Number(n.toFixed(decimals)));
  ```
  `null` = „nie zmieniaj stanu" (NIGDY nie zamieniaj nieparsowalnego wejścia na 0 — to jest sedno fixu). *(DOWÓD: wdrożone w src/lib/decimal-input.ts)*
- [x] **Test first (komponent):** `exercise-card-decimal-input.test.tsx`: wpisanie `"47,3"` → `onSetsChange` z `weight: 47.3` (nie 0, nie 47); stan pośredni `"47,"` → stan wagi się NIE zmienia; po blur pole pokazuje `"47.3"`; lbs `"185,5"` → `fromInput(185.5)`; RPE `"8,5"` → `metrics.rpe === 8.5`. Muszą PAŚĆ. *(DOWÓD: RED 4/4 (m.in. spy niewywołany — jsdom number odrzuca przecinek; "47," commitowało 47); GREEN 4/4)*
- [x] **`DecimalInput` w `ExerciseCard.tsx`:** wzorzec ISTNIEJĄCEGO `DurationInput` (`:246-273`): `type="text"` + `inputMode="decimal"`, lokalny `draft: string | null`, `onChange` → `parseDecimalInput`; wynik `null` → tylko draft, bez commitu; `onBlur` → `setDraft(null)` (pole wraca do wartości ze stanu z kropką). Podpiąć: waga główna (`:773-789`), waga wdd (`:632-643`), asysta (`:660-673`), RPE (`:1147-1157` — `handleMetricChange` bez NaN: `parseDecimalInput`, `null` → nie commituj). *(DOWÓD: komponent DecimalInput z jawnym onClear (puste pole = 0 dla wag / delete dla metryk); metryki rpe/ból/jakość przez DecimalInput, handleMetricChange przyjmuje number|null)*
- [x] **`MeasurementsForm.tsx`:** `:47-56` `Number(x)` → `parseDecimalInput(x)`; inputy (`:109-118`) → `type="text"` + `inputMode="decimal"`. Test: `"82,4"` zapisuje się bez błędu walidacji (dziś PADA — to repro). *(DOWÓD: NOWY measurements-form-decimal.test.tsx, RED = alert przy "82,4"; GREEN 2/2 z niezmiennikiem "nieparsowalne dalej blokuje")*
- [x] **`PlateCalculatorSheet.tsx`** (`:127`, `:310`, `:365`) i **`AddCardioDialog.tsx`** (`:82-83` + `type="text"`, żeby replace przestał być martwy): przepnij na `parseDecimalInput`. *(DOWÓD: 3 pola kalkulatora + minuty/dystans cardio na text+decimal, guard disabled przycisku też przez parseDecimalInput)*
- [x] **`ui/chart.tsx:212`:** `toLocaleString()` → `toLocaleString('en-US')` (koniec zależności od locale urządzenia). *(DOWÓD: wdrożone z komentarzem)*
- [x] **Niezmienniki:** `units.test.ts` bez zmian (wyświetlanie zostaje z kropką — spójne z Watch, PDF, share); żadnych zmian w `units.ts`, `share-utils.ts`, `pdf-report.ts`, wykresach poza `chart.tsx:212`. *(DOWÓD: units.test.ts zielony bez zmian; git diff nie dotyka units/share-utils/pdf-report)*
- [x] **Bramki fazy** + commit `fix(input): przecinek dziesietny akceptowany, koniec cichego zerowania wagi/RPE/pomiarow (Z178)` + DECYZJE.md. *(DOWÓD: test 133 plików / 1157, typecheck 0, lint 0, e2e:mock 192 passed — 8 speców wymagało zmiany roli spinbutton→textbox dla pól kg/Asysta, wynik biegu po fixie czysty)*

---

## FAZA 6 — udostępnianie (Z179, Z180)

### Z179: crash zdjęcia, martwy Pobierz, rozmiar obrazu

**Pliki:** `ios/App/App/Info.plist`, `src/components/ShareWorkoutDialog.tsx`, `src/lib/share-utils.ts`, test: NOWY `src/test/share-image.test.ts`.

- [x] **`Info.plist`:** dodaj `NSPhotoLibraryUsageDescription` („Wybór zdjęcia do obrazu podsumowania treningu.") i `NSCameraUsageDescription` („Zrobienie zdjęcia do obrazu podsumowania treningu.") — oba, bo picker WKWebView oferuje też aparat. *(DOWÓD: oba klucze dodane przed zamknięciem dict)*
- [x] **`ShareWorkoutDialog.tsx:113`:** usuń `capture="environment"` (wybór z galerii, nie wymuszony aparat). *(DOWÓD: atrybut usunięty)*
- [x] **Downscale (test first):** `share-image.test.ts`: helper `downscalePhoto` zwraca dataURL JPEG o wymiarach ≤1080×1920 dla wejścia większego (mock `createImageBitmap`/canvas jak w istniejących testach jsdom). Implementacja w `ShareWorkoutDialog.tsx` (`handlePhotoSelect`, `:63-69`): zamiast `readAsDataURL` → `createImageBitmap(file, { imageOrientation: 'from-image' })` → canvas „cover" max 1080×1920 → `toDataURL('image/jpeg', 0.8)` → `bitmap.close()`. Fallback (Safari <16.4 / brak opcji): `const img = new Image(); img.src = URL.createObjectURL(file); await img.decode();` → `drawImage` → revoke. *(DOWÓD: RED = downscalePhoto is not a function; GREEN — helper w share-utils (3024×4032 → ≤1080×1920, toDataURL jpeg 0.8, bitmap.close), dialog woła downscalePhoto zamiast readAsDataURL)*
- [x] **Format wynikowy (`share-utils.ts:197-207`):** `backgroundColor: '#0f172a'` (JPEG bez alfy — bez tego czarne artefakty), `canvas.toBlob(cb, 'image/jpeg', 0.85)`, nazwa `trening-${date}.jpg`, MIME `image/jpeg` w `new File`. `scale: 2` zostaje (1080×1920 = format IG Story). Oczekiwany rozmiar ~250-400 KB. *(DOWÓD: wdrożone w generateWorkoutImage + nazwy .jpg w dialogu)*
- [x] **Lazy import:** `share-utils.ts:1` → `const { default: html2canvas } = await import('html2canvas-pro')` wewnątrz `generateWorkoutImage` (zdejmuje ~150 KB z chunka WorkoutDay — zapas bundle). *(DOWÓD: check:bundle-budget PASS, initial JS 1 531 095 / 1 536 000)*
- [x] **Pobierz (`ShareWorkoutDialog.tsx:78-84`):** gałąź `Capacitor.isNativePlatform()` → użyj tej samej ścieżki co Udostępnij (`navigator.share` z plikiem; sheet iOS ma „Zapisz obraz" — bez nowych pluginów). Web: anchor download zostaje + `URL.revokeObjectURL` po kliku. `handleShare` (`:86-97`): `try/catch` z ignorowaniem `AbortError` (wzorzec `Analytics.tsx:190-195`). *(DOWÓD: wspólny systemShare z ignorowaniem AbortError; gate przez navigator.canShare — TS2774 dla zawsze-zdefiniowanego navigator.share)*
- [x] **Bramki fazy** + commit `fix(share): zdjecie bez crasha (plist + downscale), Pobierz natywnie, JPEG ~0.3 MB (Z179)`. *(DOWÓD: test 134 pliki / 1160, typecheck 0, lint 0, e2e:mock 192; commit WSPÓLNY z Z180 — oba zadania modyfikują te same dwa pliki share-utils/ShareWorkoutDialog, rozdzielenie wymagałoby sztucznego git add -p)*

### Z180: szablony z logo Strength Save + własne zdjęcie

**Pliki:** `src/lib/share-utils.ts`, `src/components/ShareWorkoutDialog.tsx`, `src/assets/app-icon.png` (istnieje), i18n `pl.ts`/`en.ts`, test: `src/test/share-image.test.ts`.

- [x] **Test first:** `buildShareHtml`/`buildShareHtmlWithPhoto` zwracają HTML zawierający `<img` z logo (src z importu `app-icon.png`) i NIE zawierają tekstowego „SS"; wybór szablonu `'minimal'` daje inny wariant HTML. *(DOWÓD: RED = buildery nieeksportowane; GREEN 3/3 w share-image.test.ts)*
- [x] **`share-utils.ts`:** `type ShareTemplate = 'gradient' | 'photo' | 'minimal'`; wspólny `renderFooter(logoDataUrl)` — `import appIcon from '@/assets/app-icon.png'`, `<img src="${appIcon}" style="width:28px;height:28px;border-radius:6px">` + napis „Strength Save"; podmień stopki w obu istniejących builderach (`:91-94`, `:174-177`); nowy wariant `minimal` (ciemne tło `#0b0b0f`, duży tonaż, logo) — reuse istniejących sekcji, bez nowej logiki danych. Parametr `dim: number` (0.3-0.6) dla przyciemnienia zdjęcia (`:132-139` już ma filter — sparametryzuj). *(DOWÓD: renderFooter w 3 szablonach, minimal z dużym tonażem, dim z clampem 0.3-0.7 → brightness; asset PNG jako hashowany URL, zero wpływu na initial JS)*
- [x] **`ShareWorkoutDialog.tsx`:** rząd 3 przełączników szablonu nad podglądem (chips, wzorzec istniejących chipów w apce); „Dodaj zdjęcie" automatycznie przełącza na `photo`; wybór zapamiętany w localStorage (`fittracker_share_template_v1`). Klucze i18n do OBU locale. *(DOWÓD: chips z aria-pressed + testid; chip Zdjęcie bez zdjęcia otwiera picker (reguła 6); klucze comp.share.tpl* w pl.ts i en.ts)*
- [x] **Bramki fazy** + commit `feat(share): szablony z logo Strength Save + wlasne zdjecie jako tlo (Z180)` + DECYZJE.md (FAZA 6). *(DOWÓD: bramki jak wyżej; commit wspólny z Z179 — uzasadnienie przy Z179)*

---

## FAZA 7 — wydanie (Z181)

- [x] **Pełne bramki:** `npm run test` · `npm run typecheck` · `npm run lint` · `npm run build` · `npm run check:bundle-budget` · `npm run build:mobile` + `npm run check:dist-smoke` · `npm run build` + `npm run check:dist-offline` · `npm run e2e:mock` (reguła 9 przy failach). Bez `| tail` (pipe maskuje exit code). *(DOWÓD: wszystkie z JAWNYM exit code 0 — test 134/1160, typecheck, lint, build, budget 1 531 095/1 536 000, dist-smoke „#root wyrenderowany", dist-offline „SW aktywny", e2e:mock 192 passed EXIT=0)*
- [x] **Sekwencje obowiązkowe (e2e/symulator):** (1) plan → wyjście → szybki trening → powrót do planu → wszystkie ćwiczenia na miejscu → zakończenie → sync; (2) dodaj serię → wpisz dane → usuń → wyjdź → wróć → seria usunięta; (3) start provisional → wyjście na Dashboard → jeden CTA → online → baner znika. *(DOWÓD: (1) full-app.spec.ts:1422 zielony w 192; (2) exercise-card-v3.spec.ts „Z171: usunięta seria nie wraca..." zielony; (3) dashboard-active-session.test.tsx (dokładnie 1 CTA) + auto-sync-provisional.test.ts (promocja kind=checkpoint po online) + e2e „wariant offline (sesja provisional)" w plan-edit-during-workout.spec.ts)*
- [x] **Bump:** `CURRENT_PROJECT_VERSION` → 81 (6 wystąpień). Commit `chore(ios): bump CURRENT_PROJECT_VERSION 80 -> 81 (X22)`. *(DOWÓD: sed 6×80→6×81, preflight w pipeline; commit c7c8cf38)*
- [x] **Push + web deploy:** `git push` + `npm run deploy` (sam push NIE aktualizuje strony). Weryfikacja hasha bundla curl-em. *(DOWÓD: push na main OK; gh-pages „Published", origin/gh-pages:index.html = index-DtDIdtPz.js; curl live po propagacji CDN: „LIVE-HASH-OK: index-DtDIdtPz.js")*
- [x] **iOS:** `scripts/release-ios.sh "Naprawy: usuwanie serii, Dashboard, wideo cwiczen, dzwiek i blokada ekranu, przecinek w wadze, udostepnianie z logo"` a po uploadzie ZAWSZE `uv run --with "pyjwt[crypto]" --with requests scripts/testflight_external.py 81 --whats-new "..."` (obie grupy + Beta App Review). Nigdy dwa pipeline'y iOS równolegle. *(DOWÓD: pipeline exit 0 — UPLOAD SUCCEEDED (UUID fe1ad26d), build 81 VALID, grupa wewnętrzna HTTP 204 + whatsNew HTTP 200 + grupa zewnętrzna HTTP 204, Beta App Review zgłoszone, betaReviewState = APPROVED; testflight_external.py wbudowany w release-ios.sh jako krok [2/2], jeden pipeline)*
- [x] **Zamknięcie:** wpis zbiorczy do `DECYZJE.md` (X22: Z170-Z181, root cause + weryfikacja per zadanie), tech debt do `PLAN.md` (sekcja ODŁOŻONE poniżej), raport końcowy: tabela zadań, dowody bramek, KROKI USERA. *(DOWÓD: DECYZJE.md wpis zbiorczy + 5 wpisów per faza (commity da671335…8951600a), tech debt w PLAN.md — 5 nowych wierszy + Z155 oznaczone jako zmitygowane przez Z175 (PLAN.md gitignored, zapis lokalny zgodnie z konwencją); raport końcowy w sesji /loop)*

---

## KOLEJNOŚĆ I ZALEŻNOŚCI

FAZA 1 → 2 → 3 → 4 → 5 → 6 → 7, sekwencyjnie. FAZA 3 (wideo) PRZED FAZĄ 4 (audio), bo autoplay wideo jest współprzyczyną ciszy — po Z176 weryfikacja Z177 jest czysta. Fazy 5 i 6 niezależne od reszty, ale nie zmieniaj kolejności (wspólne pliki: ExerciseCard w F1/F3/F5).

## KROKI USERA (po buildzie 81, fizyczny iPhone)

1. Dialog usuwania: w trakcie wpisywania wagi (klawiatura otwarta) tapnij X → USUŃ — seria znika za pierwszym razem.
2. Dźwięk/ekran: scenariusz A z analizy — trening z animacjami, przerwa do końca bez dotykania telefonu (gong gra, ekran nie gaśnie), blokada ekranu → powrót → kolejna przerwa (gong nadal gra).
3. Share: Dodaj zdjęcie (galeria) → bez crasha; Pobierz → sheet z „Zapisz obraz"; rozmiar pliku < 0.5 MB.
4. Wideo: karty pokazują statyczne miniatury; dialog odtwarza animację (także w Low Power Mode — wtedy przycisk play).
5. Decyzja: jeśli raport pokaże `preferences.timerSound=false` w Firestore — potwierdź czy wyczyścić.

## ODŁOŻONE (dopisuj w trakcie)

- Postery .jpg dla miniatur na Bunny CDN (ffmpeg pierwsza klatka + upload `animacje-cwiczen/na_bunny.sh`) — wątek folderu animacji, poza X22.
- `@capacitor/filesystem` + `@capacitor/share` dla „prawdziwego" zapisu do Rolki — na teraz wystarcza share sheet.
- Ujednolicenie separatora tysięcy (`toLocaleString(pl-PL)` wąska spacja vs reszta apki) — osobny mały ticket.
- Skan-guard zabraniający `parseFloat(e.target.value)` + `type="number"` z `inputMode="decimal"` (wzorzec `i18n-hardcoded-scan`).
- Architektura: Dashboard na maszynie stanów sesji (`deriveWorkoutSessionPhase`) zamiast 3 luźnych predykatów.
