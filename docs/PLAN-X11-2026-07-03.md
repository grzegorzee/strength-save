# PLAN X11: przemodelowanie UX/UI — nawigacja, plany i ćwiczenia "po prostu działają", dane w akcji, porządki (2026-07-03)

> **Dla agenta wykonawcy:** WYMAGANY SUB-SKILL: `superpowers:executing-plans` (albo `superpowers:subagent-driven-development`). Kroki mają checkboxy `- [ ]`. Wykonuj zadania PO KOLEI, fazy rozdzielone checkpointami. Nie przeskakuj checkpointów.
> Kontekst: plany Z13-Z28 (R1), Z29-Z46 (R2) i Z47-Z65 (X10) są WYKONANE I WDROŻONE (web + functions + rules + iOS build 49 na TestFlight). Core loop treningu jest stabilny: auto-resume, maszyna stanów sesji, Adaptive Coach za flagą `adaptiveCoach`.
> Źródło znalezisk: 3 raporty rozpoznawcze z 2026-07-03 (mapa nawigacji i stron; flow planów i ćwiczeń; inwentarz danych zbierane-vs-pokazywane). Wszystkie odwołania plik:linia pochodzą z tych raportów — przed edycją ZAWSZE zweryfikuj aktualny stan pliku (numery linii mogły się przesunąć).

**Goal:** Aplikacja z ambicją top 1 App Store w swojej kategorii: (1) nawigacja bez ślepych zaułków — każda strona osiągalna na telefonie, linki krzyżowe tam, gdzie user ich oczekuje, zero martwych przycisków; (2) dodawanie ćwiczeń, dodawanie planów i komponowanie własnych planów to JEDEN spójny, prosty system, który po prostu działa (w tym własne ćwiczenia spoza biblioteki i naprawiony wybór 6 dni); (3) dane, które już zbieramy, zaczynają pracować dla usera (notatki historyczne, ból, jakość techniki, czas trwania, pomijane ćwiczenia, obwody ciała), a to, co zbieramy na darmo bez planu na pokazanie — wylatuje; (4) postępy przestają być rozsypane po 5 ekranach z duplikatami; (5) polish pod App Store (empty states, haptyka, prośba o ocenę). Na końcu JEDEN release train: web + rules (+ functions jeśli ruszone) + iOS na TestFlight.

**Architektura podejścia:** Najpierw fundament nawigacyjny (bez niego reszta jest nieosiągalna na telefonie), potem najważniejszy dla usera obszar planów i ćwiczeń, potem dane i konsolidacja postępów, na końcu porządki i polish. Każda faza zostawia działający, przetestowany soft. Zasada projektowa: NIE dodajemy nowych ekranów, jeśli można wzbogacić istniejący; NIE zostawiamy dwóch implementacji tej samej rzeczy.

**Tech stack:** React + TypeScript + Vite, Firebase (Firestore + rules z zamkniętymi schematami), IndexedDB + localStorage, Capacitor iOS, vitest + Playwright (mock i emulator), shadcn/ui.

---

## 0. AUTORYZACJE I TWARDE ZASADY

1. **Deploy na produkcję BEZ pytania o zgodę** — wyłącznie w FAZIE 7 (release train), wszystkie kanały: `git push`, `firebase deploy` (rules, functions jeśli ruszone), `npm run deploy` (web), `scripts/release-ios.sh` (TestFlight). Autoryzacja stała usera z 2026-07-03 (X10, sekcja 0): "wszystkie zmiany mają polecieć od razu na produkcję; dostęp do aplikacji mam tylko ja i działam z nią tylko na TestFlight". Uruchomienie tego planu przez usera = potwierdzenie tej autoryzacji.
2. Przed FAZĄ 7 niczego nie wdrażaj.
3. Środowisko docelowe: siłownia, telefon, ekran gaśnie. Kontrakty X10/R2/R1 są NIETYKALNE: silnik syncu (`workout-sync-engine.ts`), kolejka referencyjna, writeId/pendingWriteVersion, snapshot draftu, tombstone promocji, auto-resume (`workout-resume.ts`, `ActiveWorkoutResume`), maszyna stanów (`workout-session-state.ts`), hydracja (`workout-hydration.ts`). Zmiany UI w WorkoutDay NIE dotykają logiki zapisu.
4. Dane usera święte: zero testowych zapisów na realnym koncie; przy zmianach w Measurements/planach proponuj eksport w scenariuszu testowym.
5. TDD z bezpiecznikiem: każde zadanie zaczyna się od failing testu odtwarzającego problem/brak (vitest w `src/test/`, e2e w `e2e/`). **Jeśli test NIE potwierdza znaleziska (np. numer linii nieaktualny, zachowanie już naprawione): STOP dla zadania, wpis do DECYZJE.md, idź dalej.**
6. Rules mają ZAMKNIĘTE schematy (hasOnly): **każda nowa kolekcja/pole zapisywane przez klienta wymaga aktualizacji `firestore.rules` + testów w `scripts/test-firestore-rules.mjs`** (JDK21: `export JAVA_HOME=/opt/homebrew/opt/openjdk@21 && export PATH="$JAVA_HOME/bin:$PATH"`). Dotyczy zwłaszcza Z71 (custom_exercises).
7. i18n: każdy nowy klucz do OBU plików `src/i18n/locales/pl.ts` i `en.ts`. Teksty PL bez pauz (em-dash), pełne polskie znaki.
8. Chirurgicznie: jedno zadanie = izolowane commity `feat|fix|refactor|chore(obszar): opis (Z<nr>)`. Zero poprawiania sąsiedniego kodu. Usuwasz kod → usuń też osierocone importy/klucze i18n (rg przed usunięciem).
9. Weryfikacja przy checkpointach (minimum): `npm run test`, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run check:bundle-budget`. Fazy dotykające rules: + `npm run test:rules`. Nawigacja/plany/treningi: + `npm run e2e:mock`. FAZA 2 dodatkowo `npm run e2e:emulator`.
10. Wpis do DECYZJE.md po każdej fazie (co, dlaczego, root cause, weryfikacja).
11. Pułapki narzędziowe: globalny `firebase` z PATH (nie `npx firebase`); testy klienta w `src/test/`; hak e2e `fittracker_e2e_workouts` (workout-read-store) i `fittracker_e2e_cycles` (usePlanCycles) do wstrzykiwania danych w mock e2e; helpery w `e2e/helpers.ts` (`setE2EWorkouts`, `setE2ECycles`, `writeWorkoutDraftDb`).
12. Design: trzymaj się istniejącego języka wizualnego (karty, badge, chipy, tokeny `fitness-*`, font-heading uppercase). Żadnych nowych bibliotek UI. Mockupów nie upraszczaj — ale tu mockupów nie ma, więc wzoruj się 1:1 na istniejących sekcjach (np. karta Coach z Dashboardu jako wzorzec nowych kart).

---

## FAZA 0: Baseline

### Zadanie Z66.0: zielony baseline

- [x] Krok 1: `git status` czyste, `git log --oneline -1` (HEAD = fe7f2b4 lub nowszy).
- [x] Krok 2: `npm run test` (oczekiwane 560/560), `npm run typecheck`, `npm run lint`, `npm run build`, `npm run check:bundle-budget`. Jeśli nie zielone: STOP, zgłoś userowi.

---

## FAZA 1: Nawigacja bez ślepych zaułków (fundament)

Stan obecny (raport nawigacji): mobilny drawer jest MARTWY — `Layout.tsx:28` deklaruje `sidebarOpen`, ale `setSidebarOpen(true)` nie jest wołane nigdzie, `AppHeader.tsx` nie ma hamburgera, więc `Sheet` w `AppNavigation.tsx:226-231` jest nieosiągalny. Skutek: na telefonie żyje tylko 5 pozycji dolnego paska, a `/history`, `/measurements`, `/cycles` mają ZERO linków wchodzących (sieroty), `/achievements` tylko warunkową kartę PR. Do tego `ExerciseDetail` osiągalny wyłącznie z biblioteki (z treningu NIE DA SIĘ otworzyć instrukcji ćwiczenia), a w `ExerciseDetail` są dwa martwe elementy: "Dodaj do treningu" (toast "wkrótce", `ExerciseDetail.tsx:160-165`) i zakładki zapisywane do localStorage, którego nikt nie czyta (`:69-74`).

### Zadanie Z66: hamburger + żywy drawer mobilny

**Goal:** Każda strona z bocznego menu jest osiągalna na telefonie w maksymalnie 2 tapnięcia.

**Fix:** (a) `AppHeader.tsx`: przycisk hamburger (ikona `Menu` z lucide, po lewej; wzorzec przycisków `rounded-2xl bg-muted/60` jak w WorkoutDay) widoczny TYLKO na mobile (`md:hidden`), woła `onMenuClick` przekazane z Layout. (b) `Layout.tsx`: przekaż `onMenuClick={() => setSidebarOpen(true)}` do AppHeader (stan już istnieje). (c) Upewnij się, że `Sheet` w AppNavigation zamyka się po kliknięciu pozycji (jeśli `onClose` nie jest wołane w linkach — dodaj). (d) Dolny pasek zostaje bez zmian (5 pozycji) — drawer uzupełnia, nie zastępuje.

**Workflow:**
- [x] Krok 1: failing test e2e w `e2e/full-app.spec.ts` (viewport 375x667): na mobile nie widać linku "Historia"; po tapnięciu hamburgera (aria-label, dodaj `aria-label` z i18n `nav.openMenu`) drawer widoczny, link "Historia" klikalny, nawigacja na `/history` działa; drugi test: `/measurements` i `/cycles` osiągalne z drawera.
- [x] Krok 2: implementacja (a)-(d); klucz i18n `nav.openMenu` do OBU locale.
- [x] Krok 3: `npm run test`, typecheck, lint, e2e:mock; commit `fix(nav): hamburger otwiera drawer mobilny, sieroty osiągalne (Z66)`.

### Zadanie Z67: linki krzyżowe, których user oczekuje

**Goal:** Z miejsca, w którym user o czymś myśli, dociera do szczegółów jednym tapnięciem.

**Fix:** (a) **Trening → instrukcje ćwiczenia (kluczowa luka):** w `ExerciseCard.tsx` nazwa ćwiczenia (h3, okolice linii 493 wg raportu) dostaje akcję otwarcia `/exercise/:slug` (użyj `slugifyExercise` z `@/lib/exercise-media`, wzorzec `ExerciseLibrary.tsx:162`); ta sama akcja w liście ćwiczeń `DayPlan.tsx:245-273` (dziś nieklikalne divy). UWAGA: w AKTYWNYM treningu nawigacja w środku sesji jest bezpieczna (draft w IndexedDB, auto-resume nie odpala się przy nawigacji wewnątrz apki — kontrakt Z49), ale dodaj ikonę `Info`/chevron zamiast czynić cały nagłówek linkiem, żeby nie łapać przypadkowych tapnięć przy odhaczaniu. Dla ćwiczeń bez szczegółów (custom w przyszłości / brak slug-matcha) — nie renderuj ikony. (b) Dashboard stat "waga" (`Dashboard.tsx:794`) prowadzi do `/measurements` (dziś do Analytics charts). (c) Dashboard sekcja "Trening w tym tygodniu" dostaje na dole link "Pełna historia" → `/history`. (d) `Cycles` ↔ Dashboard: karta "Twój plan" na Dashboardzie dostaje drugorzędny link "Cykle" → `/cycles`. (e) Achievements i Analytics-progression (te same 1RM): w zakładce progression `AnalyticsChartsTab.tsx:405-450` link "Wszystkie rekordy" → `/achievements`.

**Workflow:**
- [x] Krok 1: failing test e2e: wejdź na `/workout/day-1`, tapnij ikonę info przy pierwszym ćwiczeniu → URL `/exercise/...`, widać instrukcje; wróć (navigate(-1)) → trening dalej na miejscu (draft nietknięty — asercja na obecność karty ćwiczenia).
- [x] Krok 2: implementacja (a)-(e); klucze i18n (`card.details`, `dash.fullHistory`, `dash.cycles`, `charts.allRecords`) do OBU locale.
- [x] Krok 3: pełne bramki + e2e:mock; commit `feat(nav): linki krzyżowe trening→ćwiczenie, waga→pomiary, historia i cykle z Dashboardu (Z67)`.

### Zadanie Z68: zero martwych przycisków

**Znalezisko:** `ExerciseDetail.tsx:160-165` przycisk "Dodaj do treningu" pokazuje toast "wkrótce" (stub od miesięcy). Zakładki (`:69-74`, klucz localStorage `bookmarked-exercises`) są zapisywane, ale ŻADEN inny plik ich nie czyta — funkcja-sierota.

**Fix:** Usuń OBA elementy (przycisk "Dodaj do treningu" + przycisk zakładki + stałą `BOOKMARK_KEY` + `readBookmarks` + stan + osierocone klucze i18n z obu locale). Zamiast stubu: w Z71 ćwiczenia dodaje się do planu przez edytor planu (właściwe miejsce), a "dodanie do dzisiejszego treningu" już istnieje jako swap/add w WorkoutDay. Martwy przycisk obiecujący funkcję = gorszy niż brak przycisku (recenzje App Store).

**Workflow:**
- [x] Krok 1: `rg "bookmarked-exercises|addedSoon"` — potwierdź zero konsumentów poza ExerciseDetail (bezpiecznik).
- [x] Krok 2: usunięcie + osierocone i18n; typecheck/lint/test; e2e:mock (ExerciseDetail renderuje się).
- [x] Krok 3: commit `chore(exercise-detail): usunięcie martwego przycisku Dodaj do treningu i zakładek-sierot (Z68)`.

### CHECKPOINT FAZY 1
- [x] `npm run test`, typecheck, lint, build, `npm run e2e:mock`.
- [x] Wpis do DECYZJE.md.

---

## FAZA 2: Plany i ćwiczenia — jeden system, który po prostu działa

Stan obecny (raport flow): trzy niezależne implementacje pickera ćwiczeń (PlanBuilder `:319-386`, PlanEditor `:158-234`, UserPlanEditor `:256-324`) + osobny inline swap w WorkoutDay (`:2225-2278`, full-text bez kategorii); PlanEditor i UserPlanEditor to ~90% ten sam kod; PlanBuilder umie zarządzać dniami ale nie ma reorderu ćwiczeń, PlanEditor odwrotnie — ma reorder, ale NIE umie dodać/usunąć dnia, zmienić weekday/focus/czasu trwania; wybór 6 dni w wizardzie cicho degraduje do 5 (brak szablonu 6-dniowego + `applyWeekdaysToPlanDays` robi `slice`); poziom "elite" mapuje się na advanced (martwa opcja); onboarding NIE ma ekranu preview mimo labela "dalej do podglądu"; custom ćwiczeń nie da się dodać NIGDZIE.

### Zadanie Z69: jeden wspólny ExercisePicker

**Goal:** Wybór ćwiczenia wygląda i działa identycznie wszędzie: wyszukiwarka PL/EN, chipy kategorii, sensowna kolejność, opcjonalne wykluczenia.

**Fix:** Nowy komponent `src/components/ExercisePicker.tsx`: props `{ open, onOpenChange, onPick(exercise), excludeNames?: string[], title?: string, description?: string }`. Wnętrze: Input szukajki + chipy `categoryLabels` + lista z `exerciseLibrary` filtrowana funkcją `matchesQuery` (przenieś z PlanBuilder — szuka po nazwie PL, EN przez `EXERCISE_NAME_EN` i kategorii); pozycja listy pokazuje nazwę (zlokalizowaną `localizeExerciseName`), kategorię i badge compound/isolation/BW. Podmień na niego: (1) picker w `PlanBuilder.tsx:319-386`, (2) `ExerciseSwapDialog` w `PlanEditor.tsx:158-234` (swap i add), (3) `UserPlanEditor.tsx:256-324`, (4) inline swap w `WorkoutDay.tsx:2225-2278` — z zachowaniem dwóch opcji zakresu "tylko dziś"/"na stałe" (footer pickera dostaje opcjonalny slot `renderFooter(picked)` — WorkoutDay wstrzykuje tam swoje 2 przyciski scope; pozostali używają domyślnego "Wybierz"), (5) `ExerciseSwapDialog.tsx` używany w NewPlan preview — zostaje jako cienki wrapper na ExercisePicker albo znika (rg po użyciach). Przy okazji: JEDNA stała `WEEKDAYS` i JEDNA para domyślnych serii (`'3 x 6-8'`/`'3 x 10-12'`) w `src/lib/plan-cycle-utils.ts` (dziś zduplikowane w 3 plikach — raport, sekcja Duplikacje 4-5).

**Workflow:**
- [x] Krok 1: failing test `src/test/exercise-picker.test.tsx` (render + filtrowanie po query PL i EN + chip kategorii + excludeNames ukrywa pozycję + onPick zwraca ćwiczenie) — wzorce testów komponentów jak `error-boundary.test.tsx`.
- [x] Krok 2: implementacja komponentu + testy zielone.
- [x] Krok 3: podmiana w 5 miejscach (po jednej weryfikacji e2e na miejsce: PlanBuilder dodanie ćwiczenia, PlanEditor swap, WorkoutDay swap "tylko dziś" — istniejące testy e2e swapu muszą przejść, zaktualizuj selektory jeśli trzeba). Nowe testy e2e ujawniły pre-existing bug swapu "tylko dziś" (draft nie utrwalany) — naprawiony przez saveDraftSnapshot z exerciseNames.
- [x] Krok 4: pełne bramki + e2e:mock; commit `refactor(plans): jeden ExercisePicker zamiast czterech implementacji (Z69)`.

### Zadanie Z70: jeden edytor planu — dni i ćwiczenia w komplecie

**Goal:** Edycja istniejącego planu i budowa nowego to ten sam zestaw możliwości: dodaj/usuń/duplikuj dzień, zmień dzień tygodnia i focus, reorder ćwiczeń, czas trwania planu.

**Fix:** (a) Wydziel z PlanEditor współdzielony komponent `src/components/PlanDaysEditor.tsx` (dni jako karty; per dzień: weekday-chipy, focus-input, lista ćwiczeń z reorderem strzałkami up/down, add/swap/remove przez ExercisePicker; per plan: add day do 6, remove day, duplikuj dzień, chipy czasu trwania [8,10,12,16]). Logika mutacji planu przez ISTNIEJĄCE akcje `useTrainingPlan` (`moveExercise`, `swapExercise`, `addExercise`, `removeExercise`, `updateSets`) + NOWE czyste funkcje w `src/lib/plan-day-edit.ts`: `addPlanDay(days, weekday)`, `removePlanDay(days, dayId)`, `duplicatePlanDay(days, dayId)` (nowe id dnia, kopiuje ćwiczenia z nowymi id wzorcem `buildSwappedExerciseId`), `setPlanDayWeekday(days, dayId, weekday)` (z auto-zamianą jak `PlanBuilder.setWeekday:109`), `setPlanDayFocus(days, dayId, focus)`. (b) `PlanEditor.tsx` renderuje PlanDaysEditor + zapisuje przez istniejący `savePlan` (transakcja z revision — kontrakt nietykalny; zmiany dni synchronizują aktywny cykl jak dotychczasowe edycje in-place). (c) `UserPlanEditor.tsx` (admin) renderuje TEN SAM PlanDaysEditor (props: `plan, onSave, i18nPrefix?` — utrzymaj istniejące teksty admina albo ujednolić na `planeditor.*`; decyzja wykonawcy, odnotuj w DECYZJE.md). (d) `PlanBuilder.tsx` renderuje PlanDaysEditor w trybie "builder" (bez zapisu do Firestore, stan lokalny + autozapis szkicu do localStorage jak dziś). (e) Reorder ćwiczeń działa więc też w builderze (luka nr 4 z raportu), a zarządzanie dniami też w edytorze (luka nr 3).

**Workflow:**
- [x] Krok 1: failing testy `src/test/plan-day-edit.test.ts`: add (max 6, pierwszy wolny weekday), remove, duplicate (nowe id dnia i ćwiczeń, treść skopiowana), setWeekday z auto-zamianą, setFocus; minimum 8 przypadków (10 zrobionych).
- [x] Krok 2: implementacja czystych funkcji; testy zielone.
- [x] Krok 3: PlanDaysEditor + podpięcie w PlanEditor; e2e: pełny flow dni (add/weekday/duplicate/remove/reorder) na builderze (stan lokalny — mock e2e nie zapisze do Firestore), na `/plan/edit` asercje na dostępność zarządzania dniami. Istniejące testy e2e planów przechodzą.
- [x] Krok 4: podpięcie w PlanBuilder i UserPlanEditor; usuń zduplikowany kod (rg po starych nazwach); pełne bramki + `npm run e2e:emulator` (test plan-lifecycle korzysta z edycji planów).
- [x] Krok 5: commit(y) `refactor(plans): PlanDaysEditor — jeden edytor dni dla builder/editor/admin (Z70)`.

### Zadanie Z71: własne ćwiczenia (custom exercises)

**Goal:** User dodaje własne ćwiczenie (nazwa, kategoria, typ, bodyweight) i używa go wszędzie tam, gdzie ćwiczeń z biblioteki: plan, swap, trening, historia, rekordy.

**Fix:** (a) Model: kolekcja Firestore `custom_exercises/{autoId}`: `{ userId, name (string, 2-80 znaków), category (jeden z 8 kluczy categoryLabels), isBodyweight (bool), type ('compound'|'isolation'), createdAt (number) }`. Rules: create/update/delete tylko właściciel, zamknięty schemat hasOnly + typy + widełki długości nazwy (wzorzec `client_errors` w `firestore.rules:342`); read tylko właściciel + admin. (b) Hook `src/hooks/useCustomExercises.ts`: listener z `limit(100)` (stała `CUSTOM_EXERCISES_LISTENER_LIMIT`), zwraca `{ customExercises, addCustomExercise, removeCustomExercise, isLoaded }`; obiekty w kształcie `Exercise` z biblioteki (`id: 'custom-'+docId`, `instructions: []`). (c) `ExercisePicker` (Z69) dostaje props `customExercises?: Exercise[]` i sekcję "Twoje ćwiczenia" nad wynikami biblioteki + przycisk "Dodaj własne ćwiczenie" (formularz inline w pickerze: nazwa, kategoria-chipy, przełączniki BW/typ; po zapisie od razu onPick). (d) Zapisy treningu i planu już przenoszą `name` jako snapshot (resolver historii działa po nazwach — raport R2/M15), więc historia i PR działają bez zmian; sprawdź `isBodyweightExercise` (heurystyka po nazwie) — dla customów źródłem prawdy jest pole `isBodyweight` z pickera, przekazywane jak dotąd przez props. (e) `ExerciseDetail` dla customów: NIE linkuj (Z67 pokazuje ikonę info tylko dla ćwiczeń z biblioteki — slug-match). (f) i18n `custom.*` do OBU locale.

**Bezpiecznik zakresu:** jeśli okaże się, że jakiś konsument twardo zakłada, że każde `exercise.id` istnieje w `exerciseLibrary` (rg po `exerciseLibrary.find`), napraw punktowo z fallbackiem na dane z planu/customów; jeżeli takich miejsc jest >6 — STOP, wpis do DECYZJE.md z listą, dokończ te, które zdążyłeś, resztę wypisz.

**Workflow:**
- [x] Krok 1: failing testy rules w `scripts/test-firestore-rules.mjs`: create własnego OK; create z cudzym userId DENIED; category spoza listy DENIED; nazwa 200 znaków DENIED; update cudzego DENIED. `npm run test:rules`.
- [x] Krok 2: rules + testy zielone (95 -> 110).
- [x] Krok 3: failing test `src/test/use-custom-exercises.test.tsx` (mock firestore jak w testach hooków; add zwraca kształt Exercise z prefiksem custom-) + implementacja hooka.
- [x] Krok 4: picker + formularz; e2e mock: formularz w builderze tworzy "Moje wiosłowanie" i dodaje do planu; hak `fittracker_e2e_custom_exercises` (w E2E mode hook robi pełny CRUD na localStorage) + helper `setE2ECustomExercises` w `e2e/helpers.ts`.
- [x] Krok 5: pełne bramki + test:rules + e2e:mock; commit `feat(exercises): własne ćwiczenia usera w pickerze, planie i treningu (Z71)`.

### Zadanie Z72: naprawa wyboru 6 dni + martwa opcja elite + luki szablonów

**Znalezisko:** wizard oferuje 6 dni (`PlanWizard.tsx:273`), ale nie ma szablonu 6-dniowego — `getRecommendedPlan` zwraca 5-dniowy, a `applyWeekdaysToPlanDays` (`plan-cycle-utils.ts:34`) robi `days.slice(0, selected.length)` i 6. zaznaczony dzień jest ignorowany PO CICHU. Poziom "elite" mapuje się na advanced (`PlanWizard.tsx:65`) — iluzoryczny wybór. Cele fat_loss i athletic mają po 1 szablonie.

**Fix:** (a) NOWY szablon 6-dniowy Push/Pull/Legs ×2 w `planTemplates.ts` (objective `build_muscle`, level intermediate/advanced; ćwiczenia WYŁĄCZNIE z istniejącej biblioteki — komponuj z nazw używanych w innych szablonach; 6 dni pon-sob; serie w konwencji pozostałych szablonów). (b) Twardy bezpiecznik dopasowania: w `PlanWizard` krok 4→5, jeśli `getRecommendedPlan(...).days.length !== daysPerWeek`, pokaż pod kartą rekomendacji ostrzeżenie (i18n `wizard.daysMismatch`: "Ten plan ma {n} dni treningowych, wybrałeś {m}. Dopasuj liczbę dni albo wybierz inny plan.") i mapuj TYLKO tyle dni tygodnia, ile plan ma — user widzi prawdę zamiast cichej degradacji. (c) Usuń poziom "elite" z `LEVELS` (`PlanWizard.tsx:44-49`) + `mapLevel` + osierocone i18n (4 opcje UI → 3 realne były kłamstwem). (d) OPCJONALNIE (wykonaj, jeśli fazy idą sprawnie): po jednym dodatkowym szablonie dla fat_loss (4 dni, obwodowo-siłowy) i athletic (4 dni) — te same zasady co (a). Jeśli pominięte: wpis do DECYZJE.md.

**Workflow:**
- [x] Krok 1: failing test `src/test/planTemplates.test.ts`: `getRecommendedPlan('build_muscle','intermediate',6).days.length === 6`; test integralności WSZYSTKICH szablonów już istniał; mismatch jako czysta funkcja `planDaysMismatch(plan, daysPerWeek)` w `plan-cycle-utils.ts` + test.
- [x] Krok 2: implementacja (a)-(c); testy zielone; e2e: wizard z 6 dniami rekomenduje plan 6-dniowy (PPL×2, Legs B widoczny, bez ostrzeżenia). Opcja (d) — dodatkowe szablony fat_loss/athletic — POMINIĘTA (wpis w DECYZJE.md).
- [x] Krok 3: pełne bramki; commit `feat(plans): szablon 6-dniowy PPL×2, ostrzeżenie mismatch dni, usunięcie martwego poziomu elite (Z72)`.

### Zadanie Z73: onboarding z podglądem + start własnego planu z szablonu

**Znalezisko:** onboarding commit-uje plan BEZ ekranu preview (NewPlan go ma), mimo że przycisk buildera nazywa się "dalej do podglądu" (`planbuilder.nextToPreview`) — label kłamie. PlanBuilder startuje zawsze od zera (brak "kopiuj z szablonu").

**Fix:** (a) Onboarding przechodzi przez TEN SAM ekran preview co NewPlan: wydziel preview z `NewPlan.tsx:262-314` do komponentu `src/components/PlanPreview.tsx` (lista dni/ćwiczeń + swap przez ExercisePicker + confirm) i użyj w obu miejscach; w onboardingu po confirm — dotychczasowe `completeOnboardingPlan`. (b) PlanBuilder dostaje na starcie (gdy pusty i brak szkicu) wybór: "Zacznij od zera" / "Zacznij od szablonu" (lista 11+ szablonów; wybór kopiuje `days` szablonu do stanu buildera z nowymi id dni — użyj `duplicatePlanDay` z Z70 albo dedykowanej `clonePlanDays` w `plan-day-edit.ts`).

**Workflow:**
- [x] Krok 1: failing test e2e onboardingu (scenariusz `new-user` przez `setE2EAuthScenario`): wybór planu → widać ekran podglądu (nagłówek preview) PRZED zapisem; swap w podglądzie działa.
- [x] Krok 2: ekstrakcja PlanPreview + podpięcie w NewPlan (bez zmiany zachowania — istniejące e2e NewPlan przechodzą) i w Onboarding.
- [x] Krok 3: "Zacznij od szablonu" w PlanBuilder + test `clonePlanDays` (nowe id, głęboka kopia, brak współdzielenia referencji).
- [x] Krok 4: pełne bramki + e2e:mock + `npm run e2e:emulator` 12/12 (test onboarding own-plan zaktualizowany do nowego flow: start od zera + podgląd przed zapisem); commit `feat(plans): podgląd planu w onboardingu, własny plan startuje z szablonu (Z73)`.

### CHECKPOINT FAZY 2
- [x] `npm run test`, typecheck, lint, build, `npm run test:rules`, `npm run e2e:mock`, `npm run e2e:emulator`.
- [x] Wpis do DECYZJE.md.

---

## FAZA 3: Dane, które mamy, zaczynają pracować

Stan obecny (raport inwentarza): `quality` (technika 1-5) zbierane i NIGDZIE nie pokazywane; `pain` tylko jako globalna średnia tygodniowa; `notes` per ćwiczenie i `dayNotes` znikają po sesji; `durationSec` bez trendu; `skippedExercises` bez żadnej analityki; 7 pól obwodów ciała (`armLeft/armRight/hips/thighLeft/thighRight/calfLeft/calfRight`) zapisywanych i nigdy nie renderowanych; `averageWorkoutsPerWeek` cyklu liczone i nie pokazywane.

### Zadanie Z74: notatki wracają do usera (per ćwiczenie + dzień)

**Fix:** (a) Czysta funkcja `getExerciseNoteHistory(workouts, exerciseId, limit=5)` w NOWYM `src/lib/exercise-notes.ts`: zwraca `[{date, note}]` z ukończonych sesji (pole `ExerciseProgress.notes`), najnowsze pierwsze, resolver nazw NIE potrzebny (id + name snapshot już w sesji). (b) `ExerciseProgressionDialog.tsx`: sekcja "Twoje notatki" (lista data + treść). (c) `ExerciseCard` w AKTYWNYM treningu: pod istniejącą podpowiedzią nextAdvice pokaż OSTATNIĄ notatkę z poprzedniej sesji tego ćwiczenia ("Ostatnio: «pas za luźny»") — dane przez `exerciseInsights` w WorkoutDay (rozszerz memo o `lastNote`, wzorzec `coachRecommendation` z Z64). (d) `WorkoutHistory`: wpis rozwinięty pokazuje `dayNotes` i notatki per ćwiczenie (dziś ich tam nie ma wcale).

**Workflow:**
- [x] Krok 1: failing testy `src/test/exercise-notes.test.ts` (kolejność, limit, pomijanie sesji bez notatek, pusta historia → []).
- [x] Krok 2: implementacja + (b)(c)(d); i18n `notes.*` do OBU locale. WorkoutHistory dostała rozwinięcie wpisu (Szczegóły) — Z80 je rozszerzy. Hak mock E2E w workout-read-store obsługuje też paginowaną historię.
- [x] Krok 3: e2e mock: `setE2EWorkouts` z sesją z notatką → wejdź w trening tego dnia, widać "Ostatnio:"; drugi test na rozwinięcie historii; pełne bramki; commit `feat(insights): historia notatek per ćwiczenie w dialogu progresji, karcie treningu i historii (Z74)`.

### Zadanie Z75: ból i technika jako trend (dane już są)

**Fix:** (a) `getExerciseMetricHistory(workouts, exerciseId)` w `src/lib/rza-metrics.ts` (obok istniejących): `[{date, rpe?, pain?, quality?}]` z ukończonych sesji. (b) `ExerciseProgressionDialog`: pod wykresem 1RM mini-sekcja "RPE / Ból / Technika" — 3 sparkline'y (recharts LineChart, wzorzec istniejących wykresów dialogu; jeśli dialog nie ma jeszcze recharts — lazy jak `TonnageTrendChart` z Z54). (c) Analytics, subzakładka z metrykami RZA (`RzaMetricsCard`): dodaj listę "Ćwiczenia z bólem ≥3 w ostatnich 4 tygodniach" (nazwa + max ból + liczba sesji z bólem; klik → ExerciseProgressionDialog) — czysta funkcja `getPainWatchlist(workouts, now, weeks=4)` w `rza-metrics.ts`. (d) `quality` przestaje być zbierane na darmo: średnia techniki ostatnich 4 tygodni jako trzecia liczba w RzaMetricsCard (obok objętości i RPE).

**Workflow:**
- [x] Krok 1: failing testy w `src/test/rza-metrics.test.ts` (rozszerzony): metric history kolejność/pomijanie braków; watchlist tylko ból ≥3 i okno 4 tygodni; średnia quality liczona z sesji, które mają quality.
- [x] Krok 2: implementacja + UI; i18n do OBU locale (dialog ma już recharts statycznie — lazy niepotrzebny).
- [x] Krok 3: pełne bramki + e2e:mock; commit `feat(insights): trend bólu i techniki per ćwiczenie + watchlist bólu (Z75)`.

### Zadanie Z76: czas trwania, gęstość, pomijane ćwiczenia

**Fix:** (a) `src/lib/workout-time-stats.ts`: `getDurationTrend(workouts, months=6)` → `[{month, avgMinutes, densityKgPerMin}]` (gęstość = tonaż/min; tylko sesje z `durationSec`), `getSkippedStats(workouts, resolver, limit=5)` → `[{exerciseName, count}]` z `skippedExercises` (id → nazwa przez istniejący `buildWorkoutResolver`). (b) `AnalyticsChartsTab`: subzakładka workouts dostaje drugi wykres "Czas i gęstość" (bar avgMinutes + linia density; wzorzec istniejących wykresów) — bez nowej zakładki. (c) Sekcja "Najczęściej pomijane" (lista top 5 z licznikiem) w zakładce workouts + podpowiedź "Pomijasz regularnie? Podmień ćwiczenie w planie" z linkiem do `/plan/edit`. (d) `Cycles.tsx`: dopisz do statystyk aktywnego cyklu już policzone `averageWorkoutsPerWeek` (`cycle-insights.ts` — liczone, nie renderowane).

**Workflow:**
- [x] Krok 1: failing testy `src/test/workout-time-stats.test.ts`: trend pomija sesje bez durationSec; gęstość liczona z tonażu bez rozgrzewek; skipped zlicza po id i tłumaczy nazwę; puste dane → [].
- [x] Krok 2: implementacja + UI + i18n.
- [x] Krok 3: pełne bramki + e2e:mock; commit `feat(analytics): trend czasu trwania i gęstości, najczęściej pomijane, śr. treningi/tydzień cyklu (Z76)`.

### Zadanie Z77: pomiary ciała — obwody widoczne albo won z formularza

**Znalezisko:** `MeasurementsForm` zbiera 9 pól wymiarów, z których 7 (ramiona, biodra, uda, łydki) NIE jest renderowanych NIGDZIE, a `chest/waist` tylko jako liczby w liście, bez wykresu.

**Fix (pokazujemy, nie usuwamy — dane cielesne to inwestycja usera):** (a) `Measurements.tsx`: wykres z selektorem pola (chipy: Waga, Klatka, Talia, Biodra, Ramię L/P, Udo L/P, Łydka L/P) — LineChart z historii pomiarów (lazy recharts, wzorzec TonnageTrendChart); chipy pól, które nie mają ani jednego wpisu, ukryte. (b) Lista ostatnich pomiarów pokazuje WSZYSTKIE wypełnione pola wpisu (dziś tylko weight/chest/waist). (c) Delta vs poprzedni pomiar przy każdym polu (+0.5 cm itd., kolor wg kierunku — dla talii spadek zielony, dla ramion wzrost zielony: prosta mapa "cel pola" w kodzie z komentarzem).

**Workflow:**
- [x] Krok 1: failing test czystej funkcji `buildMeasurementSeries(measurements, field)` (nowy `src/lib/measurement-stats.ts`): sortowanie po dacie, pomijanie wpisów bez pola, delta.
- [x] Krok 2: implementacja + UI + i18n (krótkie etykiety pól `measurements.short.*` do obu locale — klucze formularza mają placeholder {unit}, niewygodne do chipów).
- [x] Krok 3: pełne bramki + e2e:mock (strona renderuje się z pustym stanem); commit `feat(measurements): wykres i delty dla wszystkich obwodów ciała (Z77)`.

### CHECKPOINT FAZY 3
- [x] `npm run test`, typecheck, lint, build, `npm run e2e:mock`.
- [x] Wpis do DECYZJE.md.

---

## FAZA 4: Postępy bez duplikatów

Stan obecny (raport nawigacji, sekcja 3): tonaż w 7 miejscach, licznik treningów w 6, streak liczony osobno w 3, PR/1RM rozsypane po 7 miejscach bez wzajemnych linków; zakładka Weekly w Analytics czyta ZAMROŻONĄ kolekcję `weekly_summaries` (generator usunięty w R2) — pokazuje wyłącznie stare dane i nigdy nie pokaże nowych.

### Zadanie Z78: zakładka Weekly liczona lokalnie (koniec zombie-danych)

**Fix:** `AnalyticsWeeklyTab.tsx` przestaje czytać `weekly_summaries`; tygodnie liczone client-side z `workouts` + `stravaActivities` ISTNIEJĄCĄ funkcją `prepareWeeklyData` (`src/lib/weekly-summary.ts` — liczy workoutCount/tonnageKg/runKm/totalTimeMinutes/prs) dla ostatnich 12 tygodni. Usuń `useWeeklySummary.ts` + import; kolekcja w rules zostaje (stare dane, admin), ale klient przestaje jej dotykać (koszt czytań = 0). Jeżeli `prepareWeeklyData` wymaga kształtu, którego nie mamy po stronie klienta — STOP, wpis do DECYZJE.md z alternatywą (osobna czysta funkcja `buildLocalWeeklySummaries`).

**Workflow:**
- [x] Krok 1: failing test `src/test/weekly-local.test.ts`: 2 tygodnie danych → 2 wpisy z poprawnym tonażem/liczbą treningów/PR; tydzień bez treningów → pominięty (zgodnie z obecnym UI).
- [x] Krok 2: implementacja + usunięcie martwego hooka; rg po `weekly_summaries` w src/ → zero klienckich odwołań (zostały komentarze + test funkcji).
- [x] Krok 3: pełne bramki + e2e:mock; commit `feat(analytics): tygodnie liczone lokalnie, koniec zamrożonej kolekcji weekly_summaries w kliencie (Z78)`.

### Zadanie Z79: rekordy w jednym miejscu, reszta linkuje

**Fix (konsolidacja bez wielkiej przebudowy):** (a) Achievements zostaje JEDYNYM domem rekordów (ma już: lifePRs, allRecords, personalRecords/1RM). (b) `AnalyticsChartsTab` zakładka "progression" (`:405-450` — duplikat 1RM per ćwiczenie): zostaw wykres progresji (to wartość unikalna), ale usuń zduplikowaną listę rekordów jeśli istnieje, dodaj link "Wszystkie rekordy →" (`/achievements`) — spójnie z Z67(e). (c) Analytics Summary "Nowe PR" (`Analytics.tsx:226`) zostaje (świeżość ma wartość), ale klik prowadzi do `/achievements`. (d) Dashboard "Ostatni PR" (`:913`) już linkuje do achievements — bez zmian. (e) NIE ruszaj statystyk tonaż/liczba treningów na Dashboardzie i w Analytics Summary (świadoma redundancja "przegląd vs szczegół") — wpisz to rozróżnienie do DECYZJE.md jako zasadę: **agregaty wolno powtarzać, LISTY rekordów żyją w jednym miejscu**.

**Workflow:**
- [ ] Krok 1: rg po komponentach rekordów w Analytics; potwierdź duplikaty przed cięciem (bezpiecznik — jeśli progression NIE dubluje list z Achievements, ogranicz zadanie do linków).
- [ ] Krok 2: implementacja + aktualizacja testów e2e, które dotykały usuwanych sekcji.
- [ ] Krok 3: pełne bramki + e2e:mock; commit `refactor(analytics): rekordy w jednym miejscu (Achievements), Analytics linkuje (Z79)`.

### Zadanie Z80: historia jako pełnoprawne archiwum

**Fix:** `WorkoutHistory.tsx`: (a) wiersz wpisu dostaje czas trwania (`durationSec` → "1h 12m", helper `fmtDuration` już istnieje w WorkoutDay — przenieś do `src/lib/utils.ts` jeśli lokalny) i liczbę PR z tej sesji (`detectNewPRs` per sesja — licz w memo raz dla całej listy, nie per wiersz w renderze). (b) Rozwinięcie wpisu (istniejący wzorzec expand z podsumowania WorkoutDay) pokazuje: serie per ćwiczenie, notatki (Z74d), metryki RPE/ból/technika jeśli są. (c) Filtr "tylko z PR" obok istniejących filtrów.

**Workflow:**
- [ ] Krok 1: failing test czystej funkcji `buildHistoryRowMeta(workouts)` (nowy plik `src/lib/history-stats.ts`): duration formatowane, PR per sesja liczone raz, sesje bez duration bez crasha.
- [ ] Krok 2: implementacja + i18n; e2e mock z `setE2EWorkouts` (wpis z notatką i metrykami → rozwinięcie pokazuje).
- [ ] Krok 3: pełne bramki; commit `feat(history): czas trwania, PR, metryki i notatki w historii + filtr PR (Z80)`.

### CHECKPOINT FAZY 4
- [ ] `npm run test`, typecheck, lint, build, `npm run e2e:mock`.
- [ ] Wpis do DECYZJE.md.

---

## FAZA 5: Profil/Ustawienia — jedno kryterium, zero dubli

### Zadanie Z81: porządek w Profile vs Settings

**Znalezisko (raport nawigacji, sekcja 5):** podział bez kryterium — preferencje (jednostki, język, timer, dźwięk) tylko w Profile; dane/powiadomienia/Strava/serwis tylko w Settings; Profile w połowie jest launcherem do Settings; sekcja "Konto" istnieje na obu (Profile edytuje, Settings pokazuje read-only pod TYM SAMYM tytułem i18n); DataManagement renderowany na 2 stronach (Settings `:187` i Measurements `:50`).

**Fix (porządkowanie, NIE wielki merge):** Kryterium: **Profil = kim jestem i jak apka się zachowuje** (konto, preferencje, język, jednostki, dźwięk, timer, powiadomienia-link); **Ustawienia = dane i integracje** (backup, Strava, sync, narzędzia naprawcze). Zmiany: (a) Usuń z Settings kartę "Konto" read-only (`Settings.tsx:165-186` wg raportu — duplikat; email pokazuj w Profile pod nickiem, jeśli go tam nie ma). (b) Usuń `DataManagement` z `Measurements.tsx:50` (zostaje w Settings; na Measurements zamiast tego link "Kopia zapasowa danych →" do `/settings?section=data` — deep-scroll już działa). (c) NotificationSettings przenieś do Profile (sekcja APP, gdzie już jest link do nich) LUB zostaw i popraw launcher — wybierz mniejszy diff, odnotuj w DECYZJE.md. (d) Nagłówki sekcji Settings dostają spójne opisy (dane, integracje, serwis).

**Workflow:**
- [ ] Krok 1: rg po `profile.section.account` i użyciach DataManagement (bezpiecznik duplikatów).
- [ ] Krok 2: implementacja + usunięcie osieroconych i18n; aktualizacja testów e2e settings (eksport kopii jest w testach — musi dalej przechodzić).
- [ ] Krok 3: pełne bramki + e2e:mock; commit `refactor(settings): jedno kryterium Profil vs Ustawienia, koniec zdublowanych sekcji (Z81)`.

### CHECKPOINT FAZY 5
- [ ] `npm run test`, typecheck, lint, build, `npm run e2e:mock`.
- [ ] Wpis do DECYZJE.md.

---

## FAZA 6: Polish pod App Store

### Zadanie Z82: empty states + haptyka odhaczenia

**Fix:** (a) Audyt pustych stanów: przejdź strony `/history /measurements /cycles /achievements /analytics /exercises` w e2e mock BEZ danych i wypisz, które sekcje renderują pustkę/zero bez zaproszenia; każda dostaje empty state z 1 zdaniem i CTA (wzorzec istniejącego empty w `Cycles.tsx:341`): np. Historia → "Zacznij pierwszy trening" → `/`; Pomiary → "Dodaj pierwszy pomiar" (scroll do formularza); Achievements → "Rekordy pojawią się po pierwszych treningach". (b) Haptyka: przy odhaczeniu serii w `ExerciseCard` lekki impact (`@capacitor/haptics` — sprawdź `package.json`; jest używany w projekcie przy timerze — reuse istniejącego helpera, rg po `Haptics`; jeśli helpera brak, dodaj `src/lib/haptics.ts` z guardem na web) i przy ukończeniu treningu notification-success. Web: no-op.

**Workflow:**
- [ ] Krok 1: lista pustych sekcji (wpis do DECYZJE.md) → dodaj empty states z i18n do OBU locale.
- [ ] Krok 2: haptyka + test jednostkowy helpera (mock Capacitor, wzorzec `app-lifecycle.test.ts`: na webie nie woła pluginu).
- [ ] Krok 3: pełne bramki + e2e:mock; commit `feat(polish): empty states z CTA + haptyka odhaczenia serii (Z82)`.

### Zadanie Z83: prośba o ocenę w App Store (in-app review)

**Fix:** Po ukończeniu 5. treningu OGÓŁEM (licznik z `workouts.filter(completed).length` w momencie finalizacji) i nie częściej niż raz na 60 dni (guard w localStorage `fittracker_review_prompt`): wywołaj natywny in-app review. Plugin: `@capacitor-community/in-app-review` (npm i + wpis do DECYZJE.md, `cap sync` zrobi release-ios.sh w FAZIE 7). Wywołanie w ekranie podsumowania ukończonego treningu (WorkoutDay COMPLETED VIEW), fire-and-forget z catch, guard na platformę natywną. ZERO własnych modali "oceń nas" (Apple tego zakazuje poza natywnym API). **Jeśli plugin okaże się niekompatybilny z wersją Capacitora: STOP, wpis do DECYZJE.md, zadanie odpada bez blokowania release'u.**

**Workflow:**
- [ ] Krok 1: failing test czystej funkcji `shouldRequestReview(completedCount, lastPromptAt, now)` (nowy `src/lib/review-prompt.ts`): 5. trening tak, 4. nie, ponowna prośba przed upływem 60 dni nie, po 60 dniach przy kolejnym kamieniu (15., 30.) tak.
- [ ] Krok 2: implementacja + podpięcie w COMPLETED VIEW; guard natywny.
- [ ] Krok 3: pełne bramki; commit `feat(polish): natywna prośba o ocenę po 5. treningu (Z83)`.

### CHECKPOINT FAZY 6
- [ ] `npm run test`, typecheck, lint, build, `npm run e2e:mock`.
- [ ] Wpis do DECYZJE.md.

---

## FAZA 7: Release train (WSZYSTKO na produkcję, bez pytania)

### Zadanie Z84: wdrożenie

**Kolejność OBOWIĄZKOWA (checklist CLAUDE.md + lekcje R2/X10):**
- [ ] Krok 1: pełne bramki: `npm run test`, typecheck, lint, build, `npm run check:bundle-budget`, `npm run test:rules`, `npm --prefix functions test` + build (functions raczej nieruszone — jeśli nieruszone, deploy functions POMIŃ), `npm run e2e:mock`, `npm run e2e:emulator`.
- [ ] Krok 2: commit + `git push origin main`.
- [ ] Krok 3: rules: `firebase deploy --only firestore:rules --project fittracker-workouts` (Z71 dodaje custom_exercises).
- [ ] Krok 4: web: `npm run deploy` (sam push NIE aktualizuje strony). NIGDY `cap sync ios` po deployu web. Weryfikacja: hash `index-*.js` na https://grzegorzee.github.io/strength-save/ zgodny z dist/index.html.
- [ ] Krok 5: iOS: bump `CURRENT_PROJECT_VERSION` 49 -> 50 w `ios/App/App.xcodeproj/project.pbxproj` (6 wystąpień, pilnuje `release-ios-preflight.mjs`) + commit; potem **bez pytania**: `scripts/release-ios.sh "X11: nowa nawigacja, wlasne cwiczenia i edytor planow, trendy bolu i techniki"` (build + TestFlight + auto Beta App Review; nowy plugin in-app-review wymaga cap sync — robi to skrypt).
- [ ] Krok 6: weryfikacja produkcji: web wstaje, `gcloud functions logs read --project fittracker-workouts --limit 20` bez nowych błędów, TestFlight VALID (skrypt raportuje).
- [ ] Krok 7: DECYZJE.md (checkpoint X11) + `CONTEXT.md` (OSTATNIE DECYZJE) + `PLAN.md` (M46) + aktualizacja pamięci projektu.
- [ ] Krok 8: scenariusz testu terenowego dla USERA w raporcie końcowym: (1) hamburger na telefonie otwiera pełne menu, Historia/Pomiary/Cykle dostępne; (2) w treningu ikona info przy ćwiczeniu otwiera instrukcje i wraca bez utraty stanu; (3) dodaj WŁASNE ćwiczenie w edytorze planu i odhacz je na treningu; (4) w edytorze planu dodaj dzień, zmień mu dzień tygodnia, zduplikuj, usuń; (5) wybierz w kreatorze 6 dni — dostajesz plan 6-dniowy; (6) otwórz szczegóły ćwiczenia, które trenowałeś z notatką — widzisz swoją notatkę i trend RPE/bólu/techniki; (7) Pomiary: wykres obwodu ramienia; (8) Historia: czas trwania i rozwinięcie z notatkami; (9) po 5. ukończonym treningu iOS pyta o ocenę.

---

## Poza zakresem tego planu (świadomie)

- Przebudowa dolnego paska nawigacji (5 pozycji zostaje) — drawer + linki krzyżowe rozwiązują osiągalność bez ryzyka dezorientacji jedynego aktywnego usera.
- Pełny merge Profile+Settings w jeden ekran — Z81 robi porządek kryterium; merge wróci, gdy user potwierdzi kierunek po teście terenowym.
- Strukturalny model serii (koniec "3 x 6-8" jako string) — głęboki refaktor dotykający parsera w wielu miejscach; osobny plan.
- Drag&drop w edytorze planu — strzałki/duplikacja wystarczą; dnd-kit to nowa zależność.
- Superset/tempo/RIR w PlanBuilder, kadencja biegowa jako wykres, suffer score ze Stravy, wirtualizacja list, App Check enforce, timery treningowe (nadal za flagą), store listing/ASO (screenshoty, opis — praca poza kodem, zrobi user z osobnym wsparciem).

## Kolejność awaryjna (gdy user zgłosi nowy błąd w trakcie)

1. Błąd zapisu/utraty treningu: przerwij bieżące zadanie po commicie, diagnozuj z telemetrią `client_errors` (AdminDashboard), wróć do planu po hotfixie.
2. Błąd niekrytyczny: dopisz do rejestru na końcu tego pliku, nie zmieniaj kolejności faz.
