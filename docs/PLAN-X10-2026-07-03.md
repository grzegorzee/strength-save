# PLAN X10: powrót do treningu, porządki UX, wydajność, refaktor silnika sesji (2026-07-03)

> **Dla agenta wykonawcy:** WYMAGANY SUB-SKILL: `superpowers:executing-plans` (albo `superpowers:subagent-driven-development`). Kroki mają checkboxy `- [ ]`. Wykonuj zadania PO KOLEI, fazy rozdzielone checkpointami. Nie przeskakuj checkpointów.
> Kontekst: plany `docs/PLAN-NAPRAWY-2026-07-03.md` (Z13-Z28) i `docs/PLAN-NAPRAWY-2026-07-03-R2.md` (Z29-Z46) są WYKONANE I WDROŻONE (web + functions + rules + TTL na produkcji; iOS build 49 zbumpowany w repo, NIE wysłany do App Store Connect). User przetestował apkę po R2: działa poprawnie.
> Źródło znalezisk: 3 raporty przeglądowe z 2026-07-03 (mapa stron/nawigacji, stan techniczny frontendu, audyt Settings) + pytania usera.

**Goal:** Aplikacja "x10": (1) po każdym powrocie/restarcie apka sama wraca do aktywnego treningu i ostatniego ćwiczenia, (2) Settings i narzędzia serwisowe przestają mylić (martwe ustawienie, panel adminowy, żargon), (3) start apki wyraźnie szybszy (bundle), (4) najważniejszy ekran (WorkoutDay) dostaje jawną maszynę stanów sesji, (5) domknięte zaległości z FAZY 7 planu R2, (6) WYRÓŻNIK RYNKOWY: Adaptive Coach — autoregulacja progresji na bazie RPE/bólu/jakości + gotowość łącząca siłownię ze Stravą, w 100% offline (zero kosztów serverless). Na końcu JEDEN release train: web + functions + rules + iOS na TestFlight.

**Architektura podejścia:** Najpierw wartość dla usera (auto-resume + kontynuacja z Dashboardu), potem porządki UX, potem wydajność startu, potem refaktor stabilizacyjny, na końcu higiena i release. Każda faza zostawia działający, przetestowany soft.

**Tech stack:** React + TypeScript + Vite, Firebase (Firestore, Functions v2, Rules), IndexedDB + localStorage, Capacitor iOS (+ NOWY plugin @capacitor/app), vitest + Playwright (mock i emulator).

---

## 0. AUTORYZACJE I TWARDE ZASADY (od usera, 2026-07-03)

1. **Deploy na produkcję BEZ pytania o zgodę** — dotyczy WSZYSTKICH kanałów w FAZIE 7 (release train): `git push`, `firebase deploy` (functions/rules/indeksy), `npm run deploy` (web) ORAZ **wysyłki iOS na TestFlight przez `scripts/release-ios.sh`**. User: "wszystkie zmiany mają polecieć od razu na produkcję; dostęp do aplikacji mam tylko ja i działam z nią tylko na TestFlight". Nie pytaj, wykonuj.
2. Deploy TYLKO w FAZIE 7 (release train). Wcześniej niczego nie wdrażaj.
3. Środowisko docelowe: siłownia. iOS wstrzymuje JS w WKWebView po zgaszeniu ekranu; wszystko co ma przeżyć tło musi być w IndexedDB/localStorage. Przy każdej zmianie w treningu pierwsze pytanie: "co się dzieje, gdy ekran gaśnie?".
4. Dane usera święte: zero testowych zapisów na realnym koncie.
5. TDD z bezpiecznikiem: każde zadanie zaczyna się od failing testu odtwarzającego problem/brak. **Jeśli test NIE potwierdza znaleziska: STOP dla zadania, wpis do DECYZJE.md, idź dalej.**
6. Kontrakty syncu z R1/R2 są NIETYKALNE (DECYZJE.md, pamięć projektu): jeden silnik (`workout-sync-engine.ts`), kolejka referencyjna, writeId/pendingWriteVersion, snapshot draftu (`workout-draft-snapshot.ts`), tombstone promocji, markery chmury zawsze. Zakazane podejścia: konflikt po updatedAt, auto-retry konfliktu bez zmiany precondition, timery JS w tle, sessionId jako stabilny klucz UI (używać `${uid}:${date}`).
7. Rules mają ZAMKNIĘTE schematy (hasOnly): **każde nowe pole w dokumencie Firestore zapisywanym przez klienta wymaga aktualizacji `firestore.rules` + testu w `scripts/test-firestore-rules.mjs`**. Przed zaostrzaniem hasOnly sprawdzaj kształty produkcyjnych dokumentów (lekcje: `createdAt` w workouts, płaskie `counters.*` w telemetrii).
8. Pola w drafcie IndexedDB dodawaj jako opcjonalne (additive, bez bumpu DB_VERSION) i obsłuż w `normalizeDraft` w `workout-draft-db.ts` (wzorzec: `exerciseMetrics`).
9. i18n: każdy nowy klucz do OBU plików `src/i18n/locales/pl.ts` i `en.ts`. Teksty PL bez pauz (em-dash), pełne polskie znaki.
10. Chirurgicznie: jedno zadanie = izolowane commity `fix|feat|perf|chore(obszar): opis (Z<nr>)`. Zero poprawiania sąsiedniego kodu.
11. Weryfikacja przy checkpointach (minimum): `npm run test`, `npm run typecheck`, `npm run lint`, `npm run build`. Fazy dotykające rules: + `npm run test:rules`. Functions: + `npm --prefix functions test` i build. Sync/trening: + `npm run e2e:mock`; FAZA 1 i 4 dodatkowo `npm run e2e:emulator`. JDK21: `export JAVA_HOME=/opt/homebrew/opt/openjdk@21 && export PATH="$JAVA_HOME/bin:$PATH"`.
12. Wpis do DECYZJE.md po każdej fazie (co, dlaczego, root cause, weryfikacja).
13. Pułapka narzędziowa: `npx firebase` bywa przechwytywane przez npm — używaj globalnego `firebase` z PATH. Testy klienta żyją w `src/test/` (nie obok modułów).

---

## FAZA 0: Baseline

### Zadanie Z47.0: zielony baseline

- [x] Krok 1: `git status` czyste, `git log --oneline -1` (HEAD = aace1c6 lub nowszy).
- [x] Krok 2: `npm run test` (oczekiwane 501/501), `npm run typecheck`, `npm run lint`, `npm run build`. Jeśli nie zielone: STOP, zgłoś userowi.

---

## FAZA 1: Powrót do aktywnego treningu (killer feature)

Stan obecny (raport nawigacji): po zabiciu apki/zimnym starcie user ZAWSZE ląduje na Dashboardzie; draft jest bezpieczny w IndexedDB, ale nic go nie otwiera. Karta dzisiejszego treningu pokazuje "Start treningu" nawet gdy trening jest w połowie (`Dashboard.tsx:556-559`), a karta statusu synchronizacji kieruje do `/settings` zamiast do treningu (`Dashboard.tsx:510-541`). Scroll-restore wewnątrz WorkoutDay istnieje (klucz per uid:date, retry, `WorkoutDay.tsx` okolice 1027-1066), ale działa dopiero PO wejściu na stronę treningu.

### Zadanie Z47: ostatnie ćwiczenie w drafcie + scroll po hydracji

**Goal:** Draft wie, które ćwiczenie było ostatnio dotykane; po każdej hydracji draftu (powrót z tła, zimny start, wejście z Dashboardu) WorkoutDay przewija do tego ćwiczenia, jeśli nie ma świeższej zapisanej pozycji scrolla.

**Fix:** (a) Nowe opcjonalne pole `lastTouchedExerciseId?: string` w `ActiveWorkoutDraft` (`src/lib/workout-draft-db.ts`; normalizacja: `...(typeof value.lastTouchedExerciseId === 'string' && { lastTouchedExerciseId: value.lastTouchedExerciseId })`). (b) `buildWorkoutDraftSnapshot` (`src/lib/workout-draft-snapshot.ts`): przenosi pole z previousDraft, overrides mogą nadpisać (wzorzec pendingWriteId). (c) WorkoutDay: `handleSetsChange`, `handleMetricsChange` i `handleWatchSetLogged` przekazują `{ lastTouchedExerciseId: exerciseId }` w overrides do saveDraftSnapshot/persistDraftSnapshot. (d) Po hydracji draftu (`applyWorkoutState` w efekcie hydracji): jeżeli draft ma `lastTouchedExerciseId`, a mechanizm scroll-restore NIE ma zapisanej pozycji dla klucza tej sesji, przewiń kartę tego ćwiczenia w widok (`document.getElementById`/ref + `scrollIntoView({ block: 'center' })`, z retry po 300 ms jak istniejący scroll-restore; karty ćwiczeń dostają `id={'exercise-card-' + exercise.id}` w renderze listy).

**Workflow:**
- [x] Krok 1: failing testy w `src/test/workout-draft-snapshot.test.ts`: (1) snapshot przenosi `lastTouchedExerciseId` z previousDraft; (2) overrides nadpisują; (3) roundtrip w `src/test/workout-draft-db.test.ts` (save + load zachowuje pole).
- [x] Krok 2: implementacja (a)-(c); testy zielone.
- [x] Krok 3: implementacja (d) w WorkoutDay; weryfikacja e2e mock: rozszerz test w `e2e/full-app.spec.ts` wzorcem `writeWorkoutDraftDb` (jak test Z35): wstrzyknij draft z `lastTouchedExerciseId` trzeciego ćwiczenia, wejdź na `/workout/day-1`, sprawdź `toBeInViewport()` karty tego ćwiczenia.
- [x] Krok 4: `npm run test`, typecheck, lint; commit `feat(workout): draft pamięta ostatnie ćwiczenie, scroll po hydracji (Z47)`.

### Zadanie Z48: natywny cykl życia iOS (@capacitor/app) + flush przy tle

**Goal:** Zapis draftu i logika resume zaczepione o natywne `appStateChange`, nie tylko webowe `visibilitychange`/`pagehide` (raport techniczny: brak `@capacitor/app` w projekcie; webowe eventy bywają zawodne w WKWebView).

**Fix:** (a) `npm i @capacitor/app` (cap sync zrobi release-ios.sh w FAZIE 7; do testów na symulatorze w trakcie NIE jest potrzebny — kod ma guard na brak pluginu). (b) Nowy moduł `src/lib/app-lifecycle.ts`: eksportuje `addAppStateListener(onChange: (isActive: boolean) => void): () => void` — na platformie natywnej używa `App.addListener('appStateChange', ...)` (dynamiczny import `@capacitor/app`, try/catch), na webie fallback do `visibilitychange`; zwraca funkcję sprzątającą. (c) WorkoutDay: istniejący handler flusha na `visibilitychange/pagehide` dodatkowo rejestruje się przez `addAppStateListener` (flush draftu, gdy `isActive === false`). Nie usuwaj webowych handlerów (web/PWA nadal ich potrzebuje) — deduplikacja flushy jest naturalna (saveActiveDraft z tą samą wersją to no-op gate'owany przez latestWriteVersions).

**Workflow:**
- [x] Krok 1: failing test `src/test/app-lifecycle.test.ts`: mock `@capacitor/core` (`isNativePlatform: () => false`) → listener podpina się pod `visibilitychange` i woła callback z `isActive=false` przy `document.hidden=true`; unsubscribe odpina.
- [x] Krok 2: implementacja + podpięcie w WorkoutDay (flush).
- [x] Krok 3: `npm run test`, typecheck, lint; commit `feat(ios): natywny appStateChange przez @capacitor/app, flush draftu przy tle (Z48)`.

### Zadanie Z49: auto-resume do aktywnego treningu + "Kontynuuj trening" na Dashboardzie

**Goal:** Po zimnym starcie / powrocie do apki user ląduje w aktywnym treningu automatycznie; na Dashboardzie karta dzisiejszego treningu mówi "Kontynuuj trening" z postępem, a karta statusu sync prowadzi do treningu, nie do Settings.

**Fix:** (a) Nowa czysta funkcja `shouldResumeWorkoutDraft(draft, todayStr, now)` w `src/lib/workout-resume.ts`: true gdy draft istnieje, NIE jest ukończony (`!completedLocally && !finalSyncPending`), jest "żywy" (dirty lub provisional) i świeży (`date === todayStr` LUB `now - updatedAt < 12h`); zwraca też ścieżkę docelową: `/workout/${draft.dayId}?date=${draft.date}&session=${draft.sessionId}`. (b) Nowy komponent `src/components/ActiveWorkoutResume.tsx` montowany w drzewie zalogowanym (`App.tsx`, obok `WatchEventRouter`): na mount ORAZ na `addAppStateListener(isActive=true)` woła `workoutDraftDb.loadActiveDraft(uid)`; gdy `shouldResumeWorkoutDraft` daje true i user NIE jest już na trasie `/workout/`, robi `navigate(target)` + telemetria `trackTelemetryEvent(uid, 'workout_auto_resume')` (nowy TelemetryEventName w `src/lib/app-telemetry.ts`). Auto-resume odpala się MAKSYMALNIE raz na mount apki i raz per powrót z tła (ref-guard), żeby nie walczyć z nawigacją usera; jeżeli user świadomie wyjdzie z treningu (nawigacja wewnątrz apki), nie wracamy (guard: resume tylko na mount i na przejście background->active). (c) Dashboard: `todayTraining` rozpoznaje aktywny draft — gdy `localDraft` pasuje do dzisiejszego dnia i `shouldResumeWorkoutDraft` true, wariant karty `training` dostaje etykietę `t('dash.today.continue')` ("Kontynuuj trening") + licznik odhaczonych serii (`Object.values(exerciseSets).flat().filter(s => s.completed).length`) i prowadzi do tej samej ścieżki z `session=`; karta statusu synchronizacji (`Dashboard.tsx:510-541`) przy żywym drafcie linkuje do treningu (Settings zostaje dla wpisów kolejki bez draftu). (d) Klucze i18n: `dash.today.continue`, `dash.today.continueSets` (pl: "Odhaczone serie: {n}"), do OBU plików.

**Workflow:**
- [ ] Krok 1: failing testy `src/test/workout-resume.test.ts`: świeży dirty draft dzisiejszy → resume z poprawną ścieżką; draft finalSyncPending → false; draft sprzed 3 dni → false; draft ukończony lokalnie → false; provisional offline → true.
- [ ] Krok 2: implementacja (a) + testy zielone.
- [ ] Krok 3: implementacja (b)-(d); test e2e mock: wstrzyknij żywy draft przez `writeWorkoutDraftDb`, wejdź na `/` i oczekuj przekierowania na `/workout/day-1` (URL zawiera `session=`); drugi test: draft finalSyncPending → zostajesz na Dashboardzie, karta sync widoczna.
- [ ] Krok 4: pełne bramki + `npm run e2e:mock`; commit `feat(workout): auto-resume aktywnego treningu + Kontynuuj trening na Dashboardzie (Z49)`.

### CHECKPOINT FAZY 1
- [ ] `npm run test`, typecheck, lint, build, `npm run e2e:mock`, `npm run e2e:emulator` (JDK21).
- [ ] Wpis do DECYZJE.md.

---

## FAZA 2: Porządki w Settings i narzędziach

### Zadanie Z50: usunięcie martwego ustawienia "godzina podsumowania"

**Znalezisko (audyt Settings §wyróżnione pkt 1):** `Settings.tsx:322-349` Select 16:00-23:00 zapisuje `summary-hour` do localStorage, którego NIC nie czyta; realny digest chodzi cronem o stałej porze. User ustawia coś, co nie działa.

**Workflow:**
- [ ] Krok 1: `rg "summary-hour|summaryHour|SUMMARY_HOUR"` w src/ i functions/ — potwierdź zero konsumentów poza Settings (bezpiecznik).
- [ ] Krok 2: usuń kartę + stałą `SUMMARY_HOUR_KEY` + stan/handlery + osierocone klucze i18n z OBU locale (rg po kluczach przed usunięciem). Typecheck/lint/test.
- [ ] Krok 3: commit `chore(settings): usunięcie martwego ustawienia godziny podsumowania (Z50)`.

### Zadanie Z51: FeatureFlagsPanel przeniesiony do /admin

**Znalezisko (audyt Settings §2):** panel back-office (lista userów z mailami + nadawanie flagi Strava) żyje w Settings; na telefonie label flagi jest ukryty (`hidden sm:inline`), więc admin widzi goły switch bez podpisu — "klikam i nic się nie dzieje". Miejsce tego panelu to AdminDashboard.

**Fix:** Przenieś CAŁY komponent `FeatureFlagsPanel` (`Settings.tsx:53-152`) do `src/pages/admin/AdminDashboard.tsx` (jako sekcja "Zarządzanie funkcjami" przy innych narzędziach admina; import stałych/typów razem z nim; label flagi widoczny ZAWSZE, bez `hidden sm:inline`). Z Settings usuń render (`Settings.tsx:530`) i osierocone importy. Klucze i18n `settings.features.*`/`settings.feature.*` zostają (używa ich przeniesiony panel).

**Workflow:**
- [ ] Krok 1: przenieś komponent; `rg "FeatureFlagsPanel"` — jedyny render w AdminDashboard.
- [ ] Krok 2: typecheck, lint, `npm run test`; ręczna sanity: `npm run e2e:mock` (testy settings przechodzą).
- [ ] Krok 3: commit `chore(admin): FeatureFlagsPanel przeniesiony z Settings do AdminDashboard (Z51)`.

### Zadanie Z52: Sync Center jako "deska ratunkowa" + narzędzia serwisowe w akordeonie

**Znalezisko (audyt Settings §3, §wyróżnione 2-5):** Sync Center renderuje pustą kartę u zdrowego usera; komunikaty pokazują surowe kody błędów; narzędzia serwisowe (naprawa cykli, reset planu, napraw dane, wyczyść duplikaty) są stale widoczne i żargonowe.

**Fix:** (a) `Settings.tsx`: `SyncCenterCard` renderowana TYLKO gdy ma treść — dodaj do SyncCenterCard prop `onVisibilityChange?: (hasEntries: boolean) => void` LUB prościej: wydziel z SyncCenterCard hooka `useSyncCenterEntries(uid)` zwracającego `{ isLoaded, listedEntries, conflicts }` i w Settings renderuj kartę tylko przy `listedEntries.length > 0 || Object.keys(conflicts).length > 0`; pusta karta znika (user bez zaległości nie widzi Sync Center wcale). (b) `lastError` w liście wpisów mapowany przez `workoutSyncErrorMessageKey` (`src/lib/workout-sync-conflict.ts`) zamiast surowego kodu; surowy kod zostaje w `title=` (tooltip). (c) Sekcje "Naprawa cykli", "Reset planu" oraz przyciski "Napraw dane historyczne"/"Wyczyść duplikaty" w DataManagement zgrupowane w JEDEN zwijany blok "Narzędzia naprawcze" (istniejący wzorzec Collapsible/Accordion z shadcn; domyślnie zwinięty; krótki opis: "Użyj tylko, gdy widzisz zdublowane cykle albo brakujące nazwy w historii"). Eksport/Import kopii zapasowej zostają na wierzchu. Nowe klucze i18n (`settings.tools.title`, `settings.tools.hint`) do OBU locale.

**Workflow:**
- [ ] Krok 1: failing test hooka `src/test/sync-center-entries.test.ts` (jeśli ekstrakcja hooka): zwraca wpisy z draftów + kolejki z dedupem po sessionId (przenieś logikę `listedEntries` 1:1; wzorce mocków jak w testach hooków).
- [ ] Krok 2: implementacja (a)-(c); `npm run e2e:mock` (testy settings/sync przechodzą; zaktualizuj asercje, jeśli sprawdzały pustą kartę).
- [ ] Krok 3: commit `feat(settings): Sync Center tylko przy zaległościach, błędy po ludzku, narzędzia naprawcze w akordeonie (Z52)`.

### Zadanie Z53: jednorazowe sprzątanie osieroconych draftów/wpisów sprzed R2

**Znalezisko:** tombstone (Z32) i sprzątanie kolejki (Z23) nie działają wstecz — u usera mogą wisieć w IndexedDB/localStorage stare orphany provisional i martwe wpisy kolejki sprzed R2 ("stary trening wisiał w Sync Center").

**Fix:** funkcja `cleanupLegacySyncLeftovers(uid, workouts)` w `src/lib/workout-sync-cleanup.ts` (nowy plik): (1) wpisy kolejki bez draftu w IDB → `workoutSyncQueue.remove`; (2) drafty provisional NIE-dirty i NIE-finalSyncPending, których dzień/data ma już ukończony trening w chmurze (`workouts.some(w => w.completed && w.dayId === draft.dayId && w.date === draft.date)`) → `clearActiveDraftIfVersion(uid, draft.sessionId, draft.version)`; (3) guard jednorazowości w localStorage `fittracker_legacy_cleanup_v1:{uid}` ustawiany PO sukcesie (wzorzec runCycleAutoRepair). Wywołanie: raz, po załadowaniu workouts, w `AutoSyncOnReconnect` (ma już uid + dostęp do draftDb/queue) — fire-and-forget z catch.

**Workflow:**
- [ ] Krok 1: failing testy `src/test/workout-sync-cleanup.test.ts`: (1) wpis kolejki bez draftu znika; (2) czysty (dirty=false) provisional draft z ukończonym odpowiednikiem w chmurze znika; (3) dirty draft ZOSTAJE; (4) finalSyncPending ZOSTAJE; (5) guard: drugie wywołanie to no-op; (6) porażka czyszczenia nie ustawia guardu.
- [ ] Krok 2: implementacja + podpięcie; pełne testy.
- [ ] Krok 3: commit `fix(sync): jednorazowe sprzątanie osieroconych draftów i martwych wpisów kolejki sprzed R2 (Z53)`.

### CHECKPOINT FAZY 2
- [ ] `npm run test`, typecheck, lint, build, `npm run e2e:mock`.
- [ ] Wpis do DECYZJE.md.

---

## FAZA 3: Wydajność startu i danych

### Zadanie Z54: odchudzenie bundla (firebase split, recharts lazy w Achievements, dane ćwiczeń per język)

**Znalezisko (raport techniczny P1, P3, P7):** initial JS ~1.45 MB (firebase 732K + index 578K + react-vendor 142K); `Achievements.tsx` importuje recharts (371K) statycznie; `ExerciseDetail` ciągnie słownik ćwiczeń PL+EN naraz (274K, `src/data/exercise-i18n.ts` importuje oba pliki statycznie).

**Fix:** (a) `vite.config.ts` manualChunks: rozdziel `firebase-firestore`, `firebase-auth`, `firebase-core` (mniejsze chunki = lepsze cache'owanie i parsowanie; import graph sam zdecyduje co wchodzi do initial). (b) Achievements: część z wykresem za `lazy()` + `Suspense` (wzorzec `Analytics.tsx:35-37` — `AnalyticsChartsTab`). (c) `src/data/exercise-i18n.ts`: dynamiczny import pliku językowego wg aktywnego języka (`import('./exercise-details')` / `import('./exercise-details-en')`), z synchronicznym cache po pierwszym załadowaniu; konsumenci już są async-friendly (komponenty) — tam gdzie API musi zostać synchroniczne, zostaw statyczny import PL (język kanoniczny) i dynamiczny TYLKO dla EN. Jeśli refaktor konsumentów okazuje się rozległy (>5 plików wymaga zmiany sygnatur): STOP dla podpunktu (c), wpis do DECYZJE.md, zostają (a)+(b). (d) `scripts/check-bundle-budget.mjs`: obniż limit per-chunk do 600 KB, dodaj limit sumy initial (index + firebase-core + react-vendor) 1200 KB.

**Workflow:**
- [ ] Krok 1: `npm run build` i zapisz rozmiary dist/assets PRZED (do DECYZJE.md).
- [ ] Krok 2: (a)+(b) + build; porównaj rozmiary; `npm run check:bundle-budget` z nowymi limitami przechodzi.
- [ ] Krok 3: (c) jeśli wykonalne chirurgicznie; testy + e2e:mock (ExerciseDetail i Achievements renderują się).
- [ ] Krok 4: commit(y) `perf(bundle): ... (Z54)`.

### Zadanie Z55: limity na nieograniczonych listenerach

**Znalezisko (raport techniczny P5):** `usePlanCycles.ts:78-84` i `useWeeklySummary.ts:19-25` — onSnapshot bez `limit()`.

**Workflow:**
- [ ] Krok 1: dodaj `limit(60)` do kwerendy cykli (5 lat historii przy miesięcznych cyklach) i `limit(26)` do weekly_summaries (pół roku), stałe nazwane obok istniejących `*_LISTENER_LIMIT` w `src/lib/workout-read-store.ts` lub lokalnie; sort pozostaje.
- [ ] Krok 2: `npm run test`, typecheck; commit `perf(data): limity na listenerach plan_cycles i weekly_summaries (Z55)`.

### Zadanie Z56: ErrorBoundary raportuje do telemetrii + boundary per trasa

**Znalezisko (raport techniczny P8):** `ErrorBoundary.tsx:25-27` tylko console.error — crashe renderu nie trafiają do `client_errors`; jeden boundary na topie wywala całą apkę.

**Fix:** (a) `componentDidCatch` woła `reportClientError(uid?, { code: 'render-crash', phase: 'other', detail: error.message + pierwsza linia stacka })` — uid z props (App przekazuje z kontekstu; gdy brak — pomiń raport, telemetria wymaga uid). (b) Drugi boundary wokół `<Suspense>` z drzewem tras w `App.tsx` (fallback: karta "Coś poszło nie tak" + przycisk "Wróć na Dashboard" `navigate('/')` — nowe klucze i18n do OBU locale); boundary topowy zostaje jako ostatnia linia obrony.

**Workflow:**
- [ ] Krok 1: failing test `src/test/error-boundary.test.tsx`: render dziecka rzucającego błąd → fallback widoczny + `reportClientError` wywołany z code 'render-crash' (mock error-telemetry).
- [ ] Krok 2: implementacja; pełne testy.
- [ ] Krok 3: commit `feat(observability): ErrorBoundary raportuje crashe do client_errors, boundary per trasa (Z56)`.

### CHECKPOINT FAZY 3
- [ ] `npm run test`, typecheck, lint, build, `npm run check:bundle-budget`, `npm run e2e:mock`.
- [ ] Wpis do DECYZJE.md z tabelą rozmiarów bundla przed/po.

---

## FAZA 4: Maszyna stanów sesji treningowej (odłożona rekomendacja audytu R1)

### Zadanie Z57: jawna maszyna stanów + ekstrakcja hydracji z WorkoutDay

**Znalezisko (audyt R1 §3.4, raport techniczny P2):** stan sesji żyje w kilkunastu implicit flagach (26 useState, 24 useEffect, 18 useRef, 7 refów-luster); hydracja to 8 gałęzi heurystyk `shouldUseDraft` w komponencie z `eslint-disable exhaustive-deps`. To źródło całej klasy bugów napraw R1/R2.

**Fix (dwa kroki, oba czyste funkcje — komponent zostaje adapterem):**
(a) `src/lib/workout-session-state.ts`: typ `WorkoutSessionPhase = 'idle' | 'active-provisional' | 'active-remote' | 'completing' | 'final-pending' | 'completed' | 'editing' | 'conflict'` + czysta funkcja `deriveWorkoutSessionPhase(input: { sessionId: string | null; sessionOrigin?: 'provisional' | 'remote'; isCompleted: boolean; isEditing: boolean; conflictDialogOpen: boolean; finalSyncPending: boolean; isExplicitSaving: boolean }): WorkoutSessionPhase` — mapowanie 1:1 z obecnych flag (bez zmiany zachowania); WorkoutDay liczy `phase` przez useMemo i UŻYWA jej w renderze zamiast rozproszonych warunków (badge statusu, blokady przycisków).
(b) Ekstrakcja decyzji hydracji do `src/lib/workout-hydration.ts`: czysta funkcja `resolveWorkoutHydration(input: { workoutForDate: WorkoutSession | null; draft: ActiveWorkoutDraft | null; draftHasData: boolean; completedValidationOk: boolean | null }): { useDraft: boolean; clearDraft: boolean }` — przenosi DOSŁOWNIE logikę gałęzi `shouldUseDraft` + warunek czyszczenia draftu z `WorkoutDay.tsx` (efekt hydracji, okolice linii 759-830). Efekt w komponencie woła funkcję i wykonuje skutki.

**Workflow:**
- [ ] Krok 1: failing testy `src/test/workout-session-state.test.ts` (każda faza po jednym przypadku, 8 testów) i `src/test/workout-hydration.test.ts` — przypadki przeniesione Z ISTNIEJĄCYCH gałęzi: brak draftu → useDraft=false; draft innej sesji niż workoutForDate → false; brak workoutForDate + draftHasData → true; completed + walidacja ok + !finalSyncPending → clearDraft=true; completed + walidacja NIE-ok + draftHasData → useDraft=true; finalSyncPending → true; dirty → true; puste exercises w chmurze + draftHasData → true; lastFirebaseSyncAt null + draftHasData → true; updatedAt > lastFirebaseSyncAt → true.
- [ ] Krok 2: implementacja obu funkcji; podmiana w WorkoutDay (chirurgicznie: efekt hydracji woła resolveWorkoutHydration; render używa phase). ZERO zmian zachowania — to refaktor ekstrakcyjny.
- [ ] Krok 3: pełne bramki + `npm run e2e:mock` + `npm run e2e:emulator`.
- [ ] Krok 4: commit `refactor(workout): jawna maszyna stanów sesji + hydracja jako czysta funkcja (Z57)`.

### CHECKPOINT FAZY 4
- [ ] `npm run test`, typecheck, lint, build, `npm run e2e:mock`, `npm run e2e:emulator`.
- [ ] Wpis do DECYZJE.md.

---

## FAZA 5: Higiena i zaległości z FAZY 7 planu R2

### Zadanie Z58: vitest 4.x w functions (dev deps HIGH/CRITICAL)

- [ ] Krok 1: `cd functions && npm i -D vitest@^4` (+ `@vitest/coverage-v8` jeśli używany); `npm --prefix functions test` — napraw ewentualne breaking changes API testów (changelog vitest 4: głównie konfiguracja workspace/pool).
- [ ] Krok 2: `npm --prefix functions audit` — brak HIGH/CRITICAL w dev; build functions OK.
- [ ] Krok 3: commit `chore(deps): vitest 4.x w functions (Z58)`. Jeśli bump łamie testy w sposób wymagający >1 h pracy: STOP, wycofaj, wpis do DECYZJE.md.

### Zadanie Z59: saveMaxHR przez rules zamiast Cloud Function

**Kontekst (FAZA 7 pkt 3 planu R2):** `saveMaxHR` to callable tylko po to, by zapisać 2 pola profilu.

**Fix:** rules `users` update whitelist + typy: dopuść `estimatedMaxHR` (`is int && >= 100 && <= 230`) i `maxHRManualOverride` (`is bool`); klient (`src/` — znajdź callera saveMaxHR przez rg) pisze `updateDoc(users/{uid}, {...})` bezpośrednio; usuń eksport funkcji z functions + `firebase functions:delete saveMaxHR --force` zapisz jako krok FAZY 7. UWAGA: istniejące testy rules mają przypadki "user update estimatedMaxHR zablokowane" — ZAKTUALIZUJ je na dozwolone z walidacją typów (nowy przypadek: 300 → DENIED, 'wysoki' → DENIED).

**Workflow:**
- [ ] Krok 1: failing testy rules (update dozwolony w widełkach, DENIED poza, DENIED zły typ) + aktualizacja starych przypadków; `npm run test:rules`.
- [ ] Krok 2: klient + usunięcie funkcji z index.ts; functions test + build; `rg saveMaxHR` — zero użyć klienckich starych.
- [ ] Krok 3: commit `chore(serverless): saveMaxHR przez rules, -1 kontener (Z59)`.

### Zadanie Z60: martwe aliasy tras + drobiazgi nawigacji

- [ ] Krok 1: usuń trasy-aliasy `/stats`, `/summary`, `/progress` (`App.tsx:193-195`); `rg "'/stats'|'/summary'|'/progress'"` w src/ — zero linków (bezpiecznik).
- [ ] Krok 2: testy + e2e:mock; commit `chore(nav): usunięcie martwych aliasów tras (Z60)`.

### Zadanie Z61 (OPCJONALNE, wykonaj tylko jeśli FAZY 1-5 zamknięte a budżet sesji pozwala): App Check

**Kontekst:** FAZA 7 pkt 7 planu R2. Pełny App Check = rejestracja reCAPTCHA v3 (web) + App Attest (iOS) + `initializeAppCheck` w kliencie + stopniowe enforce na callable. Przy dostępie ograniczonym do 1 usera na TestFlight wartość jest niska, a ryzyko odcięcia usera od apki przy błędnej konfiguracji realne. **Rekomendacja: NIE wdrażać enforce w tej sesji.** Dozwolony zakres bez ryzyka: (a) `firebase appcheck` rejestracja App Attest dla appki iOS + reCAPTCHA v3 dla web, (b) `initializeAppCheck` w kliencie w trybie MONITOR (bez enforce na żadnej funkcji), (c) wpis do DECYZJE.md z metrykami do sprawdzenia przed enforce. Jeżeli rejestracja wymaga kroków w konsoli niedostępnych z CLI: STOP, wypisz kroki dla usera w DECYZJE.md.

### CHECKPOINT FAZY 5
- [ ] `npm run test`, typecheck, lint, build, `npm run test:rules`, `npm --prefix functions test` + build, `npm run e2e:mock`.
- [ ] Wpis do DECYZJE.md.

---

## FAZA 6: Wyróżnik rynkowy: Adaptive Coach (autoregulacja + gotowość)

**Pozycjonowanie (dlaczego to jest wyróżnik):** Strong/Hevy/Jefit liczą serie i objętość, ale ŻADNA popularna apka nie zamyka pętli autoregulacji: user już dziś wpisuje w Strength Save RPE, ból i jakość per ćwiczenie (`exerciseMetrics`) oraz ma podpięte obciążenie biegowe ze Stravy (`training-load.ts`) — a apka z tym prawie nic nie robi. Wyróżnik: **"trener, który reaguje na Twoje RPE i ból, łączy siłownię z bieganiem i działa w 100% offline"**. Wszystko czyste heurystyki na kliencie: zero nowych Functions, zero kosztów, spójne z architekturą serverless i offline-first. Fundamenty już istnieją: `src/lib/next-set-advice.ts`, `src/lib/rza-progression.ts`, `src/lib/rza-metrics.ts`, `src/lib/training-load.ts`, `exerciseInsights` w WorkoutDay (Z35).

### Zadanie Z63: silnik Adaptive Coach (czysta logika, TDD)

**Fix:** Nowy moduł `src/lib/adaptive-coach.ts` z dwiema czystymi funkcjami:
(a) `buildExerciseRecommendation(input: { history: WorkoutSession[]; exerciseId: string; exerciseName?: string; isBodyweight: boolean }): ExerciseRecommendation | null` gdzie `ExerciseRecommendation = { action: 'progress' | 'hold' | 'deload'; weightDeltaKg: number; reasonKey: 'coachx.reason.pain' | 'coachx.reason.hardSession' | 'coachx.reason.readyToProgress' (zawężona unia — t() wymaga literalnych kluczy, wzorzec workoutSyncErrorMessageKey); metrics: { avgRpe?: number; maxPain?: number; completionRate: number } }`. Reguły (ostatnia sesja z tym ćwiczeniem, z metrykami gdy są): `maxPain >= 3` → deload -10% i reasonKey `coachx.reason.pain`; `avgRpe >= 9` lub completionRate < 0.8 → hold, `coachx.reason.hardSession`; `avgRpe <= 7.5` i completionRate == 1 → progress (+2.5 kg izolowane/górne, +5 kg nogi/martwy — heurystyka po nazwie ćwiczenia jak w `next-set-advice.ts`), `coachx.reason.readyToProgress`; brak metryk → null (UI spada na istniejące nextAdvice).
(b) `buildReadiness(input: { workouts: WorkoutSession[]; stravaActivities: StravaActivity[]; now: Date }): { score: 0..100; level: 'fresh' | 'ok' | 'loaded' | 'overreached'; reasonKey: string }` — łączy tonaż 7 dni vs średnia 28 dni (wzorzec z `training-load.ts` — użyj istniejących funkcji, nie duplikuj) + obciążenie biegowe; monotoniczna skala, progi: ratio < 0.8 fresh, 0.8-1.2 ok, 1.2-1.5 loaded, > 1.5 overreached.

**Workflow:**
- [ ] Krok 1: failing testy `src/test/adaptive-coach.test.ts` — minimum 10 przypadków (po jednym na każdą regułę + brak metryk → null + puste dane → readiness ok/50).
- [ ] Krok 2: implementacja; testy zielone; typecheck/lint.
- [ ] Krok 3: commit `feat(coach): silnik Adaptive Coach — rekomendacje z RPE/bólu + readiness siła+kardio (Z63)`.

### Zadanie Z64: Adaptive Coach w UI (Dashboard + WorkoutDay + podsumowanie)

**Fix:** (a) Karta "Coach" na Dashboardzie (po karcie dzisiejszego treningu): readiness (pasek/etykieta poziomu) + JEDNA najważniejsza rekomendacja dnia (deload > hold > progress priorytetem), z wyjaśnieniem po ludzku; useMemo na workouts+strava (wzorce memo z Dashboardu). (b) WorkoutDay: `exerciseInsights` (Z35) rozszerzone o `coachRecommendation` z `buildExerciseRecommendation`; ExerciseCard pokazuje ją nad/obok istniejącego nextAdvice jako badge z akcją ("+2.5 kg dziś" / "Utrzymaj ciężar" / "Zejdź -10%, ból") — bez zmiany logiki zapisu. (c) Ekran podsumowania ukończonego treningu: sekcja "Następnym razem" z listą rekomendacji per ćwiczenie. (d) Klucze i18n `coachx.*` do OBU locale (uwaga: prefiks `coachx`, NIE `coach.*` — stare coach.* usunięto w Z39). Wszystko za nowym FEATURE_FLAGS.adaptiveCoach = true (kill-switch w `src/lib/feature-flags.ts`, wzorzec workoutTimers).

**Workflow:**
- [ ] Krok 1: implementacja (a)-(d); testy istniejące zielone (Dashboard/WorkoutDay renderują się w e2e:mock).
- [ ] Krok 2: test e2e mock: wstrzyknij przez `writeWorkoutDraftDb`/mock dane z wysokim RPE i sprawdź badge rekomendacji na karcie ćwiczenia (wzorzec testu Z47).
- [ ] Krok 3: pełne bramki; commit `feat(coach): karta Coach na Dashboardzie, rekomendacje w treningu i podsumowaniu (Z64)`.

### CHECKPOINT FAZY 6
- [ ] `npm run test`, typecheck, lint, build, `npm run e2e:mock`.
- [ ] Wpis do DECYZJE.md (reguły silnika + jak je stroić).

---

## FAZA 7: Release train (WSZYSTKO na produkcję, bez pytania)

### Zadanie Z65: wdrożenie

**Kolejność OBOWIĄZKOWA (checklist CLAUDE.md + lekcje R2):**
- [ ] Krok 1: pełne bramki: `npm run test`, typecheck, lint, build, `npm run test:rules`, `npm --prefix functions test`, `npm run e2e:mock`, `npm run e2e:emulator`.
- [ ] Krok 2: commit + `git push origin main`.
- [ ] Krok 3: rules: `firebase deploy --only firestore:rules --project fittracker-workouts` (Z59 zmienia rules; indeksy tylko jeśli zmieniane).
- [ ] Krok 4: functions (jeśli zmieniane w Z58/Z59): `firebase deploy --only functions --project fittracker-workouts --force`; po deployu `firebase functions:delete saveMaxHR --force --project fittracker-workouts` (jeśli Z59 wykonane). Globalny binarny `firebase`, NIE `npx firebase`.
- [ ] Krok 5: web: `npm run deploy` (sam push NIE aktualizuje strony). NIGDY `cap sync ios` po deployu. Weryfikacja: hash `index-*.js` na https://grzegorzee.github.io/strength-save/ zgodny z dist/index.html.
- [ ] Krok 6: iOS: build number — `CURRENT_PROJECT_VERSION` jest już 49 (6 wystąpień) i build 49 NIE był wysłany do ASC, więc zostaje 49; JEŚLI preflight/upload zgłosi kolizję numeru w ASC, bump 49 -> 50 w 6 wystąpieniach + commit. Następnie **bez pytania usera**: `scripts/release-ios.sh "X10: auto-resume treningu, szybszy start, porzadki ustawien"` (build + TestFlight + auto Beta App Review; Robert dostaje build automatycznie; skrypt sam ładuje .env po Z34).
- [ ] Krok 7: sekrety GitHub: `gh secret delete VITE_ALLOWED_EMAIL --repo grzegorzee/strength-save && gh secret delete VITE_ALLOWED_EMAILS --repo grzegorzee/strength-save` (zaległość z R2/Z45). Jeśli `gh` nie ma uprawnień: wypisz komendę userowi w raporcie.
- [ ] Krok 8: weryfikacja produkcji: web wstaje, logi functions bez błędów (`gcloud functions logs read <fn> --project fittracker-workouts --limit 20` dla zmienianych), TestFlight processing widoczny (skrypt raportuje).
- [ ] Krok 9: DECYZJE.md (checkpoint X10) + `CONTEXT.md` (OSTATNIE DECYZJE) + `PLAN.md` (statusy) + aktualizacja pamięci projektu.
- [ ] Krok 10: scenariusz testu terenowego dla USERA w raporcie końcowym: (1) start treningu, odhacz 2 serie, ZABIJ apkę (swipe), otwórz ponownie — apka sama otwiera trening i widok stoi na ostatnim ćwiczeniu; (2) zgaś ekran na 2 min w trakcie treningu, wróć — jesteś w treningu na ostatnim ćwiczeniu; (3) Dashboard w trakcie treningu pokazuje "Kontynuuj trening" z liczbą serii; (4) Ustawienia: brak "godziny podsumowania", narzędzia naprawcze zwinięte, Sync Center niewidoczne bez zaległości; (5) subiektywnie szybszy zimny start.

---

## Poza zakresem tego planu (świadomie)

- App Check enforce (Z61 to co najwyżej tryb monitor; enforce przy publicznym launchu).
- Konsolidacja admin callables w dispatcher, paginacja exportUserDataApi (FAZA 7 pkt 4-6 planu R2) — niski zysk przy 1 userze.
- Wirtualizacja list (react-window) — historia/biblioteka są znośne; wraca gdy realnie spuchną.
- Konflikt draftów multi-device jako pełny flow (M2.8), Wilks score, timery treningowe (nadal za flagą `VITE_FEATURE_WORKOUT_TIMERS=false`).
- Scalanie Profile/Settings w jeden ekran — duży ruch UX o małej wartości; Z50-Z52 rozwiązują realne bóle.

## Kolejność awaryjna (gdy user zgłosi nowy błąd w trakcie)

1. Błąd zapisu/utraty treningu: przerwij bieżące zadanie po commicie, diagnozuj z telemetrią `client_errors` (AdminDashboard, sekcja "Błędy klienta"), wróć do planu po hotfixie.
2. Błąd niekrytyczny: dopisz do rejestru na końcu tego pliku, nie zmieniaj kolejności faz.
