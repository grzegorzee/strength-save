# PLAN X12C: statystyki miesięczne. Ile treningów i ile czasu w każdym miesiącu (2026-07-17)

> **Dla agenta wykonawcy:** WYMAGANY SUB-SKILL: `superpowers:executing-plans` (albo `superpowers:subagent-driven-development`). Kroki mają checkboxy `- [ ]`. Wykonuj zadania PO KOLEI. Nie przeskakuj checkpointów.
> Kontekst: wymaga WDROŻONYCH planów X12A i X12B. Zgłoszenie usera (2026-07-17): "chciałbym móc przeglądnąć ile treningów zrobiłem w miesiącu oraz ile czasu poświęciłem na to w miesiącu, bo teraz ten czas nigdzie nie jest pokazywany, a ja loguję wszystkie swoje treningi od początku roku, chciałbym, żeby każdy miał do tego dostęp."
> Odwołania plik:linia z rozpoznania 2026-07-17. Przed edycją ZAWSZE zweryfikuj aktualny stan pliku.

**Goal:** Każdy user widzi w Analityce przegląd miesięcy: liczba ukończonych treningów, łączny czas i tonaż per miesiąc (12 miesięcy wstecz), z jawnym oznaczeniem treningów bez zmierzonego czasu (dane sprzed M32 nie mają `durationSec`). Na końcu release train: web + iOS na TestFlight (wersja pozostaje 1.0.0, bump tylko numeru buildu).

**Architektura podejścia:** Czysta funkcja agregująca w `src/lib/monthly-stats.ts` (pełne pokrycie unit testami: granice miesięcy w czasie lokalnym, przełom roku, braki czasu), potem karta w zakładce Podsumowanie Analityki zbudowana 1:1 na istniejących wzorcach kart. Zero nowych kolekcji, zero zapisów: agregacja z już załadowanych `workouts`.

**Tech stack:** React + TypeScript, vitest + Playwright (mock, hak `fittracker_e2e_workouts` + helper `setE2EWorkouts`), i18n PL/EN, tokeny Kinetic Precision.

---

## 0. AUTORYZACJE I TWARDE ZASADY

1. **Deploy bez pytania** wyłącznie w FAZIE 3 (release train). Autoryzacja stała usera.
2. Dane: liczymy WYŁĄCZNIE treningi `completed`. Czas: `durationSec`, fallback `completedAt - startedAt` (oba znaczniki istnieją w typie: `src/types/index.ts:55-58`). Brak obu = trening "bez zmierzonego czasu", liczony do liczby treningów, jawnie raportowany, NIE zaniża sumy czasu.
3. Czas lokalny: miesiąc wyznacza `date` treningu (string `YYYY-MM-DD`, kanoniczny w projekcie), NIE timestampy (strefa!). `monthKey = date.slice(0, 7)`.
4. Design: żadnych nowych bibliotek ani wykresów. Karta = wzorzec istniejących kart Analityki (Card, nagłówek `font-heading font-bold uppercase italic tracking-tight`, `tabular-nums` dla liczb). Wyjątki brandowe (Strava) nietykalne.
5. i18n: nowe klucze do OBU plików `pl.ts` i `en.ts`. Nazwy miesięcy przez `dateLocale(lang)`, nie ręczne tablice.
6. Chirurgicznie, TDD, bramki: `npm run test`, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run check:bundle-budget`, `npm run e2e:mock`.
7. Wpis do DECYZJE.md po każdej fazie.

---

## FAZA 0: Baseline

### Zadanie Z92.0: zielony baseline

- [x] Krok 1: `git status` czyste, X12B wdrożony (wpis w DECYZJE.md, wersja 1.0.0 w package.json). Jeśli nie: STOP.
- [x] Krok 2: Pełne bramki zielone.

---

## FAZA 1: Z92. Czysta agregacja miesięczna (lib + testy)

### Zadanie Z92.1: rozpoznanie istniejących helperów (DRY)

- [x] Krok 1: `rg -n "tonnage|tonaż|tonaz" src/lib src/components --ignore-case -l` : znajdź istniejącą funkcję liczącą tonaż treningu (Dashboard i Historia już go pokazują). Użyj JEJ w agregacji, nie pisz drugiej. Zanotuj w raporcie sygnaturę.
- [x] Krok 2: `rg -n "durationSec" src/` : sprawdź, czy istnieje już helper formatowania czasu trwania (Z76 liczył "Czas i gęstość" w `AnalyticsChartsTab.tsx:302`). Jeśli jest formatter `Xh Ym`, reużyj; jeśli nie, powstanie w Z92.2.

### Zadanie Z92.2: testy agregacji (test first)

- [x] Krok 1: Nowy plik `src/test/monthly-stats.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { aggregateMonthlyStats, workoutDurationSec, formatDurationHM } from '@/lib/monthly-stats';
import type { WorkoutSession } from '@/types';

const w = (over: Partial<WorkoutSession>): WorkoutSession => ({
  id: 'w1', userId: 'u1', dayId: 'day-1', date: '2026-07-10',
  completed: true, exercises: [],
  ...over,
} as WorkoutSession);

describe('workoutDurationSec (Z92)', () => {
  it('bierze durationSec gdy jest', () => {
    expect(workoutDurationSec(w({ durationSec: 3600 }))).toBe(3600);
  });
  it('fallback z completedAt - startedAt', () => {
    expect(workoutDurationSec(w({ startedAt: 1_000_000, completedAt: 4_600_000 }))).toBe(3600);
  });
  it('null gdy brak danych czasu', () => {
    expect(workoutDurationSec(w({}))).toBeNull();
  });
  it('null gdy znaczniki niespójne (completedAt <= startedAt)', () => {
    expect(workoutDurationSec(w({ startedAt: 5, completedAt: 5 }))).toBeNull();
  });
});

describe('aggregateMonthlyStats (Z92)', () => {
  const now = new Date('2026-07-17T12:00:00');
  it('grupuje po miesiącu z date (czas lokalny), tylko completed', () => {
    const stats = aggregateMonthlyStats([
      w({ id: 'a', date: '2026-07-01', durationSec: 3000 }),
      w({ id: 'b', date: '2026-07-31', durationSec: 600 }),
      w({ id: 'c', date: '2026-06-30', durationSec: 100 }),
      w({ id: 'd', date: '2026-07-15', completed: false, durationSec: 999 }),
    ], 12, now);
    const july = stats.find(s => s.monthKey === '2026-07')!;
    expect(july.workoutCount).toBe(2);
    expect(july.totalDurationSec).toBe(3600);
    expect(july.workoutsWithDuration).toBe(2);
    expect(stats.find(s => s.monthKey === '2026-06')!.workoutCount).toBe(1);
  });
  it('treningi bez czasu liczą się do workoutCount, nie do totalDurationSec', () => {
    const stats = aggregateMonthlyStats([
      w({ id: 'a', date: '2026-07-01', durationSec: 3600 }),
      w({ id: 'b', date: '2026-07-02' }),
    ], 12, now);
    const july = stats.find(s => s.monthKey === '2026-07')!;
    expect(july.workoutCount).toBe(2);
    expect(july.workoutsWithDuration).toBe(1);
    expect(july.totalDurationSec).toBe(3600);
  });
  it('okno monthsBack: przełom roku, poza oknem odpada, sortowanie od najnowszego', () => {
    const stats = aggregateMonthlyStats([
      w({ id: 'a', date: '2026-01-05' }),
      w({ id: 'b', date: '2025-12-28' }),
      w({ id: 'c', date: '2025-06-01' }),
    ], 8, now);
    expect(stats.map(s => s.monthKey)).toEqual(['2026-01', '2025-12']);
  });
  it('miesiące bez treningów nie są zwracane', () => {
    const stats = aggregateMonthlyStats([w({ date: '2026-05-05' })], 12, now);
    expect(stats.map(s => s.monthKey)).toEqual(['2026-05']);
  });
});

describe('formatDurationHM (Z92)', () => {
  it('formatuje godziny i minuty', () => {
    expect(formatDurationHM(4980)).toBe('1 h 23 min');
  });
  it('poniżej godziny tylko minuty', () => {
    expect(formatDurationHM(2940)).toBe('49 min');
  });
  it('zero i wartości ujemne dają 0 min', () => {
    expect(formatDurationHM(0)).toBe('0 min');
  });
});
```

  Uwaga: pola `WorkoutSession` w helperze `w()` dopasuj do realnego typu (`src/types/index.ts`); jeśli typ wymaga innych pól obowiązkowych, uzupełnij w helperze, nie w każdym teście. Tonaż w testach: dodaj scenariusz z `exercises` wg sygnatury znalezionej w Z92.1 (asercja `totalTonnageKg`).
- [x] Krok 2: `npm run test -- monthly-stats`: FAIL (moduł nie istnieje).

### Zadanie Z92.3: implementacja

- [x] Krok 1: Nowy plik `src/lib/monthly-stats.ts`:

```ts
import type { WorkoutSession } from '@/types';
// import istniejącej funkcji tonażu wg Z92.1, np.:
// import { calculateWorkoutTonnage } from '@/lib/workout-stats';

export interface MonthlyStat {
  monthKey: string;            // 'YYYY-MM' (z pola date, czas lokalny)
  workoutCount: number;        // ukończone treningi
  workoutsWithDuration: number;
  totalDurationSec: number;    // suma tylko ze zmierzonych
  totalTonnageKg: number;
}

// Czas trwania treningu: durationSec, fallback ze znaczników (M32).
// Null = brak danych (treningi sprzed M32): jawnie raportowane, nie zaniżają sumy.
export const workoutDurationSec = (workout: WorkoutSession): number | null => {
  if (typeof workout.durationSec === 'number' && workout.durationSec > 0) {
    return Math.round(workout.durationSec);
  }
  if (
    typeof workout.startedAt === 'number'
    && typeof workout.completedAt === 'number'
    && workout.completedAt > workout.startedAt
  ) {
    return Math.round((workout.completedAt - workout.startedAt) / 1000);
  }
  return null;
};

const monthKeyOf = (date: string): string => date.slice(0, 7);

const monthKeyFloor = (now: Date, monthsBack: number): string => {
  const d = new Date(now.getFullYear(), now.getMonth() - (monthsBack - 1), 1);
  const mm = String(d.getMonth() + 1).padStart(2, '0');
  return `${d.getFullYear()}-${mm}`;
};

export const aggregateMonthlyStats = (
  workouts: WorkoutSession[],
  monthsBack: number,
  now: Date,
): MonthlyStat[] => {
  const floor = monthKeyFloor(now, monthsBack);
  const byMonth = new Map<string, MonthlyStat>();
  for (const workout of workouts) {
    if (!workout.completed) continue;
    if (typeof workout.date !== 'string' || workout.date.length < 7) continue;
    const monthKey = monthKeyOf(workout.date);
    if (monthKey < floor) continue;
    const entry = byMonth.get(monthKey) ?? {
      monthKey, workoutCount: 0, workoutsWithDuration: 0, totalDurationSec: 0, totalTonnageKg: 0,
    };
    entry.workoutCount += 1;
    const duration = workoutDurationSec(workout);
    if (duration !== null) {
      entry.workoutsWithDuration += 1;
      entry.totalDurationSec += duration;
    }
    entry.totalTonnageKg += 0; // TU: istniejąca funkcja tonażu z Z92.1
    byMonth.set(monthKey, entry);
  }
  return [...byMonth.values()].sort((a, b) => b.monthKey.localeCompare(a.monthKey));
};

export const formatDurationHM = (totalSec: number): string => {
  const safe = Math.max(0, Math.round(totalSec));
  const hours = Math.floor(safe / 3600);
  const minutes = Math.floor((safe % 3600) / 60);
  if (hours === 0) return `${minutes} min`;
  return `${hours} h ${minutes} min`;
};
```

  Linijkę `entry.totalTonnageKg += 0;` ZASTĄP wywołaniem istniejącej funkcji tonażu (Z92.1). Jeśli funkcja liczy tonaż per trening z `exercises`, przekaż trening; dopasuj i dopisz test z realnym kształtem `exercises`.
- [x] Krok 2: `npm run test -- monthly-stats`: PASS. Cały `npm run test` zielony.
- [x] Krok 3: Commit `feat(analytics): agregacja miesięczna treningów, czasu i tonażu (Z92)`.

---

## FAZA 2: Z93. Karta "Miesiące" w Analityce

### Zadanie Z93.1: umiejscowienie i wzorzec

- [x] Krok 1: Zlokalizuj `SummaryTab` (renderowany w `Analytics.tsx:323`; sprawdź, czy to komponent w `Analytics.tsx`, czy osobny plik w `src/components/analytics/`). Obejrzyj sąsiednie karty: przejmij ich strukturę Card/nagłówek/siatkę 1:1.
- [x] Krok 2: Decyzja umiejscowienia (nie twórz 5. zakładki: pasek zakładek jest pełny na mobile): karta "MIESIĄCE" na GÓRZE zakładki Podsumowanie, nad dotychczasowymi kartami. Dostępna dla każdego zalogowanego (zakładka summary nie ma gate'ów; NIE dodawaj żadnego).

### Zadanie Z93.2: komponent karty

- [x] Krok 1: Nowy plik `src/components/analytics/MonthlyOverviewCard.tsx`. Zawartość: nagłówek karty (`font-heading font-bold uppercase italic tracking-tight`), lista wierszy per miesiąc z `aggregateMonthlyStats(workouts, 12, new Date())`:
  - etykieta miesiąca: `format(parseLocalDate(monthKey + '-01'), 'LLLL yyyy', { locale: dateLocale(lang) })` (date-fns jest w projekcie; jeśli używany jest inny helper formatowania dat, reużyj go),
  - liczba treningów: `t('analytics.months.workouts', { n })`,
  - czas: `formatDurationHM(totalDurationSec)` + gdy `workoutsWithDuration < workoutCount` dopisek `t('analytics.months.noTime', { n: workoutCount - workoutsWithDuration })`,
  - tonaż: format spójny z Historią (separator tysięcy, `tabular-nums`).
  Props: `workouts: WorkoutSession[]` (przekazane z SummaryTab; NIE twórz własnego hooka odczytu). Pusty stan (0 miesięcy): komponent zwraca `null` (nie renderuj pustej karty).
- [x] Krok 2: Klucze i18n do OBU locale (przykład PL / EN):

```ts
// pl.ts
'analytics.months.title': 'Miesiące',
'analytics.months.workouts': '{n} treningów',
'analytics.months.noTime': '{n} bez zmierzonego czasu',
// en.ts
'analytics.months.title': 'Months',
'analytics.months.workouts': '{n} workouts',
'analytics.months.noTime': '{n} without tracked time',
```

  Uwaga na odmianę: jeśli projekt ma mechanizm liczby mnogiej (sprawdź istniejące klucze z `{n}`), użyj go; jeśli nie, zaakceptuj formę "5 treningów / 1 treningów" TYLKO jeśli tak robią sąsiednie klucze, w przeciwnym razie dodaj warianty jak w istniejącym wzorcu.
- [x] Krok 3: Wepnij `<MonthlyOverviewCard workouts={workouts} />` na górze SummaryTab (źródło `workouts` = to samo, którego używają istniejące karty zakładki).
- [x] Krok 4: `npm run typecheck`, `npm run lint`, `npm run test` zielone. Dev check ręczny: karta renderuje się z danymi konta e2e/dev.
- [x] Krok 5: Commit `feat(analytics): karta Miesiące (treningi, czas, tonaż per miesiąc) (Z93)`.

### Zadanie Z93.3: e2e

- [x] Krok 1: Nowy spec `e2e/analytics-monthly.spec.ts` (wzorzec `e2e/nav-analytics.spec.ts` + `setE2EWorkouts`). Wstrzyknij treningi: 2 w bieżącym miesiącu (jeden z `durationSec: 3600`, jeden BEZ czasu), 1 w poprzednim miesiącu (`durationSec: 1800`). Daty licz dynamicznie od `new Date()` (nie hardkoduj miesiąca). Asercje na zakładce Podsumowanie:
  - wiersz bieżącego miesiąca: "2 treningów", "1 h 0 min", "1 bez zmierzonego czasu",
  - wiersz poprzedniego miesiąca: "1 treningów" i "30 min" (dopasuj dokładne stringi do finalnych kluczy i18n; jeśli mechanizm liczby mnogiej da inną formę, zaktualizuj asercje).
- [x] Krok 2: `npm run e2e:mock` zielony w całości.
- [x] Krok 3: Commit `test(e2e): karta Miesiące w Analityce (Z93)`.

### CHECKPOINT FAZY 2

- [x] Pełne bramki zielone. Wpis do DECYZJE.md.

---

## FAZA 3: Release train C

- [ ] Krok 1: Pełne bramki + `npm run check:dist-smoke`.
- [ ] Krok 2: `git push`, `npm run deploy` (web), weryfikacja live.
- [ ] Krok 3: iOS: bump `CURRENT_PROJECT_VERSION` o 1 (6 wystąpień; MARKETING_VERSION zostaje 1.0.0!), commit, `scripts/ios-testflight.sh`, poll aż VALID + grupa Wewnętrzni.
- [ ] Krok 4: Wpisy: DECYZJE.md, PLAN.md (odhacz; dopisz do backlogu następne wg ustalonej kolejności: P0 walidacja danych z Firebase w onSnapshot, M19 PWA offline mode, M20 eksport PDF/raporty, web push, Android Google Play), CONTEXT.md w FIRMA.
- [ ] Krok 5: Raport dla usera: screenshot karty Miesiące, wyjaśnienie dopisku "bez zmierzonego czasu" (treningi sprzed M32), potwierdzenie wersji 1.0.0 i numeru buildu.

---

## Samokontrola przed startem (dla agenta)

- Spec pokryty? Treningi/miesiąc TAK, czas/miesiąc TAK (z jawnym brakiem danych), dostęp dla każdego usera TAK (summary bez gate'ów), tonaż jako bonus spójny z resztą apki.
- DRY: tonaż i formatowanie dat WYŁĄCZNIE istniejącymi helperami (Z92.1, Z93.2).
- Czas lokalny przez pole `date`, nigdy przez timestampy.
