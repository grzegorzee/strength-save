# PLAN X36 (2026-08-26): Postępy z Analityką na czele, Profil w zwijanych sekcjach, Profil bez paska Wstecz

Źródło: głosówka właściciela po buildzie 124 (X35b+X35c). Tryb: autonomiczny, wydanie na produkcję krok po kroku (web → iOS 125 → AAB v40).

## Decyzje produktowe (wiążące)

1. **Postępy:** segment `Analityka | Rekordy i odznaki` (Analityka pierwsza i DOMYŚLNA). Rekordy przez `?view=records` (stare linki `?section=records|badges` bez `view` nadal otwierają rekordy).
2. **Szybki dostęp do wykresów:** w Analityce nad zakładkami rząd skrótów `Tonaż` i `Progresja` (jedno tapnięcie z Postępów zamiast Analityka → Wykresy → kafel). Kafle menu wykresów: Tonaż i Progresja na początku.
3. **Profil:** KAŻDA sekcja ustawień jako jeden wiersz z „ptaszkiem" (Collapsible), domyślnie zwinięta; nagłówek wiersza pokazuje bieżącą wartość tam, gdzie ma sens (przerwa, trener, subskrypcja, język). Tożsamość + kafle dumy zostają otwarte (to nie ustawienia).
4. **Nowe grupowanie Profilu** (najczęściej używane wyżej, konto i akcje destrukcyjne na dole):
   - Tożsamość (avatar, imię, email, poziom) + Osiągnięcia (kafle) — bez zmian
   - Kolor przewodni — zwijany
   - Trening — jednostki, **nie wygaszaj ekranu** (przeniesione z karty przerw), tryb „nie na 100%", urlop
   - Timer i przerwy — timer przerwy on/off, dźwięk timera on/off, długości przerw, dźwięk, głośność (dawne „Przerwy między seriami" + dwa przełączniki z Treningu)
   - Kalkulator talerzy — zwijany (bez podwójnego zwijania w karcie)
   - Trener — zwijany, wartość: imię / zamaskowany adres / „Nie ustawiono"
   - Urządzenia i połączenia — HealthKit + Apple Watch/Garmin + Strava w JEDNEJ sekcji (dawne „Urządzenia i dostęp" + „Połączenia")
   - Powiadomienia — zwijane, niżej (nie są najważniejsze)
   - Subskrypcja — zwijana
   - Twoje dane — Historia, Pomiary, Postępy, Rekordy sprzed aplikacji, Admin
   - Backup i przywracanie — własna zwijana sekcja
   - Zgody i prywatność — własna zwijana sekcja
   - Konto i pomoc — język, hasło, pomoc, kontakt, o aplikacji
   - Stopka: Wyloguj, Usuń konto, wersja
5. **Deep linki** `?section=` otwierają (rozwijają) sekcję i przewijają do niej. Mapa starych kotwic: `connections`/`strava` → `devices`, `rest` → `timer`.
6. **Profil bez dolnego paska Wstecz** (strzałka w nagłówku zostaje). Pozostałe trasy spoza bottom nav bez zmian.
7. **Trener:** popup „Zapisać jako trenera?" po pierwszej udanej wysyłce na nowy adres ZOSTAJE (EmailWorkoutDialog, WP-I) — bez zmian, sprawdzone testem.

## Niezmienniki (zasada #5)

- Żaden wiersz/akcja Profilu nie znika: zmienia się tylko grupowanie, zwijanie i kolejność (test `profile-sections.test.tsx`).
- Wszystkie kotwice `profile-*` nadal istnieją (deep linki z powiadomień i Pomiarów).
- Postępy: rekordy, odznaki, sezony, analityka dostępne z tego samego ekranu; stare deep linki działają.

## Kroki i weryfikacja

1. Postępy (Achievements/Analytics/ChartsTab) → `achievements-tiles.test.tsx` + nowe testy domyślnego widoku i skrótów.
2. Layout: `/profile` bez BackBar → `back-bar.test.tsx`.
3. Profil: `ProfileAccordionSection`, przebudowa sekcji, karty z `hideTitle`, keep-awake w Treningu, PlateInventorySettings bez własnego wrappera → `profile-sections.test.tsx`, `plate-inventory-settings.test.tsx`, `rest-settings.test.tsx`, `route-smoke`.
4. i18n PL+EN (typecheck pilnuje).
5. Bramki: vitest, typecheck, lint, build, dist-smoke, bundle-budget, e2e chromium (full-app „11 sekcji", mobile-nav, nav-analytics, achievements URL).
6. QA zrzuty 393×852 (Profil zwinięty/rozwinięty, Postępy Analityka, skrót Tonaż).
7. Wydanie: commit + merge main + push, `npm run deploy`, iOS bump 125 + `release-ios.sh` + `testflight_external.py`, AAB v40, DECYZJE.md, telemetria `client_errors`.
