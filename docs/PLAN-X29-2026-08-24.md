# PLAN X29 (2026-08-24) — spójność reschedule, eksport sylwetki, splash, trener, kolor z avatara, sharing infra

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development / superpowers:executing-plans. Kroki mają checkboxy. Każdy pakiet = izolowany zestaw plików, commit per pakiet.

**Goal:** Naprawa 7 bugów zgłoszonych z realnego użycia (build 116) + 4 nowe funkcje + hardening maili i sharingu, zakończone pełnym wydaniem web + functions + iOS 117 + AAB v32.

**Architecture:** React 18 + Vite + TS, Capacitor iOS/Android, Firebase (Auth/Firestore/Storage/Functions). Wzorce obowiązujące: kanoniczny resolver `resolvePlannedDay` jako jedyne źródło harmonogramu, telemetria `client_errors`, zasady CLAUDE.md 1-11 (szczególnie 5: nowa funkcja nie zabiera nic staremu przepływowi; 11: kanoniczne stany + route sweep).

**Tech stack:** bez nowych zależności npm. Natywne pobieranie przez `CapacitorHttp` z `@capacitor/core` (już w projekcie).

**Spec:** zgłoszenie usera 2026-08-24 (transkrypcja głosowa) + raporty rozpoznania w tej sesji + telemetria produkcyjna `client_errors` (wpis 2026-08-22: `body-compare-export-load: photo-load-failed getBlob=getBlob-timeout fetch=Load failed` na iOS = oba kanały JS padają).

## Global Constraints

- Bramki końcowe: `npm run test` (vitest, 0 fail), `npm run typecheck`, `npm run lint` (0 err), `npm run build`, `npm run check:dist-smoke`, e2e zielone. Route render sweep (`route-smoke.test.tsx`) po każdej zmianie komponentu czytającego Firestore.
- i18n: KAŻDY nowy klucz do OBU plików `src/i18n/locales/pl.ts` i `en.ts` (inaczej typecheck pada).
- Wersje: MARKETING_VERSION / package.json `version` = **1.0.0 na sztywno**. Bump TYLKO `CURRENT_PROJECT_VERSION` → **117** (6 wystąpień w project.pbxproj) i Android `versionCode` → **32**.
- Zakaz em/en-dash w stringach UI (guard w testach), zakaz emoji w kodzie (`check:no-emoji`).
- Radix Sheet/Dialog: nigdy nie unmountować w stanie open; dialogi zawsze zamontowane, sterowane `open`.
- Kolory statusów: tło z `/10`, tekst pełny.
- Fixtury dokumentów Firestore przez `src/test/canonical-states.ts`, nie ręczne obiekty.
- W kodzie i commitach ZERO wzmianek o Stravie/Hevy jako inspiracji.
- Dane produkcyjne usera są święte: żadnych zapisów na realnych kontach.

---

## Batchowanie wykonania (agenci równolegli w git worktree, merge do main po batchu)

| Batch | Pakiety | Uzasadnienie rozdziału |
|---|---|---|
| 1 | WP-A, WP-C, WP-E, WP-G | rozłączne pliki (TrainingPlan/Dashboard vs functions+Bell vs BodyCompare vs Profile/AppNavigation) |
| 2 | WP-B, WP-F, WP-I, WP-J | WP-B po WP-A (TrainingPlan.tsx), WP-I po WP-G (Profile.tsx), WP-J rozłączny z WP-C w functions |
| 3 | WP-D, WP-H, WP-K, WP-L | WP-D po WP-G (AppNavigation), WP-L po WP-E (BodyCompareShareDialog) |
| 4 | Bramki + audyt product-audit (screenshoty) + naprawy z audytu | main loop |
| 5 | Release: web + functions + iOS 117 + AAB v32 + DECYZJE.md | main loop |

Konflikty w `pl.ts`/`en.ts` przy merge: rozwiązywać UNIĄ (wszystkie klucze zostają).

---

### WP-A: Spójność przełożenia treningu (Dashboard hero + zakładka Plan + dead-click kalendarza)

**Root cause (rozpoznanie):** `TrainingPlan.tsx:304-307` woła `buildTrainingSchedule(trainingPlan, startDate, planDurationWeeks)`, a `buildTrainingSchedule` (`src/lib/plan-schedule.ts:247-265`) NIE przyjmuje `scheduleOverrides` — timeline tygodnia i `nextPlannedDate` (badge NASTĘPNY) jadą wyłącznie z reguły weekday. Dashboard używa resolvera z overrides, stąd rozjazd. Dead-click ikony kalendarza: `RescheduleSheet.tsx:64-69` robi `return null` przy `open=true`, gdy `resolvePlannedDay` zwraca null dla klikniętej daty (data z `overrides[date]===null` albo przed `planStartDate`).

**Files:**
- Modify: `src/lib/plan-schedule.ts` (buildTrainingSchedule + typ wyniku)
- Modify: `src/pages/TrainingPlan.tsx` (timeline, nextPlannedDate, warunek ikony)
- Modify: `src/pages/Dashboard.tsx:447-482` (renderNextSessionHero) i `:930-979` (karta dzisiejsza)
- Modify: `src/components/RescheduleSheet.tsx` (fallback zamiast return null)
- Modify: `src/i18n/locales/pl.ts`, `en.ts`
- Test: `src/test/plan-schedule-overrides.test.ts`, `src/test/trainingplan-week-pager.test.tsx`, nowy `src/test/plan-reschedule-consistency.test.tsx`, `src/test/dashboard-hero-reschedule.test.tsx`

**Interfaces (Produces):**
- `buildTrainingSchedule(planDays, startDate, weeks, opts?: { overrides?: ScheduleOverrides; planStartDateISO?: string | null })` — wynik per data liczony przez `getScheduledTrainingForDate`/`resolvePlannedDay` (kanoniczny resolver), zero własnej reguły weekday.

**Kroki:**

- [ ] 1. Test czerwony (niezmiennik spójności): po `overrides = buildScheduleMove(pon→śr)` zakładka Plan renderuje kartę dnia na środzie, badge NASTĘPNY na środzie, poniedziałek bez karty treningu (lub z etykietą przeniesienia); stary przepływ bez overrides ma wszystkie dni na miejscu (zasada 5).
- [ ] 2. `buildTrainingSchedule` z opcjonalnym `opts` (podpis wyżej; wołanie bez opts zachowuje się jak dziś — zasada 5). Wewnątrz: dla każdej daty tygodnia `getScheduledTrainingForDate(planDays, date, { overrides, startDateISO })`.
- [ ] 3. `TrainingPlan.tsx`: przekazać `{ overrides: scheduleOverrides, planStartDateISO: planStartDate }` do `buildTrainingSchedule`; `nextPlannedDate` liczone z dat wynikowych (findNextPlannedDate bez zmian — dostaje już daty z resolvera).
- [ ] 4. Warunek ikony kalendarza (`TrainingPlan.tsx:710-712`): dodatkowo `trainingDateStr >= (planStartDate ?? '')` — ikona nie renderuje się dla dat przed startem planu.
- [ ] 5. `RescheduleSheet.tsx`: gdy `resolvePlannedDay` zwraca null przy `open=true` i braku zamrożonego kontekstu → render sheeta z komunikatem `reschedule.unavailable` (i18n PL: "Tego dnia nie da się przełożyć." / EN: "This day can't be rescheduled.") + przycisk zamknij; dodatkowo reset `frozenRef.current = null` przy przejściu `open` false→true (koniec stale kontekstu z poprzedniego otwarcia).
- [ ] 6. Hero Dashboardu — nowy podział ról (dotyczy `renderNextSessionHero` i karty dzisiejszej):
  - duży nagłówek: dzień tygodnia REALNEJ daty sesji: `formatLocalDateLabel(entry.dateKey, locale, { weekday: 'long' })` (np. "Środa"),
  - eyebrow: `NASTĘPNA SESJA · {localizeDayName(entry.day.dayName)} · {data wg showDate}` — nazwa dnia PLANU w eyebrow; gdy `localizeDayName(dayName)` jest równe (case-insensitive) nazwie dnia tygodnia z nagłówka, pominąć człon dayName (bez duplikacji),
  - podtytuł (focus + liczba ćwiczeń) bez zmian.
  - Karta dzisiejsza analogicznie: nagłówek = weekday dzisiejszej daty, eyebrow z dayName planu.
- [ ] 7. Testy zielone + `npm run typecheck` + route sweep. Commit `fix(x29): plan tab i hero honoruja przelozenia (resolver w buildTrainingSchedule), sheet bez dead-click`.

**deviceTestNotes:** przełożyć trening pon→śr na koncie QA (web): Dashboard pokazuje "Środa" + eyebrow z dniem planu; Plan pokazuje kartę na środzie z badge NASTĘPNY; ikona kalendarza na każdym przyszłym dniu otwiera sheet.

---

### WP-B: Flash paska postępu 100% → 0% w zakładce Plan

**Root cause:** `TrainingPlan.tsx:164` nie pobiera `isLoaded` z `useTrainingPlan` (Dashboard robi to w `:736`). Pierwszy frame: `planStartDate=null` → `completedInPlan` = wszystkie treningi z okna recent, `remainingWorkouts=0` → 100%; po snapshotcie nowego cyklu 0%.

**Files:**
- Modify: `src/pages/TrainingPlan.tsx`
- Modify: `src/lib/plan-schedule.ts` (`computeProgressPercent` — obrona w głębi)
- Test: nowy `src/test/trainingplan-loading-gate.test.tsx`

**Kroki:**

- [ ] 1. Test czerwony: render `TrainingPlan` z hookiem w stanie `isLoaded=false` + workouts z ukończonymi sesjami → NIE renderuje `trainingplan.metaProgress` ze 100% (renderuje loader).
- [ ] 2. Destrukturyzacja `isLoaded: planIsLoaded` z `useTrainingPlan(uid)` + `cyclesLoaded` z `usePlanCycles`; wczesny return loadera (wzorzec `Dashboard.tsx:736`, ten sam spinner `common.loading`).
- [ ] 3. `computeProgressPercent`: dodatkowy parametr `planStartDate?: string | null`; zwraca 0 gdy `planStartDate == null` (dokumentacja w komentarzu: obrona w głębi, gate jest pierwszą linią). Zaktualizować wywołanie.
- [ ] 4. Testy + typecheck + route sweep. Commit `fix(x29): gate ladowania w zakladce Plan - koniec flasha 100%->0% po zakonczonym cyklu`.

---

### WP-C: Powiadomienie "Raport tygodnia gotowy" prowadzi do raportu tygodnia

**Root cause:** `functions/src/weekly-digest.ts:184` → `deepLink: "/analytics"` = tab `summary`. Ekran tygodni istnieje: `/analytics?tab=weekly` (redirect przenosi param).

**Files:**
- Modify: `functions/src/weekly-digest.ts:184`
- Modify: `src/components/NotificationBell.tsx` (fallback dla istniejących eventów)
- Test: `functions/src/__tests__` odpowiedni test digestu + nowy przypadek w `src/test` dla Bell

**Kroki:**

- [ ] 1. Test czerwony (functions): writeUserEvent dla week z `deepLink: "/analytics?tab=weekly"`.
- [ ] 2. Zmiana w `weekly-digest.ts:184` na `"/analytics?tab=weekly"`.
- [ ] 3. `NotificationBell.tsx` handler kliku: dla `event.type === 'week'` nawiguj na `'/analytics?tab=weekly'` niezależnie od `deepLink` (istniejące dokumenty user_events mają stary link; komentarz w kodzie: legacy eventy sprzed X29). Test komponentu.
- [ ] 4. Testy functions + web + commit `fix(x29): powiadomienie raportu tygodnia deep-linkuje do taba weekly`.

---

### WP-D: Dolna nawigacja widoczna zawsze (także w sesji treningowej i szczególe ćwiczenia)

**Stan:** `Layout.tsx:33` ukrywa nav i header na `/workout/*` i `/exercise/*`. User: menu ma być zawsze; w sesji brak wyjścia do Dashboard/Planu/Profilu wygląda dziwnie.

**Decyzja projektowa:** bottom nav renderowany na WSZYSTKICH trasach w Layout (w tym `/workout/*`, `/exercise/*`); header pozostaje ukryty w focused flow (fokus na treningu, własny przycisk wstecz zostaje). Paski sesji pozycjonowane NAD navem. `/new-plan` i `/paywall` bez zmian (pełny ekran).

**Files:**
- Modify: `src/components/Layout.tsx` (usunąć przekazywanie `hideMobileNav` dla focused flow)
- Modify: `src/components/AppNavigation.tsx` (prop `hideMobileNav` zostaje w API, ale Layout przestaje go ustawiać)
- Modify: `src/pages/WorkoutDay.tsx:3167` (pasek startu: `bottom-0` → `bottom-[calc(5.75rem+env(safe-area-inset-bottom))]`), `src/components/RestBar.tsx:162` (jw.), kontrola `src/components/IntervalTimer.tsx:92` (offset ponad RestBar)
- Modify: padding dolny treści WorkoutDay (rezerwa na nav + pasek)
- Test: `e2e/mobile-nav-reachability.spec.ts` (nowy przypadek: nav widoczny na `/workout/:dayId`), test komponentowy Layout

**Kroki:**

- [ ] 1. Test czerwony: render Layout na trasie `/workout/day-1` → `nav[aria-label]` obecny; na `/new-plan` nieobecny.
- [ ] 2. `Layout.tsx`: `hideMobileNav` = tylko `isFullScreenFlow` (czyli nigdy w Layout, bo fullscreen ma osobny early return) → uprościć do `<AppNavigation />`; header nadal `{!isFocusedFlow && ...}`.
- [ ] 3. Offsety pasków sesji (wartości wyżej) + `pb` treści WorkoutDay. Sprawdzić brak overlapu przy jednoczesnym RestBar + nav (RestBar nad navem, IntervalTimer nad RestBar).
- [ ] 4. Scenariusz sekwencji (zasada 5): start treningu z planu → tap PLAN w nav → powrót do sesji przez Dashboard/auto-resume → wszystkie ćwiczenia i draft na miejscu (istniejące testy draftu muszą przejść bez zmian).
- [ ] 5. e2e + vitest + commit `feat(x29): bottom nav widoczny w sesji treningowej i szczegolach cwiczenia`.

**deviceTestNotes:** realny iPhone: w trakcie sesji z odliczającym RestBarem nav nie zasłania przycisków; safe-area dół OK; wyjście do Planu i powrót nie gubi draftu.

---

### WP-E: Eksport before/after — kanał natywny (fix po nieskutecznym hotfixie 116)

**Dowód z telemetrii (2026-08-22, po 116):** `photo-load-failed getBlob=getBlob-timeout fetch=Load failed` — na iOS padają OBA kanały JS (SDK getBlob i fetch), bo oba idą przez warstwę sieciową WKWebView z originu `capacitor://localhost`. Fix systemowy: pobranie bajtów przez warstwę NATYWNĄ (`CapacitorHttp` z `@capacitor/core`), która nie podlega WKWebView/CORS.

**Files:**
- Modify: `src/components/BodyCompareShareDialog.tsx` (nowy kanał `nativeHttp` PIERWSZY na native; telemetria faz generate/share)
- Modify: `src/components/BodyPhotoCompare.tsx` (przekazanie kodów telemetrii)
- Create: `src/lib/native-photo-fetch.ts`
- Modify: `ios/App/App/Info.plist` (NSPhotoLibraryAddUsageDescription + aktualizacja opisów)
- Test: `src/test/body-compare-share.test.tsx` (nowe przypadki), nowy `src/test/native-photo-fetch.test.ts`

**Kroki:**

- [ ] 1. Test czerwony: na native (mock `Capacitor.isNativePlatform()=true`) kolejność kanałów = `[nativeHttp, getBlob, fetch]`; `nativeHttp` działa też BEZ `photoPath` (używa photoUrl); base64 → Blob poprawnie dekodowany.
- [ ] 2. `src/lib/native-photo-fetch.ts`:
```ts
import { CapacitorHttp } from '@capacitor/core';

/** Pobiera obraz przez natywny stos HTTP (poza siecią WKWebView). */
export const nativeHttpBlob = async (url: string): Promise<Blob> => {
  const res = await CapacitorHttp.get({ url, responseType: 'blob' });
  if (res.status < 200 || res.status >= 300) throw new Error(`native-http-${res.status}`);
  const base64 = typeof res.data === 'string' ? res.data : '';
  if (!base64) throw new Error('native-http-empty');
  const bytes = Uint8Array.from(atob(base64), (c) => c.charCodeAt(0));
  const type = (res.headers?.['Content-Type'] ?? res.headers?.['content-type'] ?? 'image/jpeg') as string;
  return new Blob([bytes], { type });
};
```
- [ ] 3. `BodyCompareShareDialog.tsx` `preparePhotoDataUrl`: na native `steps = [nativeHttpStep, ...(sdkStep ? [sdkStep] : []), fetchStep]`; web bez zmian. Każdy krok nadal w `withStepTimeout` 8 s.
- [ ] 4. Telemetria brakujących faz: `.catch` generowania (`:275`) → `reportClientErrorWithCurrentUid({ code: 'body-compare-export-generate', ... })`; porażka `systemShare`/`toBlob` → `body-compare-export-share`. `setError` zostaje (UX), telemetria dodatkowo.
- [ ] 5. `Info.plist`: `NSPhotoLibraryAddUsageDescription` = "Zapisywanie wygenerowanych obrazow (porownanie sylwetki, podsumowanie treningu) do Twojej biblioteki zdjec."; zaktualizować `NSPhotoLibraryUsageDescription` (dziś mówi tylko o podsumowaniu treningu, a dotyczy też zdjęć sylwetki).
- [ ] 6. Czyścić `error` po udanym share (`markSaved` → `setError(null)`).
- [ ] 7. Testy + commit `fix(x29): eksport before/after przez natywny HTTP (CapacitorHttp) - WKWebView poza sciezka pobrania`.

**deviceTestNotes (OBOWIĄZKOWE przed release):** symulator/urządzenie iOS, konto QA (ma pomiary ze zdjęciem): Pobierz i Udostępnij generują obraz < 15 s; "Zapisz obraz" z share sheet zapisuje do Zdjęć bez crasha (nowe uprawnienie add-only).

---

### WP-F: Ujednolicenie startu aplikacji (splash) — jeden kolor, jedno logo

**Root cause (3 warstwy, 3 kolory):** storyboard `#0E0E0E` → plugin SplashScreen nadpisuje tło na `#0a0a1a` (granat, `capacitor.config.ts`) → po hide czarna szczelina `UIColor.systemBackground` (brak top-level `backgroundColor` w configu) + potencjalne białe mgnienie (`:root --background` jasny, klasa `dark` dopiero po paincie Reacta).

**Files:**
- Modify: `capacitor.config.ts` (SplashScreen.backgroundColor `#0e0e0e`; NOWY top-level `backgroundColor: '#0e0e0e'`)
- Modify: `src/lib/native-setup.ts:17` (StatusBar Android `#0e0e0e`)
- Modify: `index.html` (`<html ... class="dark">`)
- Modify: `src/lib/native-splash.ts` + wywołanie (hide po pierwszym commicie Reacta, nie po `render()`)
- Verify/Delete: `ios/App/App/Assets.xcassets/Splash.imageset/` (31 MB, nieużywany — potwierdzić grep w project.pbxproj/Info.plist przed usunięciem); Android `drawable-*/splash.png` usuwać TYLKO jeśli żadne `styles.xml`/`AndroidManifest` nie referuje (plugin Android często używa `@drawable/splash` — jeśli referuje, ZOSTAWIĆ)
- Verify: `BootLogo.imageset` = aktualne logo 3D (porównać z `src/assets/app-icon.png`; jeśli stare, przegenerować `sips -z` 192/384/576 z app-icon)
- Test: istniejące testy + `check:dist-smoke`

**Kroki:**

- [ ] 1. capacitor.config: oba kolory `#0e0e0e` (komentarz: jeden kolor = storyboard = theme-color = --background dark).
- [ ] 2. `index.html`: `class="dark"` na `<html>` (next-themes forcedTheme="dark" i tak ją utrzymuje; to usuwa okno jednej klatki z jasnym `--background`).
- [ ] 3. Hide splasha: przenieść `hideNativeSplashWhenReady()` z `main.tsx` (po `render()`) do `useEffect(() => hideNativeSplashWhenReady(), [])` w `App.tsx` — gwarancja po pierwszym commicie; podwójny rAF w środku zostaje.
- [ ] 4. Weryfikacja BootLogo (md5/wymiary vs app-icon; wizualnie przez Read na PNG). Jeśli stare — przegenerować.
- [ ] 5. Usunięcie martwych assetów WYŁĄCZNIE po negatywnym grep referencji (osobny commit, łatwy rollback): `git rm -r ios/App/App/Assets.xcassets/Splash.imageset` (+ android tylko jeśli bez referencji).
- [ ] 6. `cap sync` wykona się przy release. Commit `fix(x29): jednolity start apki - #0e0e0e na wszystkich warstwach, hide splasha po pierwszym commicie Reacta`.

**deviceTestNotes:** zimny start na iPhone: logo na `#0e0e0e` bez zmiany odcienia tła i bez czarnej/białej klatki między splashem a BootScreen.

---

### WP-G: Maskowanie adresu email w Profilu

**Files:**
- Create: `src/lib/mask-email.ts`
- Modify: `src/pages/Profile.tsx:424` (maska + toggle oko), `src/components/AppNavigation.tsx:184-186` (desktop sidebar, ta sama maska)
- Modify: i18n (aria-label toggle)
- Test: nowy `src/test/mask-email.test.ts` + przypadek w testach Profile

**Kroki:**

- [ ] 1. Test czerwony dla `maskEmail`: `grzegorz@gmail.com` → `g•••••••@g••••.com` (pierwszy znak części lokalnej i domeny widoczny, kropki zachowane w TLD); edge: `a@b.co`, brak `@` → zwrot bez zmian.
- [ ] 2. Implementacja `maskEmail(email: string): string`.
- [ ] 3. Profile: stan `emailVisible` inicjowany z localStorage `ss-email-visible` (default `false` = zamaskowany); przycisk ikony Eye/EyeOff (`aria-label`: `profile.emailToggle` PL "Pokaż lub ukryj adres email" / EN "Show or hide email address"); zapis do localStorage przy zmianie. Sidebar desktop: sama maska bez toggle (mało miejsca), pełny email po hover `title`? NIE — bez title (to obejście maski); sidebar używa maski gdy `ss-email-visible` false.
- [ ] 4. Testy + commit `feat(x29): maskowanie adresu email w Profilu z przelacznikiem widocznosci`.

---

### WP-H: Automatyczny kolor akcentu z avatara (Google/Apple)

**Zasada (5):** działa TYLKO gdy user nie ma żadnego wyboru: brak `preferences.accentColor` w profilu ORAZ brak wpisu w localStorage `ss-accent-color`. Nigdy nie nadpisuje wyboru. Cichy fail (brak avatara, CORS, szary avatar) = zostaje lime.

**Files:**
- Create: `src/lib/avatar-accent.ts`
- Modify: `src/components/PreferenceSync.tsx` (trigger po załadowaniu profilu)
- Modify: `src/components/PlanWizard.tsx` (onboarding: preselekcja wyliczonego koloru w swatchach, jeśli wyliczony)
- Test: nowy `src/test/avatar-accent.test.ts`

**Kroki:**

- [ ] 1. Testy czerwone (na mockach ImageData): (a) avatar dominująco niebieski → `sky`; (b) fioletowy → najbliższy z {violet/lavender/indigo}; (c) szary/o niskiej saturacji → `null`; (d) `deriveAccentForUser` nie robi nic gdy `preferences.accentColor` ustawione lub localStorage ma wpis.
- [ ] 2. `src/lib/avatar-accent.ts`:
```ts
import { ACCENTS, type AccentTheme } from '@/lib/accent-theme';
import { Capacitor } from '@capacitor/core';

const SELECTABLE = ACCENTS.filter((a) => !['slate', 'gray'].includes(a.id));
const MIN_SATURATION = 0.18;

/** Dominujacy kolor: downsample 24x24, srednia RGB wazona saturacja, pominiete piksele niemal szare/przezroczyste. */
export const dominantColorFromBitmap = (data: Uint8ClampedArray): { r: number; g: number; b: number; sat: number } | null => { /* wagi = sat^2 */ };

/** Najblizszy akcent w przestrzeni hue (circular) + sat/lum. */
export const nearestAccent = (rgb: { r: number; g: number; b: number }): AccentTheme => { /* dystans hue*2 + sat + lum */ };

export const deriveAccentFromAvatar = async (photoURL: string): Promise<string | null> => {
  // native: nativeHttpBlob (WP-E), web: fetch; blob -> createImageBitmap -> canvas 24x24 -> getImageData
  // sat < MIN_SATURATION -> null
};
```
- [ ] 3. `PreferenceSync.tsx` (efekt 1, po zastosowaniu wartości z chmury): jeśli `!prefs?.accentColor && !readStoredAccentId(raw)` (dodać do accent-theme `hasStoredAccent()` czytający surowy localStorage, bo `readStoredAccentId` zwraca default) i `profile.photoURL` → `deriveAccentFromAvatar` → `applyAccent(id); storeAccentId(id); updateDoc('preferences.accentColor')`. Całość w `.catch(() => {})`.
- [ ] 4. `PlanWizard`: init akcentu — jeśli brak zapisanego wyboru a avatar dał kolor, preselekcja tego swatcha (user w onboardingu widzi "swój" kolor już wybrany i może zmienić).
- [ ] 5. Testy + commit `feat(x29): kolor akcentu dobierany automatycznie z avatara przy pierwszym logowaniu`.

---

### WP-I: Trener z imieniem + popup zapisu po wysyłce + hardening maili do trenera

**Files:**
- Modify: `src/lib/user-profile.ts` (typ `preferences.trainerName?: string`)
- Modify: `src/components/EmailWorkoutDialog.tsx` (popup po wysyłce zamiast bezwarunkowego zapisu; przekazanie `trainerName` do wysyłki)
- Modify: `src/lib/email-workout.ts` (parametr `trainerName`)
- Modify: `src/components/history/HistoryExportSheet.tsx:154` (desc = imię, fallback email)
- Modify: `src/pages/Profile.tsx` (sekcja "Trener": podgląd imię+email, Zmień imię / Usuń adres)
- Modify: `functions/src/email-workout.ts` (nagłówek "Czesc {imie}," gdy podane; jednostki wg `users/{uid}.preferences.unit`; `getWorkout` przez kwerendę z `where("userId","==",uid)`)
- Modify: `functions/src/index.ts:1279-1290` (adapter `getWorkout(workoutId, uid)`)
- Test: functions testy email-workout (jednostki lbs, imię, ownership), web testy dialogu

**Kroki:**

- [ ] 1. Testy czerwone functions: (a) `emailWorkoutSummary` z `trainerName: "Marek"` → HTML zawiera "Czesc Marek,"; (b) profil z `preferences.unit='lbs'` → ciężary w lbs (konwersja `kg*2.20462`, zaokrąglenie do 0.5) i tonaż w lb; (c) `getWorkout` cudzego id → `not-found` (kwerenda filtruje po uid, nie ma już ścieżki do cudzego dokumentu).
- [ ] 2. Backend: `getWorkout` = `db.collection(...).where("__name__","==",id).where("userId","==",uid)` LUB doc().get() + natychmiastowy check w adapterze (decyzja: check w ADAPTERZE, żeby logika wyżej nie mogła go ominąć); walidacja `trainerName` (string, trim, ≤ 80, opcjonalny); jednostki: odczyt `users/{uid}` już jest w digest — dodać analogiczny odczyt w email-workout.
- [ ] 3. Frontend: `EmailWorkoutDialog` — po `ok` wysyłki: jeśli `to !== profile.preferences.trainerEmail` → otwórz ConfirmDialog (zawsze zamontowany) "Zapisac {email} jako trenera?" z opcjonalnym Input imię (`email.saveTrainer.title/desc/nameLabel/save/skip` w PL+EN; PL np. "Zapisz kontakt", "Nastepnym razem podpowiemy ten adres.", "Imie trenera lub znajomego (opcjonalnie)"); Zapisz → `updateDoc({'preferences.trainerEmail': to, 'preferences.trainerName': name || deleteField()})`. Usunięty bezwarunkowy zapis z `:58-59`. Gdy adres znany: bez popupu, ale wyślij z zapisanym `trainerName`.
- [ ] 4. Profil: sekcja Trener w bloku ustawień (widoczna tylko gdy trainerEmail ustawiony): imię + zamaskowany email (maskEmail z WP-G), akcje Zmień imię (inline input) i Usuń (czyści oba pola `deleteField()`).
- [ ] 5. Testy + commit `feat(x29): trener z imieniem - popup zapisu po wysylce, personalizacja maila, jednostki wg preferencji, ownership w adapterze`.

---

### WP-J: Maile raportowe bez przycisków "Otwórz aplikację"

**Files:**
- Modify: `functions/src/weekly-digest-html.ts:186-196` (usunięcie bloku CTA: przycisk `strengthsave://open` + link "albo otwórz w przeglądarce")
- Modify: `functions/src/email-templates.ts:9,85` (welcome NIE jest raportowy — przycisk zostaje, ale href = `https://app.strengthsave.app/` zamiast martwego deep linku; stała `APP_DEEP_LINK` → `APP_WEB_URL`)
- Test: functions testy szablonów (digest bez `<a`, welcome z https)

**Kroki:**

- [ ] 1. Testy czerwone: `buildWeeklyDigest(...)` NIE zawiera `strengthsave://` ani przycisku CTA; `welcomeEmailHtml` zawiera `https://app.strengthsave.app/`.
- [ ] 2. Implementacja + przegląd `grep -rn "strengthsave://" functions/src` = zero pozostałości.
- [ ] 3. Commit `fix(x29): digest bez CTA (mail raportowy), welcome z linkiem https zamiast martwego deep linku`.

---

### WP-K: Popup pomiarów po onboardingu — weryfikacja i domknięcie ścieżek

**Stan:** funkcja ISTNIEJE (`Dashboard.tsx:87-128`, ConfirmDialog po `/?welcome=1`, pomijany gdy user ma już pomiar). Ścieżka paywall (iOS) też kończy na `/?welcome=1` przy sukcesie. Zadanie = test sekwencji + domknięcie dziur.

**Files:**
- Modify: `src/pages/Paywall.tsx` (audyt wyjść: każde wyjście świeżego usera z paywalla na dashboard musi nieść `welcome=1`)
- Test: nowy przypadek w `src/test` (dashboard-welcome/measure prompt), e2e onboarding

**Kroki:**

- [ ] 1. Test sekwencji: onboarding complete → `/?welcome=1` → dialog `dash.measurePrompt` widoczny gdy zero pomiarów; NIEwidoczny gdy pomiar istnieje; potwierdzenie → nawigacja `/measurements`.
- [ ] 2. Audyt wyjść Paywall (`navigate(-1)` w `:299`, `successRoute`): jeśli istnieje ścieżka świeżego usera lądująca na `/` bez `welcome=1` — poprawić na `'/?welcome=1'` (kryterium: `wasHard.current`).
- [ ] 3. Przegląd copy dialogu (`dash.measurePrompt.*` PL/EN) — ma jasno pytać o dodanie wymiarów ciała po ustawieniu planu.
- [ ] 4. Commit `fix(x29): popup pomiarow po onboardingu - domkniete sciezki paywall + test sekwencji`.

---

### WP-L: Wspólna infrastruktura eksportu/share + naprawa eksportów plikowych na iOS

**Stan:** `systemShare`/`handleDownload`/`handleShare` zduplikowane 1:1 w `ShareWorkoutDialog.tsx:132-178`, `CycleShareCard.tsx:177-217`, `BodyCompareShareDialog.tsx:284-324`. Eksporty CSV/JSON (`workout-csv-download.ts:44-47`, `DataManagement.tsx:207-210`, `SyncCenterCard.tsx:356-359`) używają tylko `<a download>` = na iOS klik nic nie robi.

**Files:**
- Create: `src/lib/share-export.ts` (`shareOrDownloadFile(file: File, filename: string): Promise<'shared' | 'downloaded' | 'failed'>` — native: `navigator.canShare({files})` → `navigator.share`, AbortError = nie-błąd; web: `<a download>`)
- Modify: 3 dialogi obrazków (podmiana na helper, zachowanie identyczne — testy istniejące bez zmian asercji)
- Modify: `src/lib/workout-csv-download.ts`, `src/components/DataManagement.tsx`, `src/components/SyncCenterCard.tsx` (CSV/JSON przez helper; na native idzie w share sheet)
- Test: nowy `src/test/share-export.test.ts`; istniejące testy share przechodzą bez modyfikacji logiki

**Kroki:**

- [ ] 1. Test czerwony helpera (native → share, web → download, AbortError → 'downloaded'/silent).
- [ ] 2. Implementacja + refactor 6 miejsc (surgical: tylko podmiana ciał, zero zmian UI).
- [ ] 3. Commit `refactor(x29): wspolny shareOrDownloadFile + eksport CSV/JSON dziala na iOS przez share sheet`.

**Backlog sharingu (NIE w tej fali, zapis do PLAN.md):** karta PR po treningu, przezroczysty sticker PNG (nakładka na własne zdjęcie), share do IG Stories (wymaga Facebook App ID + mini-plugin Swift/Kotlin), karta recap miesiąca, deep-link planu read-only. Źródło: research 2026-08-24.

---

### Faza 4: Audyt edge case'ów + screenshoty

- [ ] 1. Pełne bramki: `npm run test`, `typecheck`, `lint`, `build`, `check:dist-smoke`, e2e (przed e2e: `pkill -f vite` + czyszczenie `node_modules/.vite` — zasada 9).
- [ ] 2. Skill `product-audit`: przejście kluczowych flow w trybie E2E ze screenshotami (Dashboard po przełożeniu, Plan z badge, sesja z navem, Profil z maską, eksport porównania, powiadomienia). Dowolny błąd działania = fix przed release.
- [ ] 3. Audyt klikalności: każda trasa z route sweep + przegląd nowych przycisków (toggle maski, popup trenera, sheet reschedule fallback).
- [ ] 4. Konto QA na wdrożonym webie: wszystkie zakładki, zero ekranów błędu (zasada 11).

### Faza 5: Release

- [ ] 1. Commit + push main.
- [ ] 2. Functions deploy (zmiany: weekly-digest, weekly-digest-html, email-workout, email-templates, index).
- [ ] 3. Web: `npm run deploy` (gh-pages) + weryfikacja nowego hasha bundle na live.
- [ ] 4. iOS: bump `CURRENT_PROJECT_VERSION` → 117 (6 wystąpień), `scripts/release-ios.sh "<co testować>"`, potem OBOWIĄZKOWO `uv run --with "pyjwt[crypto]" --with requests scripts/testflight_external.py 117 --whats-new "..."`.
- [ ] 5. Android: versionCode → 32, build AAB, SHA do DECYZJE.md (upload do Play = krok właściciela).
- [ ] 6. Watch/Garmin: bez zmian w tej fali (nie dotykamy mirrora; `buildTrainingSchedule` to warstwa UI, kanoniczny resolver niezmieniony — parity fixture nietknięty).
- [ ] 7. DECYZJE.md wpis (root cause per bug, weryfikacja), aktualizacja PLAN.md (backlog sharingu, "onboarding kohortowy" do doprecyzowania z userem), memory.
- [ ] 8. Po wydaniu: przegląd `client_errors` w 24 h (szczególnie `body-compare-export-*`).
