# Plan obszaru: strava

## Obszar
Strava: widoczność aktywności na dashboardzie (T5), mapowanie typów (T6), redirect po połączeniu + ręczny sync + rate-limit (T7), audyt poprawności danych (T8)

## Stan istniejący
PIPELINE: functions/src/index.ts ma stravaAuthUrl, stravaCallback (wymiana kodu + INITIAL SYNC już automatyczny, syncUserActivities 365 dni), stravaSync (callable, BEZ rate-limitu, spam możliwy), stravaScheduledSync (codziennie 10:00 Warsaw), stravaDisconnect. Mapowanie API→doc w functions/src/strava-activity.ts (pure, testowane w strava-activity.test.ts): zapisuje type ORAZ sportType, jednostki kanoniczne (metry, sekundy, m/s). Klient: src/hooks/useStrava.ts (listener strava_activities z oknem sinceDate Z214, syncActivities/connectStrava/disconnectStrava), src/hooks/useActivities.ts (merge Strava + manual_activities).

T5: Dashboard (src/pages/Dashboard.tsx) destrukturyzuje unifiedActivities/stravaActivities/stravaConnection z useActivities (linie 114-121, okno od poniedziałku), ale NIGDZIE ICH NIE RENDERUJE. Sekcja km Strava została celowo zdjęta (test dashboard-order.test.tsx asertuje queryByTestId('dash-strava-km')===null, komentarz "domy: Postępy i Plan"). Aktywności widać tylko w: DayPlan (dzisiejsze), TrainingPlan (HybridWeekStrip + kropki kalendarza), Analytics→StravaTab. Gotowy przetestowany helper currentWeekCardio istnieje w src/lib/activity-window.ts (test: activity-window.test.ts).

T6: ikony typów w src/lib/activity-icons.ts (Run/Ride/Swim/Walk/Hike/WeightTraining/Yoga/Workout/Treadmill/IndoorRide/JumpRope/HIIT/Other, fallback Medal) — klucze po type, NIE po sportType (Detail używa sportType||type, karta tylko type). StravaActivityCard nie pokazuje etykiety typu (tylko nazwa ze Stravy + ikona). GŁÓWNY mechanizm "spacer jako bieg": isPaceActivity (strava-utils.ts:126) = Run||Walk||Hike i karmi fastestPace/best5k/best10k (detectCardioPRs), avgPace (computeSummaryStats, computeMonthlySummaries, computePaceTrendData) ORAZ RacePredictor (race-predictor.ts:42 findBestEffort) — spacery generują "rekordy biegowe" i predykcje wyścigów. Wzorzec poprawnego filtra biegów istnieje: weekly-summary.ts:94 i weekly-digest.ts:83 filtrują type==='Run'.

T7: StravaCallback.tsx po sukcesie robi navigate('/settings') po 3 s (klucz 'strava.callback.redirect' = "Przekierowuję do ustawień..."); auto-sync przy połączeniu JUŻ DZIAŁA (robi go backend w stravaCallback). Ręczny sync w 2 miejscach: StravaTab.tsx:112 (Analytics→Strava, do usunięcia wg feedbacku) i Settings.tsx (handleSync, zostaje). Rate-limitu brak; generyczny helper checkAndConsumeRateLimit (bucket per minuta + TTL) istnieje w functions/src/admin-api.ts:301 i jest reużywalny.

T8 znaleziska: (1) avgPace = średnia ARYTMETYCZNA pace'ów per aktywność zamiast ważonej dystansem — spacer w miksie fałszuje "średnie tempo" w 3 funkcjach strava-utils. (2) calories i description są mapowane, ale endpoint listy Stravy (SummaryActivity /athlete/activities) NIE ZWRACA tych pól (calories/description tylko w DetailedActivity; SummaryActivity ma kilojoules dla jazd) — dokumenty Strava mają je zawsze null, CaloriesChart chowa się (return null przy 0), SummaryStats pokazuje '—'. (3) totalDistance/computeWeeklyKm sumują WSZYSTKIE typy (rower+spacer+bieg) w jedno "km" — wartości zawyżone względem oczekiwań "biegowych". (4) dystans/czas/elewacja per aktywność mapowane poprawnie (movingTime, total_elevation_gain, || null zeruje tylko legalne 0 — nieszkodliwe). (5) formatPace na karcie: min/km dla Run/Walk/Hike, km/h dla reszty — poprawne, ale patrzy tylko na type.

## Zadania

### T5: Karta cardio bieżącego tygodnia na Dashboardzie, widoczna też przed startem planu (effort: M)
**Pliki:** src/components/WeekCardioCard.tsx, src/pages/Dashboard.tsx, src/i18n/locales/pl.ts, src/i18n/locales/en.ts, src/test/dashboard-week-cardio.test.tsx

**Podejście:**
1) Nowy mały komponent WeekCardioCard: props {activities: UnifiedActivity[], maxHR?, onEditManual}; filtruje przez ISTNIEJĄCE currentWeekCardio(unifiedActivities, stravaConnection.connected, weekStartStr, weekEndStr) z src/lib/activity-window.ts (weekStart = activityWindowStart już policzony w Dashboardzie, weekEnd = +6 dni); renderuje nagłówek (nowy klucz 'dash.weekCardio.title') + listę StravaActivityCard (onEdit dla source==='manual' otwiera istniejący cardioDialog); przy 0 aktywności zwraca null. data-testid='dash-week-cardio'. 2) W Dashboard.tsx wpiąć POZA jakimkolwiek warunkiem planStarted, zaraz po <WeekCard/> (linia ~849) — dzięki temu biegi widać także gdy cykl startuje w przyszłości. Wykorzystuje już destrukturyzowane, dziś martwe unifiedActivities/stravaConnection. 3) Klucz i18n do OBU locali. NIE przywracać testid 'dash-strava-km' (tamta sekcja km została świadomie usunięta i test tego pilnuje) — to inna, nowa karta z aktywnościami.

**Testy:**
Nowy src/test/dashboard-week-cardio.test.tsx na wzór dashboard-order.test.tsx (mock useActivities/useTrainingPlan): a) planStartDate w przyszłości + bieg z tego tygodnia → karta widoczna z aktywnością (scenariusz z feedbacku 1:1); b) plan wystartowany → karta nadal widoczna (niezmiennik reguły 5); c) Strava niepołączona + wpis manualny → manual widoczny, Strava nie; d) 0 aktywności → brak karty. Uruchomić dashboard-order.test.tsx bez modyfikacji — kolejność hero→week-card→quick actions ma przejść (nowa karta wchodzi między week-card a quick actions, asercje compareDocumentPosition to znoszą) i dash-strava-km ma zostać null.

**Ryzyka:**
Reguła 5: niczego nie zabieramy — tylko dokładamy render nieużywanych danych; nie dotykać WeekCard ani buildWeekCardModel. Okno listenera Z214 zaczyna się od poniedziałku bieżącego tygodnia, więc dane są w listenerze — nie poszerzać okna (koszty Firestore). Uważać na dashboard-order.test.tsx: jeśli asercja 'max jeden insight' zacznie łapać nową kartę, dopisać wyjątek świadomie w teście z komentarzem, nie zmieniać logiki. Karta odfiltrowuje WeightTraining/Crossfit (robi to currentWeekCardio) — nie duplikuje treningów siłowych.

### T6: Poprawne mapowanie i prezentacja typów aktywności (Walk nie jest biegiem) (effort: M)
**Pliki:** src/lib/activity-icons.ts, src/components/StravaActivityCard.tsx, src/lib/strava-utils.ts, src/lib/race-predictor.ts, functions/src/strava-activity.ts, src/i18n/locales/pl.ts, src/i18n/locales/en.ts, src/test/strava-utils.test.ts, src/test/race-predictor.test.ts, functions/src/strava-activity.test.ts

**Podejście:**
KROK 0 (ground truth, lekcja z MEMORY): przez konsolę Firestore/admin odczytać READ-ONLY kilka dokumentów strava_activities userki ze spacerami i potwierdzić co siedzi w type/sportType, zanim padnie linijka kodu. Potem: 1) activity-icons.ts: eksport displayActivityType(activity) = sportType || type + normalizacja wariantów sport_type na bazowe klucze (TrailRun/VirtualRun→Run, VirtualRide/GravelRide/MountainBikeRide/EBikeRide→Ride, Snowshoe/RockClimbing→Hike itd.); getActivityIcon przyjmuje wynik tej funkcji; wszystkie callsites (StravaActivityCard, StravaActivityDetail, AddCardioDialog) przechodzą na displayActivityType. 2) StravaActivityCard: obok badge'a 'Strava' dodać etykietę typu t('cardio.type.X') z fallbackiem na surowy sportType, żeby spacer był PODPISANY jako spacer; formatPace decyduje min/km vs km/h po znormalizowanym typie. 3) strava-utils.ts: nowy helper isRunActivity (type==='Run' || sportType?.includes('Run')) wzorem weekly-summary.ts:94; detectCardioPRs (fastestPace/best5k/best10k) i race-predictor.findBestEffort przechodzą z isPaceActivity na isRunActivity — spacery/wędrówki wypadają z rekordów biegowych i predykcji wyścigu; isPaceActivity ZOSTAJE do formatowania tempa. 4) Backend strava-activity.ts: mapStravaActivityToDoc mapuje type z fallbackiem 'activity.type || activity.sport_type' (odporność na deprecację pola type w API Stravy). 5) Nowe klucze cardio.type.* dla typów Strava spoza listy manualnej (co najmniej TrailRun, VirtualRun, VirtualRide, Rowing, Elliptical, NordicSki) do OBU locali.

**Testy:**
strava-utils.test.ts: spacer 5 km NIE trafia do best5k/fastestPace, bieg trafia; isRunActivity łapie sportType 'TrailRun'. race-predictor.test.ts: same spacery → zero predykcji; bieg 5 km → predykcje są (istniejące asercje mają dalej przechodzić dla biegów — niezmiennik). Nowy test ikon: Walk→PersonStanding, sportType TrailRun→ikona Run, nieznany typ→Medal. functions strava-activity.test.ts: brak type + sport_type='Walk' → doc.type='Walk'. strava-i18n.test.tsx wzorem dla etykiet PL/EN. Typecheck pilnuje kompletu kluczy w obu locale.

**Ryzyka:**
Zmiana detectCardioPRs/RacePredictor ZMIENI wyświetlane rekordy u ludzi ze spacerami — to zamierzona poprawka poprawności, wpis do DECYZJE.md z uzasadnieniem. NIE ruszać: weeklyStravaKm/computeWeeklyKm (km cardio wszystkich typów to zamierzone), filtrów WeightTraining/Crossfit, weekly-digest (już filtruje Run poprawnie), HR zones/TrainingLoad (typ nieistotny). Nie zgadywać zawartości pól — najpierw krok 0. Klucz t() z dynamicznym stringiem wymaga rzutowania jak w istniejącym kodzie karty (wzorzec linia 84).

### T7: Po połączeniu redirect na dashboard; ręczny sync tylko w Ustawieniach + rate-limit serwerowy (effort: M)
**Pliki:** src/pages/StravaCallback.tsx, src/components/strava/StravaTab.tsx, functions/src/strava-activity.ts, functions/src/index.ts, src/hooks/useStrava.ts, src/i18n/locales/pl.ts, src/i18n/locales/en.ts, functions/src/strava-activity.test.ts, src/test/strava-tab.test.tsx

**Podejście:**
Auto-sync po połączeniu JUŻ ISTNIEJE (stravaCallback robi initial sync w backendzie) — nie dublować. 1) StravaCallback.tsx: setTimeout(() => navigate('/'), 1500) zamiast '/settings' po 3000; treść klucza 'strava.callback.redirect' na 'Przekierowuję do panelu...' (pl+en); przycisk błędu backToSettings zostaje. 2) StravaTab.tsx: usunąć przycisk syncu (linie 112-115) + syncActivities/isSyncing z destrukturyzacji + osierocone importy (RefreshCw, Loader2); przycisk Rozłącz i cała reszta karty statusu zostają. Settings.tsx NIE ruszać (to jest docelowe 'miejsce zaawansowane', sync tam już mieszka). 3) Rate-limit: w functions/src/strava-activity.ts czysta funkcja manualSyncRetryAfterSeconds(lastSyncIso, nowMs, minIntervalMs=300000) → number|null (pure, testowalna bez emulatora, wzorzec repo); w index.ts stravaSync po odczycie userData wywołać ją na userData.stravaLastSync i przy odmowie rzucić HttpsError('resource-exhausted', `Retry in ${s}s`) — dotyczy też fullSync. stravaScheduledSync i stravaCallback wywołują syncUserActivities BEZPOŚREDNIO, więc guard ich nie dotyka (niezmiennik). 4) useStrava.syncActivities: rozpoznać (err as {code?:string}).code zawierający 'resource-exhausted' → komunikat z nowego klucza 'strava.err.rateLimited' ('Synchronizacja była przed chwilą. Spróbuj za kilka minut.'); Settings.handleSync pokaże go istniejącym toastem błędu.

**Testy:**
functions/src/strava-activity.test.ts: manualSyncRetryAfterSeconds — lastSync null → null (pierwszy sync przechodzi), 60 s temu → ~240 s odmowy, 6 min temu → null. Nowy src/test/strava-tab.test.tsx (RTL, mock useStrava): połączony stan NIE renderuje przycisku synchronizacji, renderuje Rozłącz; osobna asercja że Settings nadal ma przycisk syncu (albo test snapshotowy sekcji) — niezmiennik reguły 5. Ręczny scenariusz: pełny OAuth na urządzeniu → po 'Połączono' ląduje na dashboardzie i (po T5) widzi tygodniowe cardio bez klikania.

**Ryzyka:**
Zasada 6 (stan błędu ma wyjście): odmowa rate-limitu mija z czasem i komunikat mówi ile czekać — OK; NIE blokować pierwszego syncu (lastSync null przechodzi). stravaLastSync jest ustawiane też przez scheduled sync (10:00) i callback — manual sync tuż po nich odbije się na 5 min; akceptowalne i pożądane (tokeny). Zmiana wymaga DEPLOY FUNCTIONS (stravaSync) — bez niego klient działa po staremu (guard jest tylko serwerowy, brak twardej zależności wersji, degradacja łagodna). Nie używać checkAndConsumeRateLimit z admin-api (nowe dokumenty/kolekcja per minuta — niepotrzebna infra, prostszy guard po stravaLastSync wystarcza).

### T8: Audyt danych Strava: naprawa średniego tempa, decyzja ws. kalorii, dokumentacja poprawności reszty (effort: S)
**Pliki:** src/lib/strava-utils.ts, src/test/strava-utils.test.ts, src/components/strava/StravaSummaryStats.tsx, DECYZJE.md

**Podejście:**
Audyt wykonany (wyniki w existing_state): dystans/czas/elewacja per aktywność są mapowane POPRAWNIE (metry, movingTime s, m), błędne są agregaty. 1) FIX avgPace (realny bug): w computeSummaryStats, computeMonthlySummaries i computePaceTrendData zamienić średnią arytmetyczną pace'ów per aktywność na ważoną: sum(movingTime)/sum(distance/1000) po aktywnościach spełniających isPaceActivity — po T6 spacery i tak wypadają z rekordów, ale w avgPace zostają (tempo marszu to legalne tempo; jeśli właściciel chce avgPace tylko biegowe → jedno słowo, użyć isRunActivity). 2) Kalorie: dokumenty Strava mają calories ZAWSZE null (endpoint listy ich nie zwraca) — UI już się degraduje ('—', chowany wykres), więc NIC nie jest kłamane; przedstawić właścicielowi decyzję: (a) zostawić jak jest + dopisać ograniczenie do DOCUMENTATION.md (rekomendacja, zero kodu), (b) mapować kilojoules→calories dla jazd (przybliżenie), (c) dociągać DetailedActivity per aktywność (ODRADZAM: 1 request/aktywność, pali limit API — sprzeczne z T7). NIE wdrażać b/c bez jawnej decyzji. 3) totalDistance/km tygodniowe sumują wszystkie typy — zamierzone 'km cardio'; dla czytelności ewentualnie doprecyzować etykietę 'strava.totalDistance' ('Dystans (wszystkie aktywności)') w obu locali — kosmetyka, opcjonalna. 4) Wpis do DECYZJE.md: co audyt potwierdził jako poprawne (dystans, czas, elewacja, data z start_date_local), co naprawiono (avgPace, rekordy po T6), co jest ograniczeniem API (kalorie, opis).

**Testy:**
strava-utils.test.ts: avgPace ważone — 5 km w 25 min + 10 km w 40 min → 4:20/km (średnia arytmetyczna dałaby 4:30 — asercja odróżnia implementacje); pace trend tygodnia z jedną długą i jedną krótką aktywnością; monthly summary avgPace spójne z summary stats na tym samym fixture. Istniejące testy strava-utils/strava-i18n muszą przejść po korekcie oczekiwanych wartości TYLKO tam, gdzie testowały starą (błędną) średnią — każdą zmianę oczekiwania opisać w teście komentarzem.

**Ryzyka:**
Wartości avgPace na ekranach userów SIĘ ZMIENIĄ (to korekta błędu) — wpis do DECYZJE.md. Reguła 5: nie zmieniać totalDistance/totalTime/elevation ani weeklyStravaKm — audyt potwierdził ich poprawność, zmiana = regres. Kalorie: bez decyzji właściciela zero zmian w mapowaniu (opcja b to przybliżenie fizjologiczne, łatwo o zarzut 'fałszywe dane'). Nie dodawać fetchy DetailedActivity przy okazji (limit API Stravy 100 req/15 min — T7 właśnie go chroni).

## Notatki
Kolejność wdrożenia (surgical, osobne commity per zadanie): T6 krok 0 (ground truth z Firestore) → T8+T6 (pure lib) → T5 (UI Dashboard) → T7 (functions + UI). Wdrożenie wymaga: npm run test/typecheck/lint/build, deploy functions (stravaSync z rate-limitem, mapper), npm run deploy (web), bump iOS CURRENT_PROJECT_VERSION na 113 i versionCode 28 (stan wg pamięci projektu z 2026-08-20). Klucze i18n ZAWSZE do pl.ts i en.ts (typecheck pilnuje). Scenariusz ręczny end-to-end po całości: świeże konto → połącz Stravę → automatyczny powrót na dashboard → biegi i spacery z tego tygodnia widoczne z poprawnymi typami mimo startu planu w przyszłości → spam przycisku sync w Ustawieniach odbija się komunikatem rate-limitu. Ważny kontekst produktowy: sekcja km Strava była wcześniej ŚWIADOMIE zdjęta z Dashboardu (test dashboard-order.test.tsx tego pilnuje) — T5 nie przywraca tamtej statystyki, tylko dodaje listę aktywności tygodnia; jeśli właściciel chce więcej (np. pełną historię na dashboardzie), to osobna decyzja, bo zwiększa okno listenera Firestore (Z214 = koszty).
