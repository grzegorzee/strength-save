# Plan obszaru: onboarding

## Obszar
Onboarding: krok "Zatwierdź protokół" (T1), chipy daty startu z dniem tygodnia (T2), popup po onboardingu o pomiary ciała (T4)

## Stan istniejący
Żadne z zadań T1/T2/T4 NIE jest jeszcze zrobione. Istniejąca infrastruktura do wykorzystania:

1. KROK PROTOKOŁU: src/components/PlanWizard.tsx, step 4 (linie 352-419). Nagłówek pytania o dni: klucz 'ob.protocol.daysQ' = 'Ile dni w tygodniu?' (pl.ts:2212, en.ts:2190). Chipy daty startu: linie 390-402, pętla 7 dni, każdy chip = 2 linie (góra: 'Dziś' z klucza 'ob.today' albo miesiąc short, dół: numer dnia). Pole "Wybierz konkretną datę": linie 404-408 (natywny input type=date). Pod spodem linia podglądu 'ob.protocol.startPreview' (linie 409-414) formatująca selected/start przez toLocaleDateString(dateLocale(lang), { day, month }) BEZ dnia tygodnia. PlanWizard jest współdzielony przez Onboarding.tsx i NewPlan.tsx (replan), więc zmiany kroku 4 obejmą oba konteksty (pożądana spójność).

2. HELPERY: dateLocale(lang) w src/i18n/index.ts (pl-PL/en-US), toLocaleDateString z { weekday: 'short' } daje 'śr.'/'Wed' bez żadnych nowych kluczy i18n. localizeWeekdayShort w src/lib/plan-i18n.ts (używany do kółek wyboru dni tygodnia).

3. ZAKOŃCZENIE ONBOARDINGU: Onboarding.tsx linia 113 nawiguje na '/?welcome=1' (web) LUB '/paywall' (iOS bez PRO); Paywall.tsx (linie 49, 94) po zakupie/trialu też nawiguje na '/?welcome=1'. Dashboard.tsx (linie 64-84) czyta searchParams 'welcome' w inicjalizatorze stanu (showConfetti) ZANIM effect skasuje param z URL. To jedyny wspólny punkt obu ścieżek = właściwe miejsce na popup T4. Dashboard ma już getLatestMeasurement z useFirebaseWorkouts(uid, { measurements: 'latest' }) (linia 96-100).

4. POMIARY: gotowy ekran src/pages/Measurements.tsx pod trasą /measurements (AuthenticatedApp.tsx:227) z MeasurementsForm (src/components/MeasurementsForm.tsx): waga + 9 pomiarów (biceps L/P, klatka, talia, biodra, uda, łydki), walidacja, przecinek dziesiętny, jednostki. Formularz odblokowany bo zgoda zdrowotna jest obowiązkowa w onboardingu. NIC nie trzeba budować od nowa, wystarczy nawigacja.

5. DIALOG: src/components/ConfirmDialog.tsx (AlertDialog, wspólny wzorzec), ale ma sztywny cancel label t('common.cancel') — wymaga opcjonalnego propa cancelLabel.

6. TESTY-WZORCE: src/test/plan-wizard-welcome.test.tsx (harness PlanWizard: LanguageProvider+UnitProvider, mock PlanBuilder, localStorage app-language=pl), src/test/onboarding-accent.test.tsx (harness pełnego Onboarding z mockami firestore/hooks), src/test/dashboard-completion-highlight.test.tsx (harness Dashboardu: MemoryRouter + navigateSpy przez useNavigate mock + pełne mocki firestore/hooks). E2E: e2e/full-app.spec.ts (11t) i e2e/onboarding-accent.spec.ts przechodzą wizard, ale NIE asertują tekstu 'Ile dni w tygodniu?' ani nie docierają do /?welcome=1 — zmiany nie łamią e2e.

## Zadania

### T1: Krok 'Zatwierdź protokół': nagłówek 'Ile dni treningowych w tygodniu?' + notatka o późniejszej zmianie planu (effort: S)
**Pliki:** src/i18n/locales/pl.ts, src/i18n/locales/en.ts, src/components/PlanWizard.tsx, src/test/plan-wizard-protocol.test.tsx

**Podejście:**
1) pl.ts:2212 zmień 'ob.protocol.daysQ' na 'Ile dni treningowych w tygodniu?'; en.ts:2190 na 'How many training days per week?'. 2) Dodaj nowy klucz 'ob.protocol.flexNote' do OBU plików: pl 'Plan możesz później dostosować, a treningi przekładać na inne dni.' / en 'You can adjust the plan later and reschedule workouts to other days.'. 3) W PlanWizard.tsx step 4, w pierwszej karcie (rounded-2xl bg-surface-low, linie 362-370), pod rzędem kółek 2-6 dodaj jedną linię: <p className="text-[11px] text-muted-foreground mt-3">{t('ob.protocol.flexNote')}</p>. Zero zmian w logice setDays/toggleDay/walidacji.

**Testy:**
Nowy plik src/test/plan-wizard-protocol.test.tsx (harness skopiowany z plan-wizard-welcome.test.tsx): render <PlanWizard confirmLabelKey="newplan.toReview" onConfirm={noop} /> (start = step 2, bez showWelcome), klik 'Następny krok' → 'Dalej' → jesteś na step 4; asercje: screen.getByText('Ile dni treningowych w tygodniu?') widoczny, notatka flexNote widoczna. Po zmianie: npm run test + npm run typecheck (typ TranslationKey wymusza oba locale) + npm run lint + npm run build.

**Ryzyka:**
Klucz i18n OBOWIĄZKOWO do pl.ts I en.ts (inaczej typecheck padnie). Grep potwierdził: żaden test vitest/e2e nie asertuje starego tekstu 'Ile dni w tygodniu?' — zmiana bezpieczna. PlanWizard współdzielony z NewPlan (replan): nowy nagłówek i notatka pojawią się też tam — treść jest tam równie prawdziwa, to spójność, nie regresja. Niezmiennik (zasada 5): przycisk 'Dalej', walidacja weekdaySelectionValid i przepływ kroków bez zmian — tylko tekst + jedna dodatkowa linia prezentacyjna.

### T2: Chipy daty startu z dniem tygodnia (śr, czw...) + dzień tygodnia przy 'Wybierz konkretną datę' (effort: S)
**Pliki:** src/components/PlanWizard.tsx, src/test/plan-wizard-protocol.test.tsx

**Podejście:**
Wszystko w PlanWizard.tsx step 4, ZERO nowych kluczy i18n (dzień tygodnia z Intl). 1) Chipy (linie 396-399): układ 3-liniowy — górna linia: i===0 ? t('ob.today') : d.toLocaleDateString(dateLocale(lang), { weekday: 'short' }) (daje 'śr.'/'czw.' po polsku, 'Wed'/'Thu' po angielsku); środek: numer dnia (bez zmian, font-heading bold); dół: nowa mini-linia <span className="text-[9px] uppercase opacity-70">{d.toLocaleDateString(dateLocale(lang), { month: 'short' })}</span> — miesiąc nie ginie na granicy miesięcy. Szerokość w-16 i py-2 zostają (pill się lekko wydłuży w pionie, mieści się). 2) Pole konkretnej daty: w linii podglądu 'ob.protocol.startPreview' (linie 410-413) rozszerz opcje formatowania selected i start o weekday: 'short' → 'Wybrano śr., 21 sie; cykl startuje pon., 19 sie.' — dzień tygodnia wybranej daty widoczny natychmiast po zmianie w input[type=date], bez zmiany klucza (parametry {selected}/{start} to stringi). Opcjonalnie (decyzja przy wdrożeniu): obok inputa daty dodać mały <span> z weekday wybranej daty — ale linia podglądu tuż pod polem już to załatwia, rekomendacja: bez tego.

**Testy:**
W tym samym src/test/plan-wizard-protocol.test.tsx: dojdź do step 4; policz oczekiwane etykiety DYNAMICZNIE tym samym algorytmem co komponent (const d = new Date(); d.setDate(d.getDate()+1); const expected = d.toLocaleDateString('pl-PL', { weekday: 'short' })) — bez vi.useFakeTimers (waitFor + fake timers = pułapka); asercje: chip dzisiejszy pokazuje 'Dziś', chip jutrzejszy pokazuje expected; linia startPreview zawiera weekday dzisiejszej daty. Drugi test z localStorage app-language='en': chip jutrzejszy pokazuje wersję en-US ('Wed' itd.) — pilnuje T18-podobnej spójności języka dat. Ręcznie: otwórz wizard na web (npm run dev) i sprawdź, że chipy się nie zawijają i overflow-x-auto scrolluje jak dotąd.

**Ryzyka:**
Format Intl: pl-PL daje 'śr.' z kropką (feedback chciał 'śr' — akceptowalne, spójne z resztą apki, ewentualnie .replace('.','') jeśli właściciel się uprze). NIE używać localizeWeekdayShort (mapuje stałe planu, nie Date) — dla Date właściwy jest toLocaleDateString+dateLocale, ten sam wzorzec co reszta pliku (zgodny z duchem T18: data podąża za językiem UI). Niezmiennik: onClick={() => setStartDate(ds)}, logika snapowania do poniedziałku (getCycleStartPreview) i min= na input NIETKNIĘTE — zmiana czysto prezentacyjna. Chip 'Dziś' celowo zostaje 'Dziś' (bez weekday) — czytelniejszy; jeśli feedback wróci, łatwo dodać weekday też tam.

### T4: Popup po onboardingu: 'Czy dodać pomiary ciała?' (tak → /measurements, nie → zamknij) (effort: M)
**Pliki:** src/pages/Dashboard.tsx, src/components/ConfirmDialog.tsx, src/i18n/locales/pl.ts, src/i18n/locales/en.ts, src/test/dashboard-welcome-measurements.test.tsx

**Podejście:**
Punkt zaczepienia: Dashboard przy ?welcome=1 (pokrywa OBA zakończenia onboardingu: web bezpośrednio i iOS po paywallu — Paywall też nawiguje na /?welcome=1). NIE dodawać kroku do Onboarding.tsx (wydłużałby drogę do paywalla i dublował się z przepływem iOS). Kroki: 1) ConfirmDialog.tsx: dodaj OPCJONALNY prop cancelLabel?: string, w AlertDialogCancel renderuj {cancelLabel ?? t('common.cancel')} — zmiana addytywna, dotychczasowi wywołujący bez zmian. 2) Dashboard.tsx (obok istniejącego showConfetti, linie 64-84): const wantsMeasurePrompt = useRef(searchParams.get('welcome') === '1') (ref czyta param ZANIM effect go skasuje — ten sam trick co showConfetti); const [measurePromptOpen, setMeasurePromptOpen] = useState(false); nowy useEffect z zależnością [isLoaded]: jeśli wantsMeasurePrompt.current && isLoaded && !getLatestMeasurement() → wantsMeasurePrompt.current = false; setMeasurePromptOpen(true). 3) W JSX (obok ConfettiBurst, linia ~722) render ZAWSZE zamontowanego <ConfirmDialog open={measurePromptOpen} onOpenChange={setMeasurePromptOpen} title={t('dashboard.measurePrompt.title')} description={t('dashboard.measurePrompt.desc')} confirmLabel={t('dashboard.measurePrompt.confirm')} cancelLabel={t('dashboard.measurePrompt.decline')} onConfirm={() => navigate('/measurements')} /> — formularz wagi+biceps itd. to ISTNIEJĄCY ekran Measurements, zero nowego formularza. 4) Klucze do pl.ts i en.ts: dashboard.measurePrompt.title ('Dodać pomiary ciała?' / 'Add body measurements?'), .desc ('Zapisz wagę i obwody na start — po cyklu zobaczysz różnicę.' / 'Log your starting weight and girths — you will see the change after the cycle.'), .confirm ('Tak, dodaj pomiary' / 'Yes, add measurements'), .decline ('Nie teraz' / 'Not now').

**Testy:**
Nowy src/test/dashboard-welcome-measurements.test.tsx, harness 1:1 z dashboard-completion-highlight.test.tsx (mock react-router-dom useNavigate → navigateSpy, pełne mocki firestore/hooks; sprawdzić czy tamtejszy mock useFirebaseWorkouts eksponuje getLatestMeasurement i isLoaded — jeśli nie, dodać w mocku nowego pliku). Scenariusze: (a) MemoryRouter initialEntries=['/?welcome=1'] + brak pomiarów → dialog widoczny, klik 'Tak, dodaj pomiary' → navigateSpy z '/measurements'; (b) wejście bez welcome → dialogu NIE ma (niezmiennik: zwykłe wejście na Dashboard bez zmian); (c) welcome=1 ale getLatestMeasurement zwraca pomiar → dialogu NIE ma; (d) klik 'Nie teraz' zamyka dialog (onOpenChange(false)), nic nie nawiguje. Dodatkowo pobieżny bieg istniejących testów Dashboardu (dashboard-order, dashboard-completion-highlight) — nowy stan nie może ich ruszyć. E2E: żaden spec nie dociera do /?welcome=1 (zweryfikowane grepem), więc bez zmian; opcjonalnie dopisać krok do full-app tylko jeśli zespół uzna.

**Ryzyka:**
PUŁAPKA Radix (CLAUDE.md, regresja builda 92): dialog NIGDY nie może być odmontowany w stanie open — dlatego ConfirmDialog renderowany bezwarunkowo, a widoczność wyłącznie przez open/onOpenChange (nie przez {condition && <Dialog/>} zależne od danych). Warunek 'brak pomiarów' decyduje JEDNORAZOWO w effekcie przy otwarciu, nie w renderze (napływ snapshotu nie zamknie/odmontuje otwartego dialogu). Niezmienniki (zasada 5): showConfetti i effect czyszczący URL NIETKNIĘTE (osobny ref/stan); przepływ paywall iOS nietknięty (dialog czeka aż user przejdzie paywall i wyląduje na dashboardzie); ConfirmDialog wstecznie kompatybilny (cancelLabel opcjonalny). Edge: redirect /onboarding → /?welcome=1 dla usera z ukończonym onboardingiem (AuthenticatedApp.tsx:215) — guard 'brak pomiarów' sprawia, że stały user z pomiarami popupu nie zobaczy; świeży user bez pomiarów zobaczy ponownie, co jest OK (prompt nadal zasadny). Dialog może otworzyć się równolegle z confetti — akceptowalne (confetti to krótki burst); jeśli zgrzyta wizualnie, otwarcie przenieść do onDone confetti (drobna zmiana). Zgoda zdrowotna: obowiązkowa w onboardingu, więc formularz na /measurements nie będzie zablokowany banerem consent.

## Notatki
Kolejność wdrożenia: T1+T2 razem (jeden plik PlanWizard.tsx, jeden nowy plik testowy plan-wizard-protocol.test.tsx, jeden commit per zadanie zgodnie z surgical changes), potem T4 (Dashboard + ConfirmDialog + i18n + test). Wszystkie zmiany są web-only UI bez timerów/draftu/tła — checklist background/resume nie jest wymagany, ale scenariusz przerwania (plan → wyjście → powrót) i tak przejść przy okazji builda. T4 świadomie wybiera Dashboard zamiast dodatkowego kroku onboardingu: jedna implementacja pokrywa web i iOS-po-paywallu, onboarding pozostaje nieruszony (mniejsze ryzyko regresji w najbardziej krytycznym przepływie nowego usera). Nawigacja 'tak' → /measurements zamiast osadzania formularza w dialogu: reuse całego istniejącego MeasurementsForm (waga + 9 obwodów, walidacja, jednostki, przecinek dziesiętny) i zapisu przez addMeasurement — zero duplikacji. Uwaga na przyszłość: T13 (zdjęcia before/after) będzie dotykał tego samego ekranu Measurements — T4 nie wprowadza tam żadnych zmian, więc brak konfliktu. Bramki przed wdrożeniem: npm run test, npm run typecheck (pilnuje kompletu kluczy pl/en), npm run lint, npm run build; przy e2e pamiętać o pułapce zwietrzałego dev servera Vite (pkill -f vite + czyszczenie node_modules/.vite przy masowych failach page.goto).
