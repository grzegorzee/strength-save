# Plan obszaru: measurements-photos

## Obszar
Pomiary ciała: zdjęcia before/after (T13) + toggle feature per użytkownik w panelu admina (T14)

## Stan istniejący
NIC z T13/T14 nie jest jeszcze zrobione (brak śladów "photo/bodyPhotos" w pomiarach). Istniejące klocki do wykorzystania: (1) Firebase Storage już działa: src/lib/firebase.ts eksportuje storage, storage.rules ma wzorzec avatars/{uid}/ (write owner, image/*, <5MB), upload wzorcowy w src/pages/Profile.tsx l.185-205 (zwykły input file + uploadBytes + getDownloadURL, bez pluginu Capacitor Camera; w WKWebView input file otwiera natywny sheet Aparat/Biblioteka, plugin Camera NIE jest zainstalowany i NIE jest potrzebny). (2) Pomiary: typ BodyMeasurement w src/types/index.ts l.87, zapis addMeasurement w src/hooks/useFirebaseWorkouts.ts l.191 (generyczna sanityzacja, przepuści nowe pola), formularz src/components/MeasurementsForm.tsx, ekran src/pages/Measurements.tsx (z gate zgody zdrowotnej healthConsent), zamknięty schemat w firestore.rules (validMeasurementShape, keys().hasOnly, l.148-165) i testy rules w scripts/test-firestore-rules.mjs l.290-300. (3) Per-user feature toggle (wzorzec dla T14) JEST gotowy dla Stravy: lista AVAILABLE_FEATURES w src/pages/admin/admin-user-types.ts (jedyny wpis 'strava'), renderowana pętlą w src/pages/admin/AdminUserDetail.tsx l.406-418 (sekcja Uprawnienia) i src/pages/admin/UsersActivityTable.tsx l.172-187, zapis toggleFeature w src/pages/admin/AdminDashboard.tsx l.253 (updateDoc users/{uid} pole features.{key}; admin ma pełny write do users przez isAdmin() w firestore.rules l.88, więc zero zmian rules), gate klienta canUseStrava w src/contexts/UserContext.tsx l.213 (features.strava ?? role==='admin'), mapper user-profile.ts już przenosi features (l.130). Dodanie wpisu do tablicy automatycznie pokazuje toggle POD Stravą w obu miejscach panelu. (4) GDPR: purgeUserData w functions/src/registration.ts kasuje measurements i prefix avatars/{uid}/ w Storage (l.1185-1218), zdjęcia sylwetki trzeba tam dopiąć. (5) Zakończenie cyklu: karta closeout w src/pages/Cycles.tsx l.233-256 (recommendation.canCloseout) to naturalne miejsce na wejście "zdjęcie po cyklu". Uwaga: test src/test/admin-i18n-scan.test.ts blokuje polskie literały w plikach admina.

## Zadania

### T14: Per-user toggle 'bodyPhotos' w panelu admina (pod Stravą) + gate canUseBodyPhotos (effort: S)
**Pliki:** src/pages/admin/admin-user-types.ts, src/contexts/UserContext.tsx, src/i18n/locales/pl.ts, src/i18n/locales/en.ts, src/test/admin-body-photos-feature.test.ts

**Podejście:**
1) admin-user-types.ts: rozszerz union key na 'strava' | 'bodyPhotos' i dodaj wpis PO stravie: { key: 'bodyPhotos', label: 'Before/After', descriptionKey: 'admin.featBodyPhotosDesc', defaultOn: false }. Kolejność w tablicy = kolejność renderu, więc toggle pojawi się automatycznie poniżej Stravy w AdminUserDetail.tsx (l.406) i UsersActivityTable.tsx (l.174), a zapis pójdzie istniejącym toggleFeature (AdminDashboard.tsx l.253, updateDoc features.bodyPhotos). Zero zmian w rules i functions: admin write do users już dozwolony (firestore.rules l.88), features to generyczna mapa. 2) UserContext.tsx: dodaj canUseBodyPhotos: boolean do UserContextValue (l.17-28) i do value providera wzorem canUseStrava (l.213): hasAppAccess && (currentProfile?.features?.bodyPhotos ?? currentProfile?.role === 'admin'). 3) i18n: klucz 'admin.featBodyPhotosDesc' do pl.ts (obok l.1835 'admin.featStravaDesc') I en.ts (obok l.1813).

**Testy:**
Nowy vitest src/test/admin-body-photos-feature.test.ts: kontrakt AVAILABLE_FEATURES (strava na indeksie 0, bodyPhotos na 1 = 'poniżej Strava', defaultOn === false). npm run typecheck wymusi klucz w OBU locale (TranslationKey). Istniejący admin-i18n-scan.test.ts musi przejść. Ręcznie: w panelu admina przełącz toggle userowi testowemu, sprawdź w Firestore pole features.bodyPhotos.

**Ryzyka:**
Label bez polskich znaków (test admin-i18n-scan wywala diakrytyki w src/pages/admin, stąd ASCII 'Before/After' jak brandowe 'Strava'). NIE ruszaj mappera user-profile.ts: features już przechodzi (l.130), lekcja builda 88 dotyczy tylko NOWYCH pól dokumentu. defaultOn: false = nikt nie dostaje feature bez decyzji admina; admin ma feature domyślnie przez '?? role===admin' (spójne ze Stravą). Gate jest wyłącznie kliencki, jak przy UI Stravy; twarde wymuszenie po stronie Storage nie jest konieczne (user bez feature co najwyżej zapisze plik we WŁASNYM prywatnym folderze).

### T13a: Zdjęcie sylwetki przy pomiarze: schemat, rules Storage/Firestore, upload w formularzu, miniatura w historii, GDPR cleanup (effort: L)
**Pliki:** storage.rules, firestore.rules, scripts/test-firestore-rules.mjs, src/types/index.ts, src/lib/image-compress.ts, src/components/MeasurementsForm.tsx, src/pages/Measurements.tsx, functions/src/registration.ts, src/i18n/locales/pl.ts, src/i18n/locales/en.ts, src/test/measurements-form-photo.test.tsx

**Podejście:**
1) storage.rules: nowy match /body-photos/{userId}/{fileName} wzorem avatars, ale read/delete TYLKO owner (prywatność zdjęć sylwetki, w odróżnieniu od avatarów czytanych przez każdego zalogowanego); write owner + contentType image/* + limit 5 MB. 2) firestore.rules validMeasurementShape (l.148-165): dodaj 'photoUrl' i 'photoPath' do keys().hasOnly + walidacja: oba opcjonalne stringi (photoUrl size<=1000, photoPath size<=300). 3) src/types/index.ts BodyMeasurement (l.87): photoUrl?: string; photoPath?: string. addMeasurement w useFirebaseWorkouts.ts NIE wymaga zmian (generyczna pętla sanityzacji przepuszcza stringi), validateMeasurement też nie (sprawdza tylko pola z MEASUREMENT_LIMITS). 4) Nowy src/lib/image-compress.ts: File -> canvas downscale (max 1280 px dłuższy bok) -> JPEG 0.8 Blob; przy błędzie kompresji zwróć oryginał tylko gdy <5MB, inaczej błąd (re-encode przez canvas rozwiązuje przy okazji HEIC z iPhone'a). 5) MeasurementsForm.tsx: nowy opcjonalny prop photosEnabled (DEFAULT false) + rozszerzenie onSave o drugi argument photoFile?: File|null. Gdy photosEnabled: sekcja ze zwykłym input type=file accept=image/* (wzór 1:1 z Profile.tsx l.185-205, natywny sheet WKWebView Aparat/Biblioteka, bez nowego pluginu Capacitor = bez cap sync) + podgląd wybranego zdjęcia (URL.createObjectURL) + przycisk usuń. 6) Measurements.tsx: pobierz canUseBodyPhotos z useCurrentUser (z T14), przekaż photosEnabled={canUseBodyPhotos} (sekcja zdjęcia siedzi wewnątrz istniejącego gate healthConsent, nic nie zmieniaj w tym gate). handleSave: jeśli jest photoFile, NAJPIERW compress + uploadBytes(storageRef(storage, `body-photos/${uid}/${date}-${Date.now()}.jpg`)) + getDownloadURL, potem addMeasurement({...measurement, photoUrl, photoPath}); gdy upload padnie, zapisz pomiar BEZ zdjęcia + toast o nieudanym zdjęciu (pomiar nigdy nie przepada przez zdjęcie). W historii (recentMeasurements, l.151-181): gdy m.photoUrl, miniatura <img> (h-16, rounded, loading=lazy, klik otwiera pełny podgląd np. w Dialog). 7) functions/src/registration.ts deleteAvatarFilesForUser (l.1185): dołóż drugi deleteFiles({prefix: `body-photos/${uid}/`}) (GDPR, bez zmiany nazwy depsa DeletionOperationDeps). 8) i18n: klucze measurements.photo.* (addLabel, hint, remove, uploadFailed, preview) do OBU pl.ts i en.ts. 9) scripts/test-firestore-rules.mjs (l.290-300): przypadki 'measurements: photoUrl/photoPath (string) ALLOWED' i 'photoUrl zlego typu DENIED'. Deploy w kolejności: firebase deploy --only storage,firestore:rules PRZED webem, potem functions, na końcu web.

**Testy:**
Nowy src/test/measurements-form-photo.test.tsx (wzór: src/test/measurements-form-decimal.test.tsx): (a) NIEZMIENNIK: bez photosEnabled formularz renderuje się bez sekcji zdjęcia, a onSave dostaje identyczny payload jak dziś; (b) z photosEnabled wybór pliku przekazuje File jako drugi argument onSave; (c) zapis bez zdjęcia przy włączonym photosEnabled nadal działa. npm run test:rules (nowe przypadki measurements). npm run test + typecheck + lint + build. Ręcznie na realnym iPhonie (checklist CLAUDE.md): zrób zdjęcie aparatem z poziomu formularza, zapisz pomiar, zgaś ekran w trakcie uploadu i wróć (scenariusz background/resume), sprawdź zapis pomiaru przy wyłączonym Wi-Fi (pomiar MUSI się zapisać mimo padniętego uploadu).

**Ryzyka:**
NIEZMIENNIK (zasada 5 CLAUDE.md): zapis pomiaru bez zdjęcia to istniejący przepływ główny; zdjęcie tylko DOKŁADA, błąd uploadu nie może odrzucić pomiaru, a photosEnabled=false daje bajt w bajt dzisiejsze zachowanie (default false chroni też wszystkie istniejące testy formularza). Pułapka hasOnly: rules widzą CAŁY dokument po merge, więc pola muszą wejść do hasOnly zanim web zacznie je pisać (kolejność deployu rules przed webem). validateMeasurement wymaga min. 1 pola liczbowego: wpis 'samo zdjęcie' się nie zapisze; v1 świadomie wymaga choć jednej wartości (np. wagi), odnotować w DECYZJE.md. Prywatność: body-photos read tylko owner (świadomie inne niż avatars); URL z tokenem żyje w dokumencie pomiaru, który czyta tylko owner+admin. RODO art. 9: sekcja zdjęć wyłącznie wewnątrz istniejącego gate healthConsent w Measurements.tsx. Siłownia/słaby zasięg: upload w foreground, bez prób utrzymania JS w tle; brak retry w v1 (pomiar bez zdjęcia + komunikat). iOS HEIC i 4-10 MB z aparatu: obowiązkowa kompresja canvas do JPEG; nie podnoś limitu 5 MB w rules. e2e mock nie ma Storage, ale user e2e nie ma features.bodyPhotos, więc feature jest niewidoczny dla istniejących e2e (bezpieczne). Dane usera są święte: testy uploadu tylko na koncie testowym.

### T13b: Porównanie przed/po (najstarsze vs najnowsze zdjęcie) + wejście po zakończeniu cyklu (effort: M)
**Pliki:** src/components/BodyPhotoCompare.tsx, src/pages/Measurements.tsx, src/pages/Cycles.tsx, src/i18n/locales/pl.ts, src/i18n/locales/en.ts, src/test/body-photo-compare.test.tsx

**Podejście:**
1) Nowy src/components/BodyPhotoCompare.tsx: props { measurements: BodyMeasurement[] }. Filtruje wpisy z photoUrl, sortuje po date (parseLocalDate jak w Measurements.tsx l.53). Przy >=2 zdjęciach: Card z grid-cols-2, lewa kolumna default najstarsze ('przed'), prawa najnowsze ('po'), pod każdym data (toLocaleDateString z dateLocale(lang), wzór l.154) + waga (fmt z useUnit) i delta wagi między wybranymi; nad każdą kolumną Select (shadcn, jak w innych ekranach) z listą dat zdjęć, żeby dało się porównać dowolne dwa. Przy dokładnie 1 zdjęciu: sam kafel z zachętą 'wróć po cyklu po zdjęcie PO' (klucz i18n). 2) Measurements.tsx: pod sekcją formularza (obok MeasurementTrendChart) renderuj <BodyPhotoCompare measurements={measurements} /> tylko gdy canUseBodyPhotos i jest >=1 zdjęcie. 3) Cycles.tsx: w istniejącej karcie closeout (blok recommendation.canCloseout, l.246-256) dodaj trzeci Button variant=ghost/outline 'Zdjęcie po cyklu' -> navigate('/measurements'), renderowany tylko gdy canUseBodyPhotos (useCurrentUser już jest w tym pliku przez uid; dciągnij canUseBodyPhotos). To wyczerpuje wymaganie 'po zakończeniu cyklu kolejna fotka i porównanie' bez ruszania maszyny stanów plan-next-step.ts. 4) i18n: measurements.photo.compareTitle, before, after, onlyOneHint, cycles.photoAfterCta do OBU pl.ts i en.ts.

**Testy:**
Nowy src/test/body-photo-compare.test.tsx (wzór renderu z providerami jak measurements-form-decimal.test.tsx): (a) 3 pomiary, 2 ze zdjęciami -> pokazuje najstarsze po lewej i najnowsze po prawej (alt/teksty dat); (b) 1 zdjęcie -> hint zamiast porównania; (c) 0 zdjęć -> null. Test niezmiennika Cycles: istniejące testy cykli muszą przejść bez zmian, a przy canUseBodyPhotos=false karta closeout renderuje dokładnie dotychczasowe przyciski (rozszerz istniejący test cykli, jeśli jest snapshot przycisków, albo dopisz asercję w nowym teście). npm run test + typecheck + lint.

**Ryzyka:**
NIEZMIENNIK: karta closeout w Cycles.tsx ma dziś 2 przyciski (Powtórz plan / Zmień plan) i to jest przepływ decyzyjny końca cyklu; nowy przycisk tylko DOKŁADA link i znika przy wyłączonym feature, niczego nie zastępuje. Nie ruszaj plan-next-step.ts (maszyna stanów wspólna dla Dashboard/Plan/Cycles, C-T4). Obrazy w porównaniu: loading=lazy i max-width 100% (WKWebView, zasada 7: overflow-x zablokowany). Kolor statusów przy ewentualnych badge zgodnie z zasadą 8 (tło /10). Radix Select w tej karcie: zamykanie tylko przez open=false, nie unmountuj przy otwartym (lekcja builda 92 dotyczy Sheet/Dialog, Select bezpieczny, ale nie podmieniaj danych źródłowych selecta w trakcie otwarcia).

## Notatki
Kolejność wdrożenia: T14 -> T13a -> T13b (T13 używa canUseBodyPhotos z T14). Decyzje wymagające potwierdzenia właściciela: (1) zdjęcie zawsze przypięte do pomiaru z min. jedną wartością liczbową (brak wpisu 'samo zdjęcie' w v1), (2) brak retry uploadu w tle w v1 (pomiar zapisuje się bez zdjęcia przy błędzie), (3) read zdjęć tylko owner, admin NIE widzi zdjęć sylwetki użytkowników (prywatność ponad monitoring). Deploy: rules (storage + firestore) i functions przed webem; iOS wymaga zwykłego bumpa CURRENT_PROJECT_VERSION (następny = 113, versionCode 28 wg pamięci projektu), bez nowych pluginów Capacitor. Wpis do DECYZJE.md po wdrożeniu (checklist CLAUDE.md).
