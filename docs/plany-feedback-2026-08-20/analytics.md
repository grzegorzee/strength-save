# Plan obszaru: analytics

## Obszar
Progress -> Analytics: T11 (layout przycisków Week/Month/PDF/Copy) + T12 (eksport CSV obok PDF)

## Stan istniejący
Komponent z przyciskami to SummaryTab wewnątrz src/pages/Analytics.tsx (wiersze 254-273). Kontener: `<div className="flex items-center justify-between">` z dwiema grupami `flex gap-2`: lewa Tydzień/Miesiąc (analytics.period.*), prawa PDF (report.download = "PDF") i Kopiuj (analytics.copy). Brak flex-wrap, więc na viewport 390px (PL etykiety + ikony h-4 w-4 mr-2 + padding Layoutu) rząd przekracza szerokość i "Kopiuj" wystaje poza ekran. To jest T11, NIC z tego nie jest naprawione. Analytics renderuje się w dwóch miejscach: osadzony w Postępach (src/pages/Achievements.tsx:273, prop embedded) oraz przez redirect /analytics (AuthenticatedApp.tsx:222), fix w SummaryTab obejmuje oba.

T12: mechanizm eksportu CSV z zakresem JUŻ ISTNIEJE w całości (wydany jako J-T5, 2026-08-20): src/components/ExportWorkoutsDialog.tsx (dialog z chipami week/month/last10/last30/cycle/custom, podgląd liczby treningów, fetch przez fetchWorkoutHistoryPage, Blob download), src/lib/workout-export-range.ts (czysta logika granic zakresu), src/lib/workout-csv.ts (format CSV). Dwa punkty wejścia: Historia (WorkoutHistory.tsx:323, przycisk data-testid="history-export-csv") i Ustawienia -> Dane (Settings.tsx:221 + DataManagement.tsx). Klucze i18n exportCsv.* są w OBU plikach locale (pl.ts ~302-316, en.ts ~296-310). Testy istnieją: src/test/workout-export-range.test.ts, src/test/workout-csv.test.ts (vitest) oraz e2e/export-csv-dialog.spec.ts (spy na URL.createObjectURL). Do zrobienia zostaje TYLKO trzeci punkt wejścia z zakładki Analytics/Podsumowanie. SummaryTab ma już wszystko czego dialog potrzebuje: uid (useCurrentUser, linia 54) i cycles (usePlanCycles, linia 57). Wzorzec e2e dla tej zakładki: e2e/analytics-pdf.spec.ts (nawigacja /analytics?tab=summary, setE2EWorkouts, blockFirebase).

## Zadania

### T11: Fix layoutu rzędu Tydzień/Miesiąc/PDF/Kopiuj (Copy wystaje poza ekran) (effort: S)
**Pliki:** src/pages/Analytics.tsx, e2e/analytics-summary-actions.spec.ts

**Podejście:**
1) W src/pages/Analytics.tsx linia 254 zmienić kontener z `flex items-center justify-between` na `flex flex-wrap items-center justify-between gap-2` (wzorzec flex-wrap już jest w tym pliku, linia 357). Na wąskim ekranie prawa grupa (PDF/CSV/Kopiuj) łamie się do drugiego wiersza zamiast wystawać poza viewport; na szerokim nic się nie zmienia. Wewnętrzne grupy `flex gap-2` zostają bez zmian. 2) Dodać data-testid="analytics-copy" do przycisku Kopiuj (linia 268), żeby e2e miał stabilny selektor. 3) Nowy spec e2e (viewport 390x844 z playwright.config.ts): wejść na /analytics?tab=summary (wzorzec analytics-pdf.spec.ts: setE2EAuthScenario + blockFirebase + setE2EWorkouts), pobrać boundingBox przycisku Kopiuj i zasertować box.x + box.width <= 390 oraz document.documentElement.scrollWidth <= window.innerWidth. To 1 linia zmiany w komponencie, zero zmian logiki.

**Testy:**
e2e: nowy e2e/analytics-summary-actions.spec.ts (wzorzec: analytics-pdf.spec.ts dla setupu, export-csv-dialog.spec.ts dla struktury). Asercja boundingBox + scrollWidth przy defaultowym viewport 390px. Vitest: brak sensownego testu jednostkowego layoutu (jsdom nie liczy layoutu), nie dopisywać sztucznego testu klas CSS. Regresja: istniejący e2e/analytics-pdf.spec.ts musi zostać zielony (przycisk PDF bez zmian). npm run test + typecheck + lint + build wg checklisty.

**Ryzyka:**
1) Z justify-between + flex-wrap zawinięta prawa grupa ląduje wyrównana do LEWEJ (pojedynczy element w wierszu ze space-between idzie na start); jeśli właściciel woli do prawej, dać prawej grupie ml-auto zamiast justify-between na kontenerze, ale to decyzja kosmetyczna, nie blokować nią fixa. 2) Nie ruszać niczego innego w SummaryTab (Karpathy: surgical). 3) Zasada 9 z CLAUDE.md: e2e czerwone lub wolne -> najpierw pkill -f vite + wyczyść node_modules/.vite, dopiero potem szukać buga. 4) Fix musi działać też w wariancie embedded (Achievements) i po dodaniu piątego przycisku CSV z T12, flex-wrap obsługuje oba.

### T12: Przycisk CSV obok PDF w Podsumowaniu (reuse ExportWorkoutsDialog) (effort: S)
**Pliki:** src/pages/Analytics.tsx, src/i18n/locales/pl.ts, src/i18n/locales/en.ts, e2e/analytics-summary-actions.spec.ts

**Podejście:**
Czyste reuse, ZERO nowej logiki eksportu. W SummaryTab (src/pages/Analytics.tsx): 1) import ExportWorkoutsDialog z '@/components/ExportWorkoutsDialog' i ikony FileSpreadsheet z lucide-react; 2) nowy stan `const [showExportDialog, setShowExportDialog] = useState(false)`; 3) w prawej grupie przycisków, między PDF a Kopiuj (feedback: "obok PDF"), dodać `<Button variant="outline" size="sm" onClick={() => setShowExportDialog(true)} data-testid="analytics-export-csv"><FileSpreadsheet className="h-4 w-4 mr-2" />{t('exportCsv.analyticsButton')}</Button>`; 4) na końcu JSX SummaryTab zamontować NA STAŁE (nie warunkowo) `<ExportWorkoutsDialog open={showExportDialog} onOpenChange={setShowExportDialog} uid={uid} cycles={cycles} />`, dokładnie jak w WorkoutHistory.tsx:323 (uid i cycles już są w SummaryTab: linie 54 i 57); 5) i18n: JEDEN nowy klucz `'exportCsv.analyticsButton': 'CSV'` do OBU plików pl.ts i en.ts, obok istniejącego bloku exportCsv.* (pl ~linia 316, en ~linia 310); krótka etykieta 'CSV' celowo, żeby rząd przycisków się mieścił (spójnie z 'PDF'). Dialog sam obsługuje okres (tydzień/miesiąc/ostatnie N/cykl/własny zakres), czyli dokładnie to, o co prosi T12.

**Testy:**
e2e: w analytics-summary-actions.spec.ts dopisać test wg wzorca e2e/export-csv-dialog.spec.ts (spy na URL.createObjectURL w addInitScript, setE2EWorkouts z 2 treningami): klik analytics-export-csv -> widoczny export-workouts-dialog -> domyślny chip week aria-checked -> export-preview pokazuje liczbę -> klik export-submit -> blob type text/csv. Vitest: logika już pokryta (src/test/workout-export-range.test.ts, src/test/workout-csv.test.ts), NIE duplikować. Regresja niezmiennika: istniejące e2e export-csv-dialog.spec.ts (Historia + Ustawienia) oraz analytics-pdf.spec.ts muszą zostać zielone bez modyfikacji. Klucz i18n w obu locale, inaczej typecheck padnie.

**Ryzyka:**
1) Niezmiennik (zasada 5 CLAUDE.md): przyciski PDF i Kopiuj oraz ich handlery zostają NIETKNIĘTE, CSV tylko DOKŁADA punkt wejścia; nie ruszać ExportWorkoutsDialog ani workout-export-range.ts (zmiana tam dotknęłaby Historii i Ustawień). 2) Pułapka Radix (regresja builda 92): dialog zamontowany bezwarunkowo, zamykanie wyłącznie przez open=false; NIE renderować go za `{showExportDialog && ...}`. Teoretyczna dziura wspólna z WorkoutHistory: unmount SummaryTab przy otwartym dialogu, ale modal overlay blokuje przełączenie zakładki, ryzyko akceptowane jak w istniejących punktach wejścia. 3) e2e analytics-pdf.spec.ts używa getByRole name 'PDF' exact, etykieta 'CSV' nie koliduje; nie nazywać przycisku np. 'CSV/PDF'. 4) Nie mylić z T19 (wysyłka mailem, POMINIĘTE): tu tylko pobranie pliku. 5) Kolejność wdrożenia: najpierw T11 (wrap), bo piąty przycisk pogarsza overflow; oba tasksy w osobnych commitach (surgical, rollback punktowy).

## Notatki
Kolejność: T11 przed T12 (dodanie przycisku CSV bez flex-wrap pogłębiłoby overflow). Oba zadania dotykają wyłącznie SummaryTab w src/pages/Analytics.tsx + i18n + e2e; łączny diff w komponencie to ok. 10 linii. Uwaga na przyszły konflikt z T7 z tego samego feedbacku (zabranie ręcznej synchronizacji Strava z widoku analityki) - inny tab (StravaTab), brak kolizji plikowej w SummaryTab, ale ten sam plik Analytics.tsx, warto zgrać kolejność merge. Etykieta 'CSV' jest identyczna w pl i en, ale klucz i18n i tak obowiązkowy w obu plikach (typecheck). Wszystkie ścieżki zweryfikowane w repo: src/pages/Analytics.tsx, src/components/ExportWorkoutsDialog.tsx, src/lib/workout-export-range.ts, src/lib/workout-csv.ts, src/pages/WorkoutHistory.tsx (wzorzec użycia), e2e/analytics-pdf.spec.ts, e2e/export-csv-dialog.spec.ts, src/test/workout-export-range.test.ts.
