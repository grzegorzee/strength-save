# PLAN X16C: aplikacja Garmin Connect IQ v1. Logowanie serii z zegarka Garmin + natywny zapis do Garmin Connect (2026-07-19, wersja 2)

> **Dla agenta wykonawcy:** WYMAGANY SUB-SKILL: `superpowers:executing-plans` (albo `superpowers:subagent-driven-development`). Kroki mają checkboxy `- [ ]`. TRYB 100% AUTONOMICZNY.
> **WERSJA 2 (decyzja usera 2026-07-19 wieczór):** zastępuje pierwotny plan X16C oparty o Garmin Health/Activity API. Powód: Health API wymaga akceptacji Garmin Connect Developer Program (gatekeeper, tygodnie, możliwa odmowa), a import cardio od userów Garmina już w większości pokrywa istniejąca Strava (auto-sync Garmin→Strava). Aplikacja Connect IQ nie ma gatekeepera (SDK darmowe, dystrybucja przez Connect IQ Store ze zwykłą recenzją) i daje więcej: logowanie serii z nadgarstka na Garminie (nie ma tego NIKT w kategorii: Strong/Hevy tylko Apple Watch/Wear OS) + trening siłowy natywnie w Garmin Connect (sesja FIT z HR, bez żadnego API). Health API import = backlog (opcja, gdyby Garmin kiedyś przyznał dostęp).
> Kontekst: wymaga WDROŻONYCH X16A (cele tygodniowe w kontrakcie dnia) i X16B (wspólny kontrakt `watch-contract.ts`, który tu REUŻYWAMY). Research: prośba o Connect IQ z r/Hevy z dopiskiem "I'd even be willing to pay for it".
> **BLOKADY ZEWNĘTRZNE tego planu:** (a) recenzja Connect IQ Store (dni), (b) test na fizycznym zegarku Garmin = KROK USERA (symulator CIQ pokrywa resztę). Obsługa wg pętli z PROMPT-WDROZENIE: wykonaj wszystko co możliwe, odłóż resztę z listą kroków usera.
> UWAGA STALENESS: przed implementacją zweryfikuj aktualne wersje Connect IQ SDK i listę urządzeń (web search).

**Goal:** Użytkownik z zegarkiem Garmin paruje go z kontem (6-cyfrowy kod), a potem na zegarku: widzi dzisiejszy trening (ćwiczenia, cele serii, przypięte notatki), odhacza serie z korektą ciężaru, dostaje rest timer z wibracją, a zegarek równolegle nagrywa natywną sesję siłową (HR, kalorie) do Garmin Connect. Ukończony trening ląduje w Strength Save jako normalna sesja (historia, rekordy, obciążenie hybrydowe). Działa bez telefonu w zasięgu (kolejka na zegarku, wysyłka po odzyskaniu łączności). Release train na końcu.

**Scope v1 (ŚWIADOMIE WĄSKI, lustro Apple Watch v1 z X16B):**
- JEST: parowanie kodem, pobranie dnia, lista ćwiczeń, cel serii, odhaczenie ze stepperem ciężaru (+/- 2.5 kg) i powtórzeń (+/- 1), rest timer z wibracją, podgląd przypiętej notatki, zakończenie treningu, natywna sesja FIT (strength) do Garmin Connect, kolejka offline.
- NIE MA w v1: edycji planu, dodawania ćwiczeń z zegarka, historii na zegarku, live-syncu z draftem na telefonie (trening z Garmina = standalone sesja zapisywana przez backend po zakończeniu; jednoczesny trening na telefonie i Garminie to nie jest wspierany scenariusz v1, guard opisany w Z125).

**Architektura podejścia:**
- **Zegarek rozmawia z NASZYM backendem, nie z telefonem.** `Communications.makeWebRequest` (przez Garmin Connect Mobile na telefonie albo Wi-Fi/LTE zegarka) na trzy endpointy Cloud Functions (HTTP): `garminPair`, `garminDay`, `garminIngest`.
- **Parowanie:** user w Ustawieniach Strength Save generuje 6-cyfrowy kod (kolekcja `device_pair_codes`: hash kodu, uid, TTL 10 min, jednorazowy). Zegarek wysyła kod → `garminPair` wydaje token urządzenia (random 32B; w Firestore `device_tokens` tylko hash + uid + label + createdAt/lastUsedAt; token w `Application.Storage` zegarka). Unlink w Ustawieniach = revoke tokenu.
- **Kontrakt danych = REUŻYCIE X16B:** `buildWatchDayContext` (kompaktowy JSON dnia: ćwiczenia, serie z celami, notatki) serwowany przez `garminDay`; zdarzenia odhaczeń idempotentne po `eventId` jak w `applyWatchEvent`. Jedna definicja kontraktu w `src/lib/watch-contract.ts`, functions importują typy/walidację z współdzielonego modułu (wzorzec testów parytetu klient<->functions z X13C).
- **Zapis treningu:** `garminIngest` przyjmuje paczkę zdarzeń + zakończenie, waliduje token, składa `WorkoutSession` (snapshot nazw ćwiczeń: architektura snapshot+resolver sprawia, że sesja z zegarka nie potrzebuje planu po stronie klienta) i zapisuje przez Admin SDK. Idempotencja: docId `garmin-<deviceId>-<workoutId>`.
- **Apka CIQ:** device app w Monkey C (Connect IQ SDK, VS Code extension, symulator), min API dobrane tak, by pokryć popularne zegarki 2022+ (Fenix 7/8, Epix 2, Forerunner 255/265/955/965, Venu 2/3, vívoactive 5; finalna lista w Z126.0). Sesja `ActivityRecording` typu strength (HR z nadgarstka) zapisywana lokalnie → Garmin sync robi resztę.

**Tech stack:** Monkey C (Connect IQ SDK 7/8), Cloud Functions (Node, wzorce auth z registration.ts), Firestore rules + `test:rules` (JDK21), vitest (kontrakt i walidacje), symulator CIQ.

---

## 0. TWARDE ZASADY

1. Deploy wyłącznie w FAZIE 3 (release train).
2. Bezpieczeństwo: endpointy HTTP wyłącznie po tokenie urządzenia (hash w Firestore, porównanie timing-safe jak webhook Stravy), rate limiting per token, CORS zamknięty. Kod parowania jednorazowy z TTL. Zero sekretów w apce CIQ.
3. Koszty Firestore/Functions: `garminDay` max 1 odczyt kontekstu na start treningu + odświeżenie na żądanie (bez pollingu), `garminIngest` batched (flush zdarzeń przy odhaczeniu gdy łączność, inaczej kolejka; zapis dokumentu treningu raz, na zakończenie).
4. Jednoczesny trening telefon+Garmin: `garminIngest` przy zapisie sprawdza, czy na ten dzień istnieje już completed sesja z tego samego dnia planu; jeśli tak, zapisuje jako osobną sesję ad-hoc z dopiskiem źródła (żadnego mergowania w v1, zero ryzyka nadpisania danych z telefonu).
5. Zdarzenia idempotentne (eventId), deduplikacja po stronie functions z testem.
6. i18n apki CIQ: PL+EN w resources. TDD dla całej logiki TS/functions, chirurgiczne commity, pełne bramki, DECYZJE.md po fazach.

---

## FAZA 0: Toolchain + baseline

### Zadanie Z125.0

- [x] Krok 1: `git status` czyste; X16A i X16B wdrożone (DECYZJE.md). Pełne bramki zielone.
- [x] Krok 2: Research 2026-07-20: SDK 9.2.0 (2026-06-08); SDK przez SDK Manager (pobranie SDK wymaga zalogowania kontem Garmin => KROK USERA); makeWebRequest: praktyczny limit odpowiedzi ~8KB (BLE), timeouty -2/-300, BLE_REQUEST_TOO_LARGE -102, limit równoległych żądań => kontrakt garminDay kompaktowy (test <8KB). Min API: 4.0.0 (fenix7+/epix2/FR255+/venu2+; vivoactive5 5.x kompatybilne). Publikacja Store: konto developerskie Garmin (rejestracja) => KROK USERA.
- [ ] Krok 3: BLOKADA ZEWNĘTRZNA — instalacja SDK wymaga zalogowania kontem Garmin w SDK Managerze (może tylko user). Projekt `garmin/` W REPO (kompletne źródła scope v1 + build.sh + README z krokami usera), poza buildem web. NIEZBUDOWANY: hello-world/symulator do wykonania po instalacji SDK (KROK USERA -> potem agent dokończy).

---

## FAZA 1: Z125. Backend: parowanie + wymiana danych

### Zadanie Z125.1: parowanie kont (test first)

- [x] Krok 1: Testy functions (`functions/src/garmin-pair.test.ts`, 8 testów): generacja kodu (6 cyfr, hash, TTL 10 min, jednorazowość), wymiana kod→token (złych kod odrzucony, wygasły odrzucony, drugi raz ten sam kod odrzucony), revoke tokenu. FAIL → implementacja `functions/src/garmin-pair.ts` (callable `garminPairStart` dla zalogowanego usera + HTTP `garminPair` dla zegarka) → PASS.
- [x] Krok 2: Rules: deny-all dla `device_pair_codes`/`device_tokens` (5 testów, w tym read admina DENIED — tokeny to sekrety). Commit ede6902.

### Zadanie Z125.2: garminDay + garminIngest (test first)

- [x] Krok 1 (DOSTOSOWANY): `buildGarminDayContext` w functions (silnik progresji żyje w kliencie — do kontraktu Garmina idzie pre-fill + cel z UPROSZCZONEJ double progression, parytet progress/hold z decideNextSet pilnowany testami; plateau/ból/deload = v2 po wydzieleniu silnika do wspólnego pakietu). garminIngest: walidacja ręczna (wzorzec repo bez zod), dedup po eventId, local-wins po timestamp, idempotentny docId, guard jednoczesności => ad-hoc. Testy: 5 (day) + 7 (ingest). Oryginalny opis: `garminDay` zwraca kontekst dnia z `buildWatchDayContext` (X16B) dla usera z tokenu (401 bez tokenu, kompaktowy JSON, cele z X16A gdy włączone, przypięte notatki z X14A); `garminIngest` waliduje paczkę zdarzeń (schemat zod), deduplikuje po eventId, składa i zapisuje WorkoutSession ze snapshotami nazw, idempotentny docId, reguła jednoczesności z TWARDEJ ZASADY 4. FAIL → implementacja `functions/src/garmin-day.ts`, `functions/src/garmin-ingest.ts` → PASS.
- [x] Krok 2: Test parytetu: sesja z buildSessionFromEvents przechodzi sanitizeWorkoutDoc klienta bez strat (src/test/garmin-parity.test.ts, plan i ad-hoc). Bramki functions (build tsc + 115 testów). Commit ede6902.

### Zadanie Z125.3: UI parowania w aplikacji

- [x] Krok 1: Ustawienia → sekcja "Zegarek Garmin": [Sparuj zegarek] → kod 6-cyfrowy z odliczaniem TTL, lista sparowanych urządzeń (label, lastUsedAt), [Odłącz]. i18n PL+EN. E2E mock (garmin-api z gałęzią E2E + fittracker_e2e_garmin_devices).
- [x] Krok 2: Bramki + commit 32118cf `feat(garmin): parowanie w Ustawieniach (Z125)`.

### CHECKPOINT FAZY 1

- [x] Pełne bramki + testy functions (115) + rules (167) + DECYZJE.md (wpis przy release).

---

## FAZA 2: Z126. Aplikacja Connect IQ

### Zadanie Z126.0: szkielet + urządzenia docelowe

- [x] Krok 1: `garmin/manifest.xml` (12 urządzeń, min API 4.0.0 — listę id zweryfikować przy pierwszym buildzie z SDK), struktura (views/delegates/moduły, resources PL+EN), `garmin/build.sh`. Commit 9b39c14. UWAGA: NIEZBUDOWANE (SDK = KROK USERA).

### Zadanie Z126.1: parowanie + pobranie dnia na zegarku

> **ODŁOŻONE — BLOKADA ZEWNĘTRZNA (2026-07-20):** kod ekranów NAPISANY w `garmin/source/` (PairView picker cyfr + token do Application.Storage, DayView z fetch/cache/retry i obsługą 401 → powrót do parowania), ale weryfikacja w symulatorze wymaga SDK (logowanie kontem Garmin = KROK USERA). Po instalacji SDK: build, poprawki kompilacji, screenshoty Fenix+Venu.

- [ ] Krok 1: Ekran parowania (wpisanie kodu picker'em cyfr), `makeWebRequest` → token do `Application.Storage`; ekran główny: pobierz dzień (`garminDay`), render listy ćwiczeń ze stanem. Obsługa błędów: brak łączności (komunikat + retry), token revoked (powrót do parowania).
- [ ] Krok 2: Symulator: scenariusz parowania i renderu dnia na min. 2 rozmiarach ekranu (Fenix + Venu). Screenshoty do raportu. Commit `feat(ciq): parowanie i widok dnia (Z126)`.

### Zadanie Z126.2: trening: serie, timer, sesja FIT

> **ODŁOŻONE — BLOKADA ZEWNĘTRZNA (jak wyżej):** kod NAPISANY (ExerciseView: cel/stepper/odhacz/notatka + rest timer z Attention.vibrate; SessionRecorder: ActivityRecording SUB_SPORT_STRENGTH_TRAINING start przy pierwszym odhaczeniu, stop+save przy zakończeniu; EventQueue w Storage z finalną wysyłką do garminIngest — idempotencja po stronie backendu PRZETESTOWANA). Scenariusze symulatorowe po instalacji SDK.

- [ ] Krok 1: Ekran ćwiczenia (cel serii, stepper ciężar/powtórzenia, odhacz), rest timer pełnoekranowy z wibracją (`Attention.vibrate`), podgląd przypiętej notatki, zakończenie treningu z potwierdzeniem.
- [ ] Krok 2: Nagrywanie sesji: `ActivityRecording.createSession` (sport: strength/training) start przy pierwszym odhaczeniu, stop+save przy zakończeniu → FIT z HR trafia do Garmin Connect natywnym syncem.
- [ ] Krok 3: Kolejka zdarzeń w Storage (idempotentne eventId), flush przy łączności, wysyłka finalna przy zakończeniu; wskaźnik "niezsynchronizowane" + retry ręczny.
- [ ] Krok 4: Scenariusze symulatorowe: pełny trening → sesja w Strength Save (weryfikacja na emulatorze Firestore lub koncie testowym) + FIT zapisany; trening bez łączności → wysyłka po przywróceniu; podwójna wysyłka → jedna sesja (idempotencja). Wyniki do raportu. Commit `feat(ciq): trening na zegarku (Z126)`.

### CHECKPOINT FAZY 2

- [ ] Bramki TS/functions + build CIQ na wszystkie targety + DECYZJE.md.

---

## FAZA 3: Z127. Publikacja + release

### Zadanie Z127.1: Connect IQ Store

> **ODŁOŻONE — KROK USERA:** konto developerskie Garmin (rejestracja na dane usera) + pierwsza kompilacja przed submisją. Materiały opisowe przygotowane w garmin/README.md; screenshoty dopiero po symulatorze.

- [ ] Krok 1: Przygotuj submission: nazwa, opis PL+EN, ikony/screenshoty (symulator), polityka prywatności (link do istniejącej), kategoria. Konto developerskie Garmin (bezpłatne): jeśli założenie wymaga danych usera, przygotuj wszystko i wypisz jako KROK USERA.
- [ ] Krok 2: Submit do recenzji (albo KROK USERA jeśli konto na usera). Status i dalsze kroki do raportu.

### Release train

- [ ] Krok 1: Pełne bramki + `check:dist-smoke` + `check:dist-offline` (web: doszła tylko sekcja Ustawień).
- [ ] Krok 2: `git push`; `firebase deploy --only firestore:rules`; `firebase deploy --only functions`; `npm run deploy`; weryfikacja live.
- [ ] Krok 3: iOS: bump `CURRENT_PROJECT_VERSION` +1, `scripts/ios-testflight.sh`, poll VALID, `testflight_external.py <build> --whats-new "Parowanie zegarka Garmin: trening z nadgarstka"`.
- [ ] Krok 4: DECYZJE.md, PLAN.md, CONTEXT.md w FIRMA. Raport: status recenzji Store, KROKI USERA (test na fizycznym zegarku, ewentualne konto developerskie), architektura tokenów, koszty Functions, backlog (Health API import jako opcja, live-sync draftu telefon<->Garmin jako v2).
