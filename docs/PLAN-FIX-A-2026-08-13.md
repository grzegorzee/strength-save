# PLAN FIX-A: Stabilność przed launchem (zgłoszenia z treningu 2026-08-13)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Zero sytuacji, w których user traci kontrolę: awaria Firestore SDK kończy się kontrolowanym restartem (nie czarnym ekranem), zakończenie treningu to zwykły przycisk z potwierdzeniem, a czerwony „Błąd zapisu" pojawia się WYŁĄCZNIE gdy dane są realnie zagrożone.

**Architecture:** Trzy warstwy obrony: (1) globalny crash-guard wykrywający `INTERNAL ASSERTION FAILED` Firestore (SDK po tym błędzie jest martwe do końca życia strony — każda kolejna operacja rzuca; jedyne wyjście to pełny reload; draft przeżywa w IndexedDB/localStorage), (2) sprzątacz blokad body zostawianych przez Radix przy awaryjnym unmount otwartego Sheeta (lekcja builda 92 — root cause czarnego ekranu), (3) degradacja komunikatów zapisu: fallback localStorage działa = stan neutralny, nie czerwony.

**Tech Stack:** React 18 + TS, ErrorBoundary klasowy (istnieje), workout-draft-db (istnieje), vitest + RTL.

## Global Constraints

- CLAUDE.md projektu w całości (Karpathy; sekwencje, nie ekrany; każdy stan błędu ma wyjście).
- Ekran treningu = strefa chroniona: zmiany tylko w wskazanych miejscach.
- i18n: każdy nowy klucz do OBU plików `src/i18n/locales/pl.ts` i `en.ts`.
- Testy komponentów z polskimi diakrytykami w nazwach → `src/test/` (guard i18n Z168 skanuje `components/`).
- Po każdym tasku: `npm run test` + `npm run typecheck` zielone, commit tylko plików taska.
- Bramka `npm run check:no-emoji` obowiązuje.
- NASTĘPNY bump iOS = 101, versionCode Android = 17 (zweryfikuj realną wartość w pbxproj przed release).

---

### Task 1: Crash-guard Firestore — kontrolowany restart zamiast martwej apki

Kontekst: screen usera `E-RM6GU`: `FIRESTORE (12.8.0) INTERNAL ASSERTION FAILED: Unexpected state (ID: b815)` po powrocie WKWebView z tła. Po tej asercji instancja Firestore jest nieodwracalnie zepsuta — stąd późniejszy „Błąd zapisu" i czarny ekran. „Wróć na Dashboard" w ErrorBoundary robi nawigację SPA, która NIC nie naprawia.

**Files:**
- Create: `src/lib/firestore-crash-guard.ts`
- Test: `src/test/firestore-crash-guard.test.ts`
- Modify: `src/main.tsx` (instalacja guardu — znajdź: `grep -n "global-error-telemetry\|render(" src/main.tsx`)
- Modify: `src/components/ErrorBoundary.tsx` (hard reload zamiast nawigacji przy asercji Firestore)

**Interfaces:**
- Produces: `isFirestoreInternalAssertion(message: unknown): boolean`, `shouldAutoReload(nowMs: number): boolean` (guard anti-loop w localStorage), `installFirestoreCrashGuard(reload: () => void): void`.

- [ ] **Step 1: Failing test**

```ts
// src/test/firestore-crash-guard.test.ts
import { beforeEach, describe, expect, it, vi } from 'vitest';
import {
  isFirestoreInternalAssertion,
  shouldAutoReload,
  installFirestoreCrashGuard,
} from '@/lib/firestore-crash-guard';

describe('firestore-crash-guard', () => {
  beforeEach(() => localStorage.clear());

  it('rozpoznaje asercję Firestore w message i w Error', () => {
    expect(isFirestoreInternalAssertion('FIRESTORE (12.8.0) INTERNAL ASSERTION FAILED: Unexpected state (ID: b815)')).toBe(true);
    expect(isFirestoreInternalAssertion(new Error('INTERNAL ASSERTION FAILED: Unexpected state'))).toBe(true);
    expect(isFirestoreInternalAssertion('zwykly blad sieci')).toBe(false);
    expect(isFirestoreInternalAssertion(undefined)).toBe(false);
  });

  it('anti-loop: reload najwyżej raz na 2 minuty', () => {
    const now = 1_000_000_000;
    expect(shouldAutoReload(now)).toBe(true);        // pierwszy raz: tak + zapis znacznika
    expect(shouldAutoReload(now + 30_000)).toBe(false); // 30 s później: nie
    expect(shouldAutoReload(now + 121_000)).toBe(true); // po oknie: znowu tak
  });

  it('unhandledrejection z asercją woła reload; inne błędy nie', () => {
    const reload = vi.fn();
    installFirestoreCrashGuard(reload);
    window.dispatchEvent(new PromiseRejectionEvent('unhandledrejection', {
      promise: Promise.resolve(),
      reason: new Error('INTERNAL ASSERTION FAILED: Unexpected state (ID: b815)'),
    }));
    expect(reload).toHaveBeenCalledTimes(1);
    window.dispatchEvent(new PromiseRejectionEvent('unhandledrejection', {
      promise: Promise.resolve(),
      reason: new Error('network flake'),
    }));
    expect(reload).toHaveBeenCalledTimes(1);
  });
});
```

Uwaga jsdom: jeśli `PromiseRejectionEvent` nie istnieje w jsdom, zbuduj event ręcznie: `const ev = new Event('unhandledrejection') as PromiseRejectionEvent; Object.defineProperty(ev, 'reason', { value: new Error('...') });` — dostosuj test, nie implementację.

- [ ] **Step 2: Uruchom, ma paść** — `npx vitest run src/test/firestore-crash-guard.test.ts` → FAIL (module not found).

- [ ] **Step 3: Implementacja**

```ts
// src/lib/firestore-crash-guard.ts
// Po `INTERNAL ASSERTION FAILED` (np. b815 po resume WKWebView) instancja
// Firestore jest martwa do końca życia strony — każda kolejna operacja rzuca.
// Jedyne skuteczne wyjście to pełny reload. Draft treningu przeżywa
// (IndexedDB + fallback localStorage), więc reload jest bezpieczny.
export const RELOAD_GUARD_KEY = 'ss_firestore_crash_reload_at';
const RELOAD_WINDOW_MS = 2 * 60 * 1000;

export const isFirestoreInternalAssertion = (input: unknown): boolean => {
  const message = input instanceof Error ? input.message : typeof input === 'string' ? input : '';
  return message.includes('INTERNAL ASSERTION FAILED');
};

/** true = wolno przeładować (i zapisuje znacznik); anti-loop na wypadek crash-przy-starcie. */
export const shouldAutoReload = (nowMs: number): boolean => {
  try {
    const last = Number(localStorage.getItem(RELOAD_GUARD_KEY) ?? 0);
    if (Number.isFinite(last) && nowMs - last < RELOAD_WINDOW_MS) return false;
    localStorage.setItem(RELOAD_GUARD_KEY, String(nowMs));
    return true;
  } catch {
    return false; // brak localStorage = nie ryzykuj pętli reloadów
  }
};

export const installFirestoreCrashGuard = (reload: () => void): void => {
  const maybeReload = (candidate: unknown) => {
    if (!isFirestoreInternalAssertion(candidate)) return;
    if (!shouldAutoReload(Date.now())) return;
    reload();
  };
  window.addEventListener('unhandledrejection', (event) => maybeReload(event.reason));
  window.addEventListener('error', (event) => maybeReload(event.error ?? event.message));
};
```

- [ ] **Step 4: Instalacja w main.tsx** — obok istniejącej inicjalizacji telemetrii błędów (PRZED renderem Reacta):

```ts
import { installFirestoreCrashGuard } from '@/lib/firestore-crash-guard';
installFirestoreCrashGuard(() => window.location.reload());
```

- [ ] **Step 5: ErrorBoundary — restart zamiast nawigacji przy asercji** — w `src/components/ErrorBoundary.tsx` znajdź render fallbacku (przycisk „Wróć na Dashboard", `grep -n "Dashboard\|window.location\|navigate" src/components/ErrorBoundary.tsx`). Zmiana: gdy `isFirestoreInternalAssertion(this.state.error)`, przycisk ma etykietę z istniejącego mini-słownika ErrorBoundary (dodaj wpis `restart: 'Uruchom ponownie' / 'Restart app'` do obu języków słownika) i robi `window.location.reload()` zamiast nawigacji. Pozostałe błędy: zachowanie bez zmian.

- [ ] **Step 6: Testy zielone** — `npx vitest run src/test/firestore-crash-guard.test.ts` PASS + `npm run test` + `npm run typecheck`.
- [ ] **Step 7: Commit** — `git add src/lib/firestore-crash-guard.ts src/test/firestore-crash-guard.test.ts src/main.tsx src/components/ErrorBoundary.tsx && git commit -m "fix(stability): crash-guard Firestore — kontrolowany reload po INTERNAL ASSERTION (FIX-A T1)"`

---

### Task 2: Sprzątacz blokad body — koniec czarnego ekranu po awarii sheeta

Kontekst: czarny nie-do-zamknięcia ekran po wejściu w „Twoje liczby" = otwarty Radix Sheet odmontowany awaryjnie (ErrorBoundary przejął drzewo) zostawia na `<body>` `pointer-events: none` + scroll-lock (dokładnie mechanizm regresji builda 92).

**Files:**
- Create: `src/lib/release-body-locks.ts`
- Test: `src/test/release-body-locks.test.ts`
- Modify: `src/components/ErrorBoundary.tsx` (wywołanie w componentDidCatch)

**Interfaces:**
- Produces: `releaseBodyLocks(): void` (idempotentny; bezpieczny bez DOM — try/catch).

- [ ] **Step 1: Failing test**

```ts
// src/test/release-body-locks.test.ts
import { describe, expect, it } from 'vitest';
import { releaseBodyLocks } from '@/lib/release-body-locks';

describe('releaseBodyLocks', () => {
  it('zdejmuje pointer-events/overflow i atrybut scroll-lock Radixa', () => {
    document.body.style.pointerEvents = 'none';
    document.body.style.overflow = 'hidden';
    document.body.setAttribute('data-scroll-locked', '1');
    releaseBodyLocks();
    expect(document.body.style.pointerEvents).toBe('');
    expect(document.body.style.overflow).toBe('');
    expect(document.body.hasAttribute('data-scroll-locked')).toBe(false);
  });

  it('jest idempotentny na czystym body', () => {
    expect(() => { releaseBodyLocks(); releaseBodyLocks(); }).not.toThrow();
  });
});
```

- [ ] **Step 2: Uruchom, ma paść** (module not found).
- [ ] **Step 3: Implementacja**

```ts
// src/lib/release-body-locks.ts
// Awaryjny unmount otwartego Radix Sheet/Dialog zostawia body z
// pointer-events:none i scroll-lockiem (regresja b.92 — "czarny ekran").
// ErrorBoundary woła to przy każdym złapanym błędzie.
export const releaseBodyLocks = (): void => {
  try {
    document.body.style.pointerEvents = '';
    document.body.style.overflow = '';
    document.body.removeAttribute('data-scroll-locked');
    // Osierocone overlaye portali Radix (bez Reacta nikt ich nie zdejmie).
    document.querySelectorAll('[data-radix-popper-content-wrapper], [data-state="open"][data-aria-hidden]')
      .forEach((el) => el.remove());
  } catch {
    // brak DOM / środowisko testowe — nic do sprzątania
  }
};
```

- [ ] **Step 4: Wywołanie w ErrorBoundary** — w `componentDidCatch` (albo `getDerivedStateFromError` — tam, gdzie klasa już loguje błąd) dodaj `releaseBodyLocks();` z importem. Dzięki temu fallback „Coś poszło nie tak" jest zawsze klikalny.

- [ ] **Step 5: Testy zielone + commit** — `git add src/lib/release-body-locks.ts src/test/release-body-locks.test.ts src/components/ErrorBoundary.tsx && git commit -m "fix(stability): releaseBodyLocks w ErrorBoundary — czarny ekran po awarii sheeta (FIX-A T2)"`

---

### Task 3: Zakończ trening — zwykły przycisk + potwierdzenie (koniec press-and-hold)

Kontekst: press-and-hold (ring 900 ms) zawodził na siłowni (drgnięcie palca = `onPointerLeave` anuluje hold; user nie mógł zakończyć treningu i timer nabił 1:18:44). Decyzja usera: powrót do zwykłego przycisku z potwierdzeniem. Przepływ potwierdzenia JUŻ ISTNIEJE (`showCompleteConfirm`, `WorkoutDay.tsx:2901-2920` — pasek [Anuluj]/[Potwierdź]).

**Files:**
- Modify: `src/pages/WorkoutDay.tsx:2921-2931` (blok `HoldToFinishButton` — numery mogły się przesunąć, `grep -n "HoldToFinishButton" src/pages/WorkoutDay.tsx`)
- Delete: `src/components/HoldToFinishButton.tsx`, `src/test/hold-to-finish.test.tsx`

- [ ] **Step 1: Podmiana w WorkoutDay** — zamiast bloku `<HoldToFinishButton ...>`:

```tsx
// Zwykły przycisk + istniejące potwierdzenie (decyzja usera 2026-08-13:
// press-and-hold zawodził na siłowni — drgnięcie palca anulowało hold).
<Button
  size="lg"
  className="kinetic-primary-button w-full py-6 text-base hover:brightness-105"
  onClick={() => setShowCompleteConfirm(true)}
  disabled={isExplicitSaving}
  data-testid="finish-workout"
>
  <Check className="h-5 w-5 mr-2" />
  {t('workout.finishWorkout')}
</Button>
```

Usuń import `HoldToFinishButton` z WorkoutDay. Klucz `workout.holdToFinishHint` zostaje w locale (osierocony, nieszkodliwy).

- [ ] **Step 2: Usuń osierocone pliki** — `git rm src/components/HoldToFinishButton.tsx src/test/hold-to-finish.test.tsx`.
- [ ] **Step 3: Testy** — `npm run test` + `npm run typecheck`; e2e dotykające przycisku (`e2e/full-app.spec.ts:819,1503`, `e2e/resume-after-kill.spec.ts:89`) asertują tylko widoczność `getByRole('button', { name: 'Zakończ trening' })` — przechodzą bez zmian; jeśli któryś e2e KLIKA zakończenie (`grep -rn "hold-to-finish" e2e/`), zamień na `click()` + klik w `t('workout.confirmFinish')`.
- [ ] **Step 4: Szybki e2e** — `npx playwright test full-app.spec.ts resume-after-kill.spec.ts` → PASS.
- [ ] **Step 5: Commit** — `git add -u && git add src/pages/WorkoutDay.tsx && git commit -m "fix(workout): zakończenie treningu zwykłym przyciskiem z potwierdzeniem (FIX-A T3)"`

---

### Task 4: „Błąd zapisu" tylko przy realnym zagrożeniu danych

Kontekst: czerwony chip `workout.status.error` + karta `saveError` pochodzą z JEDNEGO miejsca: `persistDraftSnapshot` (`WorkoutDay.tsx:698-705`) — catch po `workoutDraftDb.saveActiveDraft`. Łańcuch zapisu (`workout-draft-db.ts:904-916`): IDB → retry na świeżym połączeniu → `withFallbackSave` (localStorage). Jeśli fallback zadziała, błąd NIE leci — czyli czerwony stan u usera = wszystkie 3 ścieżki padły ALBO `withFallbackSave` rzucił (QuotaExceeded). Zadanie: (a) twarda telemetria który etap padł, (b) automatyczny retry zamiast natychmiastowego czerwonego, (c) czerwony dopiero po 2 totalnych failach z rzędu.

**Files:**
- Modify: `src/lib/workout-draft-db.ts` (`withFallbackSave` w try/catch + typowany błąd `DraftSaveTotalFailure`)
- Modify: `src/pages/WorkoutDay.tsx:677-708` (`persistDraftSnapshot`: retry + licznik failów)
- Test: `src/test/workout-draft-db.test.ts` (rozszerzenie istniejącego — `ls src/test | grep draft`)
- i18n: `pl.ts`/`en.ts`

**Interfaces:**
- Produces: `class DraftSaveTotalFailure extends Error { stage: 'idb' | 'fallback' }` eksportowana z `workout-draft-db.ts`.

- [ ] **Step 1: Failing test** — dopisz do istniejącego pliku testów draft-db (znajdź: `ls src/test | grep -i draft`); jeśli testy mockują IDB, wymuś ścieżkę fallbacku:

```ts
it('total failure: gdy IDB i localStorage padną, leci DraftSaveTotalFailure ze stage', async () => {
  // wymuś błąd IDB (istniejący wzorzec mocka w tym pliku) ORAZ localStorage:
  const setItem = vi.spyOn(Storage.prototype, 'setItem').mockImplementation(() => {
    throw new DOMException('QuotaExceededError');
  });
  await expect(workoutDraftDb.saveActiveDraft(draftFixture)).rejects.toMatchObject({ stage: 'fallback' });
  setItem.mockRestore();
});

it('fallback OK: zapis do localStorage NIE rzuca (user nie widzi błędu)', async () => {
  // błąd tylko IDB — saveActiveDraft ma się rozwiązać bez wyjątku
  await expect(workoutDraftDb.saveActiveDraft(draftFixture)).resolves.toBeUndefined();
});
```

(Dokładny sposób wymuszenia błędu IDB przejmij z istniejących testów tego pliku — jest tam wzorzec resetu/mocka połączenia. Jeśli go nie ma, zamockuj `indexedDB.open` przez `vi.stubGlobal`.)

- [ ] **Step 2: Uruchom, ma paść.**
- [ ] **Step 3: Implementacja w workout-draft-db** — na górze pliku:

```ts
export class DraftSaveTotalFailure extends Error {
  constructor(public stage: 'idb' | 'fallback') {
    super(`draft save failed at ${stage}`);
  }
}
```

W łańcuchu zapisu (`saveActiveDraft`, okolice :904-916) zamień gołe `withFallbackSave(normalized)` na:

```ts
try {
  withFallbackSave(normalized);
} catch {
  throw new DraftSaveTotalFailure('fallback');
}
```

(`withFallbackSave` używa `localStorage.setItem` — QuotaExceeded przy pełnym storage to realny przypadek totalnego faila.)

- [ ] **Step 4: Retry + degradacja w persistDraftSnapshot** — w catch (`WorkoutDay.tsx:698-705`):

```tsx
} catch (error) {
  // Fallback localStorage łapie wszystko poza totalnym failem — nie strasz
  // czerwonym przy pierwszym potknięciu: jeden cichy retry po 3 s, czerwony
  // stan dopiero przy DRUGIM totalnym failu z rzędu (dane wtedy realnie
  // tylko w pamięci Reacta).
  draftFailStreakRef.current += 1;
  if (uid) {
    trackTelemetryEvent(uid, 'local_save_failed', {
      stage: error instanceof DraftSaveTotalFailure ? error.stage : 'unknown',
      streak: draftFailStreakRef.current,
    });
  }
  if (draftFailStreakRef.current === 1) {
    window.setTimeout(() => { void persistDraftSnapshot(overrides, options); }, 3000);
  } else {
    setSaveError(t('workout.err.localSaveFailed'));
    setAutoSaveStatus('error');
  }
  return null;
}
```

Na sukcesie (początek `try` po `saveActiveDraft`): `draftFailStreakRef.current = 0;`. Deklaracja obok innych refów: `const draftFailStreakRef = useRef(0);`. Jeśli `trackTelemetryEvent` nie przyjmuje trzeciego argumentu-obiektu (`grep -n "trackTelemetryEvent" src/lib/app-telemetry.ts`), przekaż stage w nazwie eventu: `` `local_save_failed_${stage}` `` — dostosuj szczegół, nie intencję.

- [ ] **Step 5: Copy czerwonego stanu** — `workout.err.localSaveFailed` ma mówić prawdę o sytuacji total-fail; zaktualizuj w OBU locale:

```ts
// pl.ts
'workout.err.localSaveFailed': 'Nie mogę zapisać na tym urządzeniu (pamięć pełna?). Nie zamykaj apki — dane tej sesji są tylko na ekranie.',
// en.ts
'workout.err.localSaveFailed': 'Cannot save on this device (storage full?). Keep the app open — this session lives only on screen.',
```

- [ ] **Step 6: Testy zielone + commit** — `npm run test`, `npm run typecheck`; `git add src/lib/workout-draft-db.ts src/pages/WorkoutDay.tsx src/i18n/locales/pl.ts src/i18n/locales/en.ts src/test/<plik-testow-draft> && git commit -m "fix(save): czerwony błąd zapisu tylko po totalnym failu z retry (FIX-A T4)"`

---

## Wydanie FIX-A

- [ ] `npm run test && npm run typecheck && npm run lint && npm run build && npm run check:no-emoji`
- [ ] E2e pełne na świeżym vite (`pkill -f vite`, wyczyść `node_modules/.vite` przez mv).
- [ ] Checklist wdrożeniowy z CLAUDE.md: web `npm run deploy` + weryfikacja hasha live, iOS bump **101** + `scripts/release-ios.sh` (dystrybucja w kroku [2/2]), Android versionCode **17** + AAB `jar verified`, wpis DECYZJE.md.
- [ ] Scenariusz sekwencji: start z planu → serie → wyjście → powrót → zakończ (przycisk+potwierdzenie) → sync.
