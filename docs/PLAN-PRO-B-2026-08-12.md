# PLAN PRO-B: Profil do headera, centrum powiadomień, Postępy w nawigacji

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Avatar w headerze otwiera Profil, dzwonek z badge otwiera lokalny inbox powiadomień, a zwolniony 5. slot dolnej nawigacji dostaje Postępy (`/achievements`).

**Architecture:** Trzy niezależne warstwy: (1) czysty moduł inboxa na localStorage per uid, (2) UI headera (avatar + dzwonek + istniejący licznik), (3) rekonfiguracja nawigacji (mobile nav, rootPaths, i18n). Inbox jest lokalny (bez Firestore) — zasilany zdarzeniami w apce; pierwszym źródłem są PR-y po treningu. Sheet powiadomień montowany NA STAŁE w headerze, sterowany wyłącznie `open` (lekcja regresji builda 92: nigdy nie unmountować otwartego Radix Sheet).

**Tech Stack:** React 18 + TS, lucide-react, Radix Sheet (shadcn `@/components/ui/sheet`), localStorage, vitest + RTL.

## Global Constraints

- Wzorzec z rynku: avatar = jedyna trasa do Profilu na mobile (nie duplikować w bottom nav). Sidebar desktopowy zostaje bez zmian (sekcja Konto ma `/profile`).
- Sheet/Dialog Radix: zamykanie tylko przez `open=false`, komponent zawsze zamontowany (CLAUDE.md, pułapka builda 92).
- i18n: nowe klucze do OBU plików `pl.ts` i `en.ts`.
- Header renderuje się na każdym ekranie poza focused flows — bez nowych szerokich listenerów Firestore (komentarz Z216 w `AppHeader.tsx:20-22`). Inbox czyta z localStorage, zero odczytów sieciowych.
- Bez zmian w `/workout/*` (strefa chroniona).
- Po każdym tasku: `npm run test` + `npm run typecheck` zielone, commit.

---

### Task 1: Moduł inboxa `notification-inbox.ts`

**Files:**
- Create: `src/lib/notification-inbox.ts`
- Test: `src/lib/__tests__/notification-inbox.test.ts`

**Interfaces:**
- Produces:
  - `type InboxItemType = 'pr' | 'badge' | 'week' | 'plan' | 'system'`
  - `interface InboxItem { id: string; type: InboxItemType; title: string; body?: string; date: string; read: boolean }`
  - `addInboxItem(uid: string, item: Omit<InboxItem, 'id' | 'read' | 'date'> & { date?: string }): void`
  - `getInbox(uid: string): InboxItem[]` (najnowsze pierwsze)
  - `unreadCount(uid: string): number`
  - `markAllRead(uid: string): void`
- Konsumowane przez Task 2 (UI) i Task 4 (emisja PR); plan PRO-D użyje `type: 'badge'`.

- [ ] **Step 1: Failing test**

```ts
// src/lib/__tests__/notification-inbox.test.ts
import { beforeEach, describe, expect, it } from 'vitest';
import { addInboxItem, getInbox, markAllRead, unreadCount } from '@/lib/notification-inbox';

const UID = 'user-1';

describe('notification-inbox', () => {
  beforeEach(() => localStorage.clear());

  it('dodaje wpis jako nieprzeczytany, najnowsze pierwsze', () => {
    addInboxItem(UID, { type: 'pr', title: 'Rekord: Przysiad 100 kg', date: '2026-08-01T10:00:00Z' });
    addInboxItem(UID, { type: 'week', title: 'Tydzień domknięty', date: '2026-08-02T10:00:00Z' });
    const items = getInbox(UID);
    expect(items).toHaveLength(2);
    expect(items[0].title).toBe('Tydzień domknięty');
    expect(unreadCount(UID)).toBe(2);
  });

  it('markAllRead zeruje licznik, wpisy zostają', () => {
    addInboxItem(UID, { type: 'pr', title: 'x' });
    markAllRead(UID);
    expect(unreadCount(UID)).toBe(0);
    expect(getInbox(UID)).toHaveLength(1);
  });

  it('trzyma max 50 wpisów (FIFO od najstarszych)', () => {
    for (let i = 0; i < 55; i++) {
      addInboxItem(UID, { type: 'system', title: `n${i}`, date: new Date(2026, 0, 1, 0, i).toISOString() });
    }
    expect(getInbox(UID)).toHaveLength(50);
    expect(getInbox(UID)[0].title).toBe('n54');
  });

  it('izoluje per uid i przeżywa uszkodzony JSON', () => {
    localStorage.setItem('ss_inbox_v1_user-2', '{zepsute');
    addInboxItem(UID, { type: 'pr', title: 'x' });
    expect(getInbox('user-2')).toEqual([]);
    expect(getInbox(UID)).toHaveLength(1);
  });
});
```

- [ ] **Step 2: Uruchom, ma paść** — `npx vitest run src/lib/__tests__/notification-inbox.test.ts`.
- [ ] **Step 3: Implementacja**

```ts
// src/lib/notification-inbox.ts
// Lokalny inbox powiadomień (PRO-B). Bez sieci: zdarzenia emitują ekrany apki,
// storage per uid, limit 50. Wersjonowany klucz na wypadek zmiany kształtu.
export type InboxItemType = 'pr' | 'badge' | 'week' | 'plan' | 'system';

export interface InboxItem {
  id: string;
  type: InboxItemType;
  title: string;
  body?: string;
  date: string; // ISO
  read: boolean;
}

const MAX_ITEMS = 50;
const key = (uid: string) => `ss_inbox_v1_${uid}`;

const load = (uid: string): InboxItem[] => {
  try {
    const raw = localStorage.getItem(key(uid));
    const parsed = raw ? JSON.parse(raw) : [];
    return Array.isArray(parsed) ? parsed : [];
  } catch {
    return [];
  }
};

const save = (uid: string, items: InboxItem[]) => {
  try {
    localStorage.setItem(key(uid), JSON.stringify(items.slice(0, MAX_ITEMS)));
  } catch {
    // pełny storage nie może wywrócić treningu — inbox jest best-effort
  }
};

export const addInboxItem = (
  uid: string,
  item: Omit<InboxItem, 'id' | 'read' | 'date'> & { date?: string },
): void => {
  const entry: InboxItem = {
    id: `${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
    read: false,
    date: item.date ?? new Date().toISOString(),
    type: item.type,
    title: item.title,
    body: item.body,
  };
  const items = [entry, ...load(uid)];
  items.sort((a, b) => (a.date < b.date ? 1 : -1));
  save(uid, items);
  window.dispatchEvent(new CustomEvent('ss-inbox-change'));
};

export const getInbox = (uid: string): InboxItem[] => load(uid);
export const unreadCount = (uid: string): number => load(uid).filter((i) => !i.read).length;

export const markAllRead = (uid: string): void => {
  save(uid, load(uid).map((i) => ({ ...i, read: true })));
  window.dispatchEvent(new CustomEvent('ss-inbox-change'));
};
```

- [ ] **Step 4: Test zielony.**
- [ ] **Step 5: Commit** — `git commit -m "feat(inbox): lokalny inbox powiadomień per uid (PRO-B T1)"`

---

### Task 2: Dzwonek + sheet powiadomień w headerze

**Files:**
- Create: `src/components/NotificationBell.tsx`
- Modify: `src/components/AppHeader.tsx:58-97` (prawa strona)
- Test: `src/components/__tests__/notification-bell.test.tsx`
- i18n: `src/i18n/locales/pl.ts` + `en.ts`

**Interfaces:**
- Consumes: `getInbox`, `unreadCount`, `markAllRead` z Taska 1.

- [ ] **Step 1: Klucze i18n (oba pliki)**

```ts
// pl.ts
'inbox.title': 'Powiadomienia',
'inbox.open': 'Otwórz powiadomienia',
'inbox.empty.title': 'Wszystko ogarnięte',
'inbox.empty.desc': 'Rekordy, raporty tygodnia i zmiany planu pojawią się tutaj.',
// en.ts
'inbox.title': 'Notifications',
'inbox.open': 'Open notifications',
'inbox.empty.title': 'All caught up',
'inbox.empty.desc': 'Records, weekly reports and plan changes will show up here.',
```

- [ ] **Step 2: Failing test**

```tsx
// src/components/__tests__/notification-bell.test.tsx
import { describe, expect, it, vi, beforeEach } from 'vitest';
import { fireEvent, render, screen } from '@testing-library/react';
import { NotificationBell } from '@/components/NotificationBell';
import { addInboxItem } from '@/lib/notification-inbox';

vi.mock('@/contexts/LanguageContext', () => ({
  useTranslation: () => ({ t: (k: string) => k, lang: 'pl' }),
}));

describe('NotificationBell', () => {
  beforeEach(() => localStorage.clear());

  it('bez nieprzeczytanych: brak kropki', () => {
    render(<NotificationBell uid="u1" />);
    expect(screen.queryByTestId('inbox-unread-dot')).toBeNull();
  });

  it('nieprzeczytany wpis pokazuje kropkę, otwarcie czyta wszystko', () => {
    addInboxItem('u1', { type: 'pr', title: 'Rekord: Przysiad 100 kg' });
    render(<NotificationBell uid="u1" />);
    expect(screen.getByTestId('inbox-unread-dot')).toBeTruthy();
    fireEvent.click(screen.getByLabelText('inbox.open'));
    expect(screen.getByText('Rekord: Przysiad 100 kg')).toBeTruthy();
    expect(screen.queryByTestId('inbox-unread-dot')).toBeNull();
  });

  it('pusty inbox po otwarciu pokazuje empty state', () => {
    render(<NotificationBell uid="u1" />);
    fireEvent.click(screen.getByLabelText('inbox.open'));
    expect(screen.getByText('inbox.empty.title')).toBeTruthy();
  });
});
```

- [ ] **Step 3: Uruchom, ma paść.**
- [ ] **Step 4: Implementacja**

```tsx
// src/components/NotificationBell.tsx
import { useEffect, useState } from 'react';
import { Bell, CalendarCheck, ClipboardList, Info, Trophy } from 'lucide-react';
import { Sheet, SheetContent, SheetHeader, SheetTitle } from '@/components/ui/sheet';
import { getInbox, markAllRead, unreadCount, type InboxItem } from '@/lib/notification-inbox';
import { useTranslation } from '@/contexts/LanguageContext';
import { dateLocale } from '@/i18n';

const TYPE_ICONS = {
  pr: Trophy,
  badge: Trophy,
  week: CalendarCheck,
  plan: ClipboardList,
  system: Info,
} as const;

export const NotificationBell = ({ uid }: { uid: string }) => {
  const { t, lang } = useTranslation();
  const [open, setOpen] = useState(false);
  const [items, setItems] = useState<InboxItem[]>([]);
  const [unread, setUnread] = useState(0);

  useEffect(() => {
    const refresh = () => {
      setItems(getInbox(uid));
      setUnread(unreadCount(uid));
    };
    refresh();
    window.addEventListener('ss-inbox-change', refresh);
    return () => window.removeEventListener('ss-inbox-change', refresh);
  }, [uid]);

  const handleOpenChange = (next: boolean) => {
    setOpen(next);
    if (next) markAllRead(uid);
  };

  return (
    <>
      <button
        type="button"
        aria-label={t('inbox.open')}
        onClick={() => handleOpenChange(true)}
        className="relative flex h-9 w-9 items-center justify-center rounded-full text-muted-foreground transition-colors hover:bg-muted hover:text-foreground"
      >
        <Bell className="h-5 w-5" />
        {unread > 0 && (
          <span data-testid="inbox-unread-dot" className="absolute right-1.5 top-1.5 h-2 w-2 rounded-full bg-primary" />
        )}
      </button>

      {/* Zawsze zamontowany, sterowany open — lekcja builda 92. */}
      <Sheet open={open} onOpenChange={handleOpenChange}>
        <SheetContent side="right" className="w-full max-w-sm border-0 bg-surface-low">
          <SheetHeader>
            <SheetTitle className="font-heading uppercase">{t('inbox.title')}</SheetTitle>
          </SheetHeader>
          {items.length === 0 ? (
            <div className="flex flex-col items-center gap-3 py-16 text-center">
              <div className="flex h-16 w-16 items-center justify-center rounded-full bg-surface-highest">
                <Bell className="h-7 w-7 text-muted-foreground" />
              </div>
              <p className="font-semibold">{t('inbox.empty.title')}</p>
              <p className="max-w-[26ch] text-sm text-muted-foreground">{t('inbox.empty.desc')}</p>
            </div>
          ) : (
            <div className="mt-4 space-y-2 overflow-y-auto">
              {items.map((item) => {
                const Icon = TYPE_ICONS[item.type];
                return (
                  <div key={item.id} className="flex items-start gap-3 rounded-xl bg-surface-container p-3">
                    <div className="mt-0.5 flex h-8 w-8 shrink-0 items-center justify-center rounded-lg bg-primary/10">
                      <Icon className="h-4 w-4 text-primary" />
                    </div>
                    <div className="min-w-0">
                      <p className="text-sm font-medium">{item.title}</p>
                      {item.body && <p className="text-xs text-muted-foreground">{item.body}</p>}
                      <p className="mt-0.5 text-[11px] text-muted-foreground/70">
                        {new Date(item.date).toLocaleDateString(dateLocale(lang), { day: 'numeric', month: 'short' })}
                      </p>
                    </div>
                  </div>
                );
              })}
            </div>
          )}
        </SheetContent>
      </Sheet>
    </>
  );
};
```

W `AppHeader.tsx` (prawa strona, przed licznikiem treningów):

```tsx
import { NotificationBell } from '@/components/NotificationBell';
// w JSX, wewnątrz <div className="flex items-center gap-2">, przed chipem offline:
{uid && <NotificationBell uid={uid} />}
```

- [ ] **Step 5: Testy zielone + typecheck.**
- [ ] **Step 6: Commit** — `git commit -m "feat(header): dzwonek powiadomień z lokalnym inboxem (PRO-B T2)"`

---

### Task 3: Avatar w headerze + Postępy w bottom nav

**Files:**
- Modify: `src/components/AppHeader.tsx:46-56` (lewa strona)
- Modify: `src/components/AppNavigation.tsx:23-35` (navItems), `:227` (mobile slice)
- Modify: `src/components/Layout.tsx:24` (rootPaths)
- i18n: `pl.ts` + `en.ts` (klucz `nav.progress`)
- Test: `src/components/__tests__/app-navigation-mobile.test.tsx`

- [ ] **Step 1: Failing test**

```tsx
// src/components/__tests__/app-navigation-mobile.test.tsx
import { describe, expect, it, vi } from 'vitest';
import { render } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import { AppNavigation } from '@/components/AppNavigation';

vi.mock('@/contexts/LanguageContext', () => ({
  useTranslation: () => ({ t: (k: string) => k, lang: 'pl' }),
}));
vi.mock('@/contexts/UserContext', () => ({
  useCurrentUser: () => ({ profile: { displayName: 'Test' }, isAdmin: false }),
}));
vi.mock('@/hooks/useAuth', () => ({ useAuth: () => ({ logout: vi.fn() }) }));

describe('AppNavigation mobile', () => {
  it('bottom nav: 5. slot to /achievements, /profile poza navem', () => {
    const { container } = render(<MemoryRouter><AppNavigation /></MemoryRouter>);
    const mobileLinks = Array.from(
      container.querySelectorAll('nav[aria-label="nav.ariaMobile"] a'),
    ).map((a) => a.getAttribute('href'));
    expect(mobileLinks).toEqual(['#/', '#/plan', '#/analytics', '#/exercises', '#/achievements']);
  });
});
```

Uwaga: projekt używa HashRouter w apce, ale w teście `MemoryRouter` daje href bez `#` — po pierwszym uruchomieniu testu dostosuj asercję do faktycznych `href` (`['/','/plan','/analytics','/exercises','/achievements']`).

- [ ] **Step 2: Uruchom, ma paść** (dziś 5. slot = `/profile`).
- [ ] **Step 3: Implementacja nawigacji** — w `AppNavigation.tsx`:

```tsx
// navItems — pierwsze 5 = mobilny bottom nav; Profil wypada z mobile (wejście przez avatar w headerze):
const navItems = [
  { to: '/', icon: Home, labelKey: 'nav.dashboard' as const },
  { to: '/plan', icon: Calendar, labelKey: 'nav.plan' as const },
  { to: '/analytics', icon: BarChart3, labelKey: 'nav.analytics' as const },
  { to: '/exercises', icon: Library, labelKey: 'nav.exercises' as const },
  { to: '/achievements', icon: Trophy, labelKey: 'nav.progress' as const },
  // Pozostałe — sidebar (desktop)
  { to: '/history', icon: ScrollText, labelKey: 'nav.history' as const },
  { to: '/measurements', icon: Ruler, labelKey: 'nav.measurements' as const },
  { to: '/cycles', icon: History, labelKey: 'nav.cycles' as const },
  { to: '/profile', icon: User, labelKey: 'nav.profile' as const },
];
```

`NAV_GROUPS` bez zmian (achievements już jest w grupie progress, profile w koncie). i18n:

```ts
// pl.ts
'nav.progress': 'Postępy',
// en.ts
'nav.progress': 'Progress',
```

W `Layout.tsx:24`:

```tsx
const rootPaths = new Set(['/', '/plan', '/history', '/exercises', '/achievements', '/analytics']);
```

(`/profile` wypada z rootPaths → dostaje strzałkę wstecz, wejście z avatara; `/analytics` dochodzi, bo jest w bottom nav i nie może mieć strzałki donikąd.)

- [ ] **Step 4: Avatar w AppHeader** — lewa strona, tylko na ekranach bez strzałki:

```tsx
import { useNavigate } from 'react-router-dom';
import { useCurrentUser } from '@/contexts/UserContext'; // uid już importowany — dodaj profile
// w komponencie:
const { uid, profile } = useCurrentUser();
const navigate = useNavigate();
const displayName = profile?.displayName || '';
const initials = displayName.split(' ').map((n) => n[0]).join('').toUpperCase().slice(0, 2) || 'SS';

// w JSX, w lewym <div className="flex items-center gap-3">, PRZED tytułem, zamiast onBack || avatar:
{onBack ? (
  <Button variant="ghost" size="icon" onClick={onBack} aria-label={t('comp.header.back')}>
    <ArrowLeft className="h-5 w-5" />
  </Button>
) : (
  <button
    type="button"
    onClick={() => navigate('/profile')}
    aria-label={t('nav.profile')}
    data-testid="header-avatar"
    className="h-9 w-9 shrink-0 overflow-hidden rounded-full ring-1 ring-border transition-transform active:scale-95"
  >
    {profile?.photoURL ? (
      <img src={profile.photoURL} alt="" className="h-full w-full object-cover" referrerPolicy="no-referrer" />
    ) : (
      <span className="flex h-full w-full items-center justify-center bg-surface-highest text-xs font-bold text-primary">
        {initials}
      </span>
    )}
  </button>
)}
```

- [ ] **Step 5: Testy zielone** — nowy test nav + `npm run test` (aktualizuj e2e/testy odwołujące się do zakładki Profil: `grep -rn "nav.profile\|/profile" e2e/ tests/ src/**/__tests__ | grep -i nav`).
- [ ] **Step 6: Scenariusz ręczny** — mobile viewport: avatar → Profil (strzałka wstecz działa), 5. zakładka → Postępy, desktop sidebar bez zmian.
- [ ] **Step 7: Commit** — `git commit -m "feat(nav): avatar w headerze, Postępy w bottom nav (PRO-B T3)"`

---

### Task 4: Emisja zdarzeń PR do inboxa

**Files:**
- Modify: `src/pages/WorkoutDay.tsx` — miejsce finalizacji treningu, gdzie wywoływany jest toast `workout.toast.newPRTitle` (`grep -n "newPRTitle" src/pages/WorkoutDay.tsx`)

**Interfaces:**
- Consumes: `addInboxItem` (Task 1); `PRComparison { exerciseName, type, newValue, oldValue }` z `src/lib/pr-utils.ts:169`.

- [ ] **Step 1: Implementacja** — obok istniejącego toastu PR (nie zamiast), dla każdego wykrytego PR:

```tsx
import { addInboxItem } from '@/lib/notification-inbox';
// w bloku, gdzie po zakończeniu treningu jest lista `prs: PRComparison[]`:
prs.forEach((pr) => {
  addInboxItem(uid, {
    type: 'pr',
    title: t('inbox.pr.title', { name: pr.exerciseName }),
    body: t('inbox.pr.body', { value: fmtPrValue(pr) }),
  });
});
```

Gdzie `fmtPrValue` to istniejący formatter wartości PR używany w podsumowaniu (ta sama funkcja co `prValue` w `WorkoutCompletionSequence.tsx:73-77` — jeśli jest lokalna, wynieś ją do `src/lib/pr-utils.ts` jako `formatPRValue(pr, fmt)` i użyj w obu miejscach). Klucze i18n:

```ts
// pl.ts
'inbox.pr.title': 'Nowy rekord: {name}',
'inbox.pr.body': '{value}',
// en.ts
'inbox.pr.title': 'New record: {name}',
'inbox.pr.body': '{value}',
```

- [ ] **Step 2: Test** — rozszerz istniejący test completion (`grep -rln "newPRTitle\|detectNewPRs" src --include=*.test.*`) o asercję: po zakończeniu treningu z PR `getInbox(uid)` zawiera wpis `type: 'pr'`. Jeśli test completion nie istnieje, dodaj unit test na wywołanie `addInboxItem` z zamockowanym localStorage.
- [ ] **Step 3: Scenariusz ręczny (sekwencja!)** — trening z PR → zakończ → Dashboard: kropka na dzwonku → otwórz: wpis PR → zamknij → kropka znika. Potem: start treningu z planu → wyjście → powrót (niezmiennik: nic nie zniknęło).
- [ ] **Step 4: Commit** — `git commit -m "feat(inbox): PR-y trafiają do centrum powiadomień (PRO-B T4)"`

---

## Wydanie

- [ ] `npm run test && npm run typecheck && npm run lint && npm run build && npm run check:no-emoji`
- [ ] e2e: `npx playwright test` — najpierw świeży dev server (`pkill -f vite`, wyczyść `node_modules/.vite` — CLAUDE.md zasada 9).
- [ ] Checklist wdrożeniowy z CLAUDE.md (web deploy, iOS bump, TestFlight, DECYZJE.md).
