# PLAN X12A: zaufanie do danych. Wskrzeszony plan, local-wins konfliktu, "Kontynuuj trening" (2026-07-17)

> **Dla agenta wykonawcy:** WYMAGANY SUB-SKILL: `superpowers:executing-plans` (albo `superpowers:subagent-driven-development`). Kroki mają checkboxy `- [ ]`. Wykonuj zadania PO KOLEI, fazy rozdzielone checkpointami. Nie przeskakuj checkpointów.
> Kontekst: plany Z13-Z28 (R1), Z29-Z46 (R2), Z47-Z65 (X10), Z66-Z84 (X11) i Z85 (hotfix białego ekranu) są WYKONANE I WDROŻONE (web index-BN5cnETa + iOS build 51 na TestFlight).
> Źródło znalezisk: realny incydent usera z treningu (screenshoty 2026-07-06 i okolice): (1) po jednym treningu "odnowił się" stary plan trzydniowy z poprzedniego cyklu i wyskoczył błąd konfliktu planu, (2) w trakcie treningu pojawił się dialog "Trening edytowany na innym urządzeniu" + badge "Błąd zapisu", (3) po przypadkowym wyjściu z aplikacji Dashboard pokazywał "Rozpocznij dzisiejszy trening" zamiast "Kontynuuj trening".
> Wszystkie odwołania plik:linia z rozpoznania 2026-07-17. Przed edycją ZAWSZE zweryfikuj aktualny stan pliku.

**Goal:** Użytkownik na siłowni nigdy nie traci planu, nie widzi dialogów o konfliktach i zawsze może wrócić do przerwanego treningu jednym tapnięciem. Trzy rzeczy: (1) root cause i fix wskrzeszonego planu z poprzedniego cyklu + błędu PLAN_CONFLICT, (2) konflikt rewizji treningu rozwiązywany automatycznie na korzyść wersji lokalnej (zero dialogu), (3) każdy nieukończony dzisiejszy szkic = "Kontynuuj trening" na Dashboardzie. Na końcu release train: web + iOS na TestFlight.

**Architektura podejścia:** Najpierw diagnoza incydentu na danych produkcyjnych (read-only) i repro testami, dopiero potem fixy. Zmiany minimalne: local-wins to zmiana WYŁĄCZNIE w warstwie decyzji WorkoutDay (silnik syncu nietykalny), "Kontynuuj trening" to nowa czysta funkcja obok `shouldResumeWorkoutDraft` (auto-nawigacja zostaje ostrzejsza, karta Dashboardu dostaje łagodniejszy warunek).

**Tech stack:** React + TypeScript + Vite, Firebase (Firestore + rules), IndexedDB + localStorage, Capacitor iOS, vitest (`src/test/`) + Playwright (`e2e/`, mock chromium przez `npm run e2e:mock`).

---

## 0. AUTORYZACJE I TWARDE ZASADY

1. **Deploy na produkcję bez pytania o zgodę** wyłącznie w FAZIE 4 (release train). Autoryzacja stała usera z 2026-07-03 (X10/X11): "wszystkie zmiany mają polecieć od razu na produkcję; dostęp do aplikacji mam tylko ja, działam na TestFlight". Uruchomienie tego planu = potwierdzenie autoryzacji.
2. **Naprawa danych usera (Z86.5): autonomicznie, z obowiązkowym backupem.** Autoryzacja usera z 2026-07-17: nie pytaj o zgodę, nie zatrzymuj się. Bezpiecznik zamiast pytania: przed KAŻDYM zapisem korygującym pełny backup dokumentów do tmp/ (poza gitem) + diff przed/po w raporcie i DECYZJE.md. Minimalna ingerencja: dotykasz wyłącznie dokumentów objętych incydentem (plan, cykle), NIGDY treningów i pomiarów. Dane usera są święte, loguje treningi od początku roku.
3. Kontrakty R1/R2/X10/X11 NIETYKALNE: `workout-sync-engine.ts`, kolejka referencyjna, writeId/pendingWriteVersion, snapshot draftu, tombstone promocji, maszyna stanów (`workout-session-state.ts`), hydracja (`workout-hydration.ts`). Z87 zmienia wyłącznie obsługę wyniku `outcome.conflict` w `WorkoutDay.tsx` i czyste helpery w `workout-sync-conflict.ts`.
4. Środowisko docelowe: siłownia, ekran gaśnie, iOS wstrzymuje JS. Wszystko, co ma przeżyć tło, żyje w IndexedDB/localStorage.
5. TDD z bezpiecznikiem: każde zadanie zaczyna failing test odtwarzający problem. **Jeśli test NIE potwierdza znaleziska: STOP dla zadania, wpis do DECYZJE.md, idź dalej.**
6. i18n: zmiany kluczy ZAWSZE w OBU plikach `src/i18n/locales/pl.ts` i `en.ts` (typecheck pilnuje). Usuwasz kod, usuń osierocone klucze i importy (rg przed i po).
7. Chirurgicznie: jedno zadanie = izolowane commity `fix|feat|refactor(obszar): opis (Z<nr>)`. Zero poprawiania sąsiedniego kodu.
8. Bramki checkpointów (minimum): `npm run test`, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run check:bundle-budget`, `npm run e2e:mock`. Zanotuj liczbę testów baseline w raporcie.
9. Wpis do DECYZJE.md po każdej fazie (co, dlaczego, root cause, weryfikacja).
10. Pułapki narzędziowe: globalny `firebase` z PATH (nie `npx firebase`); RTK hook przepisuje `npx cap`, używaj `./node_modules/.bin/cap`; haki e2e `fittracker_e2e_workouts`, `fittracker_e2e_cycles`, helpery `setE2EWorkouts`, `setE2ECycles`, `writeWorkoutDraftDb` w `e2e/helpers.ts`.

---

## FAZA 0: Baseline

### Zadanie Z86.0: zielony baseline

- [x] Krok 1: `git status` czyste (jeśli nie: STOP, zgłoś userowi), `git log --oneline -3` do raportu.
- [x] Krok 2: `npm run test`, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run check:bundle-budget`, `npm run e2e:mock`. Wszystko zielone albo STOP.

---

## FAZA 1: Z86. Wskrzeszony stary plan + PLAN_CONFLICT (diagnoza, fix, regresja)

**Obraz incydentu:** User ukończył trening. Potem: (a) aktywny stał się jego STARY plan trzydniowy z poprzedniego cyklu, Dashboard pokazywał "Tydzień 1 z 12" (czyli powstał ŚWIEŻY cykl ze starych dni), (b) pojawił się błąd konfliktu planu. `PLAN_CONFLICT` rzuca `saveTrainingPlanWithRevision` (`src/lib/training-plan-save.ts:53`), gdy `expectedRevision` (stan Reacta `planRevision` z `useTrainingPlan.ts:141`) nie zgadza się z rewizją w Firestore. Czyli: JAKIŚ kod wywołał `savePlan` ze stanem nieaktualnym, a JAKIŚ kod skutecznie zapisał stare dni i utworzył nowy cykl.

**Hipotezy (kolejność badania):**
- **H1 (główna): auto-przedłużenie / "Powtórz plan" użyło dni ze STAREGO cyklu.** M33 dodał auto-przedłużenie planu przeterminowanego >7 dni przez `startCycleWithPlan`. Wywołania: `Dashboard.tsx:140`, `Cycles.tsx:60`, `NewPlan.tsx:141`. Jeśli źródłem `days` jest cykl (np. `active?.days` albo cykl z listy) zamiast bieżącego planu, a stan cykli był nieświeży (stale closure, urządzenie po wybudzeniu z tła ze starym snapshotem), powstaje świeży cykl ze starych dni. To tłumaczy "Tydzień 1 z 12".
- **H2: wyścig dwóch zapisów planu.** Auto-przedłużenie odpala się na dwóch urządzeniach / dwóch instancjach (iPhone + web na Macu) albo dubluje się w jednej sesji (remount Dashboardu). Pierwszy zapis wygrywa i podbija rewizję, drugi dostaje PLAN_CONFLICT (to widział user). Sprawdź guard: czy trigger auto-przedłużenia ma trwały guard jak `runCycleAutoRepair` (`cycle-actions.ts:119`), czy tylko stan Reacta.
- **H3: druga instancja ze stale stanem po powrocie z tła.** iOS wstrzymuje JS; po wybudzeniu stare `plan`/`planRevision`/`activeCycle` w zamknięciach mogą odpalić logikę "plan przeterminowany" zanim onSnapshot dośle świeży stan. Sprawdź, czy trigger auto-przedłużenia czeka na `isLoaded` planu ORAZ cykli i czy reaguje na powrót z tła.
- **H4 (mało prawdopodobna): import danych.** `useFirebaseWorkouts.ts:332` zapisuje `training_plans` przez `setDoc merge` BEZ podbicia rewizji (celowo: import). User nie zgłaszał importu; sprawdź w telemetrii, wyklucz.

### Zadanie Z86.1: dowody z produkcji (read-only)

- [x] Krok 1: Napisz jednorazowy skrypt `tmp/x12-diagnoza.mjs` (Node, firebase-admin, credentials przez `GOOGLE_APPLICATION_CREDENTIALS` lub `gcloud auth application-default login`; projekt `fittracker-workouts`). Skrypt TYLKO CZYTA i wypisuje dla uid admina (g.jasionowicz@gmail.com):
  - `training_plans/{uid}`: `revision`, `updatedAt`, `startDate`, `durationWeeks`, liczba dni + nazwy dni,
  - `plan_cycles` where `userId==uid` posortowane po `createdAt` desc (10 szt.): `id`, `status`, `startDate`, `endDate`, `createdAt`, liczba dni. Szukamy: cykl utworzony w dniu incydentu, DWA aktywne cykle, cykl active ze starymi dniami,
  - `client_errors` where `uid==uid` z okna 2026-07-01..2026-07-17: `code`, `phase`, `detail`, `createdAt` (struktura: `src/lib/error-telemetry.ts`).
- [x] Krok 2: Uruchom, zapisz wynik do `tmp/x12-diagnoza-wynik.txt`. NIE wklejaj do repo, nie commituj (tmp/ w .gitignore, zweryfikuj).
- [x] Krok 3: Ustal timeline: kiedy powstał podejrzany cykl, co było w `detail` błędów PLAN_CONFLICT, czy błędy revision-conflict treningu (Z87) korelują czasowo. Wnioski do raportu i DECYZJE.md.

### Zadanie Z86.2: przeczytaj kod ścieżek podejrzanych

- [x] Krok 1: Przeczytaj W CAŁOŚCI: `src/lib/cycle-actions.ts`, `src/hooks/useTrainingPlan.ts`, `src/hooks/usePlanCycles.ts`, `src/lib/plan-next-step.ts` oraz fragmenty `Dashboard.tsx` wokół linii 120-170 (trigger auto-przedłużenia i "Powtórz plan") i `Cycles.tsx` wokół linii 40-80.
- [x] Krok 2: Odpowiedz pisemnie (w raporcie zadania) na pytania: (a) skąd dokładnie pochodzą `days` przekazywane do `startCycleWithPlan` w każdym z 3 wywołań, (b) jaki warunek odpala auto-przedłużenie i czy może odpalić się na stale stanie / podwójnie, (c) co się dzieje, gdy `savePlan` wewnątrz `startCycleWithPlan` rzuci PLAN_CONFLICT PO utworzeniu nowego aktywnego cyklu (czy zostaje osierocony aktywny cykl ze starymi dniami?). Uwaga na kolejność w `startCycleWithPlan` (`cycle-actions.ts:44`): najpierw `savePlan`, potem `createActiveCycle`, na końcu archiwizacja; przy częściowej porażce sprawdź ścieżki rollbacku.

### Zadanie Z86.3: repro testem (bezpiecznik TDD)

- [x] Krok 1: Do `src/test/cycle-actions.test.ts` dopisz testy odtwarzające potwierdzoną hipotezę. Minimum dwa scenariusze (dostosuj do wyników Z86.1/Z86.2, poniżej szkielet dla H1+H2):

```ts
describe('Z86: wskrzeszenie starego planu', () => {
  it('auto-przedłużenie używa dni BIEŻĄCEGO planu, nie dni przeterminowanego cyklu', async () => {
    // deps z currentPlan = [dzień A, B, C, D] (plan 4-dniowy)
    // i przeterminowanym cyklem o dniach [stary1, stary2, stary3]
    // po wywołaniu ścieżki auto-przedłużenia: savePlan dostał dni A-D, NIE stare1-3
  });

  it('drugi równoległy start cyklu nie nadpisuje planu po PLAN_CONFLICT', async () => {
    // savePlan pierwszego wywołania: success; drugiego: rzuca Error('PLAN_CONFLICT')
    // oczekiwane: drugi zwraca { success: false }, createActiveCycle drugiego NIE został wywołany,
    // brak drugiego aktywnego cyklu
  });
});
```

- [x] Krok 2: `npm run test -- cycle-actions` i potwierdź, że nowe testy FAILUJĄ na obecnym kodzie (odtwarzają buga). Jeśli NIE failują: STOP, wróć do hipotez, wpis do DECYZJE.md co wykluczono.

### Zadanie Z86.4: fix root cause

- [x] Krok 1: Napisz minimalny fix potwierdzonego mechanizmu. Kierunki (wybierz wg diagnozy, nie wdrażaj wszystkich na ślepo):
  - dni do auto-przedłużenia ZAWSZE z bieżącego planu (`plan` z `useTrainingPlan`), nigdy ze snapshotu cyklu,
  - trigger auto-przedłużenia z trwałym guardem (wzorzec `runCycleAutoRepair` z `cycle-actions.ts:119`: guard ustawiany przed async, czyszczony przy porażce) + warunek `isLoaded` planu i cykli,
  - `startCycleWithPlan`: przy porażce `savePlan` NIE twórz cyklu; przy porażce `createActiveCycle` istniejący rollback planu zostaje (jest w `cycle-actions.ts:60`), dołóż test.
- [x] Krok 2: `npm run test -- cycle-actions`: testy Z86.3 przechodzą. Cały `npm run test` zielony.
- [x] Krok 3: Jeżeli fix dotyka warunku w komponencie (Dashboard/Cycles): wyciągnij warunek do czystej funkcji w `src/lib/` i przetestuj ją unitowo (komponentów nie testujemy integracyjnie w tym projekcie).
- [x] Krok 4: Commit `fix(cycles): <root cause jednym zdaniem> (Z86)`.

### Zadanie Z86.5: naprawa danych usera (AUTONOMICZNIE, Z OBOWIĄZKOWYM BACKUPEM)

Autoryzacja usera 2026-07-17: wykonaj naprawę BEZ pytania o zgodę i bez zatrzymywania pętli. Bezpiecznik zamiast pytania: backup + diff w raporcie.

- [x] Krok 1: Na podstawie Z86.1 ustal docelowy stan konta admina: który cykl jaki status, jaki plan aktywny. Format roboczy: tabela "dokument, pole, wartość teraz, wartość po naprawie". Zasada minimalnej ingerencji: WYŁĄCZNIE `training_plans/{uid}` i `plan_cycles`, NIGDY treningi i pomiary.
- [x] Krok 2: Backup: zrzuć PEŁNĄ treść wszystkich dokumentów do zmiany do `tmp/x12-backup-<data>.json` (tmp/ poza gitem, zweryfikuj .gitignore). Bez poprawnego, odczytanego z powrotem backupu NIE wykonuj zapisu. *(bezprzedmiotowe: naprawa zbędna, patrz krok 4)*
- [x] Krok 3: Wykonaj naprawę skryptem (rozszerzenie `tmp/x12-diagnoza.mjs` o tryb `--apply`), zweryfikuj ponownym odczytem. Tabela diff przed/po trafia do raportu i DECYZJE.md. *(bezprzedmiotowe: naprawa zbędna, patrz krok 4)*
- [x] Krok 4: Jeśli stan konta jest już poprawny (user naprawił ręcznie przez UI), odnotuj w DECYZJE.md i pomiń. *(stan poprawny, naprawa pominięta, wpis w DECYZJE.md)*

### CHECKPOINT FAZY 1

- [x] `npm run test`, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run check:bundle-budget`, `npm run e2e:mock` zielone.
- [x] Wpis do DECYZJE.md: root cause, mechanizm, fix, dowody (bez danych wrażliwych), wynik naprawy danych.

---

## FAZA 2: Z87. Konflikt rewizji treningu: local-wins, bez dialogu

**Stan obecny:** `WorkoutDay.tsx:601` przy `outcome.conflict` otwiera `AlertDialog` (linie ~2381-2394) z wyborem "Zachowaj moją wersję" / "Pobierz z chmury". Decyzja produktowa usera (2026-07-17): **wersja lokalna wygrywa ZAWSZE, dialogu nie pokazujemy.** Mechanika "zachowaj moją" już istnieje: `resolveConflictKeepMine` (`WorkoutDay.tsx:695`) podbija znaczniki chmury w drafcie (`cloudUpdatedAt`/`cloudRevision` ze stanu serwera) i ponawia sync.

**Ochrona przed pętlą:** jeśli drugie urządzenie aktywnie pisze, auto-retry mógłby się zapętlić (konflikt, podbicie, konflikt...). Limit prób na sesję zapisu + reset po sukcesie. Po wyczerpaniu limitu: zostajemy przy lokalnym drafcie (danych nie tracimy, dosyła kolejny checkpoint), pokazujemy istniejący `saveError` z klucza `workout.err.newerVersion`.

### Zadanie Z87.1: czysty helper decyzji (test first)

- [ ] Krok 1: Do `src/test/workout-sync-conflict.test.ts` dopisz:

```ts
import { shouldAutoResolveConflict, MAX_CONFLICT_AUTO_RESOLVES } from '@/lib/workout-sync-conflict';

describe('Z87: local-wins auto-resolve', () => {
  it('pozwala na auto-resolve poniżej limitu', () => {
    expect(shouldAutoResolveConflict(0)).toBe(true);
    expect(shouldAutoResolveConflict(MAX_CONFLICT_AUTO_RESOLVES - 1)).toBe(true);
  });
  it('blokuje po wyczerpaniu limitu', () => {
    expect(shouldAutoResolveConflict(MAX_CONFLICT_AUTO_RESOLVES)).toBe(false);
    expect(shouldAutoResolveConflict(MAX_CONFLICT_AUTO_RESOLVES + 5)).toBe(false);
  });
});
```

- [ ] Krok 2: `npm run test -- workout-sync-conflict`: FAIL (funkcja nie istnieje).
- [ ] Krok 3: W `src/lib/workout-sync-conflict.ts` dodaj:

```ts
// Local-wins (Z87): konflikt rewizji rozwiązujemy automatycznie na korzyść wersji
// lokalnej. Limit chroni przed pętlą, gdy drugie urządzenie aktywnie pisze; po
// wyczerpaniu zostajemy przy lokalnym drafcie (kolejny checkpoint dośle dane).
export const MAX_CONFLICT_AUTO_RESOLVES = 2;

export const shouldAutoResolveConflict = (attemptsSoFar: number): boolean =>
  attemptsSoFar < MAX_CONFLICT_AUTO_RESOLVES;
```

- [ ] Krok 4: `npm run test -- workout-sync-conflict`: PASS. Commit `feat(workout-sync): decyzja auto-resolve local-wins (Z87)`.

### Zadanie Z87.2: przepięcie WorkoutDay na auto-resolve

- [ ] Krok 1: W `WorkoutDay.tsx`:
  - dodaj `const conflictAutoResolveAttemptsRef = useRef(0);`,
  - zmień nazwę `resolveConflictKeepMine` na `keepLocalOnConflict` i USUŃ z niej `setConflictDialogOpen` (obie linie). Reszta ciała bez zmian (podbicie znaczników + retry). Funkcja jest zdefiniowana PO `syncDraftToFirebase`, więc do wywołania z gałęzi konfliktu użyj refa: `const keepLocalOnConflictRef = useRef<null | (() => Promise<void>)>(null);` i po definicji `keepLocalOnConflictRef.current = keepLocalOnConflict;`,
  - w gałęzi `if (outcome.conflict)` (okolice linii 601) zastąp otwarcie dialogu:

```ts
if (outcome.conflict) {
  // Local-wins (Z87): wersja lokalna zawsze wygrywa. Telemetria zostaje,
  // żeby widzieć skalę zjawiska po wyłączeniu dialogu.
  void reportClientError(uid, {
    code: 'revision-conflict',
    phase: requiresFinalSync ? 'final' : 'checkpoint',
    detail: outcome.error,
    sessionId: targetSessionId,
  });
  if (shouldAutoResolveConflict(conflictAutoResolveAttemptsRef.current)) {
    conflictAutoResolveAttemptsRef.current += 1;
    trackTelemetryEvent(uid, 'revision_conflict_auto_resolved');
    setSaveError(null);
    setAutoSaveStatus(requiresFinalSync ? 'final-sync-pending' : 'syncing');
    void keepLocalOnConflictRef.current?.();
    return { success: false, error: outcome.error };
  }
  setSaveError(t('workout.err.newerVersion'));
  setAutoSaveStatus(requiresFinalSync ? 'final-sync-pending' : 'error');
  return { success: false, error: outcome.error };
}
```

  - w ścieżce sukcesu syncu (zaraz po `const syncedAt = Date.now(); setSaveError(null);`) dodaj `conflictAutoResolveAttemptsRef.current = 0;`.
- [ ] Krok 2: Usuń martwe elementy: stan `conflictDialogOpen` (linia ~160) i jego wpis w obiekcie debug (linia ~374), `resolveConflictUseCloud` (linie ~722-735), blok `<AlertDialog>` konfliktu (linie ~2381-2394), nieużywane importy AlertDialog jeśli nic innego ich nie używa (rg w pliku).
- [ ] Krok 3: Usuń osierocone klucze i18n z OBU locale: `workout.conflict.title`, `workout.conflict.desc`, `workout.conflict.keepMine`, `workout.conflict.useCloud` (pl.ts ~1022-1025 + odpowiedniki en.ts). Klucz `workout.err.newerVersion` (pl.ts ~1017) ZOSTAJE (używany po wyczerpaniu limitu). `rg "workout.conflict" src/` = 0 trafień.
- [ ] Krok 4: Weryfikacja kluczy: `rg "conflictDialogOpen|resolveConflictUseCloud|resolveConflictKeepMine" src/` = 0 trafień. `npm run test`, `npm run typecheck`, `npm run lint` zielone.
- [ ] Krok 5: Commit `feat(workout): konflikt rewizji rozwiązywany automatycznie local-wins, bez dialogu (Z87)`.

### Zadanie Z87.3: scenariusz ręczny wielourządzeniowy (potwierdzenie)

- [ ] Krok 1: Symulacja konfliktu bez drugiego urządzenia: w DevTools (web, konto testowe e2e/emulator, NIE realne konto) zmodyfikuj `cloudRevision` aktywnego draftu w IndexedDB na wartość niższą niż serwerowa, odhacz serię, obserwuj: brak dialogu, zapis przechodzi, w telemetrii event `revision_conflict_auto_resolved`. Jeśli projekt ma prostszy sposób wywołania konfliktu w emulatorze (dwie karty na koncie emulatora), użyj go.
- [ ] Krok 2: Opisz w raporcie krok po kroku scenariusz terenowy dla usera (iPhone + web równolegle) do potwierdzenia po release.

### CHECKPOINT FAZY 2

- [ ] Pełne bramki z sekcji 0 pkt 8 zielone.
- [ ] Wpis do DECYZJE.md (jawnie: cofamy decyzję M18 o dialogu, powód: decyzja produktowa usera 2026-07-17, dialog wyskoczył w trakcie treningu na siłowni; telemetria zjawiska zostaje).

---

## FAZA 3: Z88. "Kontynuuj trening" dla każdego nieukończonego dzisiejszego szkicu

**Root cause (potwierdzony w kodzie):** `shouldResumeWorkoutDraft` (`src/lib/workout-resume.ts`) wymaga `isAlive = draft.dirty || sessionOrigin === 'provisional'`. Szkic W PEŁNI zsynchronizowany (autosave zdążył, `dirty=false`, origin `remote`) NIE jest "żywy", więc Dashboard (`Dashboard.tsx` ~581, warunek `continueDraft` oparty o `draftResume.resume`) pokazuje "Rozpocznij" zamiast "Kontynuuj". Fix: karta Dashboardu dostaje WŁASNY, łagodniejszy warunek (czysta funkcja). Auto-nawigacja (`ActiveWorkoutResume`) celowo zostaje na ostrym warunku, żeby nie porywać usera, który świadomie wyszedł.

### Zadanie Z88.1: czysta funkcja + testy (test first)

- [ ] Krok 1: Do `src/test/workout-resume.test.ts` dopisz (użyj istniejącego `makeDraft`):

```ts
import { isDraftContinuableToday, continuableDraftTarget } from '@/lib/workout-resume';

describe('isDraftContinuableToday (Z88)', () => {
  it('w pełni zsynchronizowany dzisiejszy szkic JEST kontynuowalny (dirty=false, remote)', () => {
    const draft = makeDraft({ dirty: false, sessionOrigin: 'remote', lastFirebaseSyncAt: NOW });
    expect(isDraftContinuableToday(draft, TODAY)).toBe(true);
  });
  it('szkic ukończony lokalnie NIE jest kontynuowalny', () => {
    expect(isDraftContinuableToday(makeDraft({ completedLocally: true }), TODAY)).toBe(false);
  });
  it('szkic czekający na finalny sync NIE jest kontynuowalny', () => {
    expect(isDraftContinuableToday(makeDraft({ finalSyncPending: true }), TODAY)).toBe(false);
  });
  it('szkic z innego dnia NIE jest kontynuowalny', () => {
    expect(isDraftContinuableToday(makeDraft({ date: '2026-07-02' }), TODAY)).toBe(false);
  });
  it('brak szkicu = false', () => {
    expect(isDraftContinuableToday(null, TODAY)).toBe(false);
  });
  it('target prowadzi do treningu z sesją szkicu', () => {
    expect(continuableDraftTarget(makeDraft())).toBe(
      `/workout/day-1?date=${TODAY}&session=workout-u1-day-1-2026-07-03`,
    );
  });
});
```

- [ ] Krok 2: `npm run test -- workout-resume`: FAIL (funkcje nie istnieją).
- [ ] Krok 3: W `src/lib/workout-resume.ts` dodaj:

```ts
// Z88: warunek karty "Dzisiejszy trening" na Dashboardzie. Łagodniejszy niż auto-resume:
// KAŻDY nieukończony dzisiejszy szkic jest kontynuowalny, także w pełni zsynchronizowany
// (dirty=false). Auto-nawigacja (shouldResumeWorkoutDraft) celowo zostaje ostrzejsza.
export const isDraftContinuableToday = (
  draft: ActiveWorkoutDraft | null,
  todayStr: string,
): draft is ActiveWorkoutDraft => {
  if (!draft) return false;
  if (draft.completedLocally || draft.finalSyncPending) return false;
  return draft.date === todayStr;
};

export const continuableDraftTarget = (draft: ActiveWorkoutDraft): string =>
  `/workout/${draft.dayId}?date=${draft.date}&session=${draft.sessionId}`;
```

- [ ] Krok 4: `npm run test -- workout-resume`: PASS. Commit `feat(workout-resume): kontynuowalność dzisiejszego szkicu niezależna od dirty (Z88)`.

### Zadanie Z88.2: przepięcie karty Dashboardu

- [ ] Krok 1: W `Dashboard.tsx` w bloku karty dzisiejszego treningu (~581) zamień warunek:

```ts
// PRZED:
const continueDraft = draftResume.resume
  && localDraft
  && localDraft.dayId === todayTraining.dayId
  && localDraft.date === todayTraining.dateStr
  ? { target: draftResume.target, completedSets: ... }
  : null;

// PO:
const continueDraft = isDraftContinuableToday(localDraft, todayTraining.dateStr)
  && localDraft.dayId === todayTraining.dayId
  ? {
    target: continuableDraftTarget(localDraft),
    completedSets: Object.values(localDraft.exerciseSets).flat().filter(s => s.completed).length,
  }
  : null;
```

  Import obu funkcji z `@/lib/workout-resume`. Sprawdź, czy `draftResume` jest jeszcze gdzieś używany w Dashboardzie (karta Sync Center wyżej go używa: NIE ruszaj tamtej karty, to inny przypadek: zaległości syncu).
- [ ] Krok 2: `npm run typecheck`, `npm run test` zielone.
- [ ] Krok 3: Commit `fix(dashboard): Kontynuuj trening także dla zsynchronizowanego szkicu (Z88)`.

### Zadanie Z88.3: e2e potwierdzające

- [ ] Krok 1: Nowy spec `e2e/continue-workout.spec.ts` (wzorzec: istniejące specy + `e2e/helpers.ts`). Scenariusz: `writeWorkoutDraftDb` wstrzykuje szkic aktywny na DZIŚ z `dirty: false`, `sessionOrigin: 'remote'`, `completedLocally: false`, `finalSyncPending: false`, `dayId` zgodny z dzisiejszym dniem planu e2e; wejście na Dashboard; asercje: przycisk z tekstem "Kontynuuj trening" widoczny, "Rozpocznij" niewidoczny, klik prowadzi do `/workout/<dayId>` (użyj `expectHashRoute`). Drugi scenariusz: szkic z `completedLocally: true` -> przycisk "Kontynuuj" NIEwidoczny.
- [ ] Krok 2: `npm run e2e:mock` zielony (całość, nie tylko nowy spec).
- [ ] Krok 3: Commit `test(e2e): Kontynuuj trening po wyjściu z aplikacji (Z88)`.

### CHECKPOINT FAZY 3

- [ ] Pełne bramki zielone. Wpis do DECYZJE.md.

---

## FAZA 4: Release train A

- [ ] Krok 1: Pełne bramki: `npm run test`, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run check:bundle-budget`, `npm run e2e:mock`, `npm run check:dist-smoke` (bramka Z85: bundle musi wystartować w headless Chromium).
- [ ] Krok 2: `git push` (main).
- [ ] Krok 3: Web: `npm run deploy` (gh-pages; sam push NIE aktualizuje strony). Zweryfikuj nowy hash indexu na https://grzegorzee.github.io/strength-save/.
- [ ] Krok 4: iOS: sprawdź aktualny `CURRENT_PROJECT_VERSION` w `ios/App/App.xcodeproj/project.pbxproj`, bump o 1 we WSZYSTKICH 6 wystąpieniach (pilnuje `release-ios-preflight.mjs`), commit. Potem `set -a; source _secrets/oauth/appstore-connect.env; set +a` i `scripts/ios-testflight.sh` (pipeline sam robi `build:mobile`; NIGDY `cap sync ios` po webowym `npm run deploy`).
- [ ] Krok 5: Poll statusu buildu (`uv run scripts/asc_api.py builds`) aż VALID, podpięcie do grupy Wewnętrzni wg istniejącego pipeline'u.
- [ ] Krok 6: Wpisy: DECYZJE.md (podsumowanie X12A), PLAN.md (odhacz pozycje), CONTEXT.md w FIRMA ("OSTATNIE DECYZJE" + "Ostatnia sesja").
- [ ] Krok 7: Raport dla usera: co naprawione, root cause Z86, scenariusz testu terenowego (trening z zgaszonym ekranem, wyjście z apki w trakcie, powrót przez "Kontynuuj trening", równoległa edycja web+iPhone bez dialogu).

---

## Samokontrola przed startem (dla agenta)

- Spec pokryty? Z86 (wskrzeszony plan + PLAN_CONFLICT), Z87 (local-wins bez dialogu), Z88 (Kontynuuj trening) = 3 zgłoszenia usera z 2026-07-17. TAK.
- Numery linii mogły się przesunąć: przed każdą edycją czytaj aktualny plik.
- Niczego nie wdrażaj przed FAZĄ 4. Naprawa danych usera (Z86.5) autonomicznie, ale NIGDY bez backupu z kroku 2.
