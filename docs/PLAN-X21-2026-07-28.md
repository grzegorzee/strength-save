# PLAN X21: Rozgrzewka + spójność i18n (2026-07-28)

> **Dla agenta wykonawcy:** WYMAGANY SUB-SKILL: `superpowers:executing-plans` lub `superpowers:subagent-driven-development`. Postęp = checkboxy `- [ ]` w tym pliku.
> Numeracja zadań: Z162-Z168 (X20 skończył na Z161).
> Źródło: zgłoszenie usera (odhaczenia rozgrzewki znikają + mieszane języki nazw) + pełny audyt i18n `docs/AUDYT-I18N-2026-07-28.md` (2026-07-28, zweryfikowany w kodzie).

**Goal:** (1) Odhaczenia rozgrzewki przeżywają zamknięcie dialogu i wyjście z apki (persystencja w drafcie sesji), (2) nazwy rozgrzewki/stretchingu w 100% po polsku w PL i po angielsku w EN, (3) wszystkie znalezione rozjazdy tłumaczeń naprawione (Strava, plan RZA, admin, a11y, push, mail zaproszenia), (4) fundament pod kolejne języki (np. hiszpański): rejestr `LANGUAGES` jako źródło prawdy, wzorzec nakładek per język, checklist dodania języka, test-guard na hardcodowane stringi.

**Architecture:** Stan odhaczeń rozgrzewki wchodzi jako pole additive `warmupChecked?: string[]` (klucze `nameKey`, nie indeksy) do istniejącego draftu sesji (`ActiveWorkoutDraft`, wzorzec jak `lastTouchedExerciseId`), dialog staje się kontrolowany. i18n: wszystkie stringi user-facing przez `t()`/`translate()`, binarne `lang === 'en'` zastąpione nakładkami `Record<LanguageCode, ...>` z fallbackiem PL.

**Tech Stack:** React + TS + Vite, vitest, IndexedDB (workout-draft-db), Firebase Functions, Capacitor iOS.

## 0. TWARDE ZASADY

1. **Dane usera są święte.** Zero zapisów na produkcyjnych danych. Testy na emulatorze / trybie e2e mock.
2. **WIP innych wątków NIE należy do X21.** W repo: zmodyfikowane `garmin/source/AppSettings.mc`, `garmin/source/DayView.mc` (Garmin v3 w toku, NIE kompiluje się, patrz `docs/PLAN-GARMIN-V3-2026-07-28.md`) oraz nietrackowany folder `animacje-cwiczen/` (699 MB wideo). **NIGDY `git add -A` / `git add .`** Stage'uj pliki imiennie. Plików garmin/* nie dotykać i nie commitować.
3. **Reguła 5 CLAUDE.md.** Nowa funkcja nie może zabrać niczego istniejącemu przepływowi: test niezmiennika starego przepływu przy każdej zmianie, sekwencje zamiast pojedynczych ekranów.
4. **i18n do OBU plików** (`src/i18n/locales/pl.ts` + `en.ts`), inaczej typecheck padnie. Kg kanoniczne, konwersje tylko w UI.
5. **Kanoniczne PL nietykalne.** Nazwy ćwiczeń/dni/focus zapisywane do Firestore, slugi CDN, klucze lookup: NIGDY nie tłumaczyć wartości zapisywanych, tylko wyświetlanie. Lista "świadomie PL" w audycie, sekcja "Świadomie PL".
6. **Bundle budget bez podnoszenia limitu.** Po X20 zapas ~16 KB (1 520 006 / 1 536 000 B initial JS). Nowe klucze locales (~150 × 2 języki) muszą się zmieścić; jeśli `check:bundle-budget` padnie, NIE podnosić limitu, tylko zgłosić w raporcie z propozycją (np. lazy locale dla `admin.*`).
7. **Surgical changes.** 7 zadań = 7 izolowanych commitów `(Z16x)` w opisie, rollback punktowy. Nieużywane komponenty shadcn (pagination, breadcrumb, sidebar) zostają nietknięte.
8. **Deploy:** web + functions (Z167) + iOS build **80** (bump `CURRENT_PROJECT_VERSION` ×6, `release-ios.sh`, potem OBOWIĄZKOWO `testflight_external.py 80`). Jeden build iOS na końcu trainu.

## ROOT CAUSE (zweryfikowane w kodzie 2026-07-28)

| # | Problem | Root cause | Dowód |
|---|---|---|---|
| Z162 | Odhaczenia rozgrzewki znikają po wyjściu i powrocie | `WarmupRoutineDialog` trzyma stan wyłącznie w lokalnym `useState<Set<number>>` (`:32`), a efekt `:63-68` jawnie robi `setChecked(new Set())` przy każdym `open === false` (X, Esc, overlay). Zero persystencji (dialog nie importuje żadnej warstwy draftu). Klucz = indeks pozycji (offset stretchingu zależy od `warmupExercises.length`, `:122`), więc niestabilny. Zero testów dialogu. | `src/components/WarmupRoutineDialog.tsx:32,63-68,122` |
| Z163 | Mieszane PL/EN w nazwach rozgrzewki | Wartości w polskim słowniku po angielsku: `'Jumping Jacks'`, `'Kręcenie biodrami (Hip Circles)'`, `'Cat-Cow (na czworakach)'`, `"Child's Pose"`, `'Pigeon Pose'` | `src/i18n/locales/pl.ts:1438-1440,1451-1452,1457` |
| Z164 | PL na ekranach EN (Strava, plan RZA, Watch preview) | Strefy HR mają `name` PL renderowane wprost; `getWeekLabel` hardcoded PL; `date-fns/locale/pl` na sztywno; brak `tpl-rza-3` w `PLAN_DESC`; payload podglądu Watch bez `lang` | `src/types/strava.ts:56-60`, `src/components/strava/HRZoneDistribution.tsx:33-38`, `src/components/StravaActivityDetail.tsx:152-164`, `src/lib/strava-utils.ts:72-76,2,284`, `src/lib/plan-i18n.ts:86-129`, `src/hooks/useWatchPlanPreview.ts:42-58,84-105` |
| Z165 | Panel admina pół na pół | 7 plików ma po 30-50 wywołań `t()` ORAZ ~70 literałów PL (dialogi, toasty, kafle, filtry) | `src/pages/admin/AdminDashboard.tsx:479-960`, `AdminCommsCard.tsx`, `UsersActivityTable.tsx`, `AdminFeatureFlagsCard.tsx`, `AdminUserLogs.tsx`, `admin-user-types.ts:6`, `src/lib/admin-audit.ts:50,54` |
| Z166 | Hardcodowane EN w UI | sr-only `Close`, aria `Expand/Collapse navigation`, `Max HR`, błędy `cycle-actions` po EN pokazywane userowi | `src/components/ui/dialog.tsx:49`, `ui/sheet.tsx:62`, `src/components/AppNavigation.tsx:134`, `src/pages/Settings.tsx:341,353`, `src/lib/cycle-actions.ts:89,122,137` |
| Z167 | Push i mail zaproszenia w 100% PL | `daily-reminder` ignoruje `users.language`; `inviteEmailHtml` bez parametru lang; subject zaproszenia hardcoded | `functions/src/daily-reminder.ts:114-115`, `functions/src/email-templates.ts:90-104`, `functions/src/registration.ts:780` |
| Z168 | Architektura dwujęzyczna, nie n-językowa | ~20 twardych `pl\|en` (dateLocale, selector języka, nakładki exercise/plan-i18n, typy), brak checklisty dodania języka, brak guardu na nowe hardcodowane stringi | `src/i18n/index.ts:35`, `src/pages/Profile.tsx:260-261`, `src/data/exercise-i18n.ts:534,549,569`, `src/lib/plan-i18n.ts:47-138`, `src/lib/user-profile.ts:69`, `src/lib/registration-api.ts:13-14,64` |

---

## FAZA 1: Z162. Persystencja odhaczeń rozgrzewki (P1, zgłoszenie usera)

**Files:**
- Modify: `src/components/WarmupRoutineDialog.tsx` (kontrolowany stan)
- Modify: `src/pages/WorkoutDay.tsx` (`:188` showWarmup, `:546-564` buildDraftSnapshot, `:566-602` persist/saveDraftSnapshot, `:843-854` hydratacja draftu, `:2413-2417` mount dialogu)
- Modify: `src/lib/workout-draft-db.ts` (`:12-52` interfejs `ActiveWorkoutDraft`, `:182-227` normalizeDraft, `:271-288` withFallbackSave)
- Modify: `src/lib/workout-draft.ts` (`:8-22` interfejs fallbacku localStorage)
- Modify: `src/lib/workout-draft-snapshot.ts` (`:10-29` DraftSnapshotContext, `:74-83` sameDraftContent, `:85` builder)
- Test: `src/test/warmup-routine-dialog.test.tsx` (NOWY), `src/test/workout-draft-db.test.ts`, `src/test/workout-draft-snapshot.test.ts`

**Interfaces:**
- Consumes: `workoutDraftDb.saveActiveDraft/loadDraft`, `buildWorkoutDraftSnapshot(context, overrides)`, wzorzec pola additive (`lastTouchedExerciseId`).
- Produces: `ActiveWorkoutDraft.warmupChecked?: string[]` (lista `nameKey`, np. `'warmup.jumpingJacks'`); props dialogu `checked: ReadonlySet<string>`, `onToggle: (nameKey: string) => void`.

- [x] **Krok 1: Failing testy dialogu kontrolowanego** (`src/test/warmup-routine-dialog.test.tsx`, wzorzec renderu z `LanguageProvider` jak w innych testach komponentów): DOWÓD: plik utworzony, 3 testy.

```tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { describe, it, expect, vi } from 'vitest';
import { WarmupRoutineDialog } from '@/components/WarmupRoutineDialog';
import { LanguageProvider } from '@/contexts/LanguageContext';
import { translate } from '@/i18n';

const noop = () => {};
const renderDialog = (over: Record<string, unknown> = {}) =>
  render(
    <LanguageProvider>
      <WarmupRoutineDialog
        focus="Klatka" open onOpenChange={noop}
        checked={new Set<string>()} onToggle={noop}
        {...over}
      />
    </LanguageProvider>,
  );

describe('WarmupRoutineDialog (kontrolowany)', () => {
  it('klik w pozycję woła onToggle z nameKey', () => {
    const onToggle = vi.fn();
    renderDialog({ onToggle });
    fireEvent.click(screen.getByText(translate('pl', 'warmup.jumpingJacks')));
    expect(onToggle).toHaveBeenCalledWith('warmup.jumpingJacks');
  });

  it('odhaczenia przychodzą z propsa i przeżywają cykl zamknij/otwórz', () => {
    const checked = new Set(['warmup.jumpingJacks']);
    const { rerender, container } = renderDialog({ checked });
    const item = () => screen.getByText(translate('pl', 'warmup.jumpingJacks'));
    expect(item().className).toContain('line-through');
    rerender(
      <LanguageProvider>
        <WarmupRoutineDialog focus="Klatka" open={false} onOpenChange={noop} checked={checked} onToggle={noop} />
      </LanguageProvider>,
    );
    rerender(
      <LanguageProvider>
        <WarmupRoutineDialog focus="Klatka" open onOpenChange={noop} checked={checked} onToggle={noop} />
      </LanguageProvider>,
    );
    expect(item().className).toContain('line-through');
  });

  it('licznik done/total liczy tylko pozycje z bieżącej listy', () => {
    const checked = new Set(['warmup.jumpingJacks', 'stretch.pigeonPose']); // pigeonPose NIE ma go w liście dla focus=Klatka
    renderDialog({ checked });
    // description dialogu: {focus} ... 1/11 (5 warmup + 6 stretch dla chest), nie 2/11
    expect(screen.getByText(/1\/11/)).toBeTruthy();
  });
});
```

- [x] **Krok 2: uruchom, potwierdź FAIL** (`npx vitest run src/test/warmup-routine-dialog.test.tsx`): oczekiwane błędy o nieznanych propsach / braku zachowania. DOWÓD: `PASS (0) FAIL (3)` — spy nie wołany, brak `line-through`, brak `1/11`.
- [x] **Krok 3: refaktor dialogu na kontrolowany** (`WarmupRoutineDialog.tsx`):
  - Props: `+ checked: ReadonlySet<string>; + onToggle: (nameKey: string) => void`.
  - USUŃ lokalny `checked`/`toggle`/reset `setChecked(new Set())` w efekcie `open` (reset `setTimerActive(false)` przy zamknięciu ZOSTAJE).
  - Odhaczenie po `nameKey`: `checked.has(ex.nameKey)`, `onClick={() => onToggle(ex.nameKey)}`, `key={ex.nameKey}` w obu sekcjach (usuwa offsetowy `idx`).
  - Licznik: `const done = allItems.filter(i => checked.has(i.nameKey)).length;` i `progress` z `done/allItems.length` (odporne na klucze spoza bieżącej listy po zmianie focusu).
- [x] **Krok 4: testy z kroku 1 PASS.** DOWÓD: `npx vitest run src/test/warmup-routine-dialog.test.tsx` → `PASS (3) FAIL (0)`.
- [x] **Krok 5: failing testy persystencji draftu**: DOWÓD: 5 FAIL (`expected undefined to deeply equal [ 'warmup.hipCircles' ]` itd.).
  - `workout-draft-db.test.ts`: round-trip `saveActiveDraft` → `loadDraft` zachowuje `warmupChecked: ['warmup.jumpingJacks','stretch.catCow']`; `normalizeDraft` odfiltrowuje nie-stringi; ścieżka fallbacku localStorage też zachowuje pole.
  - `workout-draft-snapshot.test.ts`: `sameDraftContent` zwraca `false`, gdy różni się tylko `warmupChecked` (odhaczenie = zmiana treści, bump `version`).
- [x] **Krok 6: implementacja pola additive**: (normalizeDraft: spread warunkowy jak inne pola opcjonalne, żeby legacy draft NIE dostawał klucza undefined)
  - `workout-draft-db.ts`: w interfejsie `ActiveWorkoutDraft` dodaj `warmupChecked?: string[];` (komentarz: klucze nameKey rozgrzewki/stretchingu, pole additive bez bumpu wersji DB). W `normalizeDraft`:

```ts
warmupChecked: Array.isArray(raw.warmupChecked)
  ? raw.warmupChecked.filter((k): k is string => typeof k === 'string')
  : undefined,
```

  - `workout-draft.ts` (fallback): `warmupChecked?: string[];` w interfejsie `WorkoutDraft` + przepuść pole w `withFallbackSave` (sprawdź mapowanie pól, `workout-draft-db.ts:271-288`).
  - `workout-draft-snapshot.ts`: `warmupChecked?: string[]` w `DraftSnapshotContext` i overrides; w `sameDraftContent` porównanie `JSON.stringify(a.warmupChecked ?? []) === JSON.stringify(b.warmupChecked ?? [])`.
- [x] **Krok 7: testy z kroku 5 PASS.** DOWÓD: `npx vitest run src/test/workout-draft-db.test.ts src/test/workout-draft-snapshot.test.ts` → `PASS (50) FAIL (0)`.
- [x] **Krok 8: okablowanie WorkoutDay**: (reset przez applyWorkoutState + adopcja draftu przy starcie; snapshot dokłada pole tylko gdy sesja ma/miała odhaczenia)
  - Stan: `const [warmupChecked, setWarmupChecked] = useState<string[]>([]);`
  - Hydratacja: w istniejącym bloku hydratacji draftu (`:843-854`) `if (draft.warmupChecked) setWarmupChecked(draft.warmupChecked);`
  - Handler:

```tsx
const toggleWarmupItem = useCallback((nameKey: string) => {
  setWarmupChecked(prev => {
    const next = prev.includes(nameKey) ? prev.filter(k => k !== nameKey) : [...prev, nameKey];
    saveDraftSnapshot({ warmupChecked: next });
    return next;
  });
}, [saveDraftSnapshot]);
```

  - `buildDraftSnapshot` (`:546-564`): dołóż `warmupChecked` do kontekstu.
  - Mount dialogu (`:2413-2417`): `checked={new Set(warmupChecked)} onToggle={toggleWarmupItem}` (memoizuj `Set` przez `useMemo` po `warmupChecked`).
- [x] **Krok 9: weryfikacja, że pole NIE wycieka do Firestore** DOWÓD: `grep -n warmupChecked src/lib/workout-sync-engine.ts src/lib/workout-sanitizers.ts` → exit 1 (0 trafień); payload budowany JAWNIE: `buildDraftExercisesPayload(draft)` + `saveOptions` z wyliczonych pól (`workout-sync-engine.ts:239,250-263`), `...draft` na `:224` to lokalny obiekt draftu, nie payload.: `grep -n "warmupChecked" src/lib/workout-sync-engine.ts src/lib/workout-sanitizers.ts` → 0 trafień, a payload syncu budowany jawnie z pól (nie spread draftu). Jeśli spread: jawnie wyklucz. DOWÓD w checkboxie (wynik grep + wskazanie miejsca budowy payloadu).
- [x] **Krok 10: test sekwencji (reguła 5)** DOWÓD: `warmup-routine-dialog.test.tsx` (sekwencja start→3 odhaczenia→powrót→nowa sesja + niezmiennik legacy) i `workout-draft-db.test.ts` (odhaczenia giną z sesją) → PASS (5).: w `warmup-routine-dialog.test.tsx` lub `workout-day-view.test.ts` (wzorzec harnessu): draft z `warmupChecked` → hydratacja → stan odhaczony; ORAZ niezmiennik starego przepływu: draft BEZ pola `warmupChecked` (legacy) ładuje się bez błędu, lista ćwiczeń dnia kompletna.
- [x] **Krok 11: bramki + commit** DOWÓD: `npm run test` → Test Files 118 passed, Tests 1093 passed; `typecheck` exit 0; `lint` exit 0.: `npm run test`, `npm run typecheck`, `npm run lint` zielone → commit `fix(warmup): odhaczenia rozgrzewki persystowane w drafcie sesji (Z162)` (imienny `git add`).

**Weryfikacja na urządzeniu (checklist przed buildem):** start treningu → otwórz rozgrzewkę → odhacz 3 pozycje → zamknij dialog → otwórz: odhaczenia SĄ → wyjdź z treningu do Dashboard → wróć → otwórz rozgrzewkę: odhaczenia SĄ → zgaś ekran 2 min → wróć: SĄ → zakończ trening → nowy trening: rozgrzewka CZYSTA (nowa sesja = nowy draft).

## FAZA 2: Z163. Nazwy rozgrzewki bez mieszania języków (P1, zgłoszenie usera)

**Files:**
- Modify: `src/i18n/locales/pl.ts:1438-1459`
- Test: `src/test/warmup-i18n.test.ts` (NOWY)

**Interfaces:** zmieniają się WYŁĄCZNIE wartości wyświetlane; klucze `warmup.*`/`stretch.*` (kanoniczne identyfikatory, od Z162 też w draftach) BEZ ZMIAN.

- [x] **Krok 1: failing test** `src/test/warmup-i18n.test.ts`: DOWÓD: plik utworzony (3 testy, w tym guard na pusty skan kluczy).

```ts
import { describe, it, expect } from 'vitest';
import { pl } from '@/i18n/locales/pl';
import { en } from '@/i18n/locales/en';

const nameKeys = (Object.keys(pl) as Array<keyof typeof pl>).filter(
  k => (k.startsWith('warmup.') || k.startsWith('stretch.')) && !k.includes('.dur.'),
);

describe('nazwy rozgrzewki/stretchingu bez mieszania języków', () => {
  it('PL bez angielskich wtrąceń', () => {
    const forbidden = /jumping|jacks|circles|pose|cat-cow|child|pigeon/i;
    for (const k of nameKeys) expect(pl[k], String(k)).not.toMatch(forbidden);
  });
  it('EN bez polskich znaków', () => {
    for (const k of nameKeys) expect(en[k], String(k)).not.toMatch(/[ąćęłńóśźż]/i);
  });
});
```

- [x] **Krok 2: potwierdź FAIL** (5 kluczy PL łamie regułę). DOWÓD: `PASS (2) FAIL (1)` — `warmup.jumpingJacks: expected 'Jumping Jacks' not to match /jumping|jacks|circles|pose|cat-cow|c…/i`.
- [x] **Krok 3: nowe wartości PL** (dokładne mapowanie, tylko `pl.ts`): DOWÓD: 6 podmian (jumpingJacks, hipCircles, armCircles, catCow, childsPose, pigeonPose).

```ts
'warmup.jumpingJacks': 'Pajacyki',
'warmup.hipCircles': 'Krążenia bioder',
'warmup.armCircles': 'Krążenia ramion',
// bodyweightSquats i lightJog już PL, bez zmian
'stretch.catCow': 'Koci grzbiet (na czworakach)',
'stretch.childsPose': 'Pozycja dziecka',
'stretch.pigeonPose': 'Pozycja gołębia',
```

- [x] **Krok 4: zweryfikuj EN** DOWÓD: `npx vitest run src/test/warmup-i18n.test.ts` → `PASS (3) FAIL (0)` (EN bez polskich znaków, PL bez angielskich wtrąceń). (`en.ts:1417-1443`): wszystkie nazwy po angielsku (audyt: tak). Test z kroku 1 PASS.
- [x] **Krok 5: bramki + commit**: DOWÓD: `npm run test` → Test Files 119 passed, Tests 1096 passed; `typecheck` exit 0.

## FAZA 3: Z164. Rozjazdy PL widoczne w trybie EN (P2)

**Files:**
- Modify: `src/types/strava.ts:47-61` (HRZoneConfig + HR_ZONES), `src/components/strava/HRZoneDistribution.tsx:33-38`, `src/components/StravaActivityDetail.tsx:152-164`, `src/lib/hr-zones.ts` (jeśli używa `.name`)
- Modify: `src/lib/strava-utils.ts:2,72-76,284` (+ callery funkcji dotkniętych `lang`)
- Modify: `src/lib/plan-i18n.ts` (PLAN_DESC), `src/hooks/useWatchPlanPreview.ts:42-58,84-105`
- Modify: `src/i18n/locales/pl.ts` + `en.ts` (nowe klucze)
- Test: `src/test/strava-utils.test.ts` (istniejący lub nowy), `src/test/plan-i18n.test.ts` (istniejący)

- [x] **Krok 1: strefy HR przez i18n.** DOWÓD: `HRZoneConfig.name` → `nameKey: TranslationKey`, render przez `t(z.nameKey)` w `HRZoneDistribution.tsx:38` i `StravaActivityDetail.tsx:164`; `hr-zones.ts` nie używał `.name`. Test renderowy `strava-i18n.test.tsx` (EN: Recovery, PL: Regeneracja) PASS. Nowe klucze (pl / en): `'strava.zoneRecovery': 'Regeneracja' / 'Recovery'`, `'strava.zoneAerobic': 'Aerobowa' / 'Aerobic'`, `'strava.zoneTempo': 'Tempo' / 'Tempo'`, `'strava.zoneThreshold': 'Próg' / 'Threshold'`, `'strava.zoneVo2max': 'VO2 Max' / 'VO2 Max'`. W `types/strava.ts` pole `name: string` → `nameKey: TranslationKey` (import typu z `@/i18n`); render: `HRZoneDistribution.tsx` i `StravaActivityDetail.tsx` przez `t(z.nameKey)`. `grep -rn "\.name" src/lib/hr-zones.ts` i wszystkie użycia `HR_ZONES` → zaktualizuj konsumentów. Failing test najpierw tam, gdzie istnieje plik testowy stref; inaczej asercja renderowa w teście komponentu.
- [x] **Krok 2: `getWeekLabel` + locale dat w strava-utils.** DOWÓD: klucze `strava.weekThis/weekLast/weeksAgo` + `DF_LOCALES = {pl, en}`; `lang: LanguageCode = 'pl'` w computeWeeklyKm/PaceTrend/MonthlySummaries/WeeklyElevation/WeeklyCalories, 5 komponentów przekazuje `lang` z `useTranslation`. Test: EN → 'This wk'/'January 2026', domyślny bez parametru → PL (niezmiennik). Nowe klucze: `'strava.weekThis': 'Ten tydz.' / 'This wk'`, `'strava.weekLast': 'Ost. tydz.' / 'Last wk'`, `'strava.weeksAgo': '{n} tyg. temu' / '{n} wks ago'`. `getWeekLabel(weekIndex, totalWeeks, lang)` przez `translate`. Import `date-fns`: `import { pl as plDF, enUS } from 'date-fns/locale'` + `const DF_LOCALES = { pl: plDF, en: enUS } as const;` i wybór po `lang` w `:284`. Callery funkcji publicznych, które tego dotykają, dostają `lang` (default `'pl'`, wzorzec lib z pamięci projektu). Test: `getWeekLabel(..., 'en')` zwraca `'This wk'`; miesiąc formatowany po EN dla `lang='en'`.
- [x] **Krok 3: opis planu RZA po EN.** DOWÓD: `PLAN_DESC['tpl-rza-3']` dodany; `plan-i18n.test.ts` skanuje WSZYSTKIE szablony (EN bez polskich znaków) → PASS. `src/lib/plan-i18n.ts` → `PLAN_DESC['tpl-rza-3'] = 'Three A/B/C days over 12 weeks. Emphasis on side delts, back and V-taper width, RPE-driven with conditioning finishers. For experienced lifters who like hard, measurable training.'` (PL źródłowy: `planTemplates.ts:554`). Test w istniejącym pliku testowym plan-i18n: `localizePlanDescription(<desc tpl-rza-3>, 'en')` nie zawiera polskich znaków.
- [x] **Krok 4: podgląd planu na Watch z językiem.** DOWÓD: `useWatchPlanPreview` bierze `lang` z `useTranslation()`, oba buildery `todayWorkout` mają pole `lang`; nowy test `watch-plan-preview.test.tsx` (EN/PL + nazwy kanoniczne) PASS (2). `useWatchPlanPreview.ts`: oba buildery payloadu (`:42-58`, `:84-105`) dostają `lang` z `useTranslation()` i dokładają pole `lang` (kontrakt jak w `useWatchWorkoutSync.ts:73`). Nazwy ćwiczeń/dnia ZOSTAJĄ kanoniczne (zasada 5). Jeśli istnieje test watch-bridge/preview: rozszerz o asercję pola `lang`.
- [x] **Krok 5: bramki + commit**: DOWÓD: `npm run test` → Test Files 122 passed, Tests 1105 passed; `typecheck` exit 0; `lint` exit 0.

## FAZA 4: Z165. Panel admina w 100% przez t() (P2)

**Files:**
- Create: `src/test/admin-i18n-scan.test.ts`
- Modify: `src/pages/admin/AdminDashboard.tsx`, `src/pages/admin/UsersActivityTable.tsx`, `src/pages/admin/admin-user-types.ts`, `src/components/admin/AdminCommsCard.tsx`, `src/components/admin/AdminFeatureFlagsCard.tsx`, `src/components/admin/AdminUserLogs.tsx`, `src/lib/admin-audit.ts` + `src/pages/admin/AdminUserDetail.tsx` (render admin-audit), `src/i18n/locales/pl.ts` + `en.ts` (nowe klucze `admin.*`)

- [x] **Krok 1: failing test-guard** `src/test/admin-i18n-scan.test.ts` (definiuje "done" i zostaje na stałe):

```ts
import { describe, it, expect } from 'vitest';
import { readdirSync, readFileSync, statSync } from 'node:fs';
import { join } from 'node:path';

const walk = (dir: string): string[] =>
  readdirSync(dir).flatMap((f) => {
    const p = join(dir, f);
    return statSync(p).isDirectory() ? walk(p) : [p];
  });

const stripComments = (src: string) =>
  src.replace(/\/\*[\s\S]*?\*\//g, '').replace(/^\s*\/\/.*$/gm, '');

describe('panel admina: zero hardcodowanych polskich stringów', () => {
  it('src/pages/admin + src/components/admin bez polskich znaków poza komentarzami', () => {
    const files = [...walk('src/pages/admin'), ...walk('src/components/admin')].filter((p) => /\.tsx?$/.test(p));
    const offenders = files.flatMap((p) => {
      const lines = stripComments(readFileSync(p, 'utf8')).split('\n');
      return lines.flatMap((l, i) => (/[ąćęłńóśźż]/i.test(l) ? [`${p}:${i + 1} ${l.trim()}`] : []));
    });
    expect(offenders).toEqual([]);
  });
});
```

- [x] **Krok 2: uruchom, wynik FAIL = inwentarz roboczy** DOWÓD: `expected [ …(41) ] to deeply equal []` (41 linii z diakrytykami) + własny skan literałów bez diakrytyków (76 pozycji inwentarza: AdminDashboard 36, AdminCommsCard 11, FeatureFlags 5, UserLogs 3, UsersActivityTable 3, admin-user-types 1). (lista plik:linia do przeniesienia; audyt szacuje ~70 pozycji; UWAGA: stringi PL bez diakrytyków, np. "Logi", "Wyślij mail", łap ręcznie przy przechodzeniu przez pliki z inwentarza).
- [x] **Krok 3: klucze do locales.** DOWÓD: ~90 kluczy `admin.*` w `pl.ts` + `en.ts` (parytet pilnuje typecheck); interpolacje `{name}`, `{sent}/{total}/{failed}/{invalid}`, `{n}`. Przy okazji: `admin.revoke` miał w PL angielską wartość 'Revoke' → 'Unieważnij'. Konwencja: istniejący namespace `admin.*` (np. `admin.confirmResetOnboardingTitle`, `admin.toastCodeSent`, `admin.pulseTitle`, `admin.filterAll`, `admin.suspendDialogTitle`, `admin.mailSubjectPlaceholder`, `admin.editCohortsTitle`, `admin.logsEmpty`...). KAŻDY string z inwentarza dostaje klucz w `pl.ts` (wartość = dotychczasowy literał 1:1) i `en.ts` (tłumaczenie EN). Interpolacje przez `{param}` (np. `admin.deleteAccountDesc` z `{name}`, `admin.commsDelivered` z `{sent}/{total}/{failed}/{invalid}`).
- [x] **Krok 4: podmiana literałów na `t()`** DOWÓD: 7 plików (AdminDashboard, UsersActivityTable, AdminCommsCard, AdminFeatureFlagsCard, AdminUserLogs, ApiKeysCard, AdminUserDetail); `admin-user-types.ts`: `description` → `descriptionKey: TranslationKey`; `admin-audit.ts`: `formatRepairOperations(ops, lang='pl')` przez `translate`, render w AdminUserDetail z `lang`. w 6 plikach komponentów. `admin-user-types.ts` (moduł bez Reacta): pole `description` → `descriptionKey: TranslationKey`, tłumaczenie w miejscu renderu. `admin-audit.ts:50,54`: zwracaj klucze (`admin.auditDeleted`, `admin.auditUpdated`), tłumacz w `AdminUserDetail`.
- [x] **Krok 5: test-guard PASS + istniejące testy admina zielone.** DOWÓD: `npx vitest run src/test/admin-i18n-scan.test.ts` → `PASS (1) FAIL (0)`; drugi skan (literały bez diakrytyków) → zostały tylko nie-PL ('Laptop backup', CSS, fragmenty kodu).
- [x] **Krok 6: bramki + commit**: DOWÓD: `npm run test` → 123 files / 1106 tests passed; `typecheck` 0; `lint` 0; `build` OK; `check:bundle-budget` → **initial JS 1 528 752 / 1 536 000 B** (zapas ~7 KB, limit NIE podnoszony).

## FAZA 5: Z166. Hardcodowane EN w UI (P3)

**Files:**
- Modify: `src/components/ui/dialog.tsx:49`, `src/components/ui/sheet.tsx:62`, `src/components/AppNavigation.tsx:134`, `src/pages/Settings.tsx:341,353`, `src/lib/cycle-actions.ts:89,122,137` + callery (Dashboard/Cycles/NewPlan/Onboarding), `pl.ts` + `en.ts`

- [x] **Krok 1: klucze** (pl / en): DOWÓD: `a11y.close`, `nav.expand`, `nav.collapse`, `settings.maxHrLabel`, `cycles.errActiveNotCreated`, `ob.errCompleteFailed` w obu locales. `'a11y.close': 'Zamknij' / 'Close'`, `'nav.expand': 'Rozwiń nawigację' / 'Expand navigation'`, `'nav.collapse': 'Zwiń nawigację' / 'Collapse navigation'`, `'settings.maxHrLabel': 'Max HR' / 'Max HR'`, `'cycles.errActiveNotCreated': 'Nie udało się utworzyć aktywnego cyklu' / 'Active cycle was not created'`, `'ob.errCompleteFailed': 'Nie udało się dokończyć konfiguracji' / 'Could not complete onboarding'`.
- [x] **Krok 2: podmiana.** DOWÓD: `dialog.tsx`/`sheet.tsx` (Content przerobiony na blok z `useTranslation`), `AppNavigation.tsx`, `Settings.tsx` (label + aria), `cycle-actions.ts` (`lang?: LanguageCode` w Deps, `translate(deps.lang ?? 'pl', ...)`), 4 callery (Dashboard/Cycles/NewPlan/Onboarding) przekazują `lang`. Nowy test `a11y-i18n.test.tsx` → PASS (4): sr-only PL/EN + błąd cyklu EN + domyślny PL (niezmiennik). `dialog.tsx`/`sheet.tsx`: `useTranslation()` w komponencie Content, sr-only przez `t('a11y.close')` (komponenty renderują się wewnątrz `LanguageProvider`; potwierdź w `App.tsx`, ErrorBoundary to znany wyjątek). `cycle-actions.ts`: funkcje przyjmują `lang: LanguageCode = 'pl'` i zwracają `translate(lang, ...)`; callery przekazują `lang` z `useTranslation`. Test: istniejące testy cycle-actions asertujące EN teksty zaktualizuj na klucz/PL zgodnie z regułą "wartości PL 1:1 dla testowanych kluczy".
- [x] **Krok 3: NIE dotykaj** DOWÓD: `grep -rn 'ui/pagination|ui/breadcrumb|ui/sidebar' src/ (bez src/components/ui)` → 0 trafień, nietknięte. `ui/pagination.tsx`, `ui/breadcrumb.tsx`, `ui/sidebar.tsx` (zero importów w apce, zweryfikowane grep 2026-07-28). Odnotuj w DECYZJE.md.
- [x] **Krok 4: bramki + commit**: DOWÓD: `npm run test` → 124 files / 1110 tests passed; `typecheck` 0; `lint` 0.

## FAZA 6: Z167. Backend: push i mail zaproszenia per język (P2, functions)

**Files:**
- Modify: `functions/src/daily-reminder.ts:110-120`, `functions/src/email-templates.ts:90-113`, `functions/src/registration.ts:770-790`
- Create: `functions/src/focus-en.ts` (port tokenów focus)
- Modify: `src/lib/plan-i18n.ts` (export `FOCUS_TOKEN_EN` jeśli nieeksportowany)
- Test: `functions/src/daily-reminder.test.ts`, `functions/src/email-templates.test.ts` (nowy jeśli brak), `src/test/exercise-i18n-coverage.test.ts` (parytet focus map)

- [x] **Krok 1: failing test push EN** DOWÓD: 3 nowe testy w `functions/src/daily-reminder.test.ts` (EN z imieniem, EN bez imienia, PL bez pola language) → 2 FAIL przed implementacją.: user z `language: 'en'` dostaje `title` = `` `Hey ${firstName}! Time to train 💪` `` (bez imienia: `Time to train 💪`) i `body` = `` `Today's plan: ${focusEn}. Open the app and log your first set.` ``; user PL / bez pola language: dotychczasowe PL teksty 1:1.
- [x] **Krok 2: implementacja**: DOWÓD: `ReminderUser.language`, `lang = user.language === 'en' ? 'en' : 'pl'`, `functions/src/focus-en.ts` (port 20 tokenów). Testy parytetu w `src/test/exercise-i18n-coverage.test.ts`: mapa 1:1 ORAZ `localizeFocusEn === localizeFocus(.., 'en')`. `const lang = user.language === 'en' ? 'en' : 'pl';` (typ `Lang` z `email-templates.ts`); focus przez nowy `functions/src/focus-en.ts`: `localizeFocusEn(focus)` tokenowo (port mapy `FOCUS_TOKEN_EN` z `src/lib/plan-i18n.ts`, wzorzec duplikatu jak `functions/src/exercise-name-en.ts`). Test parytetu obu map w `src/test/exercise-i18n-coverage.test.ts` (wzorzec `:26`).
- [x] **Krok 3: failing test mail zaproszenia** DOWÓD: nowy `functions/src/email-templates.test.ts` → FAIL (`expected ... to contain 'You&#39;re invited to Strength Save'`).: `inviteEmailHtml(code, url, note, 'en')` zawiera `"You're invited to Strength Save"`, nie zawiera polskich znaków; default (bez lang) = dotychczasowy PL 1:1.
- [x] **Krok 4: implementacja**: `inviteEmailHtml(code, inviteUrl, note, lang: Lang = 'pl')`; teksty EN: nagłówek `You're invited to Strength Save`, opis `You can sign in with Google or email + password using this invite code:`, `Direct link:`, przycisk `Open the app`. `registration.ts:780`: `subject: lang === 'en' ? 'Your Strength Save invite' : 'Zaproszenie do Strength Save'`; `lang` z opcjonalnego pola payloadu zaproszenia (default `'pl'`; dziś wysyłki są PL, parametr przyszłościowy).
- [x] **Krok 5: bramki + deploy**: DOWÓD: `cd functions && npm test` → 18 files / 151 passed (+4 skipped); root `npm run test` → 124 files / 1112 passed; `typecheck` 0 (root i functions); `firebase deploy --only functions` → **Deploy complete!** (wszystkie funkcje Successful update). `cd functions && npm test` zielone, `npm run typecheck` root → commit `feat(functions): push dnia i mail zaproszenia per język usera (Z167)` → `firebase deploy --only functions`. Rules bez zmian (pola nie dotykają schematu workouts).

## FAZA 7: Z168. Fundament pod n języków + globalny guard (P2)

**Files:**
- Modify: `src/i18n/index.ts`, `src/pages/Profile.tsx:255-265`, `src/data/exercise-i18n.ts:530-575`, `src/lib/plan-i18n.ts:40-140`, `src/lib/user-profile.ts:69`, `src/lib/registration-api.ts:13-14,64`, `src/components/AllTimeStatsSheet.tsx:53`
- Create: `src/test/i18n-hardcoded-scan.test.ts`, `docs/I18N-NOWY-JEZYK.md`

- [ ] **Krok 1: rejestr języków jako źródło prawdy.** `src/i18n/index.ts`:

```ts
export const LANGUAGES = [
  { code: 'pl', label: 'Polski' },
  { code: 'en', label: 'English' },
] as const;

const DATE_LOCALES: Record<LanguageCode, string> = { pl: 'pl-PL', en: 'en-US' };
export const dateLocale = (lang: LanguageCode): string =>
  DATE_LOCALES[lang] ?? DATE_LOCALES[DEFAULT_LANGUAGE];
```

  `Profile.tsx:260-261`: `<SelectItem>` generowane z `LANGUAGES.map(l => ...)` (label natywny z rejestru). `AllTimeStatsSheet.tsx:53`: `dateLocale(lang)` zamiast ternary. Test: selector renderuje pozycję dla każdego wpisu `LANGUAGES`.
- [ ] **Krok 2: nakładki zamiast binarnych warunków.** `exercise-i18n.ts`:

```ts
const NAME_OVERLAYS: Partial<Record<LanguageCode, Record<string, string>>> = { en: EXERCISE_NAME_EN };
export const localizeExerciseName = (name: string, lang: LanguageCode): string =>
  NAME_OVERLAYS[lang]?.[name] ?? name;
```

  Analogicznie `localizeExerciseInstruction`, `localizeCategory` oraz w `plan-i18n.ts` (`PLAN_NAME`/`PLAN_DESC`/`FOCUS_TOKEN_EN`/`WEEKDAY_EN` za nakładką per język). Zachowanie dla pl/en identyczne: istniejące testy exercise-i18n/plan-i18n muszą przejść BEZ zmian asercji (to jest test niezmiennika). `exercise-details.ts:1742` zostaje binarne (lazy-load szczegółów EN, przebudowa przy realnym ES): odnotuj w docs.
- [ ] **Krok 3: typy.** `user-profile.ts:69` i `registration-api.ts:64`: `language?: LanguageCode` (import z `@/i18n`); `registration-api.ts:13-14`: walidacja przez `LANGUAGES.some(l => l.code === saved)`.
- [ ] **Krok 4: globalny test-guard na hardcodowane PL** `src/test/i18n-hardcoded-scan.test.ts`: skan jak w Z165, ale `src/` szeroko, z wykluczeniami katalogów `src/i18n/`, `src/data/`, `src/test/` i allowlistą plików świadomie PL (każdy wpis z komentarzem-uzasadnieniem):

```ts
const ALLOWLIST = new Set([
  'src/lib/plan-cycle-utils.ts',        // kanoniczne dayName zapisywane do Firestore
  'src/lib/workout-import/mapper.ts',   // mapowanie hevy/strong na kanoniczne PL
  'src/lib/rza-progression.ts',         // klucze lookup progresji
  'src/components/strava/RacePredictor.tsx', // klucze mapy emoji
  'src/lib/registration-api.ts',        // fixtury trybu E2E
  'src/lib/purchases.ts',               // console.warn dev-only
  'src/pages/Profile.tsx',              // słowo potwierdzenia USUŃ (zależne od lang)
  'src/components/ErrorBoundary.tsx',   // mini-słownik poza LanguageProvider
]);
```

  Pierwsze uruchomienie = inwentarz resztek: każdy offender ALBO dostaje klucz i18n (jeśli user-facing), ALBO trafia do allowlisty z uzasadnieniem. Test zostaje na stałe (chroni każdy przyszły język).
- [ ] **Krok 5: dokument** `docs/I18N-NOWY-JEZYK.md`: checklist dodania języka na bazie audytu §G/§H (`es.ts` 1733 klucze, wpis w `LANGUAGES` + `DATE_LOCALES`, nakładki danych `NAME_OVERLAYS`/plan-i18n, `exercise-details` lazy-load, functions `Lang` + digest + push, Garmin `resources-spa`, Watch `L10n`, legal HTML, manifest). Jedna strona, konkretne pliki.
- [ ] **Krok 6: bramki + commit**: `npm run test`, `typecheck`, `lint`, `build`, `check:bundle-budget` → commit `refactor(i18n): rejestr języków, nakładki per język, guard skanujący, checklist ES (Z168)`.

---

## KOLEJNOŚĆ I ZALEŻNOŚCI

1. **Z162** (bug rozgrzewki) → **Z163** (nazwy; testy Z162 używają `translate()`, więc przejdą niezależnie od wartości).
2. **Z164 → Z165 → Z166**: wszystkie dotykają `pl.ts`/`en.ts`, wykonywać SEKWENCYJNIE (konflikt edycji jednego pliku).
3. **Z167** (functions) niezależne od klienta; deploy functions raz, po zakończeniu fazy.
4. **Z168 OSTATNIE**: guard skanujący ma sens dopiero po sprzątnięciu Z164-Z166; refactor nakładek po tym, jak Z164 dołożył wpisy do `PLAN_DESC`.

## BRAMKI (przed buildem iOS; bez `| tail`, pipe maskuje exit code)

`npm run test` · `npm run typecheck` · `npm run lint` · `npm run build` · `npm run check:bundle-budget` · `npm run check:dist-smoke` (na `build:mobile`!) · `npm run check:dist-offline` (na buildzie web!) · `npm run e2e:mock` · `cd functions && npm test`. Rules niezmieniane: `test:rules` niewymagany. Reguła 9 CLAUDE.md: e2e dziwnie wolne albo masowe faile na `page.goto` → najpierw `pkill -f vite` + wyczyść `node_modules/.vite`.

Scenariusz obowiązkowy (reguła 5): plan → wyjście → szybki trening → powrót do planu → wszystkie ćwiczenia na miejscu → zakończenie → sync. PLUS scenariusz rozgrzewki z FAZY 1.

## DEPLOY

1. Web: `npm run deploy` (sam push NIE aktualizuje strony).
2. Functions: `firebase deploy --only functions` (po Z167).
3. iOS: bump `CURRENT_PROJECT_VERSION` → **80** (6 wystąpień, pilnuje `release-ios-preflight.mjs`), `scripts/release-ios.sh "..."`, potem OBOWIĄZKOWO `uv run --with "pyjwt[crypto]" --with requests scripts/testflight_external.py 80 --whats-new "..."` (po polsku: rozgrzewka zapamiętuje odhaczenia, spójne nazwy PL/EN, poprawki tłumaczeń).
4. Wpis do `DECYZJE.md` po każdej fazie (co, dlaczego, root cause, weryfikacja) + status w `PLAN.md`.

## ODŁOŻONE / TECH DEBT (dopisać do PLAN.md)

- Instrukcje szablonów planów zapisywane do Firestore po PL (`'📋 Parametry'`, `` `Przerwa ${rest}` ``, `planTemplates.ts:68-99`): lokalizacja wymaga zmiany modelu (instrukcje jako klucze), osobny projekt.
- `functions/src/garmin-day.ts:150,174`: kanoniczne PL nazwy/focus na Garminie; lokalizacja display wymaga decyzji (rozmiar payloadu, mapy po stronie functions).
- Apple Watch: payload treningu z kanonicznymi nazwami (`WorkoutDay.tsx:1596-1598`); lokalizacja display na zegarku = osobny wątek (ryzyko dopasowania serii po nazwie).
- `ios/App/WatchWidgets/StrengthWidgets.swift:46`: opis widgetu PL hardcoded (wymaga `NSLocalizedString`).
- `index.html` (lang, title, description), manifest PWA (`vite.config.ts:59-61`), `public/strava-callback.html`, `landing/legal` bez ES: do decyzji przy realnym dodaniu języka.
- Tiery osiągnięć (`Newcomer`, `Rookie`, `Pro Tier`...) po EN w obu językach: branding, DECYZJA USERA czy tłumaczyć.
- `functions/src/registration.ts:359-362`, `admin-api.ts:228,247`: komunikaty błędów HttpsError po PL.
- Nieużywane komponenty shadcn z EN a11y (`pagination`, `breadcrumb`, `sidebar`): nieużywane, zostawione świadomie.
- `getStretchingForFocus` dopasowuje focus po substringach PL/EN (`warmupStretching.ts:60-66`): przy ES bez zmian (focus kanonicznie PL), odnotowane.
