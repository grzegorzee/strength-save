# PLAN X17A: Karta ćwiczenia v2. Czytelność i hierarchia ekranu treningu (2026-07-20)

> **Dla agenta wykonawcy:** WYMAGANY SUB-SKILL: `superpowers:executing-plans`. Kroki mają checkboxy `- [ ]`. TRYB 100% AUTONOMICZNY.
> Kontekst: user zgłosił po treningu 2026-07-20, że karta ćwiczenia jest nieczytelna i nieodkrywalna. Research konkurencji (Hevy, Strong, StrengthLog, Stronglifts) + `docs/DESIGN.md` w źródłach na dole.
> UWAGA STALENESS: `ExerciseCard.tsx` ma 1108 linii i zmienia się często. Przed każdą fazą sprawdź aktualne numery linii, nie ufaj tym z planu.

**Goal:** Karta ćwiczenia wygląda i działa profesjonalnie: hierarchia od góry czytelna w jednym spojrzeniu, ukończone serie widać z odległości ręki, „Dodaj serię" jest tam gdzie user go szuka (pod seriami), a rzadkie akcje nie zaśmiecają ekranu.

**Scope:**
- JEST: przebudowa układu karty, menu `⋯`, przeniesienie „Dodaj serię", wypełnione tło ukończonej serii, kolumna POPRZ. z treścią, kompaktowy nagłówek sesji.
- NIE MA: timerów przerw (osobno w X17C), kalkulatora talerzy (X17B), zmian w silniku progresji.

---

## 0. TWARDE ZASADY

1. **Kontrakt `memo()` jest nienaruszalny.** Callbacki do karty mają sygnaturę `(exerciseId, ...)` i są `useCallback` w rodzicu (`ExerciseCard.tsx:206-207`, `WorkoutDay.tsx:432-434`). Lambda inline per karta = powrót re-render bomby R2-07. Jeśli nowa akcja potrzebuje callbacku, dodaj go z `exerciseId` w sygnaturze i `useCallback` w `WorkoutDay`.
2. **Ścieżka logowania serii nie może zwolnić.** Zmieniamy układ, nie mechanikę odhaczania.
3. **Obie ścieżki renderu muszą przejść testy**: stara (`weight_reps`/`bodyweight_reps`) i `renderTrackedSetRow` (Z105: duration / weight_distance_duration / assisted_bodyweight).
4. Zero ramek 1px do sekcjonowania (`docs/DESIGN.md`, „No-Line Rule"). Granice przez przesunięcia tła.
5. Nowe klucze i18n do OBU plików (`pl.ts` + `en.ts`).
6. Chirurgiczne commity per zadanie, pełne bramki przed release trainem, wpis do `DECYZJE.md` po każdej fazie.
7. **RYZYKO DO OMIJANIA:** `hasLocalChanges.current` (`ExerciseCard.tsx:313-325`) nigdy nie wraca do `false` — po pierwszej lokalnej edycji karta przestaje przyjmować propsy. Nie refaktoruj tego w tym planie, ale nie dokładaj nowego stanu zależnego od propsów po edycji.

---

## FAZA 0: Baseline

### Zadanie Z128.0

- [x] Krok 1: `git status` czyste, pełne bramki zielone. Zrzut ekranu karty PRZED zmianami (symulator iPhone 17, ćwiczenie z rozgrzewką + 3 seriami roboczymi, część odhaczona) do porównania w raporcie.
  - Bramki baseline: test 863/863, typecheck, lint, build, bundle-budget (initial JS 1 490 147 / limit 1 536 000), dist-smoke (na `build:mobile`), dist-offline, e2e:mock 168 passed. Wszystkie zielone bez pipe'a maskującego exit code.
  - Zrzut PRZED: symulator iPhone 17, Safari + dev server w `VITE_E2E_MODE`, `#/workout/day-1`. Potwierdza zarzuty planu: pusty kwadrat miniatury, 6 linii instrukcji nad tabelą, sekcja rozgrzewki NAD nagłówkami kolumn, POPRZ. same „—", nad zgięciem mieszczą się tylko 2 serie robocze.
  - `.gitignore`: dopisane lokalne artefakty z katalogu głównego (`.last_session_update`, `IMG_*.PNG`, `RZA_*.xlsx`) — pliki usera, nie kasujemy ich, ale nie należą do repo.
- [x] Krok 2: Test charakteryzujący `src/test/exercise-card-layout.test.tsx`: render karty dla `weight_reps` i dla `duration` — asercje na KOLEJNOŚĆ sekcji (nagłówek → notatka przypięta → nagłówki kolumn → wiersze → dodaj serię → chipy) i obecność WSZYSTKICH serii. Ma przechodzić na obecnym kodzie tam, gdzie zachowanie zostaje, i jawnie dokumentować to, co zmienimy.
  - 9 testów, dwa bloki: „niezmienniki" (obie ścieżki renderu, komplet serii, nagłówek pierwszy, Dodaj serię tylko w edycji) i „stan PRZED X17A" (rozgrzewka przed nagłówkami, miniatura bez animacji, instrukcje w karcie, Dodaj serię w stopce) — ten drugi blok odwracamy w Z128/Z129.
  - Znalezione przy okazji: `sanitizeSets` dokłada wiersz rozgrzewki, gdy zapis go nie ma. To istniejące zachowanie, test je utrwala.

---

## FAZA 1: Z128. Hierarchia i fundament wizualny

### Zadanie Z128.1: kolejność sekcji

- [ ] Krok 1: Nagłówki kolumn (`SET | POPRZ. | KG | POWT. | ✓`) renderują się PRZED wierszami rozgrzewkowymi. Dziś sekcja rozgrzewki (`880-893`) jest nad nagłówkami (`896-940`). Rozgrzewka wchodzi do tej samej tabeli, oznaczona badge `W` w kolumnie SET (jak dziś), bez osobnego nagłówka sekcji.
- [ ] Krok 2: Ukończona seria dostaje wypełnione tło całego wiersza (`bg-primary/[0.06]`), aktywna zostaje z `ring-2 ring-primary`. Zero ramek. Test: wiersz ukończony ma klasę tła, nieukończony nie.
- [ ] Krok 3: Spójne złoto rozgrzewki na OBU ścieżkach — dziś tylko stara ścieżka dokłada inputom `--ec-warmup-gold-border` (`710`, `727`), `renderTrackedSetRow` tego nie robi.

### Zadanie Z128.2: odchudzenie nagłówka

- [ ] Krok 1: Miniatura renderuje się TYLKO gdy jest `animationUrl`. Dziś przy braku animacji rysuje pusty kwadrat 92×72 z wyszarzoną ikoną hantla (`777-796`) — czysta strata miejsca. Bez animacji tytuł dostaje pełną szerokość.
- [ ] Krok 2: Instrukcje znikają z karty na stałe (`868-878`) — przenoszą się do menu `⋯` (Z129). Akapit uzasadnienia celu (`847-851`) i `lastNote` (`852-857`) zostają, ale jako jeden zwarty blok metadanych.
- [ ] Krok 3: Usunięcie martwej klasy `.exercise-card-divider` (`index.css:182-185`, `height: 0; background: transparent`) i jej 4 użyć (`865`, `891`, `956`) na rzecz `space-y`. Naprawa `.exercise-card-header` — zahardkodowane `background: #262626` ignoruje light mode, ma iść przez token.

### CHECKPOINT FAZY 1

- [ ] Pełne bramki + zrzut porównawczy + `DECYZJE.md`. Commit `feat(card): hierarchia i odchudzony nagłówek karty ćwiczenia (Z128)`.

---

## FAZA 2: Z129. „Dodaj serię" i menu `⋯`

### Zadanie Z129.1: przycisk dodania serii

- [ ] Krok 1: „+ Dodaj serię" pełną szerokością BEZPOŚREDNIO pod ostatnim wierszem serii (dziś w pasku akcji na dole, `958-965`, tekst przed ikoną). Wzorzec Hevy/Strong.
- [ ] Krok 2: Limit 10 serii roboczych zostaje, ale zamiast niemego `disabled` (`965`) pokazuje powód. Test: 10 serii → przycisk nieaktywny z komunikatem.

### Zadanie Z129.2: menu `⋯` (decyzja usera)

- [ ] Krok 1: Menu `⋯` w prawym górnym rogu nagłówka karty, zbiera: **Instrukcje** (dziś ikona `Info`, `803-810`), **Zamień ćwiczenie** i **Pomiń** (dziś przyciski POD kartą w `WorkoutDay.tsx:2331+`), **Notatka do treningu** (dziś chip w pasku), **Przypnij notatkę** (dziś cała sekcja `PinnedNoteSection` w trybie pustym).
- [ ] Krok 2: `PinnedNoteSection` renderuje się w karcie TYLKO gdy notatka istnieje, jako jednolinijkowy pasek z ikoną pinezki, tap otwiera edycję. Pusty stan przenosi się do menu (`PinnedNoteSection.tsx:40` — dziś w trybie edytowalnym widoczna zawsze).
- [ ] Krok 3: Pasek dolny redukuje się do trzech spójnych chipów o identycznym rozmiarze: **Rozgrzewka** (dziś nagi `%`, `967-983`), **Talerze** (dziś naga ikona `Disc`, `984-999` — etykieta rozwiązuje zarzut „nie wiadomo, że to kalkulator"), **Metryki**. Dziś rząd miesza 2 ikonowe z 2 etykietowanymi bez `flex-wrap`.
- [ ] Krok 4: Nowe callbacki (zamień/pomiń) z `exerciseId` w sygnaturze, `useCallback` w `WorkoutDay` — patrz TWARDA ZASADA 1.
- [ ] Krok 5: E2E: każda pozycja menu osiągalna i działa; chip Talerze widoczny.

### CHECKPOINT FAZY 2

- [ ] Pełne bramki + `DECYZJE.md`. Commit `feat(card): dodaj serię pod listą + menu akcji ćwiczenia (Z129)`.

---

## FAZA 3: Z130. POPRZ. z treścią i bezpieczeństwo edycji

- [ ] Krok 1: Kolumna POPRZ. wypełniana z historii w formacie „60×6". Dane już są: `exerciseInsights.previousSets` (`WorkoutDay.tsx:446-476`) i prop `previousSets`. Gdy naprawdę brak historii → czytelne „pierwszy raz", nie „—". To najczęściej chwalony element Hevy/Strong (auto-populate z poprzedniej sesji).
- [ ] Krok 2: Potwierdzenie przy usuwaniu serii, która ma dane. Dziś `×` (`751-763`) kasuje natychmiast, bez pytania. Pusta seria kasuje się bez dialogu.
- [ ] Krok 3: Bramki + commit `feat(card): kolumna POPRZ. z historii i potwierdzenie usunięcia serii (Z130)`.

---

## FAZA 4: Z131. Nagłówek sesji + release train

- [ ] Krok 1: Nagłówek aktywnej sesji: Czas / Objętość / Serie w JEDNYM zwartym rzędzie (wzorzec Hevy) zamiast dwóch dużych kafelków `StatCard` (`WorkoutDay.tsx:2272-2281`). Liczba serii to nowa metryka — dziś nigdzie nie liczona zbiorczo dla sesji.
- [ ] Krok 2: Pełne bramki: `test`, `typecheck`, `lint`, `build`, `check:bundle-budget`, `check:dist-smoke` (na `build:mobile`), `check:dist-offline` (na web), `e2e:mock`.
- [ ] Krok 3: Scenariusz przerwania (reguła 5 w `CLAUDE.md`): plan → wyjście → szybki trening → powrót → wszystkie ćwiczenia i nowy układ na miejscu.
- [ ] Krok 4: Zrzuty PO z symulatora, porównanie z Z128.0.
- [ ] Krok 5: Release train: `git push`, `npm run deploy`, bump `CURRENT_PROJECT_VERSION`, `scripts/ios-testflight.sh`, poll VALID, `testflight_external.py <build> --whats-new "..."`.
- [ ] Krok 6: `DECYZJE.md`, `PLAN.md`, `CONTEXT.md`. Raport: co się zmieniło w układzie, zrzuty przed/po, co poszło do backlogu.

---

## Źródła researchu

- Strong Help Center: [My First Workout](https://help.strongapp.io/article/229-my-first-workout), [Set Tags](https://help.strongapp.io/article/166-set-tags)
- Hevy: [Track Workouts](https://www.hevyapp.com/features/track-workouts/), [12 Workout Settings](https://www.hevyapp.com/features/workout-settings/)
- Recenzje porównawcze: [PRPath Strong vs Hevy 2026](https://prpath.app/blog/strong-vs-hevy-2026.html), [Setgraph: best app to log workout](https://setgraph.app/ai-blog/best-app-to-log-workout-tested-by-lifters)
- Wewnętrznie: `docs/DESIGN.md` (Kinetic Precision), `CLAUDE.md` reguły 5-8.
