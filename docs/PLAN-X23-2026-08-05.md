# PLAN X23: zgłoszenia z builda 81 (realny trening 2026-08-04): wskrzeszone/zdublowane serie po force-quit, kuloodporny timer przerwy, niezamykalny modal wideo, czarne miniatury, obcięte "125", share (Z182-Z199)

> **Dla agenta wykonującego:** prowadź wykonanie skillem `superpowers:executing-plans`. Postęp = checkboxy `- [ ]` w TYM pliku (odhaczaj z jednolinijkowym dowodem: komenda + wynik, commituj plan razem z kodem). Przy bugu w trakcie: `superpowers:systematic-debugging`. Przed deklaracją sukcesu: `superpowers:verification-before-completion`.

**Cel:** naprawić u źródła wszystkie zgłoszenia usera z realnego treningu na buildzie 81 (2026-08-04) i przebudować timer przerwy oraz utrwalanie sesji na model kuloodporny (jedno źródło prawdy, samonaprawa, fail-open). Root cause każdego buga zweryfikowany w kodzie 2026-08-05 (4 równoległe śledztwa + analiza rdzenia timera, file:line poniżej).

**Data:** 2026-08-05 · **Build docelowy iOS:** 82 · **Poprzedni plan:** X22 (Z170-Z181)

---

## 0. TWARDE ZASADY (nadrzędne wobec tempa)

- **NIGDY `git add -A` / `git add .`** W repo: nietrackowany `animacje-cwiczen/` (699 MB, celowo poza gitem) oraz zastane zmiany `android/app/capacitor.build.gradle` + `android/capacitor.settings.gradle` (NIE commituj ich w ramach X23). Stage'uj pliki imiennie.
- **Dane usera są święte.** Zero zapisów na produkcyjnych danych. Krok diagnostyczny w FAZIE 1 to WYŁĄCZNIE odczyt (read-only) dokumentu workout z Firestore; wynik do raportu.
- **Reguła 5 CLAUDE.md:** każda zmiana ma test niezmiennika starego przepływu. Testuj SEKWENCJE (start → wyjście → kill → powrót → dokończenie → sync), nie ekrany.
- **Reguła 6 CLAUDE.md:** każdy stan błędu ma wyjście dla usera. Dialog, którego nie da się zamknąć niczym, to złamanie tej reguły pierwszego stopnia.
- **Nowe klucze i18n do OBU plików** (`src/i18n/locales/pl.ts` + `en.ts`), inaczej typecheck padnie.
- **Bundle budget:** przed FAZĄ 1 uruchom `npm run check:bundle-budget` i zapisz zapas w raporcie. Przekroczenie po zmianach: NIE podnoś limitu, szukaj code-splittingu. Postery JPEG (FAZA 4) nie dotykają bundle'a (CDN).
- **Firestore rules mają schema-checks:** NIE dodawaj nowych pól do kształtów zapisywanych do FIRESTORE. Nowe pola wolno dodawać do draftu w IndexedDB (`ActiveWorkoutDraft`) i do localStorage: to lokalne magazyny bez rules. Serie NADAL bez pola `id` (Z171: tożsamość po referencji).
- **Wersja 1.0.0 na sztywno.** Bump wyłącznie `CURRENT_PROJECT_VERSION` → **82** (6 wystąpień w `ios/App/App.xcodeproj/project.pbxproj`; pilnuje `release-ios-preflight.mjs`).
- **Reguła 9 CLAUDE.md:** e2e masowo czerwone / bieg wielokrotnie dłuższy → NAJPIERW `pkill -f vite` + wyczyść `node_modules/.vite`, potem szukaj buga.
- Kolejność dist-checków: `npm run build:mobile` + `check:dist-smoke`, POTEM `npm run build` + `check:dist-offline`.
- Chirurgiczne commity per zadanie, `(Z18x/Z19x)` w opisie. Wpis do `DECYZJE.md` po każdej fazie.
- Coś poza zakresem → sekcja ODŁOŻONE na końcu tego pliku (jednozdaniowy opis), nie rozszerzaj planu.

---

## ROOT CAUSE: mapa (zweryfikowane w kodzie 2026-08-05)

| Zgłoszenie usera | Root cause | Dowód |
|---|---|---|
| "Zduplikowane serie w RDL po force-quit i Kontynuuj trening" | Drabinka 4xW (20/25/35/45) może powstać WYŁĄCZNIE z chipa generatora (`generateWarmupSets`, jedyne wywołanie `ExerciseCard.tsx:590-601`); żadna ścieżka resume jej nie tworzy. Realne mechanizmy: **(H2) wskrzeszenie starszego snapshotu** (brak reguły "najświeższy wygrywa" między IDB a fallbackiem localStorage: `workout-draft-db.ts:764-788` czyta fallback tylko przy WYJĄTKU IDB; hydracja przy rozjeździe sessionId wybiera chmurę bez porównania świeżości: `workout-hydration.ts:37`), **(H1) dryf tożsamości po swapie "tylko dziś"** (`sessionSwaps` tylko w stanie Reacta `WorkoutDay.tsx:205`, klucz draftu `${id}__swap-<nazwa>`; po restarcie mapa ginie i `buildDayFromDraft` renderuje DWIE karty: plan + extras, `workout-day-view.ts:46-53`), **(H3) fabrykowanie wiersza W** (`sanitizeSets` dopisuje pusty wiersz rozgrzewkowy gdy draft go nie ma: `exercise-utils.ts:226-227`; fabrykat wycieka do draftu przy pierwszym `onSetsChange`) | raport śledztwa 2026-08-05; `workout-draft-db.ts:764-788, 830-841`, `workout-hydration.ts:37`, `workout-day-view.ts:25-62`, `exercise-utils.ts:226-227`, `WorkoutDay.tsx:205, 403-412` |
| "Timer między seriami się zacina / nie pokazuje odliczania" + "po zakończeniu serii timer nie startuje" | (a) Start przerwy siedzi w bloku `if (turningOn && !currentSet.isWarmup)`: po serii ROZGRZEWKOWEJ timer NIGDY nie startuje, mimo że `resolveRestSeconds` ma dedykowaną gałąź `warmupSeconds` (martwa od X17C); (b) bramka `hasRemainingWork` jest fail-closed: pusta/nieaktualna `dayExercisesRef` → `stopRest` zamiast startu; (c) w `RestBar` efekt końca gra sygnały PRZED `onFinished()` (wyjątek sygnału = pasek "Koniec przerwy" wisi na zawsze, stan właściciela nie zerowany); (d) efekt startu ma dep na `scheduleFor` (tożsamość zależy od `t` i `exerciseLabel`) → zmiana identity restartuje biegnącą przerwę; (e) stan deadline żyje w komponencie RestBar: unmount/kill = utrata przerwy, zero persystencji, zero watchdoga | `ExerciseCard.tsx:498-521`, `WorkoutDay.tsx:216-232`, `RestBar.tsx:47, 54-67, 85-96`, `rest-timer.ts:124-134` |
| "Nie mogłem zamknąć popupu z filmem, X nie działa" (force-quit!) | Warstwa MODALNEGO DropdownMenu współistnieje z warstwą dialogu (menu zostaje w DOM przez animację zamykania; `layersWithOutsidePointerEventsDisabled` czyszczone dopiero przy unmount) → `DialogContent` dostaje `pointer-events: none`: X martwy, tap w overlay martwy (guard w `usePointerDownOutside`), na iOS brak Escape = dialog niezamykalny NICZYM. Wzmacniacz: X ma tap target 16×16 px (`dialog.tsx:52-55`, HIG minimum 44 pt). Jedyny dialog otwierany z dropdownu (Instrukcje, `ExerciseCard.tsx:1050`) używa `onClick` bez `onSelect`+`preventDefault`, bez sekwencji zamknij-menu-potem-otwórz; miniatura (`:936-938`) otwiera dialog wideo bez sprawdzenia, czy menu jest otwarte | `@radix-ui/react-dismissable-layer/dist/index.mjs:38-44, 89, 104`, `ui/dialog.tsx:52-55`, `ui/dropdown-menu.tsx:60`, `ExerciseCard.tsx:936-938, 1033-1079, 1331-1369` |
| "Podgląd ćwiczeń jest czarny" | Z176 oparło miniaturę o `<video preload="metadata">` bez postera i bez autoplay, zakładając "metadata = pierwsza klatka". W WebKit/WKWebView to fałsz: Safari pobiera tylko `moov` i NIE maluje ŻADNEJ klatki do pierwszego `play()`/seeka (Chromium maluje, dlatego e2e zielone). Posterów nie ma nigdzie (tylko mp4 na CDN). Wzmacniacz: overlay `bg-black/30` | `ExerciseCard.tsx:935-972` (miniatura `:950-964`), commit `ac386059`, `exercise-media.ts:18, 29-174, 187-191`, `e2e/exercise-video.spec.ts:22` |
| "125 nie mieści się w kratce" | `px-3` (24 px) z bazowego `Input` (`ui/input.tsx:11`) + równy podział `1fr 1fr` między KG a POWT. (`ExerciseCard.tsx:655`): kolumna KG @390 px viewport ma 50.7 px, wnętrze 26.7 px, a "125" potrzebuje ~29.5 px (Inter Bold 16 px). `tabular-nums` już jest (`index.css:190`), formatter minimalny: to nie one | `ExerciseCard.tsx:647-655, 824-829, 851-879`, `ui/input.tsx:11`, `index.css:185-192` |
| "Na zdjęciu liczby nachodzą mi na nos" | Szablon ZDJĘCIE ma w jednym flex-column DWA auto-marginesy (`margin-bottom:auto` na nagłówku + `margin-top:auto` w stopce): flexbox dzieli wolną przestrzeń po równo, więc blok statystyk i lista ćwiczeń CENTRUJĄ się w pionie (pas ~35-70% wysokości = twarz na selfie). Scrim przyciemnia liniowo całe zdjęcie zamiast pasa pod tekstem; `dim=0.6` daje brightness(0.40) na całości | `share-utils.ts:197-280` (nagłówek `:248`, stopka `:62-71`, scrim `:240-243`, dim `:202`) |
| "Kliknąłem Pobierz i oba przyciski podkreślone, zero animacji" | (a) `systemShare()` nie rozróżnia sukcesu od `AbortError`, zero toastu/haptyki/stanu na przycisku; (b) sticky `:hover` w WKWebView: shadcn `Button` ma wyłącznie warianty `hover:`, w projekcie NIE MA ani jednego `@media (hover: hover)` ani `active:` press-feedbacku | `ShareWorkoutDialog.tsx:116-152, 234-238`, `ui/button.tsx:8-14`, `src/index.css:242-263` |

---

## FAZA 1: sesja nieśmiertelna (Z182-Z186). Niezmiennik: draft jest JEDYNYM źródłem prawdy o seriach; widok niczego nie syntetyzuje; restart apki nie zmienia ani liczby, ani stanu odhaczenia serii.

### Z182: diagnoza danych usera (read-only) + reguła "najświeższy wygrywa" IDB vs fallback

**Pliki:** `src/lib/workout-draft-db.ts` (loadDraft/loadActiveDraft, okolice `:764-788`), test: `src/test/workout-draft-db.test.ts`.

- [x] **Diagnoza (read-only, wynik do raportu):** *(DOWÓD: REST runQuery, token gcloud --account g.jasionowicz@gmail.com: dokument `workout-U6GDdfg7GmP1k1xJuISIsK9uSUE2-day-2-2026-08-04` (Wtorek, Dół A, completed, revision 27) — RDL sets = [[-,50,6,true],[-,70,6,true],[-,85,6,true]] (3 robocze, ZERO isWarmup); klucze ćwiczeń: tpl-ex-35..39, ZERO kluczy `__swap-`. Drabinka 4xW NIE istnieje w chmurze → wskrzeszenie było czysto lokalne = klasa H2; zero zapisów)*
- [x] **Test first (H2a):** *(DOWÓD: `npx vitest run src/test/workout-draft-db.test.ts` przed fixem: PASS 36, FAIL 2 — "Z182: fallback localStorage z wyższą wersją wygrywa" i "Z182: loadActiveDraft też preferuje świeższy fallback": expected 5 to be 8)*
- [x] **Fix:** *(`resolveFresherFallback` w workout-draft-db.ts: po udanym odczycie IDB porównanie z fallbackiem tej samej sesji — wyższa version, tiebreaker updatedAt; zwycięzca dziedziczy pola spoza fallbacku z rekordu IDB i wraca do IDB przez saveActiveDraft/runWrite z guardem Z175; starszy/obcy fallback ignorowany)*
- [x] **Bieg testu:** *(DOWÓD: `npx vitest run src/test/workout-draft-db.test.ts` → PASS (38) FAIL (0), w tym oba testy Z175 — guard nienaruszony)*
- [x] **Commit:** `fix(draft): najswiezszy snapshot wygrywa (IDB vs fallback localStorage) + diagnoza read-only (Z182)`

### Z183: hydracja nie wskrzesza starszego stanu z chmury

**Pliki:** `src/lib/workout-hydration.ts` (`:22-52`), test: `src/test/workout-hydration.test.ts`.

- [x] **Test first (H2b):** *(DOWÓD: bieg przed fixem: 18 testów, FAIL 2 — "Z183: dirty draft NOWSZY niż chmura wygrywa mimo rozjazdu sessionId" + wariant "chmura BEZ updatedAt": expected false to be true)*
- [x] **Test niezmiennika (stary przepływ):** *(dwa testy: dirty:false => false, draft starszy => false — zielone już PRZED fixem i po nim)*
- [x] **Fix:** *(workout-hydration.ts: gałąź rozjazdu sessionId zwraca `draft.dirty && draft.updatedAt > (workoutForDate.updatedAt ?? 0)` zamiast twardego false; pozostałe gałęzie nietknięte)*
- [x] **Bieg:** *(DOWÓD: `npx vitest run src/test/workout-hydration.test.ts` → PASS (18) FAIL (0))*
- [x] **Commit:** `fix(hydration): dirty draft nowszy niz chmura wygrywa mimo rozjazdu sessionId (Z183)`

### Z184: widok nie fabrykuje serii rozgrzewkowej

**Pliki:** `src/lib/exercise-utils.ts` (`sanitizeSets`, `:226-227`), `src/components/ExerciseCard.tsx` (`:392, 402-414`), testy: `src/test/exercise-utils.test.ts`, `src/test/exercise-card-layout.test.tsx`.

- [x] **Test first (H3):** *(DOWÓD: bieg przed fixem: FAIL 2 — "Z184: NIE dopisuje wiersza rozgrzewkowego": expected length 2, got 3; "Z184: draft bez rozgrzewki nie wycieka fabrykatem W do onSetsChange": emitted 3 wiersze z W)*
- [x] **Fix:** *(usunięte dopisywanie W z sanitizeSets; grep: jedyne wywołania to ExerciseCard.tsx:392 (mount) i :404 (resync) — obie renderują to, co dostaną, żadna nie polegała na fabrykacie; W przy NOWEJ liście nadal z createEmptySets/createPrefilledSets; zaktualizowany test duration, który kodował stare zachowanie)*
- [x] **Bieg:** *(DOWÓD: `npx vitest run src/test/exercise-utils.test.ts src/test/exercise-card-layout.test.tsx` → PASS (94) FAIL (0))*
- [x] **Commit:** `fix(sets): sanitizeSets nie fabrykuje serii rozgrzewkowej; usunieta rozgrzewka nie wraca po resume (Z184)`

### Z185: tożsamość swapu "tylko dziś" przeżywa restart

**Pliki:** `src/lib/workout-draft-db.ts` (kształt `ActiveWorkoutDraft`, `:12-55`), `src/lib/workout-draft-snapshot.ts` (`:88-169`), `src/pages/WorkoutDay.tsx` (`:205, 403-412`), `src/lib/workout-day-view.ts` (`:25-62`), testy: `src/test/workout-day-view.test.ts`, `src/test/workout-draft-snapshot.test.ts`.

- [x] **Test first (H1):** *(DOWÓD: bieg przed fixem: FAIL 1 — "Z185: klucz swapu ZASTĘPUJE kartę planu": dziś 4 karty zamiast 3; niezmiennik "oba klucze anormalnie" zielony już przed fixem)*
- [x] **Fix (dwie warstwy, obie konieczne):** *(1. persystencja: pole `sessionSwaps` w ActiveWorkoutDraft + normalizeSessionSwaps + withFallbackSave/Load + WorkoutDraft (workout-draft.ts) + dziedziczenie w buildWorkoutDraftSnapshot + resolveFresherFallback; zapis w handleApplySwap scope='today', odczyt w applyWorkoutState przy hydracji draftu. 2. samonaprawa: buildDayFromDraft mapuje klucz `${planId}__swap-` na kartę planu (zastępuje), chyba że draft anormalnie ma też klucz planu — wtedy swap w extras bez utraty edycji)*
- [x] **Test niezmiennika:** *(wszystkie wcześniejsze przypadki workout-day-view.test.ts zielone, extras dla prawdziwych ad-hoc bez zmian)*
- [x] **Bieg:** *(DOWÓD: `npx vitest run src/test/workout-draft-snapshot.test.ts src/test/workout-draft-db.test.ts src/test/workout-day-view.test.ts` → PASS (75) FAIL (0); pełny `npx vitest run src/test/` → PASS (1175) FAIL (0) + typecheck czysty; przy okazji zaktualizowany helper checkFirstOpenSet w rest-timer-controller.test.tsx, który zakładał fabrykat W sprzed Z184)*
- [x] **Commit:** `fix(swap): tozsamosc cwiczenia "tylko dzis" przezywa restart (persist + reconcile w widoku) (Z185)`

### Z186: bramka sekwencji "kill w trakcie treningu"

**Pliki:** `e2e/full-app.spec.ts` (okolice `:999-1010`) lub nowy `e2e/resume-after-kill.spec.ts`.

- [x] **Test e2e (nowy scenariusz):** *(`e2e/resume-after-kill.spec.ts`: start z planu → warmup-generate 4xW → odhacz 4W+2 robocze → poll draftu {warmup:4, warmupDone:4, workingDone:2} → wyjście na Dashboard → reload (kill) → powrót przez auto-resume X10 LUB "Kontynuuj trening" → asercje: 4 wiersze W, 6 przycisków "Odznacz serię" (stan 1:1), liczba nieodhaczonych bez zmian, "Zakończ trening" dostępny)*
- [x] **Bieg:** *(DOWÓD: `npx playwright test --project=chromium e2e/resume-after-kill.spec.ts` → PASS (1) FAIL (0), 6.5 s)*
- [x] **Commit:** `test(e2e): sekwencja kill -> kontynuuj: serie 1:1, zero wskrzeszen (Z186)`

---

## FAZA 2: kuloodporny timer przerwy (Z187-Z190). Niezmiennik: KAŻDA odhaczona seria (także rozgrzewkowa) startuje przerwę, chyba że to ostatnia seria treningu; koniec przerwy ZAWSZE sprząta stan (nawet gdy sygnały rzucą); deadline jest w JEDNYM miejscu (właściciel) i przeżywa kill.

### Z187: przerwa startuje też po serii rozgrzewkowej

**Pliki:** `src/components/ExerciseCard.tsx` (`:498-521`), test: `src/test/rest-timer-controller.test.tsx`.

- [x] **Test first:** *(DOWÓD: bieg przed fixem: PASS 8, FAIL 1 — "Z187: odhaczenie serii W woła onRestStart z czasem warmupSeconds (45 s)": onRestStart niewywołane; niezmiennik "robocza dostaje 90 s" zielony już przed fixem)*
- [x] **Fix:** wyciągnij start przerwy przed warunek roboczych: *(wdrożone wg szkicu poniżej, commit 36e80f34)*

```tsx
if (turningOn && FEATURE_FLAGS.workoutTimers && !intervalSpec) {
  const workingAfter = newSets.filter(s => !s.isWarmup);
  const allDone = !currentSet.isWarmup
    && workingAfter.length > 0 && workingAfter.every(s => s.completed);
  const seconds = resolveRestSeconds(loadRestSettings(), {
    isWarmup: currentSet.isWarmup,
    exerciseKey: exercise.name,
    exerciseFinished: allDone,
  });
  unlockTimerSound();
  onRestStart?.(exercise.id, seconds);
}
if (turningOn && !currentSet.isWarmup) { /* zostaje: haptic complete + playTimerSound('complete') przy allDone */ }
```

*(Fix wdrożony dokładnie wg szkicu — start przerwy przed warunkiem roboczych, allDone z guardem `!currentSet.isWarmup`.)*

- [x] **Test niezmiennika:** *(sekwencja Z144 "ostatnia gasi wszystko" i "przedostatnia startuje" zielone bez zmian)*
- [x] **Bieg:** *(DOWÓD: `npx vitest run src/test/rest-timer-controller.test.tsx` → PASS (9) FAIL (0))*
- [x] **Commit:** `fix(timer): przerwa startuje takze po serii rozgrzewkowej (martwa galaz warmupSeconds ozywa) (Z187)`

### Z188: deadline u właściciela + persystencja przez kill

**Pliki:** `src/hooks/useRestTimerController.ts`, `src/components/RestBar.tsx`, `src/pages/WorkoutDay.tsx`, testy: `src/test/rest-timer-controller.test.tsx`, `src/test/rest-bar.test.tsx`.

- [x] **Test first (kontroler):** *(DOWÓD: bieg przed implementacją: PASS 7, FAIL 8 — nowe testy Z188 (start/adjust/stop/resume×3) + stare asercje przepisane na deadlineAt/totalSeconds)*
- [x] **Fix (kontroler = źródło prawdy):** *(wdrożone wg szkicu poniżej, commit 876eb38f)*

```ts
export interface RestRunState {
  exerciseId: string;
  deadlineAt: number;      // epoch ms konca przerwy
  totalSeconds: number;    // pelna dlugosc do paska postepu
  runId: number;
}
// startRest(exerciseId, seconds): deadlineAt = Date.now() + seconds*1000; persist
// adjustRest(delta): deadlineAt += delta*1000 (min: teraz), totalSeconds za zmiana; persist
// stopRest(): stan null; localStorage.removeItem(KEY)
// resumeFromStorage(): odczyt + walidacja (deadline w przyszlosci, exerciseId niepusty)
```

*(Kontroler wdrożony wg szkicu: `useRestTimerController.ts` z persist/clear + resumeFromStorage z walidacją kształtu i cancelRestEndNotification przy przeterminowanym deadline.)*

- [x] **Fix (RestBar czysto prezentacyjny):** *(RestBar bez własnego stanu: deadlineAt/totalSeconds propsami, ±15 przez onAdjust do właściciela, tykanie 250 ms zostaje; startRest/adjustRest z rest-timer.ts wypięte z RestBar. Efekt notyfikacji: dep `[runId, deadlineAt]` z t/exerciseLabel w refach — deadlineAt zmienia się WYŁĄCZNIE przy starcie i korekcie ±15, nigdy od tykania, a korekta MUSI przeplanować notyfikację (test niezmiennika "zmiana czasu PRZEPLANOWUJE"); zmiana identity t/labelki nie restartuje niczego — nowy test "Z188: zmiana exerciseLabel NIE restartuje przerwy")*
- [x] **Fix (resume):** *(WorkoutDay: efekt raz per mount (restResumeDone ref) woła resumeFromStorage() po hydracji sesji; przy isCompleted zawsze stopRest; nowy prop onRestAdjust w ExerciseCard podpięty do adjustRestTimer)*
- [x] **Test niezmiennika:** *(rest-bar.test.tsx przepisany na OwnerHarness (10 przypadków, w tym "powrót z tła pokazuje koniec" bez zmiany semantyki); rest-timer-controller.test.tsx przepisany na deadlineAt/totalSeconds)*
- [x] **Bieg:** *(DOWÓD: `npx vitest run src/test/rest-bar.test.tsx src/test/rest-timer-controller.test.tsx` → PASS (25) FAIL (0); pełny `npx vitest run src/test/` → PASS (1184) FAIL (0); typecheck 0)*
- [x] **Commit:** `refactor(timer): deadline w kontrolerze + persystencja localStorage; RestBar czysto prezentacyjny (Z188)`

### Z189: koniec przerwy ZAWSZE sprząta + watchdog + fail-open

**Pliki:** `src/components/RestBar.tsx` (`:85-96`), `src/pages/WorkoutDay.tsx` (`:216-232`), `src/lib/workout-session-state.ts` (dokumentacyjnie), testy: `src/test/rest-bar.test.tsx`, `src/test/rest-timer-controller.test.tsx`.

- [x] **Test first (sprzątanie):** *(DOWÓD: RED — "Z189: wyjątek sygnału końca NIE blokuje onFinished" FAILED przed fixem, playTimerSound rzucający blokował onFinished)*
- [x] **Fix (kolejność):** *(RestBar: NAJPIERW onFinished, potem cancel+sygnały w try/catch → `reportClientErrorWithCurrentUid({code:'rest-finish-signal-failed'})`; mock global-error-telemetry w teście — moduł ciągnie Firestore)*
- [x] **Test first (watchdog):** *(DOWÓD: RED — "Z189: watchdog gasi wiszący stan po >3 s od deadline" FAILED przed fixem)*
- [x] **Fix (watchdog):** *(watchdog w useRestTimerController — hooku-właścicielu stanu, efekt aktywny tylko gdy restState !== null, setInterval 1000 ms → po 3 s od deadline: stan null + clear localStorage + cancelRestEndNotification; działa w WorkoutDay przez hook i jest testowalny renderHookiem)*
- [x] **Test first (fail-open):** *(DOWÓD: RED — 3 testy `shouldStartRest` FAILED przed fixem, brak eksportu; "PUSTA lista → true" pokrywa dzisiejsze fail-closed)*
- [x] **Fix (fail-open):** *(nowa czysta funkcja `shouldStartRest` w workout-session-state.ts: `exercises.length === 0 || hasRemainingWork(...)`; handleRestStart w WorkoutDay przepięty — decyzja testowalna unit-testem zamiast kopii logiki w harnessie)*
- [x] **Bieg:** *(DOWÓD: `npx vitest run src/test/workout-session-state.test.ts src/test/rest-bar.test.tsx src/test/rest-timer-controller.test.tsx` → PASS (44) FAIL (0); pełny vitest → PASS (1189) FAIL (0); typecheck 0; lint 0)*
- [x] **Commit:** `fix(timer): stan przed sygnalami + watchdog samonaprawy + fail-open bramki konca treningu (Z189)`

### Z190: bramka sekwencji timera

**Pliki:** `src/test/rest-timer-controller.test.tsx` (rozszerzenie), `e2e/` (istniejący spec timera, jeśli jest; inaczej scenariusz w `full-app.spec.ts`).

- [x] **Test sekwencji (vitest, jeden przebieg):** *(describe "Z190" w rest-timer-controller.test.tsx: W→0:45 (runId 1) → robocza→1:30 (runId 2) → jedyna seria B→2:30 przejęte przez B (runId 3, JEDNA notyfikacja z nazwą B, localStorage niesie ex-b) → ostatnia seria treningu→zero pasków/notyfikacji/storage; drugi przypadek: kill w środku przerwy = nowy mount + resumeFromStorage wraca z TYM SAMYM deadline)*
- [x] **Bieg:** *(DOWÓD: `npx vitest run src/test/rest-timer-controller.test.tsx` → PASS (18) FAIL (0); pełny `npx vitest run src/test/` → PASS (1191) FAIL (0); typecheck 0, lint 0)*
- [x] **Commit:** `test(timer): sekwencja rozgrzewka -> robocza -> przejecie -> koniec -> kill/resume (Z190)`

---

## FAZA 3: dialog zamykalny ZAWSZE (Z191-Z193). Niezmiennik: żaden dialog w apce nie może współistnieć z modalnym menu; każdy dialog ma X o tap targecie >= 44 px i zamyka się tapem w overlay.

### Z191: poprawna sekwencja menu → dialog + guard miniatury

**Pliki:** `src/components/ExerciseCard.tsx` (`:936-938, 1039-1079`), test: `src/test/exercise-card-layout.test.tsx`.

- [x] **Test first:** *(DOWÓD: RED — "Z191: klik Instrukcje zamyka menu PRZED otwarciem dialogu" FAILED przed fixem: treść dialogu w DOM synchronicznie po kliku, pod żywą warstwą menu)*
- [x] **Fix:** *(kontrolowany `menuOpen` + helper `selectFromMenu` (preventDefault → setMenuOpen(false) → rAF → akcja) na WSZYSTKICH pozycjach menu: Instrukcje, Zamień, Pomiń, Notatka, Przypnij; miniatura wideo z guardem menuOpen wg szkicu)*
- [x] **Bieg:** *(DOWÓD: PASS (50) FAIL (0) — exercise-card-layout + rest-timer-controller; komentarz nieaktualnego obejścia nad DropdownMenu zastąpiony opisem mechanizmu Z191; test callbacków Pomiń/Zamień dostał vi.waitFor — akcja odpala się klatkę po zamknięciu menu)*
- [x] **Commit:** `fix(modal): dialog otwiera sie dopiero po zamknieciu menu (koniec pointer-events lock) (Z191)`

### Z192: X 44 px + bezpiecznik warstw we współdzielonym dialogu

**Pliki:** `src/components/ui/dialog.tsx` (`:52-55`), `src/components/ui/sheet.tsx` (`:63`), test: `src/test/dialog-close-target.test.tsx` (nowy).

- [x] **Test first:** *(DOWÓD: nowy `src/test/dialog-close-target.test.tsx`, RED przed fixem: PASS 1, FAIL 3 — tap target dialog + sheet + bezpiecznik; test "blokada NIE zdejmowana przy żywym menu" zielony od początku)*
- [x] **Fix (tap target):** *(dialog.tsx + sheet.tsx: `right-2 top-2 flex h-11 w-11 items-center justify-center rounded-lg`, glify bez zmian; DialogTitle dostał `pr-8` — tytuł nie wjeżdża pod 44-pikselowy X)*
- [x] **Fix (bezpiecznik):** *(efekt w DialogContent: po 350 ms osierocony `body.style.pointerEvents === 'none'` bez `[data-state="open"][role="menu"]` w DOM jest zdejmowany; z żywym menu — nietykany)*
- [x] **Bieg:** *(DOWÓD: dialog-close-target PASS (4) FAIL (0); pełny vitest → PASS (1196) FAIL (0); typecheck 0; lint 0)*
- [x] **Commit:** `fix(ui): X dialogow 44px (HIG) + bezpiecznik pointer-events przy otwartym dialogu (Z192)`

### Z193: bramka e2e warstw

**Pliki:** `e2e/exercise-card-v3.spec.ts` (rozszerzenie).

- [x] **Test e2e:** *(nowy test w exercise-card-v3.spec.ts: (a) menu→Instrukcje→dialog→X za pierwszym kliknięciem→odhaczenie serii działa; (b) menu otwarte→tap w miniaturę: modalne menu pochłania pierwszy tap (standard UX — asercja: menu ZNIKA), dialog wideo z kolejnego tapu działa i X zamyka; (c) asercje `document.body.style.pointerEvents !== 'none'` po obu dialogach)*
- [x] **Bieg:** *(DOWÓD: `npx playwright test --project=chromium e2e/exercise-card-v3.spec.ts` → PASS (24) FAIL (0), 53 s)*
- [x] **Commit:** `test(e2e): menu -> dialog -> X za pierwszym tapem, body bez pointer-events lock (Z193)`

---

## FAZA 4: miniatury bez czarnych kafli (Z194-Z195). Niezmiennik: miniatura NIGDY nie renderuje pustej czerni: poster JPEG, a przy jego braku ikona; zero dekoderów wideo na liście treningu.

### Z194: postery JPEG w pipeline i na CDN

**Pliki:** `animacje-cwiczen/do_cdn.sh`, `animacje-cwiczen/na_bunny.sh` (poza gitem: NIE commituj, zmiany opisz w raporcie), weryfikacja: curl.

- [x] **Generacja:** *(DOWÓD: 137/137 posterów wygenerowanych komendą z planu, ~3.5 KB/szt.; krok postera dopisany do funkcji `kompresuj` w `do_cdn.sh` — przyszłe klipy dostają jpg automatycznie)*
- [x] **Upload:** *(DOWÓD: pętla curl PUT tym samym mechanizmem co na_bunny.sh: "wyslane: 137 bledy: 0"; `na_bunny.sh` rozszerzony o `*.jpg` na przyszłość; istniejące mp4 nietknięte)*
- [x] **Weryfikacja (dowód do checkboxa):** *(DOWÓD: curl -sI dla burpees + 5 losowych slugów (ab-rollout, wioslowanie-hantlami-na-lawce-przodem, sissy-squat, wyciskanie-sztangi-na-skosie, wioslowanie-sztanga): wszystkie `HTTP/2 200` + `content-type: image/jpeg`)*
- [x] **Commit:** brak (katalog poza gitem); dowód = wynik curl w checkboxie + wpis w raporcie.

### Z195: miniatura = `<img>` z posterem, fallback ikona

**Pliki:** `src/lib/exercise-media.ts` (`:187-191`), `src/components/ExerciseCard.tsx` (`:935-972`), testy: `src/test/exercise-media.test.ts` (jeśli jest; inaczej nowy), `src/test/exercise-card-layout.test.tsx`.

- [x] **Test first:** *(DOWÓD: RED przed fixem — import getExercisePosterUrl wywala suite (funkcji nie było); po fixie exercise-media PASS (4))*
- [x] **Fix (lib):** *(wdrożone wg szkicu, wariant z guardem na brak name)*

```ts
export const getExercisePosterUrl = (name: string): string | null => {
  const file = ANIMATION_FILES[slugifyExercise(name)];
  return file ? `${CDN_BASE}/${file.replace(/\.mp4$/, '.jpg')}` : null;
};
```

- [x] **Fix (karta):** *(miniatura = `<img src={posterUrl} loading="lazy" decoding="async">`, onError → ikona Dumbbell + ślad `exercise-poster-error`; `thumbVideoFailed`→`thumbFailed`; overlay `bg-black/15`; dialog wideo NIETKNIĘTY; ExerciseLibrary bez zmian; mock getExercisePosterUrl w teście layoutu)*
- [x] **Test niezmiennika:** *(e2e/exercise-video.spec.ts: miniatury = `img[src$=".jpg"]`, twardy niezmiennik `header video → count 0`, dialog nadal montuje `<video>` po otwarciu i gra/pokazuje controls; route jpg→sample-poster.jpg (nowa fixture), mp4→sample-video.mp4; selektory Z193 zaktualizowane)*
- [x] **Bieg:** *(DOWÓD: vitest exercise-media + exercise-card-layout PASS (36) FAIL (0); pełny vitest PASS (1197) FAIL (0); e2e exercise-video + exercise-card-v3: 27 passed (53 s); typecheck 0; lint 0)*
- [x] **Commit:** `fix(media): miniatura cwiczenia = poster JPEG (WebKit nie maluje klatki przy preload=metadata); fallback ikona (Z195)`

---

## FAZA 5: wiersz serii mieści "122.5" (Z196)

**Pliki:** `src/components/ExerciseCard.tsx` (`:272, 647-655, 688, 722, 736, 748, 762, 824-829, 851-879, 1086, 1110`), test: `src/test/exercise-card-layout.test.tsx`.

- [x] **Test first:** *(DOWÓD: RED przed fixem — "Z196: pola serii mają px-1..." FAILED, className bez px-1 i stary szablon 1fr/1fr)*
- [x] **Fix A (padding):** *(px-1 dopisany do WSZYSTKICH 7 wystąpień klasy `exercise-card-input h-12 text-base font-bold` w ExerciseCard; `.exercise-card-input` w index.css NIETKNIĘTA)*
- [x] **Fix B (kolumny):** *(weight_reps i assisted: `26px_minmax(0,0.9fr)_1.25fr_0.85fr_40px_44px`; wdd: `26px_1.1fr_1.1fr_0.8fr_40px_44px`; duration i bodyweight bez zmian; nagłówek z tego samego gridCols — zsynchronizowany automatycznie, asercja "minimum 2 gridy z szablonem")*
- [x] **NIE ROBIĆ:** *(font zostaje text-base 16 px — nietknięty)*
- [x] **Weryfikacja rachunkiem (do raportu):** *(@390 px: grid ~342 px, stałe 26+40+44=110 px + 5×8 px gap = 150; zostaje 192 px na 3.0fr → KG=1.25fr≈80 px kolumny, ~70 px wnętrza przy px-1; "122.5" Inter Bold 16 ≈44 px → zapas ~26 px; POWT.=0.85fr≈54 px, ~46 px wnętrza, 3 cyfry ~29.5 px — mieszczą się z zapasem)*
- [x] **Bieg:** *(DOWÓD: `npx vitest run src/test/exercise-card-layout.test.tsx` → PASS (33) FAIL (0); pełny vitest PASS (1198) FAIL (0); typecheck 0; lint 0)*
- [x] **Commit:** `fix(card): pola KG/POWT mieszcza 122.5 (px-1 + proporcjonalne kolumny, naglowek zsynchronizowany) (Z196)`

---

## FAZA 6: share bez wstydu (Z197-Z199)

### Z197: szablon ZDJĘCIE: treść w dolnej 1/3, twarz czysta

**Pliki:** `src/lib/share-utils.ts` (`:197-280`, `renderFooter :62-71`), test: `src/test/share-image.test.ts`.

- [x] **Test first:** *(DOWÓD: RED przed fixem — 3 nowe testy Z197 w share-image.test.ts FAILED: margin-bottom:auto obecny, scrim liniowy, brightness(0.40), lista 4 pozycje)*
- [x] **Fix:** *(wdrożone wg 4 punktów poniżej)*
  1. Nagłówek (`:248`): usuń `margin-bottom:auto`; zaraz po nim `<div style="flex:1"></div>` (JEDEN spacer; stopka zachowuje `margin-top:auto`: teraz bez konkurencji, więc treść klei się do dołu).
  2. Scrim (`:242`): `linear-gradient(180deg, rgba(0,0,0,0) 0%, rgba(0,0,0,0.15) 45%, rgba(0,0,0,0.75) 68%, rgba(0,0,0,0.92) 100%)`.
  3. `dim` default (`:202`): `0.6` → `0.35` (brightness 0.65; twarz przestaje być zabita, tekst i tak leży na ciemnym pasie).
  4. Lista (`:212, :221`): max 3 ćwiczenia (z `+N więcej...`), żeby dolny pas nie urósł ponad 1/3.
- [x] **Bieg:** *(DOWÓD: `npx vitest run src/test/share-image.test.ts` → PASS (6) FAIL (0); render ręczny: HTML szablonu → screenshot Playwright 540×960 — nagłówek u góry, ŚRODEK KADRU CZYSTY, statystyki+lista+PR+stopka w dolnej ~1/3, scrim przezroczysty do ~45% i ciemny pas na dole; screenshot w scratchpadzie share-photo.png)*
- [x] **Commit:** `fix(share): szablon zdjecia: statystyki i lista w dolnej 1/3, scrim strefowy, jasniejsza twarz (Z197)`

### Z198: Pobierz z widocznym sukcesem

**Pliki:** `src/components/ShareWorkoutDialog.tsx` (`:116-152, 234-238`), i18n: `src/i18n/locales/pl.ts` (okolice `:892`), `en.ts`, test: `src/test/share-dialog.test.tsx` (nowy lub istniejący plik dialogu).

- [x] **Test first:** *(DOWÓD: nowy `src/test/share-dialog.test.tsx`, RED przed fixem: PASS 1, FAIL 2 — "udany share po Pobierz" i "po Udostępnij" bez stanu Zapisano; test AbortError zielony od początku — dziś też nic nie udawał)*
- [x] **Fix:** *(systemShare(): Promise<boolean> — true po await, false przy AbortError/błędzie; markSaved('download'|'share') = setSavedAction + hapticSuccess + timeout 1800 ms; render Check + `comp.share.saved`; web anchor też dostaje markSaved; klucze "Zapisano ✓"/"Saved ✓" w OBU locale; przy okazji komentarz HTML z polskimi znakami z Z197 przeniesiony do JS — globalny guard i18n Z168 go łapał w template literalu)*
- [x] **Bieg:** *(DOWÓD: share-dialog PASS (3) FAIL (0); pełny vitest PASS (1204) FAIL (0); typecheck exit 0; lint exit 0)*
- [x] **Commit:** `feat(share): stan Zapisano na Pobierz/Udostepnij (Check + haptyka), AbortError bez falszywego sukcesu (Z198)`

### Z199: sticky hover zdjęty SYSTEMOWO + press feedback

**Pliki:** `tailwind.config.ts`, `src/components/ui/button.tsx` (`:8`), test wizualny + `npm run build`.

- [x] **Fix (wariant hover):** *(plugin addVariant('hover', '@media (hover: hover) and (pointer: fine) { &:hover }') w tailwind.config.ts — wszystkie `hover:` w apce przestają się przyklejać na dotyku)*
- [x] **Fix (press feedback):** *(button.tsx: klasy bazowe z `active:scale-[0.97] transition-[transform,background-color] duration-100`)*
- [x] **Weryfikacja:** *(DOWÓD: build exit 0; analiza dist CSS: 1 scalony blok `@media (hover: hover) and (pointer: fine)` z 67 regułami :hover; poza nim tylko 3 reguły group-hover/peer-hover sidebara shadcn (INNE warianty, desktop-only, wpis w ODŁOŻONE już to pokrywa); `--tw-scale-x: .97` obecne; desktop hover działa — reguły żyją w media query spełnionym przez urządzenia z kursorem, e2e chromium (hover:hover) zielone)*
- [x] **Test niezmiennika:** *(DOWÓD: pełny vitest PASS (1204) FAIL (0); typecheck 0; lint 0; bundle budget PASS: 1 532 326 / 1 536 000 B, limit NIE podnoszony)*
- [x] **Commit:** `fix(ui): hover tylko na urzadzeniach z kursorem (wariant globalny) + active-scale na przyciskach (Z199)`

---

## FAZA 7: bramki, wdrożenie, dystrybucja

- [ ] **Pełne bramki (dowód = wynik komendy, bez `| tail`):** `npm run test` → `npm run typecheck` → `npm run lint` → `npm run build` → `npm run check:bundle-budget` → `npm run build:mobile` + `npm run check:dist-smoke` → `npm run build` + `npm run check:dist-offline` → e2e:mock w całości.
- [ ] **Scenariusz przerwania (reguła 5, symulator):** start z planu → odhacz W (timer 45 s startuje!) → wyjście → szybki trening → powrót do planu (wszystkie ćwiczenia + serie 1:1) → zakończenie → sync.
- [ ] **DECYZJE.md:** wpis zbiorczy X23 (Z182-Z199): co, dlaczego, root cause, weryfikacja.
- [ ] **Web deploy:** `npm run deploy` + weryfikacja nowego hasha bundle na live (grep index-*.js).
- [ ] **iOS:** bump `CURRENT_PROJECT_VERSION` → 82 (6 wystąpień) → `scripts/release-ios.sh "X23: fix duplikatow serii po kill, timer po rozgrzewce + watchdog, zamykanie wideo, miniatury, szersze pola, share"`.
- [ ] **Dystrybucja (ZAWSZE po uploadzie):** `uv run --with "pyjwt[crypto]" --with requests scripts/testflight_external.py 82 --whats-new "..."` (obie grupy + Beta App Review; sam upload zostawia Roberta bez builda).
- [ ] **PLAN.md:** nowe pozycje tech debt z sekcji ODŁOŻONE.

---

## KROKI USERA (po wdrożeniu, realny iPhone)

1. Trening testowy: odhacz serię ROZGRZEWKOWĄ → pasek przerwy 45 s startuje; odhacz roboczą → 90 s; zgaś ekran na 2 min w trakcie przerwy → notyfikacja systemowa przychodzi, po odblokowaniu pasek pokazuje stan realny (nie zamrożony).
2. Force-quit w środku treningu (z wygenerowaną i odhaczoną rozgrzewką) → Kontynuuj trening → liczba i stan serii 1:1.
3. Otwórz menu "..." → Instrukcje → zamknij X (pierwszy tap). Otwórz wideo z miniatury → zamknij X.
4. Miniatury na karcie: klatka ćwiczenia zamiast czerni (Wi-Fi i LTE).
5. Wpisz 122.5 w KG → wszystkie cyfry widoczne.
6. Udostępnij ze zdjęciem: statystyki w dolnej 1/3, twarz czysta; Pobierz → "Zapisano ✓" + haptyka; przyciski nie zostają podświetlone.

## ODŁOŻONE (świadomie poza X23)

- Przycięte kadry miniatur "tułów i uda" z README pipeline'u (dziś: pełny kadr 4:3 jako poster).
- 93 klipy TODO + 13 FAIL w `animacje-cwiczen/` (generacja/retusz osobną sesją).
- Wpis `animacje-cwiczen/` do `.gitignore` (poza planem: decyzja usera, katalog celowo wisi jako untracked).
- Migracja pozostałych `hover:` na semantyczne stany (wariant globalny z Z199 rozwiązuje problem klasą, nie per komponent).
- Ujednolicenie tap targetów X w pozostałych overlayach (sheet objęty Z192; przegląd reszty osobno).
