# PLAN PRO-A: De-emojizacja + kontrakt ikon

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Usunięcie wszystkich emoji z chrome UI (komponenty, strony, i18n, share card) i zastąpienie ich ikonami lucide-react wg jednego kontraktu kolorów, plus trwała bramka `check:no-emoji`.

**Architecture:** Zostajemy przy lucide-react (już w 96 plikach). Emoji występują w ~15 skupiskach; każde skupisko = osobny task z izolowanym commitem. Mapa typów aktywności trafia do wspólnego modułu `src/lib/activity-icons.ts` (dziś zduplikowana w 3+ plikach). Na końcu skrypt-bramka skanuje repo i pilnuje regresji.

**Tech Stack:** React 18 + TS, lucide-react, vitest + React Testing Library, Node (skrypt bramki).

## Global Constraints

- Kontrakt kolorów ikon: w listach/wierszach ikona monochromatyczna (`text-muted-foreground`), kolor TYLKO gdy niesie informację (status, PR, metryka). Tła statusowe zawsze z przezroczystością `/10`, tekst/ikona w pełnym kolorze (CLAUDE.md zasada 8).
- Emoji DOZWOLONE tylko w: tekście kopiowanym do schowka (`Analytics.tsx` linie ~155-163, to treść, nie chrome) oraz w `src/data/*` (nierenderowane pola `title`, poza zakresem tego planu).
- Zakaz zmian logiki. Każdy task zmienia wyłącznie warstwę prezentacji.
- i18n: każda zmiana klucza w OBU plikach `src/i18n/locales/pl.ts` i `en.ts` (inaczej typecheck padnie).
- Zakaz dodawania framer-motion i nowych bibliotek ikon.
- Po każdym tasku: `npm run test` + `npm run typecheck` zielone, commit.
- Bump iOS przy wydaniu: NASTĘPNY `CURRENT_PROJECT_VERSION` = 96, versionCode Android = 12 (patrz memory/tracker).

---

### Task 1: Wspólny moduł ikon aktywności `activity-icons.ts`

**Files:**
- Create: `src/lib/activity-icons.ts`
- Test: `src/lib/__tests__/activity-icons.test.ts`

**Interfaces:**
- Produces: `getActivityIcon(type: string): LucideIcon` oraz `ActivityIcon` (komponent pomocniczy `{ type, className }`). Konsumowane przez Taski 5-7.

- [ ] **Step 1: Napisz failing test**

```ts
// src/lib/__tests__/activity-icons.test.ts
import { describe, expect, it } from 'vitest';
import { Bike, Dumbbell, Medal } from 'lucide-react';
import { getActivityIcon } from '@/lib/activity-icons';

describe('getActivityIcon', () => {
  it('mapuje znane typy na ikony lucide', () => {
    expect(getActivityIcon('Ride')).toBe(Bike);
    expect(getActivityIcon('WeightTraining')).toBe(Dumbbell);
  });
  it('nieznany typ dostaje Medal (fallback jak dawne 🏅)', () => {
    expect(getActivityIcon('SomethingNew')).toBe(Medal);
  });
});
```

- [ ] **Step 2: Uruchom test, ma paść** — `npx vitest run src/lib/__tests__/activity-icons.test.ts` → FAIL (module not found).

- [ ] **Step 3: Implementacja**

```ts
// src/lib/activity-icons.ts
import {
  Activity, Bike, Dumbbell, Flame, Footprints, Leaf, Medal, Mountain,
  PersonStanding, Waves, type LucideIcon,
} from 'lucide-react';

// Jedna mapa dla całej apki (dawniej emoji zduplikowane w StravaActivityCard,
// StravaActivityDetail, AddCardioDialog, strava-utils). Fallback = Medal.
const ACTIVITY_ICONS: Record<string, LucideIcon> = {
  Run: Footprints,
  Ride: Bike,
  Swim: Waves,
  Walk: PersonStanding,
  Hike: Mountain,
  WeightTraining: Dumbbell,
  Yoga: Leaf,
  Workout: Activity,
  Treadmill: Footprints,
  IndoorRide: Bike,
  JumpRope: Activity,
  HIIT: Flame,
  Other: Medal,
};

export const getActivityIcon = (type: string): LucideIcon =>
  ACTIVITY_ICONS[type] ?? Medal;
```

- [ ] **Step 4: Test zielony** — `npx vitest run src/lib/__tests__/activity-icons.test.ts` → PASS.
- [ ] **Step 5: Commit** — `git add -A && git commit -m "feat(icons): wspólna mapa ikon aktywności lucide (PRO-A T1)"`

---

### Task 2: TrainingDayCard bez emoji (Dashboard, plan tygodnia)

**Files:**
- Modify: `src/components/TrainingDayCard.tsx:44-50` (ikona statusu) i `:77-79` (podtytuł)
- Test: `src/components/__tests__/training-day-card-icons.test.tsx`

- [ ] **Step 1: Failing test**

```tsx
// src/components/__tests__/training-day-card-icons.test.tsx
import { describe, expect, it, vi } from 'vitest';
import { render } from '@testing-library/react';
import { TrainingDayCard } from '@/components/TrainingDayCard';

vi.mock('@/contexts/LanguageContext', () => ({
  useTranslation: () => ({ t: (k: string) => k, lang: 'pl' }),
}));

const day = { id: 'd1', dayName: 'Poniedziałek', focus: 'Push', exercises: [{ id: 'e1' }] } as never;
const EMOJI_RE = /[\u{1F300}-\u{1FAFF}\u{2600}-\u{27BF}\u{2B00}-\u{2BFF}]/u;

describe('TrainingDayCard', () => {
  it('nie renderuje emoji w żadnym stanie', () => {
    for (const props of [{}, { skipped: true }] as const) {
      const { container, unmount } = render(
        <TrainingDayCard day={day} onClick={() => {}} {...props} />,
      );
      expect(container.textContent ?? '').not.toMatch(EMOJI_RE);
      unmount();
    }
  });
});
```

- [ ] **Step 2: Uruchom, ma paść** (obecnie renderuje 🏋️).
- [ ] **Step 3: Implementacja** — w `TrainingDayCard.tsx`:

```tsx
// import: dodaj CheckCircle2, XCircle, Dumbbell do istniejącego importu lucide-react
// Ikona statusu (dawne '✅' : '❌' : '🏋️'):
<div className={cn(
  "h-[42px] w-[42px] rounded-xl flex items-center justify-center shrink-0",
  isCompleted ? "bg-fitness-success/10" : isMissed ? "bg-destructive/10" : "bg-primary/10"
)}>
  {isCompleted
    ? <CheckCircle2 className="h-5 w-5 text-fitness-success" />
    : isMissed
      ? <XCircle className="h-5 w-5 text-destructive" />
      : <Dumbbell className="h-5 w-5 text-primary" />}
</div>
```

```tsx
// Podtytuł (dawne `{focus} · 🏋️ {n}`):
<p className="text-[13px] text-muted-foreground mt-1 flex items-center gap-1">
  {localizeFocus(day.focus, lang)}
  <span aria-hidden>·</span>
  <Dumbbell className="h-3 w-3" aria-hidden />
  {day.exercises.length}
</p>
```

- [ ] **Step 4: Testy zielone** — nowy test + `npx vitest run src/components/__tests__/workout-day-view.test.ts` (niezmiennik listy dni nie ruszony).
- [ ] **Step 5: Commit** — `git commit -m "feat(ui): TrainingDayCard na ikonach lucide zamiast emoji (PRO-A T2)"`

---

### Task 3: Badge'e ExerciseCard (ekran treningu)

**Files:**
- Modify: `src/components/ExerciseCard.tsx:126` (🎯), `:169` (📅), `:188-198` (⬆⬇↺), `:1027-1034` (🏆 live PR)

- [ ] **Step 1: Znajdź aktualne linie** — `grep -n "🎯\|📅\|⬆\|⬇\|↺\|🏆" src/components/ExerciseCard.tsx` (numery mogły się przesunąć).
- [ ] **Step 2: Implementacja** (importy `Target, CalendarCheck, TrendingUp, TrendingDown, RotateCcw, Trophy` z lucide-react):

```tsx
// NextTargetBadge — zamiast "🎯 {label}":
<Target className="h-3 w-3" aria-hidden /> {labels[advice.kind]}: {target}

// WeeklyTargetBadge — zamiast "📅 {label}":
<CalendarCheck className="h-3 w-3" aria-hidden /> {labels[target.kind]}: {value}

// RzaAdviceBadge — zamiast const icon = '⬆'/'⬇'/'↺':
const Icon = advice.decision === 'progress' ? TrendingUp
  : advice.decision === 'deload' ? TrendingDown : RotateCcw;
// w JSX zamiast {icon}:
<Icon className="h-3 w-3" aria-hidden /> {labels[advice.decision]}: {next}

// Live PR badge — zamiast "🏆 PR {x}":
<Trophy className="h-3 w-3" aria-hidden /> PR {Math.round(toDisplay(livePRWeight))} {unit}
```

Kontener badge ma już `inline-flex items-center gap-1`, ikony wpasują się bez zmian layoutu.

- [ ] **Step 3: Testy** — `npx vitest run src/components/__tests__/` (jeśli któryś test asertuje emoji w badge'ach: zaktualizuj asercje na tekst bez emoji, to zmiana OCZEKIWANA).
- [ ] **Step 4: Commit** — `git commit -m "feat(ui): badge'e ExerciseCard na lucide (PRO-A T3)"`

---

### Task 4: DayPlan + Dashboard: sekcje i pusty stan dnia

**Files:**
- Modify: `src/pages/DayPlan.tsx:107` (💪/🧘 w kole 80px), `:220` (🔥 rozgrzewka), `:242-244` (⚡⏱️🔄 zasady), `:301` (🧘 stretching)
- Modify: `src/pages/Dashboard.tsx:936` (🧘 regeneracja)

- [ ] **Step 1: Implementacja DayPlan** (importy `Dumbbell, Leaf, Flame, Zap, Timer, Repeat`):

```tsx
// :107 — koło pustego stanu (dawne <span className="text-4xl">{💪|🧘}</span>):
{isWorkoutCompleted
  ? <Dumbbell className="h-9 w-9 text-fitness-success" />
  : <Leaf className="h-9 w-9 text-muted-foreground" />}

// :220 — nagłówek rozgrzewki (dawne <span className="text-lg">🔥</span>):
<Flame className="h-4 w-4 text-fitness-warning" aria-hidden />

// :242-244 — zasady treningu (dawne <p>⚡ …</p> itd.):
<p className="flex items-center gap-2"><Zap className="h-3.5 w-3.5 shrink-0 text-muted-foreground" aria-hidden />{trainingRules.weight}</p>
<p className="flex items-center gap-2"><Timer className="h-3.5 w-3.5 shrink-0 text-muted-foreground" aria-hidden />{trainingRules.restMain}</p>
<p className="flex items-center gap-2"><Repeat className="h-3.5 w-3.5 shrink-0 text-muted-foreground" aria-hidden />{trainingRules.supersets}</p>

// :301 — stretching (dawne 🧘):
<Leaf className="h-4 w-4 text-fitness-cyan" aria-hidden />
```

- [ ] **Step 2: Implementacja Dashboard :936** (dawne `{t('dash.recovery.title')} 🧘`):

```tsx
<p className="text-sm font-medium flex items-center gap-1.5">
  {t('dash.recovery.title')}
  <Leaf className="h-4 w-4 text-muted-foreground" aria-hidden />
</p>
```

- [ ] **Step 3: Testy + typecheck** — `npm run test`, `npm run typecheck`.
- [ ] **Step 4: Commit** — `git commit -m "feat(ui): DayPlan i karta regeneracji bez emoji (PRO-A T4)"`

---

### Task 5: Karty aktywności cardio/Strava na wspólnej mapie ikon

**Files:**
- Modify: `src/components/StravaActivityCard.tsx:12-27` (mapa), `:76` (icon), `:95-97` (render), `:118` (↗), `:120-129` (❤️)
- Modify: `src/components/StravaActivityDetail.tsx` (analogiczna mapa emoji, `grep -n "🏃" src/components/StravaActivityDetail.tsx`)
- Modify: `src/components/AddCardioDialog.tsx:20-22` (mapa), `:141` (render chipa)
- Modify: `src/lib/strava-utils.ts` (5 emoji, `grep -n` przed zmianą)

**Interfaces:**
- Consumes: `getActivityIcon(type)` z Taska 1.

- [ ] **Step 1: StravaActivityCard** — usuń lokalną mapę `activityIcons`, w komponencie:

```tsx
import { getActivityIcon } from '@/lib/activity-icons';
import { Heart, MoveUpRight } from 'lucide-react';
// ...
const Icon = getActivityIcon(activity.type);
// render kwadratu ikony (dawne {icon}):
<div className={`h-10 w-10 rounded-lg flex items-center justify-center ${isManual ? 'bg-fitness-cyan/10' : 'bg-orange-500/10'}`}>
  <Icon className={`h-5 w-5 ${isManual ? 'text-fitness-cyan' : 'text-orange-500'}`} />
</div>
// elevation (dawne ↗ {m}m):
<span className="flex items-center gap-0.5"><MoveUpRight className="h-3 w-3" aria-hidden />{Math.round(activity.totalElevationGain)}m</span>
// HR (dawne ❤️ {bpm}):
<span className="flex items-center gap-1"><Heart className="h-3 w-3" aria-hidden />{Math.round(activity.averageHeartrate)}
```

- [ ] **Step 2: AddCardioDialog** — usuń mapę emoji (`:20-22`), chip typu renderuje `<Icon className="h-4 w-4" />` przez `getActivityIcon(type)`.
- [ ] **Step 3: StravaActivityDetail + strava-utils** — te same podmiany przez `getActivityIcon`; w `strava-utils.ts` jeśli emoji trafia do STRINGA (np. tekst udostępniania), zamień na nazwę typu bez ikony.
- [ ] **Step 4: Testy** — `npm run test` (testy fixtures z emoji: aktualizuj), ręcznie: Dashboard z aktywnością Strava renderuje ikonę.
- [ ] **Step 5: Commit** — `git commit -m "feat(ui): aktywności cardio/Strava na wspólnych ikonach lucide (PRO-A T5)"`

---

### Task 6: Toasty PR, Analytics badge, puste stany Strava

**Files:**
- Modify: `src/i18n/locales/pl.ts:1385-1386` i `src/i18n/locales/en.ts:1363-1364`
- Modify: `src/pages/Analytics.tsx:288` (badge 🏆)
- Modify: `src/components/strava/StravaTab.tsx:90` (🏃 pusty stan), `src/components/strava/RacePredictor.tsx:13-16,39` (🏃)

- [ ] **Step 1: i18n** — usuń 🏆 z obu locale:

```ts
// pl.ts
'workout.toast.newPRTitle': 'Nowy rekord! ({n})',
'workout.toast.livePR': 'Nowy rekord: {name} {value}',
// en.ts
'workout.toast.newPRTitle': 'New record! ({n})',
'workout.toast.livePR': 'New record: {name} {value}',
```

- [ ] **Step 2: Analytics badge** — `<Trophy className="h-3 w-3" /> {pr.exerciseName}` (import Trophy), klasa badge zgodna z zasadą 8: `bg-fitness-warning/10 text-fitness-warning border-fitness-warning/30` (usuń `text-yellow-700`, nieczytelne na dark).
- [ ] **Step 3: StravaTab pusty stan** — w kole 48px: `<Footprints className="h-6 w-6 text-orange-500" />`. RacePredictor: mapę 🏃 zastąp `Timer`/`Medal` per dystans lub jedną ikoną `Footprints` (monochrom).
- [ ] **Step 4: Testy** — `npm run test`; `grep -rn "🏆" src/i18n` → 0 wyników.
- [ ] **Step 5: Commit** — `git commit -m "feat(ui): toasty PR i Analytics bez emoji (PRO-A T6)"`

---

### Task 7: Share card bez emoji (obraz generowany)

**Files:**
- Modify: `src/lib/share-utils.ts:77` (💪), `:116` i `:340` (🏆 ${pr})

- [ ] **Step 1: Implementacja** — share-utils generuje HTML string do renderu na obraz, więc bez komponentów React:

```ts
// :77 — usuń cały <span> z 💪 (nagłówek zostaje czysty typograficznie)
// :116 — zamiast `🏆 ${escapeHtml(pr)}`:
`<div style="font-size:14px;margin-bottom:4px;"><span style="font-weight:800;letter-spacing:0.08em;">PR</span> · ${escapeHtml(pr)}</div>`
// :340 — zamiast chipa z 🏆:
`<span style="background:rgba(245,158,11,0.3);border:1px solid rgba(245,158,11,0.5);border-radius:6px;padding:2px 8px;font-size:11px;white-space:nowrap;"><span style="font-weight:800;">PR</span> · ${escapeHtml(pr)}</span>`
```

- [ ] **Step 2: Test ręczny** — wygeneruj share card (dialog Udostępnij po treningu w dev), sprawdź wszystkie 4 szablony.
- [ ] **Step 3: Commit** — `git commit -m "feat(share): karta udostępniania bez emoji (PRO-A T7)"`

---

### Task 8: Bramka `check:no-emoji`

**Files:**
- Create: `scripts/check-no-emoji.mjs`
- Modify: `package.json` (scripts)

- [ ] **Step 1: Skrypt**

```js
// scripts/check-no-emoji.mjs
// Bramka: zero emoji w chrome UI. Whitelist: treść kopiowana do schowka.
import { readFileSync, readdirSync, statSync } from 'node:fs';
import { join } from 'node:path';

const ROOTS = ['src/components', 'src/pages', 'src/i18n', 'src/lib/share-utils.ts'];
const WHITELIST = new Set(['src/pages/Analytics.tsx']); // copy do schowka = treść, nie chrome
const EMOJI = /[\u{1F300}-\u{1FAFF}\u{2600}-\u{27BF}\u{2B00}-\u{2BFF}\u{FE0F}]/u;

const files = [];
const walk = (p) => {
  const st = statSync(p);
  if (st.isDirectory()) return readdirSync(p).forEach((f) => walk(join(p, f)));
  if (/\.(tsx?|ts)$/.test(p) && !/\.test\./.test(p)) files.push(p);
};
ROOTS.forEach(walk);

const offenders = [];
for (const f of files) {
  if (WHITELIST.has(f.replaceAll('\\', '/'))) continue;
  readFileSync(f, 'utf8').split('\n').forEach((line, i) => {
    if (EMOJI.test(line)) offenders.push(`${f}:${i + 1}: ${line.trim().slice(0, 80)}`);
  });
}
if (offenders.length) {
  console.error(`Emoji w chrome UI (${offenders.length}):\n` + offenders.join('\n'));
  process.exit(1);
}
console.log(`check:no-emoji OK (${files.length} plików)`);
```

- [ ] **Step 2: package.json** — dodaj `"check:no-emoji": "node scripts/check-no-emoji.mjs"`.
- [ ] **Step 3: Uruchom** — `npm run check:no-emoji`. Jeśli wskaże pominięte miejsca (np. `src/pages/TrainingPlan.tsx:300-301` ⚡⏱️, `src/pages/Analytics.tsx` poza whitelist, `src/components/DataManagement.tsx`), popraw je wg kontraktu z Tasków 3-4 aż bramka przejdzie.
- [ ] **Step 4: Pełna weryfikacja** — `npm run test && npm run typecheck && npm run lint && npm run build && npm run check:no-emoji`.
- [ ] **Step 5: Commit** — `git commit -m "chore(gate): bramka check:no-emoji + domknięcie resztek (PRO-A T8)"`

---

## Wydanie

- [ ] Checklist z CLAUDE.md: test/typecheck/lint/build, web `npm run deploy`, iOS bump 96 + `scripts/release-ios.sh`, `testflight_external.py`, wpis w `DECYZJE.md`.
- [ ] Scenariusz ręczny: Dashboard → DayPlan → trening (badge'e) → zakończenie (toast PR) → share card. Zero emoji na każdym kroku.
