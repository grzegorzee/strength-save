// Krok 6 (spec 2026-08-11): JEDNE klucze zapisu preferencji treningowych dla
// Profilu i skrótu w treningu (WorkoutSettingsSheet). localStorage per
// urządzenie + mirror w users/{uid}.preferences (Firestore).
export const REST_TIMER_KEY = 'rest-timer-default';
export const SOUND_KEY = 'timer-sound-enabled';
export const REST_OPTIONS = ['30', '45', '60', '90', '120', '180'];
